# Docs sweep summary

## All critical facts
- **[rfq-flow]** Combo markets are all High Volatility Markets: maker must PUT /communications/quotes/{quote_id}/confirm within 3 seconds of acceptance (30 s on standard markets) or the quote is AUTOMATICALLY VOIDED; execution then fires 1 s after confirm (15 s standard).
- **[rfq-flow]** Create Quote is POST /communications/quotes with body fields exactly: rfq_id (string), yes_bid (string FixedPointDollars), no_bid (string FixedPointDollars), rest_remainder (bool, required), post_only (bool, optional), subaccount (int, optional). Responses/WS rename the prices to yes_bid_dollars / no_bid_dollars — the request and response field names differ.
- **[rfq-flow]** All prices and sizes are fixed-point DECIMAL STRINGS: dollars up to 6 decimal places (e.g. '0.5600'), contracts_fp/count_fp with 2 decimals ('100.00', 0.01 granularity). Never send floats or cents integers on REST (integer cents 1-99 exist only in FIX).
- **[rfq-flow]** A quote has NO size field — it implicitly covers the full RFQ size (contracts_fp or target_cost_dollars). Constraint: yes_bid + no_bid <= $1; either side may be '0' to decline that side but not both.
- **[rfq-flow]** accepted_side semantics (from FIX mapping): accepted_side='yes' hits the maker's YES bid (maker buys YES at yes_bid, requester takes NO); accepted_side='no' hits the maker's NO bid (maker buys NO at no_bid, requester takes YES).
- **[rfq-flow]** Submitting a new quote on the same RFQ automatically cancels/replaces the maker's previous quote — no explicit cancel needed to re-price; explicit pull is DELETE /communications/quotes/{quote_id} (2 tokens).
- **[rfq-flow]** There is NO explicit maker decline-after-accept endpoint; the only documented escape is letting the confirmation window lapse (automatic void). Design the bot to either confirm within 3 s or deliberately not call confirm.
- **[rfq-flow]** WebSocket channel 'communications' (wss://external-api-ws.kalshi.com, auth required) has exactly 5 events: rfq_created & rfq_deleted (broadcast, creator_id empty/anonymized) and quote_created, quote_accepted, quote_executed (only to involved parties). There is NO quote_confirmed or quote_cancelled event — quote voids/cancellations must be discovered via REST polling (GET /communications/quotes, status enum: open|accepted|confirmed|executed|cancelled).
- **[rfq-flow]** Fill reconciliation: after quote_executed, the maker's order appears as creator_order_id on the quote object (and order_id/client_order_id in the WS quote_executed message); join to GET /portfolio/fills via its order_id query param. Fill prices come back as yes_price_dollars/no_price_dollars with count_fp and fee_cost.
- **[rfq-flow]** POST /communications/rfqs returns 409 Conflict when an open RFQ already exists ('Resource already exists or cannot be modified'); replace_existing:true deletes existing RFQs on creation. Max 100 open RFQs per account. Other documented errors: invalid_parameters (bad price / closed RFQ), RFQ_CLOSED (deleted/expired/executed), INSUFFICIENT_BALANCE.
- **[rfq-flow]** Auth on every REST call: KALSHI-ACCESS-KEY, KALSHI-ACCESS-SIGNATURE (RSA-PSS), KALSHI-ACCESS-TIMESTAMP in MILLISECONDS. Prod base https://external-api.kalshi.com/trade-api/v2, demo https://demo-api.kalshi.co/trade-api/v2 (paths identical).
- **[rfq-flow]** Quote prices must land on the market's price grid: validate against price_ranges ({start, end, step} dollar strings) from GET /markets/{ticker} before quoting, or expect invalid_parameters.
- **[rfq-flow]** After confirm + execution timer, orders are entered into the PUBLIC order book; post_only=true cancels the maker's resting order rather than letting it take book liquidity. On the maker side, rest_remainder is described in FIX as 'Allow partial fills (default: N)'.
- **[communications-ws]** Connect to ONE WebSocket: prod wss://external-api-ws.kalshi.com/trade-api/ws/v2, demo wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2; 'communications' is a CHANNEL on that connection, not a separate endpoint/path.
- **[communications-ws]** WS handshake auth headers (exact): KALSHI-ACCESS-KEY, KALSHI-ACCESS-TIMESTAMP (unix milliseconds), KALSHI-ACCESS-SIGNATURE = base64(RSA-PSS-SHA256, salt=digest length) over the string timestamp + "GET" + "/trade-api/ws/v2" (path without query params).
- **[communications-ws]** Subscribe with {"id": <client int, unique per session>, "cmd": "subscribe", "params": {"channels": ["communications"]}}; ack is {"id", "type": "subscribed", "msg": {"channel", "sid"}}; sid is a server integer >=1 present on every data message.
- **[communications-ws]** Exactly 5 communications message types: rfq_created, rfq_deleted, quote_created, quote_accepted, quote_executed. RFQ events are broadcast to ALL subscribers; quote events arrive ONLY if you created the quote or the RFQ — you cannot see competitors' quotes.
- **[communications-ws]** Combo RFQs: rfq_created.msg carries mve_collection_ticker (string) and mve_selected_legs (array of {event_ticker, market_ticker, side, yes_settlement_value_dollars} — all strings). rfq_deleted does NOT repeat these; correlate by msg.id.
- **[communications-ws]** creator_id is 'Currently empty for rfq_created events' — you cannot identify or filter RFQ creators at creation time (rfq_deleted does carry it).
- **[communications-ws]** All money is fixed-point dollar STRINGS (_dollars suffix, e.g. "0.35"); all contract quantities are fixed-point STRINGS with 2 decimals (_fp suffix, e.g. "100.00"); timestamps are ISO 8601 strings (created_ts/deleted_ts/executed_ts). Two RFQ sizing modes: contracts_fp XOR target_cost_dollars.
- **[communications-ws]** quote_accepted has NO timestamp field and its msg carries accepted_side ('yes'|'no') + contracts_accepted_fp; combo markets are High Volatility Markets: maker confirmation window is 3 seconds (execution 1s) vs 30s/15s for standard markets — start the confirm clock from local receipt.
- **[communications-ws]** quote_executed.msg (all required): quote_id, rfq_id, quote_creator_id, rfq_creator_id, order_id, client_order_id, market_ticker, executed_ts — no price/size; join to the fill channel via order_id/client_order_id.
- **[communications-ws]** Communications data messages have NO seq field (envelope is only type/sid/msg) — no documented gap detection; plan REST reconciliation (GET rfqs) for completeness.
- **[communications-ws]** Async error frames: {"id", "type": "error", "msg": {"code": 1-27, "msg": str}}; codes 10 (Channel error), 17 (Internal error), 25 (Subscription buffer overflow) are TERMINAL — you must resubscribe; 25 implies lost messages (client read too slowly).
- **[communications-ws]** Optional fanout sharding on subscribe: shard_factor (int 1-100) + shard_key (0 <= key < shard_factor); validation errors are codes 19-22.
- **[communications-ws]** Kalshi sends WS Ping control frames (0x9) with body 'heartbeat' every 10 seconds; client must reply Pong (0xA) or rely on a library that does (Python websockets does automatically).
- **[communications-ws]** Quote rules: yes_bid and no_bid both required, either may be "0" but not both, yes_bid + no_bid <= $1, prices must align to the market's price_ranges grid, and a maker's new quote on the same RFQ REPLACES their previous quote.
- **[communications-ws]** The 'multivariate' WS channel is deprecated (pre-RFQ lookups) — do not use; use 'communications' for RFQs and 'multivariate_market_lifecycle' for MVE market lifecycle.
- **[multivariate]** Combo market creation is a prerequisite: 'This endpoint must be hit at least once before trading or looking up a market' — POST /multivariate_event_collections/{collection_ticker} (CreateMarketInMultivariateEventCollection) with body {selected_markets: [{market_ticker, event_ticker, side: 'yes'|'no'}], with_market_payload?} returns {event_ticker, market_ticker}; limit 5000 creations per week per user; 429 on breach.
- **[multivariate]** The RFQ flow does NOT auto-create combo markets: CreateRfq (POST /communications/rfqs) requires an existing market_ticker and has no mve_* request fields; mve_collection_ticker and mve_selected_legs appear only on the RFQ read object as exchange-populated response fields.
- **[multivariate]** ALL combo markets are High Volatility Markets: maker confirmation window is 3 seconds and execution timer is 1 second (vs 30 s / 15 s standard) — the market maker must confirm accepted quotes within 3 s or lose the fill.
- **[multivariate]** The list-collections 200 response field is named multivariate_contracts (array), and the single-collection response field is multivariate_contract (singular) — not 'collections'.
- **[multivariate]** Use associated_events (per-event ticker, is_yes_only, size_min/size_max nullable, active_quoters) — associated_event_tickers, is_single_market_per_event, and is_all_yes are all DEPRECATED collection fields.
- **[multivariate]** Leg selection constraints: collection-level size_min/size_max are inclusive counts of markets passed to Create/Lookup; per-event size_min/size_max apply per associated event; if is_ordered is true, the ORDER of selected_markets changes which output market you get.
- **[multivariate]** The combined market ticker derivation is undocumented and must be treated as opaque — the only supported ways to obtain it are the CreateMarket response, GET /events/multivariate?collection_ticker=...&with_nested_markets=true, or lifecycle WS 'created' events.
- **[multivariate]** PUT /multivariate_event_collections/{ticker}/lookup and the WS channel 'multivariate' (multivariate_lookup) are DEPRECATED ('predates RFQs, should not be used for new integrations'); lookup 404s if the market was never created; the originally cited lookup-history doc page no longer exists (404).
- **[multivariate]** WS channel 'multivariate_market_lifecycle' on external-api-ws.kalshi.com carries BOTH 'multivariate_market_lifecycle' messages (event_type enum: created, activated, deactivated, close_date_updated, determined, settled, price_level_structure_updated, metadata_updated) and 'event_lifecycle' messages; market_ticker filters are NOT supported (firehose only, multivariate-only).
- **[multivariate]** Scalar settlement is real for combos: lifecycle msg settlement_value is a STRING, price_level_structure enum is linear_cent|deci_cent|tapered_deci_cent with price_ranges [{start,end,step} as strings], RFQ legs carry nullable yes_settlement_value_dollars, and 'settlement fees ... may apply for sub-cent scalar settlement' — do not assume 0/100 binary payout; the payout function lives in the collection's free-text functional_description.
- **[multivariate]** GET /multivariate_event_collections and GET /multivariate_event_collections/{ticker} require NO auth; Create (POST) and Lookup (PUT) require KALSHI-ACCESS-KEY, KALSHI-ACCESS-SIGNATURE (RSA-PSS), KALSHI-ACCESS-TIMESTAMP (milliseconds).
- **[multivariate]** GET /events/multivariate: collection_ticker and series_ticker filters are mutually exclusive; limit default 100 max 200; with_nested_markets=true to embed Market objects; pagination via cursor (empty cursor = done).
- **[multivariate]** Collection interaction is time-gated by open_date/close_date ('Before this time, the collection cannot be interacted with' / 'After this time...'); filter listings with status enum unopened|open|closed.
- **[multivariate]** Max 100 open RFQs at a time; RFQ sizing uses contracts (int) or contracts_fp (string, 0.01 increments, must match if both) or target_cost_dollars (string, up to 6 decimals); target_cost_centi_cents is deprecated; rest_remainder is a REQUIRED boolean on CreateRfq.
- **[orderbooks]** Orderbook contains BIDS ONLY on both sides; asks must be derived via reciprocality: a YES bid at X == a NO ask at (1.00 - X). best_yes_ask = 1.00 - best_no_bid.
- **[orderbooks]** Price levels are ascending-sorted 2-element string arrays [price_dollars, count_fp]; the BEST bid is the LAST element of each array; all values are fixed-point strings (counts have 2 decimals) — parse with Decimal, never float.
- **[orderbooks]** REST single: GET /markets/{ticker}/orderbook with optional query depth (int, 0-100, default 0; 0/negative = ALL levels). Response root: {"orderbook_fp": {"yes_dollars": [...], "no_dollars": [...]}}.
- **[orderbooks]** REST batch: GET /markets/orderbooks?tickers=A&tickers=B (form/explode serialization), 1-100 tickers, each ticker maxLength 200; response {"orderbooks": [{"ticker", "orderbook_fp"}]}; auth required (KALSHI-ACCESS-KEY, KALSHI-ACCESS-SIGNATURE, KALSHI-ACCESS-TIMESTAMP in ms).
- **[orderbooks]** WS URL prod: wss://external-api-ws.kalshi.com/trade-api/ws/v2 (alt wss://api.elections.kalshi.com/trade-api/ws/v2); demo: wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2 (alt wss://demo-api.kalshi.co/trade-api/ws/v2); API-key headers required on the handshake.
- **[orderbooks]** WS channel is named orderbook_delta; subscribe requires market_ticker or market_tickers (market_id/market_ids NOT supported for this channel); server sends orderbook_snapshot first, then incremental orderbook_delta messages.
- **[orderbooks]** WS snapshot msg fields: type=orderbook_snapshot, sid, seq, msg.market_ticker, msg.market_id, msg.yes_dollars_fp / msg.no_dollars_fp (OPTIONAL arrays of [price_dollars, contract_count_fp]) — note the _fp-suffixed names differ from REST's yes_dollars/no_dollars.
- **[orderbooks]** WS delta msg fields: type=orderbook_delta, sid, seq, msg.price_dollars (string), msg.delta_fp (signed 2-decimal fixed-point string, add to existing level count), msg.side ('yes'|'no'), msg.ts_ms (Unix ms; msg.ts RFC3339 is DEPRECATED), optional msg.client_order_id (your own orders only) and msg.subaccount.
- **[orderbooks]** seq is per-subscription (per sid) and 'should be checked if you want to guarantee you received all the messages'; on a gap the documented resync options are update_subscription {action:'get_snapshot'} (returns fresh orderbook_snapshot without modifying the subscription) or unsubscribe+resubscribe; control acks (ok/unsubscribed) also carry seq on the stream.
- **[orderbooks]** Explicitly set use_yes_price in orderbook subscribe params (currently default false): docs warn 'The default will be flipped to true in a future release' — a silent semantic change to delta prices if left unpinned.
- **[orderbooks]** Keep-alive: Kalshi sends WS Ping frames (0x9, body 'heartbeat') every 10 seconds; client must reply with Pong (0xA); no server ping for ~3 intervals means the feed is dead — reconnect and resubscribe (fresh snapshot arrives).
- **[orderbooks]** Rate limits: token buckets per second (Basic 200 read/100 write ... Prestige 6000/8000); most requests cost 10 tokens; batch operations charge PER ITEM; 429 body {"error":"too many requests"} with NO Retry-After/X-RateLimit headers; GET /account/endpoint_costs is authoritative for non-default costs.
- **[orderbooks]** WS command envelope: {id, cmd, params}; id unique per session, id 0 = no id; commands: subscribe, unsubscribe (params.sids), list_subscriptions, update_subscription (sid/sids + action add_markets|delete_markets|get_snapshot); error envelope {type:'error', msg:{code, msg}} with codes 1-27 (25 buffer overflow, 26 subscription market limit exceeded, 27 too many requests).
- **[auth-env]** REST base URLs — prod: https://external-api.kalshi.com/trade-api/v2 (alt https://api.elections.kalshi.com/trade-api/v2); demo: https://external-api.demo.kalshi.co/trade-api/v2 (alt https://demo-api.kalshi.co/trade-api/v2). Demo is .co, prod is .com.
- **[auth-env]** WebSocket URLs — prod: wss://external-api-ws.kalshi.com/trade-api/ws/v2; demo: wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2.
- **[auth-env]** Auth headers (exact names): KALSHI-ACCESS-KEY (Key ID, UUID), KALSHI-ACCESS-TIMESTAMP (Unix time in MILLISECONDS as string), KALSHI-ACCESS-SIGNATURE (base64 signature).
- **[auth-env]** Signed message = timestamp + UPPERCASE_METHOD + path, no separators, where path is the FULL path from root including /trade-api/v2 prefix and with query parameters STRIPPED. Example: 1703123456789GET/trade-api/v2/portfolio/balance. Request body is never signed.
- **[auth-env]** Signature algorithm: RSA-PSS with SHA256, MGF1(SHA256), salt_length = DIGEST_LENGTH (padding.PSS.DIGEST_LENGTH in Python cryptography), then standard base64 encoding. NOT PKCS1v15, NOT MAX_LENGTH salt.
- **[auth-env]** WebSocket handshake auth: same three headers on the HTTP upgrade request; signed message is timestamp + "GET" + "/trade-api/ws/v2".
- **[auth-env]** Demo and production credentials are completely separate — demo API keys only work on demo endpoints and vice versa.
- **[auth-env]** Private key is shown exactly once at creation (kalshi.com/account/profile → Create New API Key) and cannot be retrieved again.
- **[auth-env]** Rate limits are token buckets per second: Basic tier = 200 read / 100 write tokens/sec; most requests cost 10 tokens (order cancels cost 2), so Basic ≈ 20 reads/sec and 10 writes/sec. The RFQ quote flow counts as WRITE. Exceeding returns HTTP 429 with body {"error": "too many requests"}. Basic write bucket has no burst; higher tiers accumulate up to 2 seconds of budget.
- **[auth-env]** WS keep-alive: server sends Ping frames (0x9) with body 'heartbeat' every 10 seconds; client must reply with Pong frames (0xA).
- **[auth-env]** WS commands are JSON {"id": <unique int>, "cmd": subscribe|unsubscribe|list_subscriptions|update_subscription, "params": {...}}; e.g. subscribe with params.channels=["orderbook_delta"] and params.market_ticker.
- **[limits-account]** Rate limits are token buckets, Read and Write, default 10 tokens per request; tiers (read/write tokens per sec): Basic 200/100, Advanced 300/300, Expert 600/600, Premier 1000/1000, Paragon 2000/2000, Prime 4000/4000, Prestige 6000/8000.
- **[limits-account]** Write bucket covers: order placement, amends, cancels, order groups, THE RFQ QUOTE FLOW, and block trade proposal accepts; every other (GET) operation is Read. REST and FIX drain the same buckets.
- **[limits-account]** Batch requests are billed per item (Batch Create 25 orders = 250 tokens; Batch Cancel 25 = 50 tokens, i.e. cancel costs 2 tokens); the authoritative non-default cost list is runtime data from GET /account/endpoint_costs (response: default_cost int, endpoint_costs[] of {method, path, cost}).
- **[limits-account]** 429 returns body {"error": "too many requests"} with NO penalty or cooldown; retry once the bucket refills (10-token op at 1000 tokens/s refill is covered 10 ms later); use exponential backoff, never long lockouts.
- **[limits-account]** Bucket capacity: Basic and Advanced Read buckets and all Write buckets above Basic hold 2 seconds of budget (burst up to 2x per-second rate after 2 idle seconds); Read above Advanced, Perps Read, and BASIC-TIER WRITE hold only 1 second (Basic write = 100-token cap, zero burst headroom).
- **[limits-account]** Advanced tier is free self-service: POST /account/api_usage_level/upgrade (costs 30 Write tokens; requires >=1 API-created order among your last 100 Predictions orders; 201 grants permanent Advanced; 403 if not eligible). Expert+ requires volume share = 30d_volume / (prev_month_exchange_volume x 2) vs earn/keep thresholds (Expert 0.075%/0.05% ... Prestige 1.00%/0.80%), reviewed daily, 30-day grants with lapse-at-expiry hysteresis.
- **[limits-account]** GET /account/limits returns usage_tier (basic|advanced|expert|premier|paragon|prime|prestige), read/write BucketLimit {refill_rate int tokens/sec, bucket_capacity int}, and grants[] {exchange_instance: event_contract|margined, level, expires_ts (unix SECONDS, absent = permanent), source: volume|manual}.
- **[limits-account]** Auth for all authed REST calls: headers KALSHI-ACCESS-KEY, KALSHI-ACCESS-SIGNATURE (RSA-PSS), KALSHI-ACCESS-TIMESTAMP in MILLISECONDS (while expires_ts/computed_ts in responses are unix seconds).
- **[limits-account]** Base URLs: prod https://external-api.kalshi.com/trade-api/v2 (alt api.elections.kalshi.com), demo https://external-api.demo.kalshi.co/trade-api/v2 (alt demo-api.kalshi.co); prod WebSocket wss://external-api-ws.kalshi.com/ authenticated via API key headers at handshake.
- **[limits-account]** WebSocket limits are enforced but numerically undocumented: error 25 = subscription outbound buffer overflow, error 26 = per-subscription market limit exceeded, error 27 = subscription command rate limit exceeded; command id must be unique per session (id 0 treated as absent); communications channel supports shard_factor (1-100) / shard_key to split the RFQ stream across connections.
- **[limits-account]** Perps traffic uses entirely separate Read/Write buckets and its own limits endpoint; event-contract (Predictions) budgets are untouched by perps calls and vice versa.
- **[money-fees]** All price fields are strings with `_dollars` suffix (e.g. "0.1200"): fixed-point dollars, typically 4 decimals, wire max 6 decimals; all contract counts are strings with `_fp` suffix, always 2 decimals in responses, 0-2 decimals accepted in requests, minimum granularity 0.01 contracts.
- **[money-fees]** Tick size is per-market: read `price_level_structure` (`linear_cent` = 1c everywhere; `tapered_deci_cent` = 0.1c ticks in $0.00-$0.10 and $0.90-$1.00, 1c in between; `deci_cent` = 0.1c everywhere) and validate every quote price against the `price_ranges` array (`start`/`end`/`step` dollar strings) from GET /markets/{ticker}.
- **[money-fees]** Trade fee per fill is the fee-model fee rounded UP to the nearest $0.0001 (centicent) — NOT up to the next cent; then balance_change = revenue - trade_fee is floored toward negative infinity to the member's balance precision ($0.01 non-direct, $0.0001 direct), the shortfall is charged as a rounding_fee, and a per-order fee accumulator issues $0.01 rebates once accumulated rounding exceeds $0.01. Net fee = trade_fee + rounding_fee - rebate >= $0.00.
- **[money-fees]** The fee accumulator is per ORDER across all its fills, and carries over from taker fills to later maker fills of the same order.
- **[money-fees]** The actual fee formula constants (0.07 x C x P x (1-P) style) are NOT in docs.kalshi.com; the API only exposes `fee_type` enum (`quadratic` | `quadratic_with_maker_fees` | `flat`) + `fee_multiplier` (double) on Series, and points to https://kalshi.com/docs/kalshi-fee-schedule.pdf for the tables ('quadratic'=General Trading Fees Table, 'quadratic_with_maker_fees'=+Maker Fees section, 'flat'=Specific Trading Fees Table).
- **[money-fees]** Fee resolution is layered: event-level overrides (GET /trade-api/v2/events/fee_changes: `fee_type_override`/`fee_multiplier_override`, null = override CLEARED, fall back to series) on top of series fees (GET /trade-api/v2/series/fee_changes: `fee_type`, `fee_multiplier`, `scheduled_ts`; query params `series_ticker`, `show_historical` default false). Changes take effect at `scheduled_ts`, not immediately.
- **[money-fees]** GET /trade-api/v2/markets/{ticker} returns Market with combo linkage fields `mve_collection_ticker` and `mve_selected_legs` (each leg: `event_ticker`, `market_ticker`, `side`, `yes_settlement_value_dollars`), plus `fee_waiver_expiration_time` (market may be fee-free until then).
- **[money-fees]** `liquidity_dollars` on Market is deprecated and always returns "0.0000"; `title`, `subtitle`, `expiration_time` are also deprecated (use `yes_sub_title`/`no_sub_title`, `expected_expiration_time`/`latest_expiration_time`).
- **[money-fees]** Fills can be fractional (e.g. count_fp "1.55") even if you only place integer orders — accounting must use Decimal/scaled-integer (x100) arithmetic, never float.
- **[money-fees]** Prod REST base: https://api.elections.kalshi.com/trade-api/v2 (or https://external-api.kalshi.com/trade-api/v2); demo: https://demo-api.kalshi.co/trade-api/v2 (or https://external-api.demo.kalshi.co/trade-api/v2) — demo is .kalshi.co.
- **[live-data]** All live-data and milestone endpoints require NO authentication (security: []) and exist on both prod (external-api.kalshi.com/trade-api/v2, api.elections.kalshi.com/trade-api/v2) and demo (external-api.demo.kalshi.co/trade-api/v2, demo-api.kalshi.co/trade-api/v2). Prod TLD is .com, demo is .co.
- **[live-data]** Preferred live-data endpoint is GET /live_data/milestone/{milestone_id}; GET /live_data/{type}/milestone/{milestone_id} is explicitly legacy. Batch is GET /live_data/batch?milestone_ids=... with a HARD MAX of 100 milestone IDs per call; its response field is plural 'live_datas'.
- **[live-data]** The live_data payload is exactly three fields: type (string), details (object), milestone_id (string). 'details' is a documented-as-flexible object with NO schema — game clock/score/status field names are NOT in the docs and must be reverse-engineered per sport.
- **[live-data]** Market-to-live-data mapping chain: market -> event_ticker -> milestone -> milestone.id -> /live_data. Reverse lookup: GET /milestones?related_event_ticker={ticker}, or GET /events?with_milestones=true (adds top-level 'milestones' array), then use milestone.id as milestone_id. Milestone has both primary_event_tickers (outcome-defining) and related_event_tickers (superset in practice).
- **[live-data]** GET /milestones REQUIRES the 'limit' query param (min 1, max 500) and paginates via 'cursor'. Filters: category (Sports/Elections/Esports/Crypto), competition (e.g. 'Pro Football', 'Pro Basketball (M)'), type (e.g. football_game, basketball_game, hockey_match), related_event_ticker, source_id, minimum_start_date (RFC3339), min_updated_ts (Unix seconds).
- **[live-data]** Game stats (play-by-play): GET /live_data/milestone/{milestone_id}/game_stats, supported ONLY for Pro Football, College Football, Pro Basketball, College Men's Basketball, College Women's Basketball, WNBA, Soccer, Pro Hockey, Pro Baseball; returns null when the milestone type is unsupported or the milestone lacks a Sportradar ID. Response: pbp.periods[].events[] (open schema).
- **[live-data]** include_player_stats=true (default false) works only for Pro Football, Pro Basketball, and College Men's Basketball milestones that have player ID mappings configured.
- **[live-data]** There is NO WebSocket channel for live scores/milestones — courtside protection must poll HTTP. No latency SLA or update frequency is documented anywhere.
- **[live-data]** Rate limiting: token-bucket, most endpoints cost 10 tokens; Basic tier = 200 read tokens/sec (~20 reads/sec). Batch endpoints charge PER ITEM, not per call — a 100-id /live_data/batch call may cost 100 items' worth of tokens. 429 returns {"error": "too many requests"} with NO Retry-After or X-RateLimit-* headers; implement your own pacing.
- **[live-data]** The old doc URLs /api-reference/live-data/get-milestones.md and /api-reference/live-data/get-milestone.md are dead (404); milestone docs live under /api-reference/milestone/. Milestone API paths themselves are GET /milestones and GET /milestones/{milestone_id}.
- **[exchange-status]** GET /exchange/status (no auth) returns ExchangeStatus with two REQUIRED booleans: exchange_active ('False if the core Kalshi exchange is no longer taking any state changes at all') and trading_active ('True if we are currently permitting trading on the exchange'). Gate quoting on trading_active — exchange_active=true does NOT mean trading is allowed.
- **[exchange-status]** Optional ExchangeStatus fields: intra_exchange_transfers_active (boolean), exchange_estimated_resume_time (nullable date-time string, estimated maintenance completion), exchange_index_statuses (array of {exchange_index int — only 0 supported, exchange_active, trading_active, intra_exchange_transfers_active}).
- **[exchange-status]** Error responses from /exchange/status (500/503/504) are documented to use the SAME ExchangeStatus schema as 200 — no separate error envelope.
- **[exchange-status]** Routine maintenance is every Thursday 3:00–5:00 AM ET and imposes a trading pause; intensive work may escalate to a full exchange pause. Docs recommend expecting session disconnections during this window and reconnecting after 5:00 AM ET.
- **[exchange-status]** During both pause types: order placement and modification are blocked but cancellations are allowed, and resting orders PERSIST on the book by default — they are only auto-cancelled if created with cancel_order_on_pause=true (REST field on order creation, e.g. Create Order V2 POST /trade-api/v2/portfolio/events/orders; FIX Tag 21006 on 35=D). Exact description: 'If this flag is set to true, the order will be canceled if the order is open and trading on the exchange is paused for any reason.'
- **[exchange-status]** GET /exchange/schedule (no auth) returns GetExchangeScheduleResponse.schedule with required standard_hours (array of WeeklySchedule: start_time/end_time date-time effective range, plus monday..sunday each an ARRAY of DailySchedule {open_time, close_time}) and required maintenance_windows (array of {start_datetime, end_datetime}).
- **[exchange-status]** DailySchedule open_time/close_time are plain 'HH:MM' strings in Eastern Time (ET), NOT ISO timestamps and NOT UTC — client must do DST-aware America/New_York conversion. MaintenanceWindow uses different field names (start_datetime/end_datetime) than WeeklySchedule (start_time/end_time).
- **[exchange-status]** Demo base URLs are on the .co TLD: https://external-api.demo.kalshi.co/trade-api/v2 and https://demo-api.kalshi.co/trade-api/v2; production is https://external-api.kalshi.com/trade-api/v2 and https://api.elections.kalshi.com/trade-api/v2.
- **[exchange-status]** The docs say NOTHING about what happens to open RFQs or resting RFQ quotes during a pause — only orders are covered. RFQ timing constants that a pause could interact with: 30s confirmation window + 15s execution timer (standard), 3s + 1s (High Volatility Markets).
- **[exchange-status]** Create Order V2 path is POST /trade-api/v2/portfolio/events/orders (current); the legacy /portfolio/orders endpoint will be deprecated no earlier than May 6, 2026.
- **[python-sdk]** The documented SDK pages (sdks/python/quickstart.md, sdk-rest.md, sdk-websockets.md) all 404; current doc is docs.kalshi.com/sdks/overview.md and the packages are kalshi_python_sync (pip install kalshi-python-sync) and kalshi_python_async (pip install kalshi-python-async), both v3.22.0; the old kalshi-python package is deprecated/unmaintained.
- **[python-sdk]** The README inside both wheels is stale and WRONG: it says 'from kalshi_python import Configuration, KalshiClient' — the actual import packages are kalshi_python_sync / kalshi_python_async; there is no kalshi_python module in these wheels.
- **[python-sdk]** Both packages require Python >= 3.13 (wheel Requires-Python), despite the README claiming Python 3.9+.
- **[python-sdk]** The SDK covers the FULL Communications API via CommunicationsApi: create_rfq/get_rfqs/get_rfq/delete_rfq, create_quote/get_quotes/get_quote/delete_quote, accept_quote (PUT /communications/quotes/{quote_id}/accept), confirm_quote (PUT /communications/quotes/{quote_id}/confirm, 204 response, starts the execution timer), get_communications_id (GET /communications/id), plus block-trade endpoints; MultivariateApi covers MVE collection lookup/creation.
- **[python-sdk]** The SDK has ZERO WebSocket support — the communications WS channel (RFQ discovery, the latency-critical path) must be hand-rolled using the websockets library against wss://external-api-ws.kalshi.com/trade-api/ws/v2 (demo: wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2), signing message timestamp + 'GET' + '/trade-api/ws/v2'.
- **[python-sdk]** CreateQuoteRequest fields are exactly: rfq_id (str), yes_bid (str), no_bid (str), rest_remainder (bool, required), post_only (bool, optional), subaccount (int, optional) — yes_bid/no_bid are fixed-point DOLLAR STRINGS with up to 6 decimal places (e.g. "0.35"), NOT integer cents; but Quote responses use yes_bid_dollars/no_bid_dollars field names.
- **[python-sdk]** Contract counts use contracts_fp: a fixed-point string, requests accept 0-2 decimals, responses always emit 2 decimals, minimum granularity 0.01 contracts; integer 'contracts' fields are legacy/deprecated; target_cost_centi_cents is DEPRECATED in favor of target_cost_dollars (string).
- **[python-sdk]** Auth signing: headers KALSHI-ACCESS-KEY, KALSHI-ACCESS-SIGNATURE, KALSHI-ACCESS-TIMESTAMP (milliseconds); message = str(ms_timestamp) + METHOD.upper() + path-without-query; RSA-PSS with SHA256, MGF1(SHA256), salt_length = padding.PSS.DIGEST_LENGTH, base64-encoded; the SDK hardcodes the signed path prefix as f"/trade-api/v2{resource_path}".
- **[python-sdk]** Configuration does NOT accept api_key_id/private_key_pem as constructor kwargs — set them as attributes (config.api_key_id = ..., config.private_key_pem = ...) or KalshiClient silently sends unsigned requests; SDK default host is https://external-api.kalshi.com/trade-api/v2 (prod), with api.elections.kalshi.com still supported and demo at https://external-api.demo.kalshi.co/trade-api/v2.
- **[python-sdk]** HVM (combo) RFQ timing is brutal: 3-second maker confirmation window and 1-second execution timer (vs 30s/15s for standard markets) — the confirm_quote REST call must complete within 3 seconds of acceptance.
- **[python-sdk]** Combo RFQs are identified via the RFQ model's mve_collection_ticker and mve_selected_legs (List of {event_ticker, market_ticker, side, yes_settlement_value_dollars}); RFQ status enum is (open, closed).
- **[python-sdk]** On the communications WS channel: rfq_created/rfq_deleted are sent to ALL subscribers, but quote_created/quote_accepted/quote_executed are only sent if you created the quote or the RFQ; quote_executed carries client_order_id for correlating with fill messages; the market filter in the subscription is ignored; all WS money/count fields are strings (target_cost_dollars, contracts_fp, yes_bid_dollars...).
- **[python-sdk]** The convenience wrappers (client.create_quote(**kwargs)) feed ALL kwargs into the pydantic request model constructor — use the typed create_quote_with_http_info(CreateQuoteRequest(...)) variants in production code.
- **[python-sdk]** POST /communications/quotes costs 2 rate-limit tokens per request per the REST doc; async SDK uses aiohttp with optional aiohttp_retry.ExponentialRetry when Configuration(retries=N) is set — do not enable blindly for non-idempotent quote creation.
- **[openapi-comms]** Base URLs: prod https://external-api.kalshi.com/trade-api/v2 (or api.elections.kalshi.com), demo https://external-api.demo.kalshi.co/trade-api/v2 (or demo-api.kalshi.co); WS prod wss://external-api-ws.kalshi.com, channel 'communications'.
- **[openapi-comms]** Auth = 3 headers on every authed call: KALSHI-ACCESS-KEY (key id), KALSHI-ACCESS-SIGNATURE (RSA-PSS), KALSHI-ACCESS-TIMESTAMP (milliseconds).
- **[openapi-comms]** Combos are HVM markets: maker has 3 seconds to confirm after the requester accepts, then a 1-second execution timer (standard markets: 30 s / 15 s). Bot must confirm via PUT /communications/quotes/{quote_id}/confirm (204, empty body) within 3 s or lose the trade.
- **[openapi-comms]** CreateQuote request body fields are rfq_id, yes_bid, no_bid (FixedPointDollars strings), rest_remainder (required), post_only, subaccount — the request uses yes_bid/no_bid but every Quote response uses yes_bid_dollars/no_bid_dollars.
- **[openapi-comms]** Quote constraints: two-sided, for full RFQ size, yes_bid + no_bid <= $1.00, not both zero, prices must land on the market's price grid.
- **[openapi-comms]** Quote.status lifecycle enum: open -> accepted -> confirmed -> executed, or cancelled. Accept = PUT .../accept with body {accepted_side: 'yes'|'no'}; both accept and confirm return 204.
- **[openapi-comms]** Fill linkage: GET /portfolio/fills Fill has order_id (plus fill_id, and trade_id documented as a legacy alias of fill_id) but NO creator_order_id; the maker's order id comes from Quote.creator_order_id (requester side: Quote.rfq_creator_order_id) and equals Fill.order_id. WS quote_executed also delivers order_id + client_order_id.
- **[openapi-comms]** All prices are FixedPointDollars strings (up to 6 decimal places, e.g. "0.5600") and all counts are FixedPointCount strings (2 decimals, min granularity 0.01 contracts; requests accept 0-2 dp, responses always 2 dp). No integer-cents fields on RFQ/quote/order/market endpoints (deprecated target_cost_centi_cents and block trades excepted).
- **[openapi-comms]** Market price grid: Market.price_ranges = array of {start, end, step} dollar strings (possibly multiple ranges with different steps) plus price_level_structure string; contract payout is notional_value_dollars. There is no tick_size or notional_value integer field.
- **[openapi-comms]** RFQ object: id, creator_id (public communications id), market_ticker, contracts_fp, target_cost_dollars, status (open|closed), created_ts, mve_collection_ticker, mve_selected_legs[{event_ticker, market_ticker, side, yes_settlement_value_dollars}], rest_remainder; sizing is contracts_fp OR target_cost_dollars.
- **[openapi-comms]** CreateRFQ: max 100 open RFQs per user, only one open RFQ per market ticker (409 Conflict), replace_existing:true deletes existing RFQs at creation; rest_remainder is required.
- **[openapi-comms]** POST /multivariate_event_collections/{collection_ticker} (CreateMarketInMultivariateEventCollection) with selected_markets:[{market_ticker, event_ticker, side:'yes'|'no'}] must be called at least once before trading or looking up a combo market; limit 5000 creations/week; the /lookup endpoint is DEPRECATED; collection responses are keyed multivariate_contract(s).
- **[openapi-comms]** V2 orders: POST /portfolio/events/orders with {ticker, side: bid|ask (YES leg: bid=buy YES, ask=sell YES), count: FP string, price: FP dollars string, time_in_force: fill_or_kill|good_till_canceled|immediate_or_cancel, self_trade_prevention_type: taker_at_cross|maker (required), client_order_id, post_only}; cancel = DELETE /portfolio/events/orders/{order_id} (2 tokens); batch create bills 10 tokens/order, batch cancel 2 tokens/order; legacy POST /portfolio/orders deprecated no earlier than 2026-05-06.
- **[openapi-comms]** Rate limits: token buckets per second, default endpoint cost 10 tokens; CreateQuote/DeleteQuote/GetQuote/DeleteRFQQuote/GetOrder cost 2; Basic tier = 200 read + 100 write tokens/sec (Advanced 300/300 via free upgrade endpoint); 429 on empty bucket; authoritative costs from GET /account/endpoint_costs.
- **[openapi-comms]** WS visibility: rfq_created/rfq_deleted broadcast to everyone; quote_created/quote_accepted/quote_executed are only sent to participants (quote or RFQ initiator). WS field names differ from REST: quote_creator_id (not creator_id), yes/no_contracts_offered_fp (not yes/no_contracts_fp).
- **[openapi-comms]** GET /portfolio/fills params: ticker, order_id, min_ts, max_ts (Unix seconds), limit 1-1000 default 100, cursor, subaccount; Fill direction fields side/action are deprecated (gone no earlier than 2026-05-14) in favor of outcome_side (yes|no) and book_side (bid|ask); is_taker flags liquidity removal; fee_cost is FixedPointDollars.
- **[openapi-comms]** Combo discovery: GET /markets?mve_filter=only|exclude; GET /events/multivariate?collection_ticker=...&with_nested_markets=true (limit max 200); combo Market objects carry mve_collection_ticker + mve_selected_legs.
- **[asyncapi-ws]** WS URL prod: wss://external-api-ws.kalshi.com/trade-api/ws/v2 ; demo: wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2 . Connection-level auth is ALWAYS required (even for public channels) via handshake headers KALSHI-ACCESS-KEY, KALSHI-ACCESS-SIGNATURE, KALSHI-ACCESS-TIMESTAMP; signature = base64(RSA-PSS-SHA256 over ms_timestamp + "GET" + "/trade-api/ws/v2")
- **[asyncapi-ws]** Channel names (subscribe enum): orderbook_delta, ticker, trade, fill, market_positions, market_lifecycle_v2, multivariate_market_lifecycle, multivariate (DEPRECATED), communications, order_group_updates, user_orders, cfbenchmarks_value. There is NO ticker_v2 channel in this spec.
- **[asyncapi-ws]** communications channel: market filters are IGNORED (global RFQ firehose); rfq_created/rfq_deleted always sent to everyone; quote_created/quote_accepted/quote_executed sent ONLY if you created the quote or the RFQ — competitors' quotes are invisible. Optional sharding via shard_factor (1-100) + shard_key (0 <= key < shard_factor).
- **[asyncapi-ws]** quote_executed carries YOUR order_id and client_order_id (each party gets their own) plus quote_id/rfq_id/market_ticker/executed_ts — but NO price or size; correlate with fill channel messages via order_id/client_order_id to learn execution details.
- **[asyncapi-ws]** rfq_created msg: required id, creator_id (currently ALWAYS empty string), market_ticker, created_ts (RFC3339 string); optional event_ticker, contracts_fp, target_cost_dollars, mve_collection_ticker, mve_selected_legs[{event_ticker, market_ticker, side, yes_settlement_value_dollars}]. Communications timestamps are RFC3339 strings, NOT ts_ms integers.
- **[asyncapi-ws]** quote_created msg: required quote_id, rfq_id, quote_creator_id, market_ticker, yes_bid_dollars, no_bid_dollars, created_ts; optional yes_contracts_offered_fp, no_contracts_offered_fp, rfq_target_cost_dollars. quote_accepted adds optional accepted_side (yes|no) and contracts_accepted_fp and has NO timestamp.
- **[asyncapi-ws]** orderbook_delta channel requires market_ticker/market_tickers (market_id NOT supported); sends orderbook_snapshot first then orderbook_delta; both carry required seq for gap detection; update_subscription actions add_markets/delete_markets/get_snapshot (get_snapshot re-sends snapshot without modifying subscription). Snapshot levels: yes_dollars_fp / no_dollars_fp = arrays of [price_dollars_string, count_fp_string]; key absent when side empty. Delta: price_dollars (string), delta_fp (signed string), side (yes|no), optional client_order_id present ONLY when your own order caused the change.
- **[asyncapi-ws]** Subscribe with use_yes_price: true on orderbook_delta — default (false) reports no-side levels in no-leg pricing; the default will flip to true and the flag will then be removed (unified yes-leg pricing).
- **[asyncapi-ws]** seq exists ONLY on orderbook_snapshot/orderbook_delta, order_group_updates, and cfbenchmarks messages; ticker, trade, fill, market_position, lifecycle, user_order, and ALL communications messages have NO seq — you cannot gap-detect the RFQ stream.
- **[asyncapi-ws]** Command protocol: {id, cmd, params}; cmd in {subscribe, unsubscribe, update_subscription, list_subscriptions}; id client-unique per session, id=0 treated as absent; responses typed subscribed/unsubscribed/ok/error; error msg = {code 1-27, msg, optional market_ticker/market_id}; terminal errors 10 (Channel error), 17 (Internal error), 25 (Subscription buffer overflow) kill the subscription and REQUIRE resubscribe; 26 = per-subscription market limit, 27 = per-subscription command rate limit.
- **[asyncapi-ws]** update_subscription requires exactly one subscription id: either params.sid (int) or params.sids (array of EXACTLY one), plus required action in {add_markets, delete_markets, get_snapshot}; ok response msg.market_tickers = FULL list after update (omitted when skip_ticker_ack was set on subscribe).
- **[asyncapi-ws]** Numeric conventions: all prices and contract counts are fixed-point STRINGS — *_dollars (e.g. "0.3500") and *_fp (2 decimals, e.g. "278.00", fractional contracts exist); dollar_volume/dollar_open_interest are plain integers; use ts_ms (unix ms) everywhere on market data — ts (seconds) and RFC3339 time fields are deprecated.
- **[asyncapi-ws]** Direction fields: canonical are outcome_side/book_side (fill, user_order) and taker_outcome_side/taker_book_side (trade); bid==yes, ask==no; buy-yes & sell-no => yes/bid, buy-no & sell-yes => no/ask; legacy side/action/is_yes/purchased_side/taker_side deprecated (removal not before May 14, 2026 per asyncapi.yaml; May 28, 2026 per order_direction page).
- **[asyncapi-ws]** fill msg required: trade_id, order_id, market_ticker, is_taker, side (dep), yes_price_dollars (always the YES-side price), count_fp, fee_cost (fixed-point dollars string), action (dep), outcome_side, book_side, ts (dep), ts_ms, post_position_fp, purchased_side (dep); optional client_order_id, subaccount. user_orders msg uses field name `ticker` (NOT market_ticker) and status enum resting|canceled|executed.
- **[asyncapi-ws]** multivariate_market_lifecycle channel: no filters, MVE-only lifecycle, same payload schema as market_lifecycle_v2 (event_type enum: created, deactivated, activated, close_date_updated, determined, settled, price_level_structure_updated, metadata_updated; conditional fields open_ts/close_ts/result/determination_ts/settlement_value/settled_ts/is_deactivated/price_level_structure/price_ranges/strikes) but type const multivariate_market_lifecycle; MVE created messages do NOT include leg composition. The old `multivariate` channel (multivariate_lookup) is deprecated — do not use.
- **[asyncapi-ws]** Kalshi sends WS Ping (0x9) with body `heartbeat` every 10 seconds; client must Pong (0xA). price_ranges ({start, end, step} dollar strings) from lifecycle events define valid order price ticks — never hardcode tick size (price_level_structure enum: linear_cent, deci_cent, tapered_deci_cent).
- **[index-scan]** V2 order endpoints are POST/DELETE on /trade-api/v2/portfolio/events/orders (single: POST /portfolio/events/orders, cancel: DELETE /portfolio/events/orders/{order_id}, batch: POST|DELETE /portfolio/events/orders/batched, amend: POST .../{order_id}/amend, decrease: POST .../{order_id}/decrease); legacy /portfolio/orders mutations are deprecated and cost more tokens
- **[index-scan]** V2 orders use side=bid|ask (bid≡yes, ask≡no, always) with a single `price` string on the unified yes-scale; required fields: ticker, side, count, price, time_in_force (fill_or_kill|good_till_canceled|immediate_or_cancel), self_trade_prevention_type (taker_at_cross|maker); type=market no longer exists
- **[index-scan]** All quantities/prices are fixed-point STRINGS: FixedPointCount = 2 decimals min-granularity 0.01 (e.g. "10.00"), FixedPointDollars = up to 6 decimals (e.g. "0.5600"); exceptions: GET /portfolio/balance `balance`/`portfolio_value` are int cents, settlement `revenue`/`value` are int cents, incentive `period_reward` is centi-cents
- **[index-scan]** Rate limits are token buckets (separate read/write) with per-endpoint costs: default 10, create order 10, cancel 2, batch billed per item (N×10 create, N×2 cancel), quote create/get/delete 2 each; poll public GET /account/endpoint_costs for the live table and GET /account/limits (auth) for your refill_rate/bucket_capacity
- **[index-scan]** KALSHI-ACCESS-TIMESTAMP auth header is in MILLISECONDS, but order expiration_time is Unix SECONDS, and lifecycle WS timestamps (open_ts/close_ts) are SECONDS while ts_ms fields are milliseconds
- **[index-scan]** Only status=active markets accept orders; once closed, ALL operations including cancels are rejected with MARKET_INACTIVE; after a pause is lifted, all resting orders are automatically canceled and must be re-placed
- **[index-scan]** Multivariate (combo) events are EXCLUDED from GET /events — use GET /events/multivariate (max limit 200; collection_ticker XOR series_ticker); combo markets are excluded from the market_lifecycle_v2 WS channel — use the multivariate_market_lifecycle channel (no ticker filters); combo Market objects carry mve_collection_ticker and mve_selected_legs
- **[index-scan]** Fee schedule = Series.fee_type (quadratic|quadratic_with_maker_fees|flat) × Series.fee_multiplier, with per-event overrides (fee_type_override/fee_multiplier_override, null = cleared) discoverable via GET /events/fee_changes, GET /series/fee_changes (scheduled_ts = effective time) and the event_fee_update WS message
- **[index-scan]** RFQ/quote data is ephemeral: closed RFQs and cancelled quotes retained only 7 days, and RFQ quotes are durably queryable ONLY after acceptance; GET /communications/quotes no longer supports market_ticker/event_ticker filters (use min_ts/max_ts + user filters); persist all RFQ/quote state locally at creation time
- **[index-scan]** Batch create returns HTTP 201 even when individual orders fail — every element of response.orders[] must be checked for a non-null `error` object
- **[index-scan]** Amending an order preserves queue priority ONLY when it purely decreases size; price changes or size increases send it to the back of the queue (and an amend can cross and fill immediately); use POST .../decrease or the amend-with-smaller-count to shrink safely
- **[index-scan]** Order groups are the native kill-switch: contracts_limit (1–1,000,000) measured over a ROLLING 15-SECOND window of fills; on trigger, all group orders cancel and new placement is blocked until Reset; attach orders via order_group_id on create
- **[index-scan]** Fill WS channel is named `fill` and includes post_position_fp (position after the fill), fee_cost, is_taker, outcome_side/book_side, ts_ms — use it as the source of truth for fills; Fill REST objects do NOT contain client_order_id (removed 2026-03-30)
- **[index-scan]** Two distinct status vocabularies: GET /markets filter takes unopened|open|paused|closed|settled while Market.status returns initialized|inactive|active|closed|determined|disputed|amended|finalized — never conflate them
- **[index-scan]** Tick size must come from Market.price_level_structure + price_ranges[{start,end,step}] (tick_size field was removed 2026-05-07); price_level_structure enum: linear_cent, deci_cent, tapered_deci_cent
- **[index-scan]** Subaccounts: 0=primary, 1–63 numbered, created via POST /portfolio/subaccounts (requires advanced/MM access); omitting `subaccount` on fills/orders/settlements returns ALL subaccounts, while balance/positions default to 0; transfers require idempotent client_transfer_id (replay → 409)
- **[index-scan]** Maintenance window every Thursday 3:00–5:00 AM ET: no place/amend (cancels still work in trading pauses); resting orders persist unless cancel_order_on_pause=true was set at order creation; expect WS disconnects
- **[index-scan]** Live-data window is ~3 months: older settled markets, trades, fills, and completed orders only exist on GET /historical/* endpoints with cutoffs from GET /historical/cutoff
- **[index-scan]** Legacy direction fields (side, action, is_yes, purchased_side, taker_side) are deprecated in favor of outcome_side/book_side (taker_outcome_side/taker_book_side on trades); removal no earlier than May 14–28, 2026 (already past — assume they can vanish); legacy integer count/price fields were already removed 2026-03-12
- **[index-scan]** No documentation of HVM (high-volatility market) rules exists anywhere in the docs index — do not design around assumed HVM mechanics without empirical confirmation

## All open questions
- **[rfq-flow]** accepted_side economics need demo confirmation: the FIX mapping strongly implies accepted_side='yes' means the maker buys YES at yes_bid, but no REST page states counterparty legs or the requester's effective price (1 - other_bid?) explicitly. Verify with a demo round-trip and inspect both parties' fills (outcome_side, yes_price_dollars/no_price_dollars).
- **[rfq-flow]** Can a maker DELETE /communications/quotes/{quote_id} while the quote is in 'accepted' status (i.e., an explicit decline), or does delete 404/409 after acceptance leaving window-lapse as the only out? Docs are silent.
- **[rfq-flow]** What exactly happens on confirmation-window lapse from the maker's REST view: does the quote move to status='cancelled' with a specific cancellation_reason string (and what are the possible cancellation_reason values)? No enum is documented.
- **[rfq-flow]** rest_remainder maker-side semantics: REST says 'rest the remainder of the quote after execution' but FIX calls the same flag 'Allow partial fills (default: N)'. Determine on demo: with rest_remainder=false, is the execution all-or-none, and with true, whose order rests on the public book, at what price, and does post_only interact with it?
- **[rfq-flow]** During the 1 s execution timer on combos: can either party still cancel, can third parties trade against the pending orders, and do the two orders always cross each other at the quoted price or can the book improve/steal one leg (especially with post_only=false)?
- **[rfq-flow]** How target_cost_dollars converts to contract counts per side: which formula produces yes_contracts_fp vs no_contracts_fp (target / (1 - opposite_bid)? target / bid?), and does contracts_accepted_fp ever differ from the offered count (partial acceptance)? The doc example values ('0.35' target with 100/200 contracts) are internally inconsistent.
- **[rfq-flow]** Demo WebSocket URL for the communications channel is not documented (prod is wss://external-api-ws.kalshi.com); confirm the demo host and the exact subscribe command shape (cmd/params JSON) empirically.
- **[rfq-flow]** Does REST have an equivalent of FIX PreferBetterQuote (accept-best-quote), meaning a maker's quote can be executed by an accept that named a different quote? Affects whether the bot must treat all its open quotes as acceptable at any moment.
- **[rfq-flow]** Token costs are only documented for create/delete/get quote (2 tokens each); pull GET /trade-api/v2/account/endpoint_costs on demo to get actual costs for accept/confirm/create-RFQ and overall bucket size/refill rate for quoting-loop throughput planning.
- **[rfq-flow]** Are RFQ-scoped endpoint variants (PUT /communications/rfqs/{rfq_id}/quotes/{quote_id}/confirm etc.) preferred or just aliases? Both forms are documented as current; verify identical behavior and pick one.
- **[rfq-flow]** Fee treatment on RFQ executions: fee_cost appears on fills and is_taker exists, but no RFQ doc says which side is taker/maker for fee purposes on RFQ-originated fills — measure on demo.
- **[rfq-flow]** Whether quotes have any independent expiry (TTL) before acceptance, and whether RFQs auto-expire (RFQ_CLOSED mentions 'expired' but no lifetime constant is documented anywhere).
- **[communications-ws]** Does the communications channel actually work on demo (wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2), and is there any RFQ traffic there? The AsyncAPI servers block lists only production.
- **[communications-ws]** Exact values of mve_selected_legs[].side — schema has no enum (presumably 'yes'/'no' per the global marketSide enum); also whether yes_settlement_value_dollars is always present and what it means for non-$1 payout legs.
- **[communications-ws]** Is market_ticker on a combo rfq_created the already-created MVE market in the collection, or can it reference a market that doesn't exist yet (needing create-market-in-multivariate-event-collection / lookup)? Verify by capturing live combo RFQs.
- **[communications-ws]** How does shard routing assign RFQs to shard_key values (hash of rfq id? market?) — and does subscribing to communications twice on one connection (different shard_keys) return error 6 'Already subscribed'?
- **[communications-ws]** Whether communications messages ever include seq in practice despite the schema omitting it, and if not, the best reconciliation cadence via REST GET rfqs/quotes to detect dropped events.
- **[communications-ws]** Exact disconnect behavior when Pongs are missed (timeout seconds undocumented), plus any max-connection-count or per-connection subscribe limits (none documented).
- **[communications-ws]** Precision of _dollars price strings on combo markets (2 decimals vs sub-cent / centi-cent price_ranges grid) — quote prices must 'align with market price_ranges'; verify actual grid for MVE markets on demo.
- **[communications-ws]** Is the 3s/1s HVM confirmation/execution window measured server-side from acceptance time (implying our effective budget is 3s minus one-way latency), and what error does a late confirm return (RFQ_CLOSED?).
- **[communications-ws]** Whether quote_accepted can arrive without accepted_side/contracts_accepted_fp in practice (they are schema-optional) — a maker needs these to know which side and how much was accepted before confirming.
- **[communications-ws]** Whether rfq_created.creator_id staying empty is permanent ('Currently' suggests it may change) and whether get-communications-id REST maps our own anonymized comm id so we can recognize self-created RFQs in the stream.
- **[multivariate]** Is CreateMarketInMultivariateEventCollection idempotent for an already-existing combination (returns existing ticker with 200) or does it error — and does a repeat call count against the 5000/week creation limit?
- **[multivariate]** Who is expected to call CreateMarket in the RFQ flow in practice — does the Kalshi UI/requester side auto-create the market before the RFQ is broadcast (so makers never need to), or must a maker pre-create markets to quote proactively?
- **[multivariate]** Exact demo WebSocket host for the multivariate_market_lifecycle channel (docs only state external-api-ws.kalshi.com for prod) and the exact subscribe command params for this channel.
- **[multivariate]** Whether the 'multivariate_market_lifecycle' subscription's message schema label 'market_lifecycle_v2' means the wire type string is ever 'market_lifecycle_v2' vs always 'multivariate_market_lifecycle' — verify actual type field on demo.
- **[multivariate]** Units/format of the lifecycle integer timestamps (open_ts, close_ts, determination_ts, settled_ts, expected_expiration_ts): seconds vs milliseconds epoch.
- **[multivariate]** How functional_description encodes product-of-leg-values for sports combos (is it machine-parseable or free prose), and how yes_settlement_value_dollars per leg maps to the combined settlement_value — needs a live example from a settled combo on demo/prod.
- **[multivariate]** What price_level_structure do combo markets actually use (linear_cent vs deci_cent vs tapered_deci_cent) and what price_ranges look like in practice — affects quote price granularity.
- **[multivariate]** Semantics of active_quoters strings on AssociatedEvent (are these communications IDs of makers? how does one get listed?) — undocumented.
- **[multivariate]** Whether GET /multivariate_event_collections status filter values map to open_date/close_date or to a separate lifecycle state, and whether unopened collections are visible on demo.
- **[multivariate]** The exact 400 error codes/messages returned for size_min/size_max/is_yes_only violations in CreateMarket (needed for robust leg-validation error handling).
- **[multivariate]** Whether the 5000/week creation limit is per API key, per member, or per account, and what the reset boundary is (rolling 7 days vs calendar week).
- **[multivariate]** Confirm on demo that combo (HVM) confirmation window is exactly 3 s and execution timer 1 s as the guide states, and what happens to a maker quote if confirmation is missed (penalty? cooldown? Kalshi rate-limits bad quotes per prior project experience).
- **[orderbooks]** Exact gap-recovery contract: after update_subscription get_snapshot, does the returned orderbook_snapshot carry the next seq in the same stream (letting you resume deltas seamlessly), and are deltas suppressed/buffered while the snapshot generates? Docs are silent — verify on demo by forcing a gap.
- **[orderbooks]** use_yes_price semantics: docs never define what it changes (presumably delta/snapshot prices for the 'no' side expressed in YES terms when true). Verify on demo with both values before the default flips to true.
- **[orderbooks]** Is seq strictly contiguous (+1 per message) per sid across ALL message types including ok/unsubscribed acks, or only across snapshot/delta messages? The ok example (seq 222) suggests control messages consume seq slots — verify to avoid false gap alarms.
- **[orderbooks]** Whether GET /markets/{ticker}/orderbook truly works unauthenticated (getting-started says no auth needed; API reference lists the three KALSHI-ACCESS-* headers as required).
- **[orderbooks]** Token cost of GET /markets/orderbooks: charged per ticker (per-item batch rule) or flat 10 tokens? Check GET /account/endpoint_costs on demo — determines feasible REST polling cadence for ~100 combo markets.
- **[orderbooks]** Zero-level removal: docs never state that a level whose count reaches 0 via delta_fp should be deleted, nor whether the server can send a delta creating a negative count (which would indicate a missed message). Verify empirically.
- **[orderbooks]** Numeric values of WS limits behind error codes 25 (subscription buffer overflow), 26 (subscription market limit exceeded), and 27 (too many requests) — max markets per orderbook_delta subscription is undocumented.
- **[orderbooks]** When subscribing to multiple markets in one subscription, snapshots appear to arrive as one orderbook_snapshot message per market on the same sid — not explicitly confirmed in docs.
- **[orderbooks]** Exact WS handshake signing string (which method+path is signed for /trade-api/ws/v2) — docs only say it uses 'the same API key authentication and signing path as before'.
- **[orderbooks]** Whether fractional contract counts ever occur in practice (count_fp/delta_fp have 2 decimals) or counts are always whole contracts.
- **[orderbooks]** Whether send_initial_snapshot=false suppresses the initial orderbook_snapshot on the orderbook_delta channel (the channel doc says snapshot is always sent first; the generic subscribe schema lists send_initial_snapshot default false — contradiction to resolve on demo).
- **[orderbooks]** Does the demo environment mirror prod WS behavior for seq/sid and the 10s heartbeat interval?
- **[auth-env]** Timestamp tolerance / clock-skew window for KALSHI-ACCESS-TIMESTAMP is not documented anywhere — empirically probe on demo (send requests with deliberately skewed timestamps) to find the accepted window before relying on local clocks.
- **[auth-env]** Whether RSA-PSS signatures with MAX_LENGTH salt also verify server-side, or only DIGEST_LENGTH — use DIGEST_LENGTH per docs, but verify if a non-Python library defaults differently.
- **[auth-env]** Required/accepted RSA key size (docs never state it; the UI-generated key size should be checked from the downloaded PEM).
- **[auth-env]** Whether demo rate limits match production tiers (docs do not differentiate) — measure 429 onset on demo.
- **[auth-env]** Exact path and method of the endpoint-costs endpoint (docs say 'consult the /account/endpoint_costs endpoint' — presumably GET /trade-api/v2/account/endpoint_costs) and the actual token cost of Create Quote / Delete Quote / Accept Quote / Confirm Quote for RFQ throughput budgeting.
- **[auth-env]** Exact path of the 'Upgrade Account API endpoint' for Basic→Advanced tier upgrade.
- **[auth-env]** WebSocket connection limits per API key / per account (not documented) and behavior on missed pongs (no disconnect timeout documented).
- **[auth-env]** Whether the alternative hosts (api.elections.kalshi.com, demo-api.kalshi.co) are deprecated or merely non-recommended, and whether they have different rate-limit or latency characteristics.
- **[auth-env]** Demo account creation specifics live in an external Google Slides tutorial (link on demo_env page) — whether demo signup needs a separate email from the prod account.
- **[auth-env]** Whether authentication failure returns a distinct error code/body (docs list none) — capture actual 401 payloads on demo for error handling.
- **[limits-account]** Numeric values of the WS limits behind error codes 25/26/27: per-subscription market limit, command rate limit, and outbound buffer size are all undocumented — probe on demo (e.g. subscribe with growing market_tickers arrays until error 26).
- **[limits-account]** Max concurrent WebSocket connections per account/API key and max subscriptions per connection are not documented anywhere fetched.
- **[limits-account]** Demo environment WebSocket URL is not given on the websocket-connection page (only prod wss://external-api-ws.kalshi.com/), and no doc states whether demo has the same rate-limit tiers/budgets as prod — verify GET /account/limits on demo.
- **[limits-account]** Actual contents of GET /account/endpoint_costs (which endpoints are non-default, e.g. cancel=2, RFQ create-quote/accept-quote costs, upgrade=30) — the docs never publish the table; fetch it on both demo and prod at startup.
- **[limits-account]** Whether GET /account/endpoint_costs truly requires no authentication (its docs page lists no security requirements — unusual; verify).
- **[limits-account]** Whether WebSocket subscribe/unsubscribe commands also drain the REST Read/Write token buckets or are metered only by the separate per-subscription command limiter (docs name only REST and FIX for the shared buckets).
- **[limits-account]** Exact perps limits path discrepancy: rate_limits doc says GET /account/limits/perps but llms.txt points to a margin-rest get-perps-account-api-limits page — confirm the real path if perps ever matter.
- **[limits-account]** Whether the volume denominator in the tier formula (previous_month_exchange_volume x 2) counts Predictions-only volume, and whether demo trading volume counts toward tier progression (assume no).
- **[limits-account]** Whether order amends are billed at create cost (10) or a distinct cost — 'amends' are named as writes but no explicit cost is given.
- **[limits-account]** Whether the self-service Advanced grant appears in the grants[] array of GET /account/limits with level 'advanced' (the ApiUsageLevelGrant level description only enumerates expert..prestige).
- **[money-fees]** Exact fee formula constants: is the quadratic model still fee = ceil_to_centicent(multiplier x 0.07 x C x P x (1-P))? What are the maker-fee rates under `quadratic_with_maker_fees` and the `flat` table values? Must be pulled from https://kalshi.com/docs/kalshi-fee-schedule.pdf (outside docs.kalshi.com) and verified against actual demo fill fees.
- **[money-fees]** What `fee_type` and `fee_multiplier` do sports combo/MVE series actually carry (e.g., is fee_multiplier 1.0 by default), and do MVE-collection-derived events routinely carry event-level overrides? Poll GET /series/{ticker}, /series/fee_changes, /events/fee_changes on demo/prod for the target series.
- **[money-fees]** RFQ/combo executions: are both sides charged, is the quoting maker charged maker or taker fees, and does the fee-waiver field apply? None of the fetched fee docs mention RFQ-specific fee treatment — verify empirically from demo fill records after an RFQ execution.
- **[money-fees]** Which `price_level_structure` do combo (MVE) markets use in practice (linear_cent vs tapered_deci_cent vs deci_cent), and can it differ per market within one collection? Read `price_ranges` off freshly created combo markets on demo.
- **[money-fees]** Are we a 'direct member' ($0.0001 balance precision) or 'non-direct' ($0.01)? This changes rounding-fee magnitude per fill; check actual balance deltas on demo/prod fills.
- **[money-fees]** Does GET /series/fee_changes with show_historical=false return only future-scheduled changes, or also the currently-effective one? And is the response ordered by scheduled_ts? Not specified — verify.
- **[money-fees]** GetMarket lists a 401 response but the spec has `security: []` — confirm whether market data endpoints (and the fee-changes endpoints) need auth headers in prod.
- **[money-fees]** Can `_dollars` fields in responses ever exceed 4 decimals (FixedPointDollars allows up to 6)? E.g., fee or settlement fields on fractional fills — affects Decimal parsing/validation strictness.
- **[money-fees]** Exact boundary semantics of tapered_deci_cent ranges: at the shared boundary prices ($0.10, $0.90), which step applies — is $0.105 valid? Resolve by inspecting actual `price_ranges` start/end values (inclusive/exclusive) on a tapered market.
- **[money-fees]** Does the fee accumulator survive order amends (Amend Order V2 keeps the same order?) and what happens to un-rebated accumulator remainder when an order is cancelled (forfeited?) — undocumented; measure on demo.
- **[live-data]** What is the actual schema of live_data.details per sport/type (field names for score, game clock, period/quarter, game status)? Docs say only 'flexible object' — must capture real payloads on demo/prod for each sport we quote.
- **[live-data]** What are the valid values of live_data.type (the string enumerating live-data kinds, also the path param of the legacy endpoint)? Never enumerated in docs.
- **[live-data]** What is the real update latency/refresh cadence of /live_data and /game_stats relative to the live game (Sportradar-fed)? Nothing documented — measure empirically, since it bounds how much courtside protection the feed actually provides.
- **[live-data]** Do unauthenticated live-data requests count against the account token bucket at all (rate-limits doc says 'every AUTHENTICATED request costs tokens'), and is there a separate undocumented IP-based limit for anonymous calls? Also: exact token cost per item for /live_data/batch (verify via GET /account/endpoint_costs).
- **[live-data]** How does /live_data/batch behave for unknown, expired, or mixed-valid milestone_ids — partial results, omitted entries, or error? Only a 500 error is documented, no 404.
- **[live-data]** GET /milestones documents a 401 error despite no auth requirement — confirm anonymous access works on both demo and prod, and whether demo has live-data populated at all (demo may have no real Sportradar feeds, which would force prod-based read-only testing).
- **[live-data]** Does live_data exist for milestones without a Sportradar ID (e.g., score-only vs full pbp), and does demo mirror prod's milestone IDs? Milestone IDs are environment-specific presumably — verify.
- **[live-data]** For combo/parlay legs: are event tickers for every sports leg reliably present in some milestone's related_event_tickers/primary_event_tickers, and how quickly after event creation does the milestone mapping appear (min_updated_ts polling cadence for discovering new games)?
- **[exchange-status]** RFQ/quote pause semantics are undocumented: do open RFQs and resting quotes persist through a trading pause, get auto-cancelled, or have their confirmation/execution timers frozen? Does cancel_order_on_pause apply to RFQ quotes at all, or only to regular limit orders? Needs demo-window observation (e.g., Thursday 3-5 AM ET on demo, if demo mirrors the schedule).
- **[exchange-status]** Contradiction to verify: exchange_active=false is described as 'no state changes at all', yet the maintenance doc says cancellations remain available during an exchange pause. Which wins during a real exchange pause — and does the REST API even respond?
- **[exchange-status]** During a trading pause, what exact error code/body do quote-creation and order-creation calls return? (Needed to distinguish pause rejections from other errors in the bot's error handling.)
- **[exchange-status]** Do 500/503/504 responses from /exchange/status actually populate a parseable ExchangeStatus body in practice, and what does the endpoint return mid-pause (trading_active=false confirmed? exchange_estimated_resume_time populated for scheduled vs unscheduled pauses?).
- **[exchange-status]** Is there a WebSocket message/channel announcing pause start/end (websockets/communications.md or another channel), or is polling /exchange/status the only signal? Polling rate limits on the unauthenticated status endpoint are also undocumented.
- **[exchange-status]** standard_hours selection logic: can multiple WeeklySchedule entries overlap, and can a weekday array contain multiple open/close intervals in practice? What does close_time '24:00' or an overnight session look like?
- **[exchange-status]** Does the demo environment observe the same Thursday 3:00-5:00 AM ET maintenance window and the same schedule/maintenance_windows content as prod, or does demo have its own windows?
- **[exchange-status]** Do combo/multivariate (parlay) markets share the exchange-wide standard_hours, or do they have separate per-market open/close times that override the exchange schedule?
- **[exchange-status]** Does 'cannot modify orders' during a pause include the amend/decrease endpoints only, or also cancel-replace? Confirm cancels work via all protocols (REST, WebSocket order entry if any, FIX) during a pause.
- **[exchange-status]** exchange_estimated_resume_time format specifics (timezone offset, ISO 8601 exactness) and whether it is null vs absent when no maintenance is scheduled.
- **[python-sdk]** Does creating quotes via the Communications API require special maker/market-maker program enrollment, or can any funded account quote on RFQs? Docs never say — verify on demo with a fresh key.
- **[python-sdk]** Full Quote.status enum values are undocumented (model just says 'Current status of the quote'; RFQ documents 'open, closed') — enumerate empirically from GET /communications/quotes on demo (expect something like open/accepted/confirmed/executed/cancelled).
- **[python-sdk]** confirm_quote body is 'body: Any = None' with no schema — confirm on demo whether an empty body/{} is accepted and whether any optional fields exist.
- **[python-sdk]** Clock-skew tolerance for KALSHI-ACCESS-TIMESTAMP (ms) is undocumented; SDK signs with local time.time() — measure acceptable drift on demo and decide whether to NTP-sync the bot host.
- **[python-sdk]** Whether Configuration(retries=N) in kalshi_python_async retries non-idempotent POSTs (risk: duplicate quotes) — inspect aiohttp_retry usage or test before enabling.
- **[python-sdk]** Whether the wheels actually work on Python 3.11/3.12 if the >=3.13 pin is overridden (README claims 3.9+) — matters if the team runtime is older.
- **[python-sdk]** Exact price grid ('price level structure' / price_ranges from GET /markets/{ticker}) for HVM combo markets — what tick sizes are valid for yes_bid/no_bid strings; invalid_parameters is thrown for off-grid prices.
- **[python-sdk]** Does the demo environment fully support the communications WS channel and HVM/combo RFQ flow (3s/1s timers), and does GET /communications/id work there? Demo docs warn behavior may be unrepresentative.
- **[python-sdk]** Overall rate-limit budget (bucket size/refill per tier) for communications endpoints — only 'create-quote costs 2 tokens' is documented on the pages fetched; need the rate-limits page or empirical 429 testing for quoting at RFQ-storm rates.
- **[python-sdk]** Whether quote events for RFQs you did NOT create ever appear on the WS communications channel via sharding params, and whether the anonymized creator_id from GET /communications/id matches the quote_creator_id seen in WS messages (needed for self-identification) — verify on demo.
- **[python-sdk]** Latency of POST /communications/quotes and PUT .../confirm on prod vs demo — must fit inside the 3s HVM confirmation window with margin; measure p99 on demo.
- **[python-sdk]** The async KalshiClient __getattr__ dispatch and pydantic validation overhead per call — microbenchmark whether it matters at expected RFQ rates or whether direct CommunicationsApi references suffice.
- **[openapi-comms]** Exact demo WebSocket URL for the communications channel (docs only give prod wss://external-api-ws.kalshi.com); verify demo WS host and handshake auth format on demo.
- **[openapi-comms]** Whether Fill.trade_id can be joined to public trade ids from GET /markets/trades for RFQ executions — spec now says trade_id is a legacy alias of fill_id, which contradicts the older assumption that fills and public trades share trade_id. Empirically execute an RFQ on demo and compare.
- **[openapi-comms]** Partial acceptance semantics: WS quote_accepted carries contracts_accepted_fp (example shows 50.00 accepted vs 100.00 offered) but REST AcceptQuoteRequest has only accepted_side with no count — determine how accepted size is set (target_cost mode conversion? requester-side partial?) and what yes_contracts_fp vs no_contracts_fp mean per sizing mode.
- **[openapi-comms]** Where the maker's client_order_id in the WS quote_executed message comes from — CreateQuoteRequest has no client_order_id field; verify whether it is auto-generated (e.g. equals quote id) on demo.
- **[openapi-comms]** The rate-limit token cost of CreateRFQ, AcceptQuote, ConfirmQuote, DeleteRFQ (no x-mint notes in spec — presumably default 10, but CreateQuote is 2); pull GET /account/endpoint_costs on demo for the authoritative table.
- **[openapi-comms]** Exact spelling of the RFQ guide's timing classification: whether ALL MVE/combo markets are always HVM (3s confirm / 1s execution) or the class can vary per market; measure actual accept->cancel timing on demo.
- **[openapi-comms]** What price/size the post-execution book orders rest at and how rest_remainder interacts with partial execution ('Orders enter the public book after the execution timer expires') — verify resulting Order objects (price, remaining_count, post_only effect) after an RFQ execution on demo.
- **[openapi-comms]** Whether replace_existing=true on CreateRFQ deletes ALL of the caller's open RFQs or only the one on the same market ticker.
- **[openapi-comms]** Whether quotes and RFQ/quote responses on combos respect price_ranges steps finer than $0.01 (FixedPointDollars allows 6 dp) — fetch a real combo market's price_ranges on demo and test off-grid quote rejection (expected 400).
- **[openapi-comms]** Whether Quote.creator_order_id / rfq_creator_order_id are populated at accepted/confirmed status or only after executed — needed to know when the fills poller can start filtering by order_id.
- **[openapi-comms]** Whether is_taker on the maker's fill from an RFQ execution is false (maker) — memory says RFQ fills appear as REGULAR trades; confirm fee treatment (fee_cost) for the quoting side on demo.
- **[openapi-comms]** GET /communications/rfqs 'status' query values — spec types it as a bare string (RFQ.status enum is open|closed); confirm accepted filter values (e.g. 'open') empirically.
- **[asyncapi-ws]** seq scope: the spec says seq guarantees message continuity 'for snapshot/delta consistency' but never states whether seq is per-sid (across all markets in one subscription) or per-market. Verify on demo with a multi-market orderbook_delta subscription before writing gap detection.
- **[asyncapi-ws]** Numeric limits behind error codes 26 (per-subscription market limit) and 27 (per-subscription command rate limit) are undocumented — probe on demo (how many tickers per orderbook_delta subscription; how fast can update_subscription be called).
- **[asyncapi-ws]** KALSHI-ACCESS-TIMESTAMP skew tolerance for the WS handshake is undocumented ('no explicit expiry window'); verify how stale a signature can be, and whether the signed path must exclude query strings.
- **[asyncapi-ws]** fill.fee_cost is required in the schema but absent from the spec's own example payload; verify it actually appears on maker fills (and whether it's 0-valued vs omitted for fee-free maker executions).
- **[asyncapi-ws]** Whether quote_accepted arrives to the maker BEFORE quote_executed and what the timing gap is (accepted has no timestamp field); also whether an accepted-then-not-executed path exists (RFQ creator backing out) — determines state machine design.
- **[asyncapi-ws]** rfq_created.creator_id 'currently empty' — verify whether rfq_deleted.creator_id can be joined back to the rfq id reliably, and whether creator anonymized IDs are stable across RFQs (needed for toxic-flow profiling of repeat requesters).
- **[asyncapi-ws]** Does rfq_deleted fire when an RFQ is fully executed (in addition to explicit cancel/expiry)? The spec gives no reason field — verify the lifecycle empirically to avoid quoting dead RFQs.
- **[asyncapi-ws]** mve_selected_legs.side is typed as plain string (not the yes/no enum) and yes_settlement_value_dollars semantics (is it per-leg payout for non-binary legs?) — pull real MVE RFQs on demo/prod recording to confirm shapes.
- **[asyncapi-ws]** Whether ticker channel messages for MVE combo markets flow on `ticker` like normal markets (spec is silent on MVE coverage of orderbook/ticker/trade channels).
- **[asyncapi-ws]** Interaction of shard_factor/shard_key with quote-event visibility: confirm your own quote events still arrive if the RFQ hashes to a different shard than the one your connection subscribed to (spec doesn't say sharding applies only to rfq_created/deleted).
- **[asyncapi-ws]** The asyncapi.yaml says deprecated direction fields won't be removed before May 14, 2026, while the order_direction page says May 28, 2026 — treat earlier date as binding; confirm current behavior on demo.
- **[asyncapi-ws]** Demo server is absent from asyncapi.yaml (only production listed) — confirm demo supports all channels used (communications, multivariate_market_lifecycle) since demo often lags prod features.
- **[index-scan]** MveSelectedLeg object fields are never defined in any fetched page (get-market lists mve_selected_legs: array[MveSelectedLeg] without a schema) — pull openapi.yaml or hit a live KXMVE market on demo to learn exact leg field names (event_ticker/market_ticker/side?)
- **[index-scan]** HVM (high volatility market) rules: zero mentions in the entire docs index, glossary, or changelog — does the concept exist at the API level at all (e.g., forced pauses, wider bands), or only in the exchange rulebook? Needs empirical/legal-doc verification
- **[index-scan]** WebSocket connection URL ambiguity: channel pages show wss://external-api-ws.kalshi.com with per-channel 'addresses' (e.g. /fill), but the classic API uses a single WS endpoint with subscribe commands listing channels — verify actual connect URL + subscribe frame on demo (base websockets.md is in the other agent's set)
- **[index-scan]** Does Decrease Order (V2) preserve queue priority? Strongly implied by the amend rule ('preserves only when decreasing size') but never stated for the decrease endpoint itself
- **[index-scan]** self_trade_prevention_type semantics: what exactly happens under taker_at_cross vs maker when your quote would cross your own resting order (cancel which side? partial?) — not defined on any fetched page
- **[index-scan]** Subaccount creation eligibility conflict: endpoint page says 'only institutions and market makers'; changelog 2026-05-12 says 'all direct members with advanced API access' — test POST /portfolio/subaccounts on demo
- **[index-scan]** What does subaccount netting enabled/disabled actually change (position netting across yes/no? settlement behavior?) — GET/PUT /portfolio/subaccounts/netting exists but mechanics are undocumented
- **[index-scan]** GET /markets default behavior when mve_filter is omitted: are KXMVE combo markets included, excluded, or mixed in? Affects any market-scan loop
- **[index-scan]** Exact numeric rate limits per tier (refill_rate/bucket_capacity for advanced/expert/premier...) are not published in docs — must read GET /account/limits for our own account on both demo and prod, plus GET /account/endpoint_costs for the full non-default cost table (are RFQ create / quote confirm / accept non-default?)
- **[index-scan]** outer `type` field on the multivariate_market_lifecycle channel: schema says const 'market_lifecycle_v2' but the example shows 'multivariate_market_lifecycle' — confirm which string actually arrives before writing the parser
- **[index-scan]** Do combo (KXMVE) events emit event_fee_update messages, and on which channel, given KXMVE is excluded from market_lifecycle_v2?
- **[index-scan]** Maximum number of concurrent order groups per account/subaccount is not documented
- **[index-scan]** Legacy field removal dates disagree (fills/orders pages say 'not before May 14, 2026'; order_direction says 'not before May 28, 2026') and both dates have passed as of 2026-07-05 — verify empirically whether side/action/is_yes still arrive and never depend on them
- **[index-scan]** Fill WS example shows yes_price_dollars '0.750' (3 decimals) vs stated 4–6 decimal conventions elsewhere — confirm decimal emission per field on demo before strict-parsing
- **[index-scan]** V2 cancel/amend 'response correctness' changelog entry (2026-05-21) is ambiguous about when the response fails to describe the affected order — test canceling a partially-filled order on demo and compare reduced_by against WS state
- **[index-scan]** Whether GET /portfolio/positions supports min/max_ts or settlement filtering for combos, and whether event_positions aggregate across an MVE event's markets the same way as regular events
- **[index-scan]** Incentive period_reward units stated as centi-cents — confirm (100 centi-cents = 1 cent?) before using in EV math

## Gap check — resolved
- **(1) Exact string signed for RSA auth and padding** → RESOLVED via https://docs.kalshi.com/getting_started/quick_start_authenticated_requests.md. Sign the concatenation timestamp_ms + UPPERCASE_METHOD + path, where path is the FULL path from root INCLUDING the /trade-api/v2 prefix and with query params stripped (doc code does path.split('?')[0]); example signed string: '1703123456789GET/trade-api/v2/portfolio/balance'. Body is never signed. Algorithm: RSA-PSS with SHA256, MGF1(SHA256), salt_length = padding.PSS.DIGEST_LENGTH (32 bytes) — NOT MAX_LENGTH, NOT PKCS1v15 — then standard base64. Headers: KALSHI-ACCESS-KEY (key id), KALSHI-ACCESS-TIMESTAMP (unix MILLISECONDS as string), KALSHI-ACCESS-SIGNATURE. WebSocket handshake uses the same 3 headers with signed string timestamp + 'GET' + '/trade-api/ws/v2'. Clock-skew tolerance remains undocumented (see unresolved).
- **(2a) Quote price fields' fixed-point format** → RESOLVED via https://docs.kalshi.com/api-reference/communications/create-quote.md. Request body fields are exactly rfq_id, yes_bid, no_bid, rest_remainder (required bool), post_only (optional), subaccount (optional); yes_bid/no_bid are 'US dollar amount as a fixed-point decimal string with up to 6 decimal places of precision' (e.g. '0.5600'). Responses rename them to yes_bid_dollars/no_bid_dollars. Grid rule is documented in principle: create-quote says 'valid quote intervals for a given market are constrained by that market's price level structure', and https://docs.kalshi.com/getting_started/rfqs.md says 'Prices must land on the market's price grid. Check price_ranges on GET /markets/{ticker} for the valid step size.' PriceRange schema (from https://docs.kalshi.com/api-reference/market/get-market.md): {start, end, step} dollar strings, possibly multiple ranges. Bonus: get-market.md confirms GET /markets/{ticker} has security: [] — NO auth needed, so the bot can validate grids unauthenticated. Which structure combo markets actually use is NOT documented (see unresolved).
- **(3) Whether maker can explicitly decline after accept** → RESOLVED-NEGATIVE from three docs: no decline-after-accept mechanism exists anywhere in the documentation. https://docs.kalshi.com/api-reference/communications/delete-quote.md and delete-rfq-quote.md (both DELETE, 2 tokens, functionally identical descriptions) describe deletion only as making a quote 'no longer be accepted' (pre-accept framing) and document only 401/404/500 — no 409. https://docs.kalshi.com/fix/rfq-messages.md shows QuoteCancel (35=Z) applies to active quotes before acceptance, and 'Quote must be confirmed within 30 seconds of acceptance or it will be voided' with silent expiration (no void message). https://docs.kalshi.com/getting_started/rfqs.md: 'Once confirmed, neither party can withdraw.' Design conclusion: the ONLY maker escape after acceptance is deliberately not calling confirm and letting the 3s HVM window lapse. Whether DELETE happens to succeed in accepted status is an undocumented edge (see unresolved) but must not be relied on.
- **(4) How the combo market ticker/orderbook relates to the RFQ — is there even a book?** → MOSTLY RESOLVED. Yes, there is a real public order book: https://docs.kalshi.com/getting_started/rfqs.md states 'After the execution timeout, orders are placed on the public book.' Combo markets are ordinary Market objects that must be pre-created via POST /multivariate_event_collections/{ticker} ('must be hit at least once before trading or looking up a market'); the RFQ's market_ticker is that created MVE market. The MveSelectedLeg schema (previously undocumented) is now resolved via https://docs.kalshi.com/api-reference/market/get-market.md: {event_ticker: string, market_ticker: string, side: string, yes_settlement_value_dollars: FixedPointDollars nullable — 'The settlement value of the YES/LONG side of the contract in dollars. Only filled after determination'} — i.e. it is null on live RFQ legs, so do not depend on it for pricing. GET /markets/{ticker} and GET /markets/{ticker}/orderbook are usable on combo tickers (get-market is unauthenticated). Whether orderbook_delta WS accepts MVE tickers and whether combo books carry standing liquidity remains empirical.
- **(5) Fees on RFQ executions — general formula and maker rate** → PARTIALLY RESOLVED (secondary sources only — authoritative PDF is unreachable from this environment). No page on docs.kalshi.com documents RFQ-specific fee treatment; docs only expose fee_type (quadratic|quadratic_with_maker_fees|flat) + fee_multiplier on Series and point to the PDF. Web-search-corroborated formulas: taker fee = round_up(0.07 x C x P x (1-P)) dollars; maker fee (on series that have maker fees) = round_up(0.0175 x C x P x (1-P)). Sources: [help.kalshi.com Fees article](https://help.kalshi.com/en/articles/13823805-fees), [pm.wiki Kalshi fees](https://pm.wiki/learn/kalshi-fees-explained), [Maker/Taker Math on Kalshi](https://whirligigbear.substack.com/p/makertaker-math-on-kalshi). I attempted https://kalshi.com/docs/kalshi-fee-schedule.pdf and https://kalshi.com/fee-schedule repeatedly: both return HTTP 429 behind a Vercel Security Checkpoint (confirmed by downloading — got the checkpoint HTML, not the PDF). The PDF must be pulled manually in a browser; which side of an RFQ execution pays maker vs taker fees is empirical (see unresolved).
- **(6) Fills reconciliation fields** → RESOLVED via https://docs.kalshi.com/api-reference/portfolio/get-fills.md. REST Fill fields: fill_id, trade_id ('legacy field name, same as fill_id' — it is NOT a join key to public trades), order_id, ticker (market_ticker is the legacy alias), outcome_side (yes|no), book_side (bid|ask), count_fp, yes_price_dollars, no_price_dollars, is_taker, fee_cost (FixedPointDollars), created_time, subaccount_number (nullable int), ts (legacy int64); deprecated side/action retained until at least 2026-05-14. CRITICAL: REST Fill has NO client_order_id — the reconciliation join is WS quote_executed.order_id (== Quote.creator_order_id for the maker) -> GET /portfolio/fills?order_id=X (params: ticker, order_id, min_ts, max_ts unix seconds, limit 1-1000 default 100, cursor, subaccount; omitted subaccount = ALL subaccounts). The WS 'fill' channel additionally carries client_order_id (optional) and post_position_fp, so prefer WS fills as source of truth and REST as reconciliation.
- **(7) WS subscribe auth for communications channel** → RESOLVED. Auth is connection-level ONLY: the three KALSHI-ACCESS-* headers go on the HTTP upgrade request (https://docs.kalshi.com/websockets/websocket-connection.md: 'include API key headers during the WebSocket handshake'), signature over timestamp_ms + 'GET' + '/trade-api/ws/v2'. There is no per-subscribe auth. Subscribe frame: {"id": <client-unique int>, "cmd": "subscribe", "params": {"channels": ["communications"]}}; per https://docs.kalshi.com/websockets/communications.md market specification is IGNORED on this channel (global RFQ firehose) and optional sharding is shard_factor (1-100) + shard_key (0 <= key < shard_factor). quote_* events arrive only to participants; quote_executed carries no price/size (join to fills via order_id).
- **(8) Demo environment URLs for RFQ/combo work** → PARTIALLY RESOLVED. Demo URLs are now officially documented (https://docs.kalshi.com/getting_started/demo_env.md and https://docs.kalshi.com/getting_started/api_environments.md): REST https://external-api.demo.kalshi.co/trade-api/v2 (legacy https://demo-api.kalshi.co/trade-api/v2), WS wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2 (legacy wss://demo-api.kalshi.co/trade-api/ws/v2) — this closes the 'demo WS URL undocumented' question from the earlier sweep; 'external-api' hosts are the recommended ones, elections/demo-api are 'Legacy' (not deprecated). Credentials are fully separate per environment. Caveat kept open: no doc states whether the communications channel, RFQ flow, or MVE market creation actually function on demo ('The price and behavior of markets in the demo environment may not be reflective of those in real markets').
- **(bonus) accepted_side economics** → SUBSTANTIALLY RESOLVED via https://docs.kalshi.com/fix/rfq-messages.md: 'For AcceptQuote, BUY accepts the maker's NO quote and SELL accepts the maker's YES quote' (requester buying YES hits the maker's NO bid; requester selling YES / buying NO hits the maker's YES bid), and QuoteStatusReport carries AcceptedSide 1=Yes 2=No. This confirms accepted_side names the MAKER's bid that was hit: accepted_side='yes' => maker buys YES at yes_bid, requester takes NO; accepted_side='no' => maker buys NO at no_bid, requester takes YES. REST AcceptQuoteRequest (accept-quote.md) has ONLY accepted_side — no count field, so no requester-chosen partial size via REST accept. Per-party effective fill prices (requester pays 1 - maker_bid?) still deserve one demo round-trip check.
- **(bonus) accept-best-quote / PreferBetterQuote existence** → RESOLVED via https://docs.kalshi.com/fix/rfq-messages.md: FIX Tag 21022 PreferBetterQuote on AcceptQuote — 'When set to Y, the exchange will select the best available quote for the RFQ rather than the specified quote. The best quote must be at least as good as the requested quote'; response tag 21024 AcceptedQuoteId may differ from the requested QuoteId. REST accept-quote has no such flag, but since FIX accepters can use it, the maker bot MUST treat every open quote as instantly executable at any moment — an accept naming a competitor's quote can land on yours if yours is better.
- **(bonus) confirm_quote request body shape** → RESOLVED via https://docs.kalshi.com/api-reference/communications/confirm-quote.md: PUT /communications/quotes/{quote_id}/confirm, request body 'required: false' with empty schema (send no body or {}), success = 204, and 'This will start a timer for order execution'. Documented errors are only 401/404/500 — the specific error for a lapsed window is not documented (assume 404 until measured).
- **(bonus) rate-limit bucket capacities table** → RESOLVED via https://docs.kalshi.com/getting_started/rate_limits.md: per-tier refill/capacity — Basic 200 read/100 write (caps 200/100, i.e. ZERO write burst), Advanced 300/300 (caps 300/600), Expert 600/600 (600/1200), Premier 1000/1000 (1000/2000), Paragon 2000/2000 (2000/4000), Prime 4000/4000 (4000/8000), Prestige 6000/8000 (6000/8000); 'Write bucket capacity is twice the per-second budget above the Basic tier.' Create/delete/get quote = 2 tokens each (from their endpoint pages); accept/confirm/create-RFQ costs are NOT published — GET /account/endpoint_costs is named as the authoritative list.

## Gap check — unresolved (empirical verification queue)
- **Which price_level_structure and price_ranges combo (MVE/KXMVE) markets actually use — the concrete tick grid the quoter must validate yes_bid/no_bid against (linear_cent vs deci_cent vs tapered_deci_cent, and boundary semantics at $0.10/$0.90)** — Empirical, cheap and unauthenticated: GET /markets/{ticker} (security: [], no auth) on several live KXMVE combo tickers on prod (discover via GET /markets?mve_filter=only or GET /events/multivariate?with_nested_markets=true) and read price_level_structure + price_ranges verbatim; then on demo, POST an off-grid quote (e.g. '0.3550' on a linear_cent market) and record the exact 400 body for the bot's error handling.
- **Whether DELETE /communications/quotes/{quote_id} succeeds, 404s, or 409s while the quote is in 'accepted' status (i.e. is there a de-facto explicit decline), and what status/cancellation_reason the quote shows after a deliberately lapsed 3s confirmation window** — Demo round-trip with two demo accounts (or one account + subaccount if RFQ self-trade is allowed): create RFQ from account A on a pre-created combo market, quote from B, accept from A, then (run 1) attempt DELETE from B within the window and record the HTTP result; (run 2) let the window lapse and GET /communications/quotes to capture the terminal status enum value and any cancellation_reason field. Also enumerates the undocumented Quote.status values.
- **RFQ execution fee treatment: is the quoting maker charged maker or taker fees (is_taker on the maker's fill), what fee_type/fee_multiplier sports-combo series carry, and verification of the 0.07/0.0175 coefficients — the authoritative fee-schedule PDF (https://kalshi.com/docs/kalshi-fee-schedule.pdf) is behind a Vercel Security Checkpoint and could not be fetched from this environment (HTTP 429 on every attempt including curl with browser UA)** — Two steps: (a) user downloads the PDF manually in a browser and extracts the General/Maker/Specific fee tables; (b) demo: execute one RFQ round trip and read the maker-side fill's is_taker and fee_cost from GET /portfolio/fills, plus GET /series/{ticker} and /series/fee_changes + /events/fee_changes for the target combo series to get fee_type/fee_multiplier and any waivers (also check Market.fee_waiver_expiration_time on fresh combo markets).
- **rest_remainder maker-side semantics: REST says 'rest the remainder after execution', FIX Quote tag 21015 says 'Allow partial fills (default: N)' — whose order rests on the public book, at what price, is execution all-or-none with false, and how does post_only interact** — Demo matrix test: run RFQ executions with rest_remainder true/false x post_only true/false on a target_cost RFQ sized so a remainder exists; after each execution, GET /portfolio/orders (V2) for both parties and inspect resting order price/remaining_count/post_only. Docs cannot resolve this — the two descriptions genuinely conflict.
- **target_cost_dollars -> contract count conversion formula (yes_contracts_fp vs no_contracts_fp per quote price) and whether contracts_accepted_fp can differ from offered size (partial acceptance path), given REST accept has no size field but WS quote_accepted carries contracts_accepted_fp** — Demo: create a target_cost RFQ (e.g. '50.00'), post quotes at several yes_bid/no_bid levels, and GET each quote to read back yes_contracts_fp/no_contracts_fp — regress the formula (candidates: target/bid vs target/(1-opposite_bid)). Then accept and compare contracts_accepted_fp vs offered. rfqs.md only says 'The exchange derives a contract count from the quote price' with no formula.
- **Demo support for the whole combo RFQ path: does the communications WS channel deliver on demo, does MVE market creation work there, is there any RFQ traffic, and are the HVM 3s/1s timers live on demo (plus the exact error a late confirm returns and whether the 3s window is measured server-side from acceptance)** — Empirical only — no doc addresses demo feature parity ('behavior of markets in the demo environment may not be reflective'). Connect wss://external-api-ws.demo.kalshi.co/trade-api/ws/v2, subscribe communications, log for 24h; POST a market into a demo MVE collection; run the two-account round trip and timestamp accept->void to measure the real window; deliberately confirm at t+4s and capture the error body.
- **Clock-skew tolerance for KALSHI-ACCESS-TIMESTAMP (REST and WS handshake) — quick_start_authenticated_requests.md gives the signing recipe but no expiry window** — Demo probe: send GET /portfolio/balance with timestamps deliberately skewed +/-1s, 5s, 30s, 2min, 10min and bisect the 401 boundary; NTP-sync the bot host regardless. Also capture the actual 401 payload for error handling (undocumented).
- **Whether orderbook_delta / ticker / trade WS channels accept MVE combo tickers (courtside hedging and book-monitoring of our own rested remainders depends on it) — asyncapi spec is silent on MVE coverage of market-data channels** — Demo/prod: subscribe orderbook_delta with a known KXMVE ticker and check for subscribed ack + snapshot vs error code; same for ticker/trade. Fallback if unsupported: REST GET /markets/{ticker}/orderbook polling (verify its token cost per item via endpoint_costs).
- **Actual token costs for AcceptQuote, ConfirmQuote, CreateRFQ, DeleteRFQ and the batch/per-item billing of GET /markets/orderbooks — only create/get/delete quote (2 tokens) are published; confirm latency budget also unmeasured (confirm must fit in 3s minus one-way latency)** — Runtime: GET /account/endpoint_costs on demo AND prod at bot startup (also settles whether it needs auth) and cache the table; measure p50/p99 latency of POST /communications/quotes and PUT .../confirm from the production host region against demo, since no doc gives latency figures.
- **CreateMarketInMultivariateEventCollection idempotency: does re-creating an existing combination return the existing ticker or error, and does a repeat count against the 5000/week limit (also whether the limit is per key, member, or account and its reset boundary)** — Demo: call the endpoint twice with identical selected_markets and diff responses/status codes; the endpoint page documents only 400/401/429/500 with no idempotency statement. The limit scope likely needs a Kalshi support question if multiple keys are planned.
- **Quote/RFQ TTLs: whether quotes have any independent expiry before acceptance and what RFQ auto-expiry lifetime produces the 'expired' RFQ_CLOSED reason — no lifetime constant anywhere in docs; also 7-day retention of closed RFQs/cancelled quotes means history vanishes** — Empirical: leave a quote and an RFQ untouched on demo and record rfq_deleted/status transitions over hours; regardless of findings, persist every RFQ/quote locally at creation time (docs confirm durable queryability of quotes only after acceptance, 7-day retention).
- **Whether rfq_deleted fires when an RFQ is fully executed (vs only cancel/expiry), and whether rfq_created.creator_id staying empty is permanent — both drive the bot's RFQ lifecycle state machine and toxic-flow profiling** — Empirical on demo/prod recording: correlate quote_executed events with subsequent rfq_deleted for the same rfq id; log any non-empty creator_id occurrences. GET /communications/id can be called on demo to see if our own anonymized id matches quote_creator_id in our WS quote events (undocumented mapping).
