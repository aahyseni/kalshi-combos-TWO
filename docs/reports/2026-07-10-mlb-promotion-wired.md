# MLB promotion WIRED — 9/9 classification + 32-entry pair table live in config

**Date:** 2026-07-10 ~01:50 UTC · **Commits:** `4c57f86` (promotion) + `85d02cb`
(dropped-entry fix) · **Operator directive:** top-to-bottom — classification 9/9,
measure the last pairs, wire all of it; resolvers + containment next.

## What shipped

| piece | before | after | verification |
|---|---|---|---|
| Classification | 3/9 families (props UNKNOWN) | **9/9** — 6 new LegTypes + 11 ordered keywords (5 blockers, MLBHRR before MLBHR) | 87 legtypes tests; 11,305-series universe diff = exactly the 11 intended changes, zero false positives (now regression-guarded) |
| Live misclass bugs | KXMLBF5TOTAL/F5SPREAD/SERIESGAMETOTAL typed as full-game TOTAL/SPREAD | **fixed** (blockers → UNKNOWN) | dedicated regression tests |
| Pair table | 4 mlb entries | **32 entries + 32 bands, zero orphans** | forwarding test + integration tests through `build_sgp_correlation` |
| Final 6 pairs | labeled priors | **measured** (Retrosheet 2005-25, 20.8M pair-units; judge-confirmed, copula parity ≤5.6e-5) | one-factor coherence ±0.005 across the whole matrix |
| Full suite | 984 | **1013 passed / 0 failed**; mypy --strict clean; `ruff check src tests` fully clean | fresh runs |

## The wired table (32 entries)

- **[A] orientation-free measured (10):** ks|total **−0.25** (ladder-flat → one
  entry serves every K rung), hr|total **+0.24**, hit|total +0.25, tb|total
  +0.27, hrr|total **+0.40**, rfi|total **+0.37**, ml|rfi 0.00, ks|rfi −0.10,
  ks|ks +0.04, spread|total +0.13 (fallback-only).
- **[B] same-family unrouted (4):** hr|hr +0.03, hit|hit 0.00, tb|tb 0.00,
  hrr|hrr +0.08 — sign-spanning bands until teammate/opponent routing ships.
- **[C] resolver-gated neutralized (7):** ml|ks, ml|hr, ml|hit, ml|hrr, hit|ks,
  hr|ks, hrr|ks all 0.00 with bands 0.12–0.40 spanning the measured signed
  values (±0.24/±0.23/±0.37, facing −0.13/−0.075/−0.18) — commented in config,
  unlock with the ML-orientation resolver.
- **[D] cross-family measured (7):** hit|hr +0.01, hit|hrr +0.04, hit|tb +0.02,
  hr|hrr +0.03, hr|tb +0.02, hrr|tb +0.04, ks|tb −0.06 (judge re-centered).
- **Deliberately absent:** moneyline|spread (containment-shaped ±0.95 by side —
  no unoriented scalar is right; TODO(route) comment; first containment-phase target).
- Existing 4 (ml|total −0.05, extras pairs, ml|ml −0.95) unchanged.

**Live dispatch proof (spot checks 6/6):** same-game KS×TOTAL now returns
**ρ=−0.25 band 0.12** (was +0.6/0.90 — sign-wrong by ~0.85); 8-leg all-NO HR
basket prices all 28 pairs at +0.03/0.06 (was the +25–35¢ overbid); soccer
btts|total 0.70 and NFL ml|spread 0.88 byte-untouched.

## Catches during wiring (why the verification layers exist)

1. **pair_key sort trap:** the doc's label `rfi|player_ks` sorts to
   `player_ks|rfi` — a hand-copied key would have silently never matched. All
   keys were generated by executing the live helper.
2. **Dropped entry:** `player_hr|player_hrr` fell between the two judge lists —
   caught by a completeness re-count after the workflow, wired in `85d02cb`.
3. **ks|tb double-listing** ([C] neutralized vs judge's re-centered [D] −0.06)
   — resolved in favor of the judge per mission precedence, commented in config.

## Still gated / next

- **Tape-replay backtest** (`tools/backtest_mlb_pairs.py`) — the validation
  gate on the promoted config (rule 8b). Not yet built.
- **Resolver phase:** ML-team orientation (unlocks the [C] signed values) +
  teammate/opponent routing (upgrades [B]/[D] to split values — routed values
  already in config comments).
- **Containment phase:** ml|spread family, same-player cross-stat containments
  (HR⇒HIT/TB≥4/HRR≥3), nested-band handling (see the ladder-rule report).
- Quoting exposure unchanged: prod = observe-only; demo quote fills remain
  convention-gated (combo_no_pays_complement, settlement watch tonight).

## NEXT STEPS

- **Next session:** build + run the MLB tape backtest (staged-vs-promoted is
  now live-vs-history: replay recorded MLB RFQs, compare priced fairs vs
  clearings/settlements against the old flat-0.6 baseline).
- **Then:** resolver phase → containment phase, per operator sequencing.
- **Owner (operator):** none blocking tonight; LAA settlement watch stands.
