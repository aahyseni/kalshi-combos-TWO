# Reports channel — combomaker (kalshi-combos-TWO)

Dated log of every crucial **test, update, and finding**. Purpose: if this
session compacts or a new Claude/operator session opens, this channel makes them
**100% informed** on what has been done, what is in flight, and what is blocked —
without re-deriving anything.

**STANDING RULE (operator directive 2026-07-08): keep this channel current.**
Every crucial update, test, or finding gets a dated `.md` here *as it happens* —
not batched at the end. One file per test/update, newest indexed first. Cross-link
`CLAUDE.md` (agreement), `NOTES.md` (exchange mechanics + assumption audits),
`docs/calibration/` (prior-setting). This is the reporting-discipline rule; it is
also recorded in `CLAUDE.md` and operator memory so it survives compaction.

Naming: `YYYY-MM-DD-<sport-or-scope>-<what>.md`. Each report ends with a
**NEXT STEPS** footer (who owns what, decisions owed).

---

## Master status (newest first)

| Date | Area | State | One-line |
|------|------|-------|----------|
| 2026-09-05 | **BUILD: communications FAN-OUT SHARDING — the fills lever (`build/ws-fanout-sharding`, worktree; bot live on main `f55fa43`, store read-only)** | After reader isolation the 17:59 ET boot had **0** exchange-side code-25 drops (was ~1/min) and BOTH accepts STILL expired (`accept_to_confirm_ms` 547/666, `dispatch_delay_ms` 8/137, RTT 55-60 of the 3,000 ms window): the accept was late BEFORE the process. Pipe lag (rfq created_ts → us) p50 6.4 s / p90 8.6 s / 88 % > 3 s at 22:02Z (my read-only store read, matching the investigator's), inbound ≈ 1,200 frames/s on the ONE unsharded subscription (72,694 `seq` in 60.5 s between code-25s), market lane at 20,000 26× in 18 min. **Mechanism:** Kalshi's documented fan-out sharding (asyncapi-ws.md §3.2, communications-ws.md:65 "one connection per shard_key"): `CommsFanout` = a `WsManager` HOST (shared `_Lanes`, ONE dispatcher, the intake's unchanged interface) over N FOLLOWER sockets (own reader thread each, `shard_factor=N, shard_key=k`; N=1 = today's bytes). **Recovery rule:** terminal 10/17/25 on ONE shard → reconnect that shard, purge only its market frames, NO cancel_all; every shard lost at once → forwarded → cancel_all + reconnect all (today's rule, N=1 identical); on_disconnect coalesced per loss epoch; codes 19-22 (+11) → LOUD fallback to unsharded. **N DERIVED** (no yaml number): `clamp(ceil(Q_hi(inbound) × 2.0 / Q_lo(capacity per connection)), 1, 100)` from per-shard-per-minute reader measurements (frames, handling time, rfq pipe lag; a violating shard's capacity = its read rate, else rate/utilisation; snapshot windows excluded), pooled over retained boots in `ws_fanout_tape.json`; anchors = z ladder DAILY rung (3) + the stall wall's `_MARGIN` 2.0, pinned; today's numbers ⇒ **N = 3**; bootstrap = unsharded 2 windows; growth live, shrink at next boot; override only as a logged source. **Telemetry:** `ws_inbound_rate`, `ws_pipe_lag` (rfq_created + quote_created proxy — quote_accepted has no server ts), alarm `pipe_lag_exceeds_confirm_window` (p50 > 3,000 ms), `ws_fanout_derivation`. **ws.py fix found by the fake exchange:** pending subscribe acks now reserved BEFORE the send (a one-hop ack was dropped). Gates: existing ws tests 58/58 unchanged, +23 new (fake sharded exchange, exactly-once, priority-first, per-shard recovery, fallback, derivation, tape, alarm, live growth), full suite **4,212/0** (3 deselected, 334 s) and, after merging main's `f750924` (`ad06212`), **4,215 passed / 0 failed / 3 deselected in 296 s (= main's 4,192 + 23 new)**, vitals fast **8/8 GREEN** from the snapshot, ruff/mypy clean on touched files. Relight checklist: pipe lag p50 < 3 s, expired ~2-5 %, shed 0, drops 0. **REVIEW-FIX PASS (same day, `e1f6994`; verdict SHIP_WITH_FIXES, 1 must + 9 should):** must-fix — ANY non-terminal answer to a SHARDED subscribe (code 6 "Already subscribed" left a shard connected-but-unsubscribed with health green) is now a loud unsharded fallback; loss epoch closes on the casualties' re-ACK (staggered deaths = one `on_disconnect`); a live re-shard purges the retired backlog; cross-shard dedupe of quote events by (type, quote_id) under N>1 (a broadcast duplicate accept would re-confirm → `confirm.failed` → halt counter); live growth deferred while an accept is in flight; SHRINK GATE (tape v2 `sustained` evidence: never shrink below what a connection demonstrably carried — kills the review's 1→2→1 boot oscillation once violating evidence is pruned); `_Lanes.pop` binds under the lock; `FANOUT_Z` imported; refusal RECORDED in the tape (`boots_refused`) but not honoured across boots (argued in the report). Gates: fan-out tests 33/33 (+10), ws tests 58/58, suite 4,225 passed / 0 failed / 3 deselected in 303.6 s (= the merged tree's 4,215 + the 10 new tests exactly), vitals fast 8/8 GREEN (GATE PASS, 89.5 s). → `2026-09-05-build-ws-fanout-sharding.md` §Review fixes |
| 2026-09-05 | **DAY: "constantly restarting + 0 fills" — 8 confirm-halts (counter never reset, `400 expired` counted) FIXED; code-25 socket drops (~1/min, cancel_all each) FIXED by the reader thread; RELIT 17:59 ET on `f55fa43`; 0 drops / 0 halts BUT 0 fills — EXCHANGE PIPE LAG p50 6.4 s (85% > the 3 s confirm window) = one unsharded firehose > one connection drain → FIX = Kalshi fan-out sharding MERGED `022f083` (review SHIP_WITH_FIXES → fix `e1f6994`; suite 4,225/0) — RELIT 20:37 ET on the ROTATED store (213 GB → 316 MB in 7 s, ledger whole, verify ok) — **PROVEN**: at N=1 pipe lag p50 3.7–5.6 s / 68–92% > 3 s and 3/3 accepts expired; live re-shard 1→3 at 20:56:48 (Kalshi accepted 3 sharded sockets in 30 ms) → **p50 1.0 s / 0% > 3 s, 3/3 accepts filled**; inbound ~3,000 fps Saturday night. Both earlier deaths were operator stops. Next levers: main-loop saturation (raw pre-filter), tape writer to a thread (queue pinned 200k, 126k drops on the FRESH store = not store size), governor cadence 3.5–6.5 min not 60 s** | Verification 18:00–18:15 ET: sends 295–398/min, loop lag ≤0.7 s, both accepts `confirm_expired_by_exchange` (our path 0.55/0.67 s), market lane sheds 4,500 `rfq_created`/window + 21,745 stale-dropped; N shards DERIVED from measured inbound vs drain; single-shard loss ≠ cancel_all; `pipe_lag` telemetry + derived alarm. Store rotation `--apply` = operator `!` command at the stop. **ALSO FOUND + REPAIRED `f750924`: the risk denominator DOUBLE-COUNTED shard 1's PV** (base `/portfolio/balance` PV is already the whole book; the 8/17 merge added idx1 again → equity read $6,666 vs exchange $5,061; caps +31% tonight, ≈+70% at the 9/5 peak book; `portfolio_haircut 1.0`). [report](2026-09-05-crash-chain-relight-and-pipe-lag-finding.md) |
| 2026-09-05 | **BUILD: confirm-halt CLASSIFIER + DERIVED maintenance stall wall (`build/confirm-halt-and-derived-wall`, worktree; bot on main, store read-only)** | **EIGHT** `halt_confirm_timeouts` today (04:50Z, 13:04Z, 13:41Z, 15:40Z, 16:11Z, 18:45Z, 18:59Z, 20:00Z = `halt_receipt.json`), **25/25** confirm failures `HTTP 400 expired` (the taker's accept lapsed before our confirm — in-handler 0.5–0.8 s, reservation RELEASED by the reconcile every time = a lost auction), and the counter was CUMULATIVE per boot (0052: fail, **104 ok**, fail, 1 ok, fail → HALT; 0942 fail/ok/fail/fail; 1141 fail/fail/ok/fail). FIXED by mechanism: `classify_confirm_failure` — exactly `400 expired` ⇒ `confirm_expired_by_exchange` (metric + WARNING with `accept_to_confirm_ms` / `dispatch_delay_ms` / `confirm_rtt_ms` vs the 3 s `exchange_window_ms`, never counted); timeouts / connection / 5xx / other refusals = ours, counted CONSECUTIVELY with reset on success; the halt is KEPT for 3 genuinely consecutive own failures (26 tests). **Stall receipts re-read from every boot's tail (28 since 8/17):** both of TODAY's `KILL_20260905_{005252,121339}` ("maintenance age=61.1s > 60.5s") are corpse stamps 60 s AFTER a confirm halt's `joint_pool_stopped` (13 such since 8/17), but in **15** the supervisor's wedge verdict comes FIRST and the bot halts on the supervisor's KILL file 1–4 s later — event loop alive, maintenance loop alone silent 61 s = a sub-step held in an un-bounded await (8/17 ×3, 8/20 ×5, 8/26 ×6, 8/27 ×1; the first builder's "no healthy bot was ever killed by the wall" is CORRECTED). Live loop today: max inter-mark gap **3.54 s** (7,059 read-only samples at 4 Hz, 17:29–17:59Z). B = the mechanism repair for the 15 and the missing measurement: `risk/stall_wall.py` — completed inter-mark gaps recorded per boot (`data/maintenance_gap_tape.json`, retention = `live_*.log`), `wall = max(floor 60.5 s, 2.0 × Q_Φ(5))` = the hang watchdog's rule (parity test reads `_MARGIN`), derived at boot + every 60 s (`stall_wall_derivation`), supervisor reads it from the ledger unchanged; the 12 unbounded maintenance-path store awaits bounded by `wall/MARGIN` (regression guard); progress marked + timed between sub-steps (`maintenance.step_ms.*`, `maintenance.tick_ms`, `maintenance_tick_slow`); a truly hung loop is still killed (real loop + real supervisor, 29 tests). **OPEN, URGENT: the whole stack is DOWN since 16:02 ET** — the watchdog's `START_BOT -Auto` relight after the 20:00:56Z halt logged `joint_pool_started` at 20:02:08Z and nothing after (`worker_pids.txt` empty, no heartbeat/progress files, no bot or watchdog process). Also open: the shutdown wedge after `joint_pool_stopped` (O1), shutdown-aware receipts (O2), expired-accept rate = delivery/buffer-overflow class (O3), store saturation = item 7 (O4). **REVIEW FIXES (same day): the derived wall could RATCHET** (a completed gap in (wall/2, wall] doubles the wall; retention 45 days) — a timed-out store await now TAINTS the gap (never recorded) and applying a wall above the floor is GATED behind `supervisor.stall_wall_derived` (default `shadow`: derive + log, floor applied); A's two loosenings (exonerate `400 expired`, reset on success) + the alarm's halt + the wall's loosening = **4 RULINGS OWED before relight**; refresh I/O off-loop; 4 of the 12 bounded ops are on the WS fill path (disclosed); a late-landing `record_fill` is ADOPTED by the replay; `maintenance.tick_over_wall`; new `risk/confirm_expired_rate.py` = the derived replacement ALARM (`confirm_expired_rate_anomalous`: boot's expired share vs the pooled measured rate, exact binomial tail at z = 3). → `2026-09-05-build-confirm-halt-and-derived-wall.md` |
| 2026-09-05 | **BUILD: WS READER ISOLATION + LOOP-LAG INSTRUMENTATION + SHADOW-TELEMETRY SAMPLING (`build/ws-reader-isolation`, worktree; bot live on main, store read-only)** | The code-25 day: the exchange closed our communications subscription ("Subscription buffer overflow" = WE read too slowly) 34/72/80/24 times on the 09:05/11:41/14:07/14:46 boots, 4 `halt_confirm_timeouts` (all `HTTP 400 expired` — accepts SEEN late), 2 kills at the 60.5 s maintenance wall — every one a MAIN-LOOP STALL; the reader was a coroutine on that loop. **Reader isolation**: the socket now lives on a dedicated thread + loop (`_ReaderThread`), frames cross through `_Lanes` (PRIORITY never dropped / CONTROL never dropped, book socket byte-identical / MARKET: capacity shed oldest + AGE drop at dequeue on the dwell horizon); on_disconnect-before-reconnect, resubscribe-on-the-specific-socket, 10/17 force_reconnect unchanged; code 25 no longer follows from a main-loop stall (while the reader gets the GIL) and CRITICAL if it recurs; **review fix pass**: the reader thread never logs periodically (the hang watchdog's log axis stays main-loop work), single-read health properties, one subscribe per socket, tape readers re-weighted by `sampled_1_in`, recorder install/uninstall brackets the whole boot (+3 proofs). **Instrumentation**: `LoopLagProbe` (`event_loop_lag`/15 s, bound = its own period = the finest declared cadence) + `SlowCallbackRecorder` (`Handle._run` hook, 0.1 µs/callback measured; threshold = the process's p99; `slow_callback` + `slow_callbacks_window` name the task and the `file:function:line` that blocked). **Sampling**: 85.8% of log lines were six shadow read-outs (32/send, 22-31 µs each) → structlog processor keeps 1-in-ceil(lag/period) only while behind, `sampled_1_in` on kept lines, `risk_audit`/decisions never touched (pinned). **Transport replay of today's tape** (20,019 frames at 500/s through the REAL manager + intake, 1 s and 6 s synthetic stalls): reader max read gap 46 ms through the stalls, 0 overflow/disconnect/shed, 2,246 stale frames dropped on OUR side, 19/19 accepts delivered — but an accept arriving mid-stall is dispatched when the stall ends (5.0 s vs the 3.0 s window): the STALL is now the defect and the recorder names it (`…main:183` 6,000.7 ms to the line). Vitals rigs under the recorder: ≤ 12 ms lag, only the store path registers (`persistence.py:open/_write`) — production ranking lands at relight. Suite **4,070/0** (+25), vitals fast **8/8** from the snapshot, ruff/mypy unchanged vs main (pre-existing debt listed). → `2026-09-05-build-ws-reader-isolation.md` |
| 2026-09-05 | **DESIGN + TOOLS: quiesced STORE ROTATION (item 7) + the dark TAPE-RETENTION prune — `tools/ops/rotate_store.py`, `tools/ops/prune_tape.py`, `ops/tape_retention.py` (`build/store-rotation-tool`; `--apply` NOT run; store read-only; review fix pass applied the same evening)** | The 213.68 GB store (rfqs 66.4M + decisions 134.3M ids = the recorder tape; every table the bot needs is ≤ 22.6k rows) starves the single writer: 1.86M dropped rows on the 00:52 boot, 1.31M on the 09:42 boot, the 12:13 boot's tape FROZEN after 16:19Z, the seed reading 7,426 quote_sent rows/24 h (97% holes). **Rotation tool:** `--dry-run` (read-only plan, boot-reader audit, measured throughput, the prune's plan, refusals-now) / `--apply` (refuse on the repair tool's liveness evidence + ours-predicate processes + ANY other hard link (`st_nlink != 1`, never fail-open) + any FOREIGN WAL; TRUNCATE checkpoint or refuse; fresh store built FIRST through the REAL `Store.open` (live DDL + migrations; the store's own DDL only for the two vitals-owned tables) at a temp name from the quiesced live store, THEN renamed to `.archive-YYYYMMDD` + swapped (the live name is absent for two syscalls only — review must-fix #2), rows copied BY COLUMN NAME — ledgers whole with ids, `store_meta` watermark replacing the fresh open's, `sqlite_sequence` continued, the seed's 24 h of quote_sent/confirm/decline + rfqs, one rfqs row per leg set per unresolved fills ticker; quick_check + COUNT verify; swap; manifest with the exact START_BOT sequence; rollback on any failure) / `--verify`. **HAZARD: the store inode has 3 hard links** (`kct-vdata\`, `vitals_snapshot\`) each with its OWN `-wal` — the tool refuses until they are moved aside AND the extra links deleted. **Prune (DARK, `observe.tape_retention_enabled: false`):** nightly bounded DELETE of rfqs/decisions/would_quotes* older than a DERIVED window = SEED_WINDOW_S + the pass cadence + the tape's MEASURED time disorder; protected leg-provenance rows never deleted (same function the rotation carries); batch = the seed's 25k-id chunk, a batch is INTERRUPTED at STORE_OP_TIMEOUT_S (SQLite progress handler; the whole transaction rolls back; the lock is never held past the bound) and stops the pass, ≤ cadence/timeout batches per pass, only against an idle writer (`Store.writer_queue_depth()`), off-loop; on the 213 GB store the first batch stops it by design — arm AFTER one rotation. Gates: suite **4,075/0** (30 new) LOW priority, vitals fast **8/8 GREEN** from a rebuilt snapshot, ruff + mypy clean. Dry-run of the FINAL tool executed read-only with the bot DOWN (17:23–17:28 ET, 256.5 s): retention window 172,812 s (measured rfqs disorder 12.056 s), 66.3M / 134.2M / 3.25M rows older than it, 134 protected rows = the rotation's carry; liveness refusals EMPTY (bot down since 16:02 ET) — only the file refusals stand, which is why the hard-link COUNT refusal matters today. Review fixes (2 must + 6 should, all applied; +10 tests): suite **4085/0** (3 deselected, 320.28 s, exit 0), vitals fast **8/8 GREEN (GATE PASS, 104.8 s)**. — [report](2026-09-05-design-store-rotation.md) |
| 2026-09-04 | **NIGHT: "fills a lot slower" — MEASURED (not slower vs 8/17–8/19; −45% vs 8/26), driver = build A's exposure-backed rebate rule (RETIRED `671b1c2`), floor → shrunk point (merged `7cdd6ef`), per-combo 1%→2% (yaml), 4 restarts, bot LIVE 21:03 ET** | Tonight 14.1 fills/h, 0.65/1k sends = the zero-incident evenings; 8/26 was 27/h. My 19:15 "cell floor" diagnosis RETRACTED (it moved 2 quotes); the `exposure_backed` rule stripped the leg-axis rebate from **77% of sends** (0.3–0.5¢ on the wire = the auction-winning margin; club-soccer + MLB-prop families = the diversifying flow) → retired: the rebate is bounded by the MEASURED per-cell floor only (ES-value cap once the steer has priced). 2% anchor: structure cap gone from the decline census; ~9.6k RFQs/h in the ($49.80,$99.60] band ≈ +6 fills/h upper bound (whale-size class). 19:29 boot stall-killed 20:09 (61.2 s wall, item 6 parked) during a builder's 180 GB vitals scan → `tools/vitals/snapshot.py` (live-safe gate). After 21:03: rebate on 51% of sends (mean 0.45¢), 284–492 sends/min, 0 errors, 0 fills in 6.5 min at the in-play hour (noise). Tonight: 19 fills / $467 / net edge $4.66 after $6.40 fees — the margin lever (favorite band) is the EV question. → `2026-09-04-fills-diagnosis-rebate-retirement-floor-point.md` |
| 2026-09-04 | **BUILD: retained-edge floor = SHRUNK POINT shortfall, not a 3σ bound (`build/floor-point-estimate`, worktree; bot live on main, store read-only)** | Build A's floor `t_{G−1}(Φ(−3))·SE − shortfall` published pool floors 5.9/11.6/26.7/49.5¢ and populated floors 15–59¢ against 1–3¢ margins → the skew REBATE (the constitutional diversity steer) was muted on the wire from 22:45:44Z (41 of 1,158 tonight's quotes had any rebate room). Now `floor = max(0, −EB-shrunk point)`: thin cells → sport pool POINT (mlb 0.85¢, soccer/esports/other 0), unknown sport → largest pool point; a losing cell may rebate only what the margin holds BEYOND its measured loss (`margin − m_min − |shortfall|` — 0 on 69 cells / 22.6% of post-8/20 quotes, led by ML×ML cross-game at −1.76¢/ct on 70 settled rows, whose SIGN is unresolved: |post|/SE 0.29, one of 61/206 populated cells inside 1 SE — a point on a noisy record, not a measured loss; `n_populated_sign_unresolved` now logged for the ≥ 2-week read); `margin//2` gone from the measured path (fee floor `m_min`, ES-value cap, exposure-backed rule unchanged). Counterfactual (live `construct_quote`, 1 ct): rebate room on **13,077/20,000** post-8/20 quotes vs **202** under the wire's bound; tonight **779/1,158** vs 41; NO tier to zero; zero-skew bids byte-identical. Parity: prototype `tools/proto_floor_point.py` == estimator 656/656 cells, == live through `construct_quote` 20,000/20,000 + 1,158/1,158; prototype's upper rule == `main`'s estimator 656/656. Uncertainty NOT fed to the width (measured: populated-cell SE median 8.1¢ vs half-width 0.43¢ — it would re-mute on the other lever; follow-up = a fair-calibration read in probability units). Blast radius = rebate size only. Suite **4,045/0** (+4; one `tail_quantile` test DELETED with the removed function, two re-pinned, one inverted, four added — disclosed literally). **REVIEW FIX PASS (SHIP_WITH_FIXES → all applied):** vitals RUN from a store SNAPSHOT via NEW `tools/vitals/snapshot.py` (the two tables the tiers read, 3.8 MB, no tape — on a live box the fast tier otherwise rescans the whole 266 GB tape; gate finding O5): **fast 8/8 GREEN**, **pre-ship V6 RED = the adjudicated stale-ledger class** (338 open rows, 321 opened before 9/4; MC 4,078 ms vs 2,996 ms window; nothing on that path changes here — 6th ship-through decision owed to the orchestrator); tonight refreshed at 00:41Z: rebate room **1,034/1,531 vs 56** on the wire; pool-level EB test (review S1) MEASURED as undetermined — no pool thin with all four pools (weights 0.80–0.99) but τ²_global = 0 without the 3-cluster "other" pool → joined to O3 (variance prior at both levels), ratification stands. OPEN: pre-publication `margin//2` fallback (persist the table), 5 s sweep timeouts on the 213 GB store, 2-cluster cells at full EB weight, `derive.py` manifest over closed logs, main's refreshed `tape_facts.json` uncommitted. → `2026-09-04-build-floor-point-estimate.md` |
| 2026-09-04 | **RELIT 18:45 ET AFTER THE REPAIRS — gates, ledger repair, first window (bot LIVE)** | Operator ratified fee mode floor + soccer 0.746, ordered relight. Gates on `09cc6cb`: suite **4,041/0**, fee counterfactual PASS (no tier to zero), vitals fast **8/8 GREEN**, pre-ship **V6 RED = adjudicated stale-ledger artifact** (434 stale open rows ×5 vs 0 exchange positions; 5th ship-through). Phantom repair APPLIED (28 rows, row-level backup `data/backups/20260904T224444Z-*`). WMI-detached START_BOT 18:45:05; boot clean; **fee observer fit 0.0350 from 540 charged fills, 0 mismatches; series fee_type `quadratic_with_combo_maker_fees` parsed; retained floor published (646 cells)**; first window **407–474 sends/min**, 3 fills all **verified on tape in 0.2–4 s**, fee booked to the cent ($0.2278 on 30.01 ct @ 68.2¢), 35 structural_fits ACCEPT rows, 0×400, 0 halts, 0 errors, p_ruin 0.0. → `2026-09-04-relight-after-repairs-first-window.md` |
| 2026-09-04 | **FOUR REPAIR BUILDS MERGED TO MAIN (fleet: 4 builders in worktrees + 4 adversarial reviews + 4 fix passes; operator "do everything besides 6-7")** | **A — fee seam + measured rebate floor** (`build/fee-seam-rebate-floor`): live enum `quadratic_with_combo_maker_fees`; MEASURED fee schedule observer (0.0350 exact on 540/540 charged fills, taker-conservative bootstrap, drift alarm, persisted); ONE schedule shared by pricer + ledger; every EV fee-NET through one edge function (`rfq/edge.candidate_edge_cc`); `construct_quote` fee_mode floor (default) / width; measured per-cell retained-edge floor (Student-t at clusters−1 df, EB-shrunk, fail-closed to sport pool) + rebate bound; counterfactual: 140 fills / $3,157 re-priced, 59,578 quotes replayed, NO tier goes to zero, ladders untouched, ~11 µs/quote. **B — soccer** (`build/soccer-btts-total-fit-bar`): btts\|total 0.70 → 0.746 (club measurement); derived structural fit bar (leg-uncertainty based, one bar for exact + over-identified) + CHALLENGE path + marginal-consistent btts×total hybrid; structural_fits telemetry (exception-proof, slow path); 26-context parity; wide read-only replay of 553k quotes for blast radius. **C — watchdog** (`build/watchdog-network-boot-death`): NETWORK-class boot deaths retry with backoff + reach probe instead of latching (8/27 corpse fixture); 8/6 corpses re-read as post-heartbeat (retraction). **D — ledger** (`build/poll-recovery-double-book`): mechanism = PHANTOM EXECUTIONS (Kalshi sent `quote_executed` for 28 orders that never filled), not a double-book → every booked fill is verified on `/portfolio/fills` and voided if absent; one-exchange-order-one-row; per-ticker quantity alarm; `tools/ops/repair_phantom_fills.py` (dry-run 28 rows; row-level backup; --apply owed). Suite: main 3,778 → per-branch 3,924 / 3,811 / 3,830 / 3,810, 0 failed. → `2026-09-04-build-fee-seam-rebate-floor.md`, `…-soccer-btts-total-fit-bar.md`, `…-watchdog-network-boot-death.md`, `…-poll-recovery-double-book.md` |
| 2026-09-04 | **PRICING-MECHANISM DEEP DIVES: fee seam / rfi×rfi + partial-SGP / KS lines / whale / soccer btts×over (4 readers + citation verifier, 214 citations; nothing changed)** | **Fee seam CONFIRMED and bigger**: 144 fills / $3,140 (34% of post-8/20 premium) negative-EV after fee; every EV the bot judges is gross of fee (quote width, ledger, admission EV, eviction, KILL-marginal — chain verified); repair = measured coefficient observer + enum + ONE edge function net of fee + fee-aware rebate clamp (retires the razor by measurement). **rfi×rfi + HR NO-baskets = the inventory-skew REBATE ate the whole markup** (median 0.2¢ retained on a 2¢ tier pre-8/16; the 8/16 half-margin cap is a hand fraction, fee-blind) → measured retained-edge floor + ES-value rebate bound. **RETRACTED: partial-SGP correlation markup** (−$481 of −$300 is 35 HR-basket combos, same rebate defect; joint not implicated) and **KS line-2 calibration** (leg-level bias −1.2 pts t −0.3; line-3 win = five aces' 1–2 K nights; monitor only). **Whale** ≈ −0.2% ROI, loss = one band (NO 30–50¢, −$653) in 115 same-game 2-leg pairs; leg picks not informed, markouts blind → same-game pair-ρ measurement FIRST, counterparty adder DARK. **Soccer btts×over**: 4/4 club pairs rejected by the hand-set 0.005 exact-fit bar → copula at 0.70 = the July WC BLEND (club 0.746) → ~1¢/pair too rich, phantom edge; structural_fits 0 rows ever → promote 0.746 + derived fit bar + telemetry. Operator: NFL parked; MLB through postseason. → `2026-09-04-pricing-mechanism-deep-dives-fee-rfi-sgp-ks-whale-btts.md` |
| 2026-09-04 | **ALL-TIME P&L BY SPORT + COMBO MAKER FEE (9-agent sweep, exchange-reconciled to the cent; nothing changed)** | **Operator thesis "soccer+esports are the best profit, focus all capital there" = WRONG/NOISE.** MLB = +$2,179 of $2,964 (73%); soccer +$488 (63% = the closed World Cup; club soccer 3.6% = MLB's 3.2%), esports +$284 (72 combos, one fill = −53%); day-clustered P(club soccer > MLB) = 0.50; soccer+esports intake $573/day fills ~9% of the book (their FLOW binds, not our capital). Only decision-grade structural fact: the pricer quotes 1.7% edge/$ in club soccer vs 3.0% in MLB. MLB's profit is ONE cell (KS parlays at NO 0.15–0.35 = 88%); losing shapes are pricing mechanisms (rfi×rfi at 0.49% edge, partial-SGP, KS line-2, whale bands). **NEW: combo maker fee LIVE since 8/20 = 0.035·C·P·(1−P)** (2× the pinned 0.0175; ~1.1% of premium; 43–146% of the razor/ladder markups) and the bot's fee seam is DARK (fee booked $0, edge overstated, bids carry no fee). Recommendation: keep all three sports, arm the fee seam pre-relight, fix MLB pricing cells, wire NFL for October. → `2026-09-04-alltime-pnl-by-sport-and-combo-maker-fee.md` |
| 2026-09-04 | **OUTAGE TIMELINE CORRECTED + EXCHANGE TRUTH PULL (24-agent forensics; nothing relit)** | **The morning handoff below was WRONG (retracted): no 10:15 freeze, no sleep.** The 10:05 boot was supervisor stall-killed 10:14 ET; the watchdog relit 16 times and the bot quoted ~15.5 of the next 16 h (~375k sends, 401 accepts, 371 exchange fills, marked equity peak $7,806). Stop census: 12 FALSE-POSITIVE stall-kills at the 60.5s wall, 4 settlement-fact halts (3× `market_result ''` + 1 credit mismatch), 4 network deaths, 1 hung process. **Final outage = Wi-Fi/DNS flapping on an AWAKE machine** (zero Windows sleep events) → 02:08 boot died pre-heartbeat → watchdog latch (design gap: transient boot death latches forever). **Exchange truth 9/4: $4,980.09 all cash, 0 positions, 0 quotes; 8/26 session +$140 realized; all-time realized +$2,980 = new high.** NEW: phantom ledger fills (exchange-verified 96.63ct), store collapse every >1h run, p_ruin=1.0, 26-fill pick-off cluster. Bot down since 8/27 02:04 ET; relight on operator word. → `2026-09-04-outage-timeline-correction-and-truth-pull.md` |
| 2026-09-04 | **OUTAGE DISCOVERED + SESSION HANDOFF (memory-save pass; operator upgrading Claude Code for Fable 5.1)** | **Bot DOWN since 8/27 02:08 ET — 3rd multi-day outage in ~5 weeks.** The 8/26 10:05 WMI-detached relight froze at 10:15 ET (cause undiagnosed; sleep suspected — WMI detachment covers session close, not sleep); watchdog relit 8/27 02:07 but the boot died ~100s in on wake-DNS failure (getaddrinfo) → never reached liveness → watchdog latched STAYING DOWN (correct), then itself died ~13:40. No KILL file; repo clean+pushed. Bundesliga/Saudi first matchdays MISSED. | Handoff sequence for the fresh session: exchange truth pull → WMI-detached START_BOT (operator start clears the latch) → first-window verify → overdue 9/1 pre-registered reads + 9/1 list → Kernel-Power diagnosis of the 10:15 freeze (if sleep confirms: boot-persistence = 3 outages, declined 3x, re-raise). — [report](2026-09-04-outage-discovery-and-handoff.md) |
| 2026-08-26 | **LEAGUE WIRE + MARKUP RESET + WIP SHIPPED + RELIT (`d5b121a`+`e866fec`, restart 08:26 ET)** | **Operator's full soccer slate live: Bundesliga (`KXBUNDESLIGA*`, 4,867 open RFQs — 8th-largest prefix on the exchange) + Saudi Pro League (`KXSAUDIPL*`, the only Saudi base ticker, 1,300 RFQs) join the 9 wired leagues, four families each; rules verbatim-identical to the club pin (90' regulation, 48h scalar ⇒ farmable=False), anchors +3h/+4h measured; ml_parlay 0.3¢→0.6¢ AND extended to ALL SPORTS (code: MLB/soccer restriction removed — esports/racing/cross-sport ML parlays ride the razor); general ladders → 1-3¢ ALL sports (soccer 1/2/3¢, MLB 1/2/2.5/3¢, racing+mixed flattened to 3¢, esports already 3¢); the 8/19 WIP (once-counted notional wall + persistence 2nd-conn fix + dark thin-auction lever) GATED (WIP tests 94/94, suite 3,755/1 = the sha tripwire, vitals 8/8) and COMMITTED** | Operator confirmed THEY stopped the bot 8/20 (outage question closed). Account truth: $4,827.44 all cash, zero positions/quotes; SHARD1 transfer EXECUTED while down (shard1 $4,537.65 = 94% — decision closed); 4 shards now in balance_breakdown (merge is dynamic, handled). First window: **655 sends/min** (above the 300-460 benchmark), 0×400s, 0 halts, walls on fresh equity (entity ceiling $96.55 = 2.0% × $4,827.44), Saudi legs reaching entity admission; 5-min cold-cache warmup transient (429s + starvation warnings) cleared by 08:32. Traps pinned: Bundesliga 2 shared prefix, KXBBLGAME basketball. Pre-ship V6 RED = 5th stale-ledger ship-through; ledger repair stays 9/1 P1. Still open: 1v2%, friendlies, thin-auction arm, boot persistence (2nd 6-day outage). — [report](2026-08-26-league-wire-markup-relight.md) |
| 2026-08-26 | **SIX-DAY OUTAGE FORENSICS + FULL CATCH-UP (21-agent sweep of every report/doc/memory + shutdown & working-tree forensics)** | **Bot DOWN since 8/20 17:55 ET — externally terminated (bot+prober+supervisor+watchdog stopped in the same second, 16s after a modern-standby entry; NOT a reboot/crash/KILL — System log clean, machine awake 8/21 with zero bot writes; leading hypothesis operator STOP_BOT at sleep time, but TWO other untraced silent exits on 8/20 leave an unknown-killer possibility). Nothing relights after it (no scheduled task — 2nd 6-day-class outage this month). Last money state 8/20 17:33 ET: equity $6,677.71 = ALL-TIME HIGH (+$2,519 vs 8/19 morning; day realized +$810.79; the +$2,038 unbooked-wins thesis validated); 78 positions settled UNOBSERVED since** | 8/20 churn: 7 supervisor stall-kills all at the 60.5s maintenance wall on the (now 211 GB) store + 2 untraced exits; watchdog relit all 9. p_ruin pegged ~1.0 from 11:00 ET while quoting continued (chosen posture — ruin budget 1.0, loss halts disarmed; feeds the 9/1 composition-aware KILL budget + anchor-split items). WORKING TREE: the 8/19 session wrote ALL THREE proposed builds (gross-notional once-counted fix UNCONDITIONAL; persistence 2nd-conn checkpoint + fetchall + dropped-writes; thin-auction margin lever DARK) then died uncommitted/ungated — and the venv is an EDITABLE INSTALL, so the WIP **ran live all of 8/20** (~14h clean; WAL 40 MB at death vs 5.63 GB on 8/19 = the persistence fix demonstrably working; 447 test lines never executed). Decisions owed: confirm the 8/20 stop, gate-then-keep vs stash the WIP at relight, shard-1 transfer (script verified intact), 1v2%, friendlies, thin-auction arm, boot-persistence re-raise. — [report](2026-08-26-six-day-outage-forensics-and-full-catchup.md) |
| 2026-08-19 | **QUOTING/P_BOOK FORENSICS (operator: "quoting a LOT less; risk book or p_book calc is wrong") — 6 finders + 3 adversarial verifiers** | **Quoting NOT down (8/18 = 437,608 sends, 0.8% off all-time max; log=truth — real logs live in D:\kalshi-combos-TWO-data\ per CURRENT_LOG.txt, repo-root live_*.log are 7/15 decoys); the STORE is what collapsed (WAL checkpoint failing all boot = SELF-lock: single shared connection, cursors open across awaits on 191GB DB; writer 2h35m behind, ~75-80% tape rows silently dropped; store-based views show a phantom ~50% decline); p_book calc + book model CONFIRMED CORRECT to file:line and by independent MC** | RETRACTIONS: my 8/16 "0.60-0.70 at ≥60% favorite share +1.5¢" projection was WRONG (verified ceiling ~0.55-0.578 at +1.5¢ on ~15-game slates regardless of share; 0.62 needs +3.5¢×$1.2k favorites OR 85 indep games OR +2.5¢×k≈30; correlation raises p_book, fattens tail; composition target already met 58.7%); my "8/18 final −$156.51 + overnight −$121.96" were UTC-boundary slices — TRUE TD 8/18 = **−$296.94/132 groups**, TD 8/19 = $0.00 (equity flat); "2 overnight reneges" → exactly 1 ($11.47/0.086%). NEW: 272 stale-open ledger rows ($3,482) hide **≈+$2,038 of unbooked WINS** (242/244 winning groups) — day-P&L reads biased pessimistic, favorite-band autopsy win-rate biased LOW. 70% deployment truth: walls refuse ~nothing (det 58% of backstop; 3,094 utilization refusals = 100% double-count artifact); limiters = 1%-ruling per-candidate caps (74.7% of candidates refused, whale shapes) + auction win share ~0.9% + equity-scaled tickets — the 70% goal IS the 1v2% verdict. SHARD1 CRITICAL: $228 cash vs 94.8% flow share, transfer blocked by permissions, operator must run TRANSFER_SHARD1.bat. — [report](2026-08-19-quoting-pbook-forensics-and-verification.md) |
| 2026-08-19 | **MORNING READOUT — 8/18 evening slate broke against favorites; gross-notional pull-forward PROPOSED** | **Equity $4,158.92 (−$965 over 3 red days); 8/18 final −$156.51 (midday +$245 given back), 8/19 overnight −$121.96; second consecutive zero-incident night (150/154 accepts all explained, 0×400s)** | Favorite band since 8/18 boot: 54 groups 57% wins vs ~74% implied (−$142.45) — but ALL losing groups on ONE slate (26AUG18: LAD@COL/MIA@PHI/NYY@BAL/AZ@BOS +4 more = 8 games where the favorite lost), i.e. one correlated tail draw, not decay; NO refit — pre-registered game-clustered pooled check (first read ~9/1, trigger >2σ clustered = model REVIEW). Razor day-2: 20 settled 11W −$81.66 (same slate; n too small). Cap-verdict record for the operator: 153 combos, median $10.56 / p90 $27.10 / max $42.95, zero above 2% line. TOP ITEM: gross-settlement-notional wall declined 2 WON auctions by 0.09% ($11.55) — the ratified-8/12 ×3.6 per-game double-count now costs won auctions; daylight ship proposed (fix aggregation + re-derive 3× so day-one tightness unchanged; vitals+pre-ship+throughput before/after) — needs operator freeze-exception blessing. Shard 1: $907.62 cash vs ~64% flow — held overnight, first to break on a heavy slate; top-up = operator call. — [report](2026-08-19-morning-readout-favorite-slate-break-and-notional-pullforward.md) |
| 2026-08-16 | **"DO ALL OF IT" SHIPPED — composition tilt + tonight items + Serie A/Ligue 1/UCL-ADVANCE (`a39b2a5`, restart 11:06 ET)** | **Everything from the deep-dive plan live: ML-parlay 1¢ tier (cross-game ML-only MLB/soccer <35¢ fair; live-loader proof 3-leg @8¢ → 100cc while ML+prop keeps 400cc — the whale cell untouched); post-rebate edge floor (rebate ≤ half margin — kills the 24%-of-premium sub-1¢ coinflip manufacture); tie×BTTS guard extension; halt-flap 3-strike single-ticker QUARANTINE (7 cancel_alls/night from one oscillating K-market — one-off events reset, broad corruption still halts, 3 pins); marginal KILL/ruin gates → telemetry (False/1.0/0.70 — recovers ~+$700 standing det; drawdown brake + concentration walls + 0.70B backstop stay); Serie A + Ligue 1 + KXUCLADVANCE wired (LIGUE1 keyword, markup map incl. BUNDESLIGA/BRASILEIRO future-proofing, 4 families each, offsets/horizons; UCL/EPL were 8/15). Suite 3,734/0, vitals fast 8/8, probe 14/14, live-loader parse+tier proof; pre-ship 0/1 = the adjudicated V6 stale-ledger class (shipped per 8/13+8/15 precedent; ledger repair = 9/1 P1). First window: preflight green, 57 quotes/3min pre-slate, walls on live equity, 0 halts. NOT shipped: friendlies (operator call, 5,676 RFQs), blanket longshot cut, pbook-axis, entity reservation scoping (9/1). Scoreboard to watch: first ML-parlay fills (the 0/529 class), NO≥65¢ premium share → 60%, p_book → 0.62-0.70, quarantine eating tonight's flapper. — [report](2026-08-16-composition-batch-shipped.md) |
| 2026-08-16 | **THE P(BOOK)/COMPOSITION DEEP DIVE + PLAN (operator: "price better, risk better, or manage numbers?")** | **6-agent measured dive + counterfactual MC through the live sampler; cash-gate envelope bug FIXED (`c4b30d7`); decision menu delivered** | HEADLINES: (1) the week's P&L was MIS-BOOKED — corrected +$198 vs booked −$470 (~$669 ledger double-book; 8/15 "negative day" was +$109.79); (2) DIAGNOSIS = composition (53.6% of premium in 35-65¢ coinflips pins P(book)~0.45; flat in size/count) + we're ABSENT from the tape's #1 flow (cross-game ML parlays 33% of 7.1M requests; 0/529 fills; stage-1 = entity caps saturated by RESTING quotes at 100% weight + per_combo all-or-nothing, stage-2 = lose by exactly markup, field clears our-fair+0.05-0.25¢) + high band bleeds via ONE whale (−$800/−29% ROI premium-weighted) and two same-shape farmers; (3) MY 8/15 cash gate NEVER ARMED (Kalshi nests 400 bodies under 'error' → code parsed empty → 0 armings vs 478k 400s, peak hours at zero quotes) — envelope unwrap shipped; (4) halt FLAP-LOOP: 7 cancel_alls overnight on one oscillating Skenes marginal; (5) marginal KILL gate measured as the real ceiling (det at decline $1,123 = 35% of wall; 127 won-auction declines); (6) P(BOOK) TRUTH: 0.60-0.70 at $3.4k needs ≥60% premium in NO≥65¢ with +1.5¢ retained — and the LEFT TAIL INVERTS (P(kill) 0.000/CVaR99 $386 vs today 0.13-0.20/$1,245+): higher P(book) AND 3× thinner tail; diversification is a tail instrument (+0-1pp mean). PLAN: tonight = flap quarantine + gate-to-telemetry + corrected reporting; package = ML-parlay +1¢ tier + entity scoping + 40% resting haircut + rebate floor + tie×BTTS guard; measurement-first = RBI×TB conditionals, RFI fairs, 8/11 forensics, counterparty decay; NO blanket longshot cut (feeds the whale), NO pbook-axis arm. — [report](2026-08-16-pbook-composition-deep-dive-and-plan.md) |
| 2026-08-15 | **FOUR LEVERS SHIPPED + SOCCER PICKOFF CONFIRMED & GUARDED + LEAGUE EXPANSION (`e5f2793`+`958961f`, restart 12:25 ET)** | **All operator orders live: (1) CONTINUOUS EQUITY — risk_bankroll = cash+haircut×PV every poll (SOD ratchet gone; first snapshot proves walls on $4,903 live equity = +17% capacity); (2) structure 1%→2% (~$98) + per_combo→2% belt; (3) mains 2¢ soccer+MLB; (4) CashGateSender (one insufficient_balance probe per balance reading — kills the 181-225k/day 400-storm). Suite 3,708/0; vitals fast 8/8; live-RFQ probe 13/14 QUOTED + exactly 1 guarded** | ⚠ PICKOFF CONFIRMED (operator's "10x vs 6x" verbatim = NSHMIA 9.3¢ ours vs 14.0¢ field): 8 same-game tie×under fills / 2 sharp takers / $263 at risk — DC inversion rejects on draw-rich matches (residual > 0.005) → SILENT copula fallback with team-calibrated +0.28 rho WRONG-SIGNED for draws → fair BELOW independence; takers hit ONLY the 14-of-3,208 flipped states (ALAGET: 30.4¢→20.5¢ on a 0.35¢ mid move, 2-for-2 on flipped / 0-for-1,388 otherwise); all 8 fills +2.7..+6.7¢ rich vs 12-22 maker fields. GUARD LIVE: skip_structural_fallback_tie_total (declines the fallback state only); full repair recipe = tie-rho split + CHALLENGE band + record_structural_fit wiring (0 rows ever). LEAGUES: LIGAMX+EFLCHAMPIONSHIP+UCL+EPL wired (census-driven); CLUBF (5,676 RFQs!)/CHNSL/ENGCS classified but NOT allowlisted — friendlies decision owed; "CHAMPIONSHIP"⊃"SH" period-token trap fixed. INCIDENTS: pre-ship V6 RED = the adjudicated 8/6 class (335-row book, ~2× inflated by 195 stale ledger opens — ledger repair now P1 for 9/1), shipped per 8/13 precedent; boot race bit 2nd time (25min down) → busy_timeout-before-WAL-pragma FIXED. Club-mains finding: our fair clean, the WC +4¢ longshot tier loses club mains share (field clears fair+<1¢) — retune post-fix, not on poisoned ev_ledger. — [report](2026-08-15-four-levers-pickoff-guard-league-expansion.md) |
| 2026-08-15 | **POST-REARM PERFORMANCE DIVE + BANKROLL-ANCHOR DEFECT (already fired a FALSE halt)** | **Read-only 5-agent dive; operator feels checked vs tape: quoting NOT less (2× every comparable hour, 187–243/min overnight; the STORE TAPE drops 75–80% of quote rows under load — persistence.py:302/322-334 overflow + 2,398 checkpoint failures = why it FEELS less); fills 94 / $1,571 premium / +$44.50 edge, capture +0.25c ABOVE tier, 0 negative-edge, favorable markouts, 0 reneges, max ticket $39.35 (1% cap held); longshot share HALVED 33.9%→14.0% ("crazy longshots" = composition: cheap-NO lottery bids ×2, prop legs 16.8%→35.5%, 145 cross-sport combos the scoping fix unlocked); P(book) steer WORKING (2.67M events 99.5% applied, 50/94 fills through diversifier rebates), p_book 0.38–0.41 = diversification arithmetic, p_night 0.99+** | ⚠ FINDING: every wall runs off risk_bankroll = $4,181.81 (the 23:27 relight anchored at the overnight equity TROUGH; balance.py:575-597 min() + UTC-day SOD + in-memory reset = intra-day ratchet-DOWN-only) vs true equity ~$4,919–5,025 → ALL walls 10.8–16.4% tight (the 3 walls doing 75.8% of refusals all scale off it), kill_line byte-pinned 2,250 snapshots, and the 21:57 ET halt_hard_trip was FALSE ($530 give-back vs $514 lagged threshold; $600 at true equity = no trip) → caused the relight that ratcheted lower. Repair recipe in report (anchor += realized settled cash; persist SOD across relights); self-heals 20:00 ET tonight. P&L truth: 8/14 final −$5.59, 8/15 +$508.52 by 9:30am (pre-rearm book paying out), equity ~$5k CONFIRMED; ledger booking defect understates all-time by $782.79 (true ledger-era +$2,604.91). Levers ranked: anchor fix > ticket-size restoration (per_combo→2% ≈ $98 ceiling; 8/13's premium day = ~71 capped fills) > mains 1→2c (needs gap-to-best-rival; skew-rebate interaction) > dead-time (pause-new-creates) > slate 65→80 DEFER (not binding, util 48%) > store-writer repair. — [report](2026-08-15-post-rearm-performance-dive.md) |
| 2026-08-14 | **MARGINAL-GATE REPAIR BUILT + REARMED (`93ad553`, restart 19:31 ET)** | **Operator "Go": both defects repaired, all gates PASS incl. the NEW mandatory quote-production counterfactual (20,478 real refusals replayed: OLD 0.00% admit / NEW 91.27%, top ~9% concentrators still refuse); suite 3,675/0; vitals fast 8/8 + pre-ship 1/1 GREEN; first quotes ~3 min after boot (morning: 4h52m), zero halts, zero reneges** | Fix A (D2): criterion → `marginal_tail_admit` = dES99 × ES_TAIL_ALPHA(0.01) ≤ dEV — P(accept) cancels at a fill-conditional decision (stays in eviction where it IS correct) + conditional ES must carry its tail mass vs unconditional EV; confirm-site twins get derived allowances (z/n shared-CRN noise quantum + Δp×anchored-line ≤ admission_ev) ending the renege class. Fix B (D1): directional cap candidate-game-scoped (UNKNOWN ⇒ full sweep fail-closed; book-only callers byte-identical). First window: decline mix now LED by the structure cap (461 = the 1% whale rule working), cvar 11,212/10min → 175 total; det $2,580 vs backstop $3,115; p_ruin 0.233 (the exact regime that bricked all day) quoting through. Blast radius: two criterion sites + two confirm comparisons + check(4) scoping — pricing untouched, all deterministic walls untouched, det backstop stays the only level gate (the constitution now true in code). — [report](2026-08-14-marginal-gate-repair-built-and-rearmed.md) |
| 2026-08-14 | **ARMING-DAY FILL COLLAPSE FORENSICS — operator: "we've filled basically nothing"** | **CONFIRMED: 4 fills / $52.76 post-arm vs 192 / ~$4.4k on 8/13; first quote 14:25 ET (4h52m zero); 6/10 accepts RENEGED at last-look (3 positive-EV) by kill_marginal_raises_p_kill; assessment delivered, NOTHING edited — decision menu on operator's desk** | Causal chain: book booted PINNED at the 0.70B backstop AND over both tail budgets (p_ruin 0.102>0.05, P(KILL) 0.446>0.02) from minute one → marginal regime permanent. THREE DEFECTS: D1 directional cap iterates ALL book games, no candidate scoping (limits.py:1699-1712 — a book MLB direction refused a pure 7-leg MLS parlay, decision 111700015; the "8/1 freeze shape" its own comment warns about); D2 marginal KILL/ruin EV-credit asymmetry — EV × CP-lower p_accept (~2-3e-4) vs 100% of tail charge = refusals 174,000-292,000× over (163k cvar refusals; only dES99=0 fresh-game flow passes — how club soccer quoted); D3 entity accumulator folds in-flight CANDIDATES into prior_cc (soccer max prior $21.57 yet 11.9k entity refusals = batch artifacts; 1.15M/day wall). Over-determination: the UN-armed bot had already quoted zero for its final 2h41m — old walls brick at this saturation on their own. P&L TRUTH: 8/14 +$128.88 exchange (all overnight; post-restart −$140.04); 8/13 final −$215.34 (log tracker had −$183.52; seed inherited a −$33.23 ledger double-count). 1-2% DIRECTIVE: enforced since 09:33 by structure 1% ($44.79, biggest post-arm fill $22.76; today's −$141/−$143 losers were PRE-arm inventory); holes = cross-ticker structure stacking (post-freeze hash fix) + per_combo 5%→2% option. Second validate-caps-can-quote incident on record — arming checklist now requires a quote-production counterfactual. — [report](2026-08-14-arming-day-fill-collapse-forensics.md) |
| 2026-08-14 | **SPORTSBOOK PARLAY MARKUP COMPARISON — "do we have a decent markup?"** | **Verified web research (3 researchers + adversarial number-verifier + synthesis; every load-bearing figure re-derived); live ladder re-verified vs yaml before comparison; NO markup change proposed** | At every fair ≥15¢ we are the CHEAPEST parlay product in the comparison set — mains 1¢ = 2.0–2.9% of fair vs realized US book parlay hold 17.7–18.7% of handle (≈21.6¢/$1 fair; IL FY2023 + NJ FY2025, parlay/straight multiple stable 3.6–3.7×), below even straight-bet vig (the Kalshi taker FEE exceeds our mains markup); mid band cheap-to-par; longshots <15¢ flip ABOVE posted-book rates but stay under the observed Kalshi field (rival medians 3–9¢ above us) and far under realized Kalshi retail longshot losses (sub-10¢ buyers lose >60%). Our realized capture +2.5¢/$1 premium ≈ 1/7th of a book's take = cheapest-maker-in-a-competitive-auction; the schedule survives adverse selection. Headroom is bounded by the BEST rival quote, not book holds — decisive missing measurement = per-auction gap-to-best-rival by fair band (offered, not started). Esports flat 3¢ shape is inverted vs every industry curve (rich at mains, thinnest at longshots) — noted for the next pooled esports read. — [report](2026-08-14-sportsbook-parlay-markup-comparison.md) |
| 2026-08-14 | **CEILING ATTRIBUTION ($2.8–2.9k vs "allowed 70%") + ROLLING-QUOTE CYCLE MEASURED** | **Read-only (16,011 snapshots, 8/11–8/14 logs); both operator questions SOLVED; design ideas A–E surfaced (design-only, no code)** | The stopping band = the SLATE cap 65% ($2,911) binding FIRST under the 0.70B backstop ($3,135) the operator remembers as "70%" — receipt: 8/12 slate skips fired exactly at $2,904, then the book waiver-ground to 100.07% of the backstop; slate 65→80 lifts the ceiling only +$224. Rolling cycle: p_book does NOT drop late — the real late wall is EXCHANGE CASH (insufficient_balance 33.7k→225k/day, peak 7.2/s); freed nightly headroom rolls 84–94% into NEXT-day builds (8/13 night redeployed 146% → 8/14 morning pinned at 100.3%); det parking: MLB 9.9h vs esports 31.6h vs weekend club ~40h+. FLAG: the static $500 daily-loss halt (ruled DELETE 8/12, parked in freeze) came within $27 of firing 8/13 — deletion recommended under the same carve-out as the levers. — [report](2026-08-14-ceiling-attribution-and-rolling-quote-analysis.md) |
| 2026-08-14 | **VARIANCE LEVERS ARMED — marginal KILL/ruin constitution + P1 structure cap + acceptance seed LIVE (09:33 ET boot, `7a742e5`)** | **All 3 pre-arm gates PASS (live seed dry-run 107k/88 discriminating-TRUE; whale replay REFUSES both 8/13 shapes at ~$45, median $9.19 passes; pre-ship 1/1 GREEN 530.7s — the inherited 5×-book RED cleared); armed lines: kill_anchored true + seed + ruin 0.05 + det 0.36/0.70B-backstop + structure 0.01 ARMED + direction 0.40 SHADOW; bot LIVE, zero halts** | Boot state = the ratified "fully maxed at 70%": det $3,145 vs backstop $3,136, p_kill 0.388 → the bot correctly holds (0 quote_sent first window; entity/util/directional dominate the decline mix) and resumes on the MARGIN as settlements free room, with the structure cap preventing whale re-concentration. One transient at start #1: `database is locked` at Store.open's WAL pragma (watchdog store-probe boot race; busy_timeout-before-pragma fix owed) — clean restart resolved. P&L truth vs "another negative day": 8/13 finished −$183.52 (midday −$473 recovered), 8/14 +$269 overnight, equity $4,843 (~$510 off peak). In flight: ceiling attribution (~$2.9k = 65% slate cap hypothesis), rolling-quote/late-slate analysis, sportsbook parlay-markup research. — [report](2026-08-14-variance-levers-armed.md) |
| 2026-08-13 | **P1 VARIANCE LEVERS BUILT DARK (`7a742e5`) — per-STRUCTURE + per-game-DIRECTION accumulated bounds + acceptance-tape boot seed** | **Suite 3,667/0 (24 new tests) + mypy + vitals fast 8/8 + scratch proof at the pushed commit; merged to main; ALL DEFAULTS OFF = byte-identical dark; ARMING = one restart after pre-arm gates (seeded counterfactuals + whale replay + pre-ship)** | STRUCTURE bound (`skip_structure_loss_cap`): accumulated committed+reserved+candidate premium per combo market vs the ratified 1% per-combo ANCHOR — candidate-scoped, NOT waivable, no lone-candidate guard (the 8/13 −$177/271ct and −$137/282ct whales at 2.9–3.7% of bankroll REFUSE at ~$47); GAME-DIRECTION NET (`skip_game_direction_net_cap`): NEW `directional_net_by_game_cc` census (branch-max fold, committed+extra only, never resting quotes) judged vs the SUNK committed baseline (8/1 constitutional) — waivable, game-keyed, evictable-on-fill, derived fraction, shadow-first. SEED: sizing arithmetic unified (`det_consumed_cc`/`risk_qty_from_terms`, parity to the cent), `seed_counts` additive, `ops/acceptance_seed.py` reconstructs 24h from decisions+rfqs (id-bisected, chunked, mode=ro second conn; failure ⇒ empty tape = today), boots off-thread behind `acceptance_seed_from_store`; live-shaped seed flips `discriminating()` TRUE from boot (CP-lower > 0 every bucket — the 8/1 0.6% brick dead). Arming stanza + first-hour watch + rollback in the report; fallback tag stands. — [report](2026-08-13-p1-variance-levers-built-dark.md) |
| 2026-08-13 | **DRAWDOWN DISSECTION (−$1,070 from peak) + OPERATOR DECISION: build the variance levers NOW** | **Read-only dissection; verdict NO BLEED (entry EV positive every day ever measured; 8/13's 41 settles 41/41 +EV) — the swings are CONCENTRATION: whale seam 6th sighting (top-3 tickets = 82% of today's −$473; 271–282ct near-coins carrying $0.25–$2.39 EV at 2.9–3.7% of bankroll each) + one-cluster slates + unarmed tail budget. OPERATOR: "build both now" (supersedes the 8/31 freeze for these two): P1 Stage-1 per-STRUCTURE bounds + marginal-gate arm (acceptance-seed fix, four lines, one restart). FALLBACK SNAPSHOT: tag `fallback-pre-variance-levers-20260813` (= b02c451) pushed + local yaml backup `.fallback-20260813`** | 8/11 −$917 was the week's BEST entry-EV day (+$146.70) killed by a one-cluster slate; 8/12 +$320 normal; 8/13-so-far −$473 = 3 whale tickets (MC P ≈ 1.8% indep-floor). Daily EV +$30–150 vs ±$500–1,100 swings — S/N ≈ 0.1/day; the design, not the model. Club soccer uninvolved (zero club settles yet; 1 UECL fill $23.50). — [report](2026-08-13-drawdown-dissection-and-variance-lever-decision.md) |
| 2026-08-13 | **CLUB SOCCER LIVE — La Liga + MLS (GAME/TOTAL/SPREAD/BTTS) + UECL (all four + ADVANCE) on the WC Dixon-Coles machinery** | **SHIPPED `67579a5` + RELIT 09:28 ET; club RFQs QUOTING (3 UECL-legged quote_sent within minutes); suite 3,643/0 + scratch-proof + vitals fast 8/8×2; probe 14/14 live pure-club RFQs QUOTED (incl. target-cost sizing); MLB byte-identical differential; pre-ship 0/1 = the KNOWN inherited 5×-book RED (8/6 adjudication, P1 queue, frozen to 8/31)** | Edits: UECL sport keyword ("UCL"⊄"UECL" — was UNKNOWN-sport), LALIGAADVANCE blocker, club→soccer markup map (was "other"=ZERO markup), **farmable series-scoped to KXWC** (club 48h reschedule scalar breaks the airtight bar — all 8 grant sites gated on ALL legs), **two-legged ADVANCE regime guard in sgp** (same-game UECL advance pairs price UNTYPED — single-match advance ρ never applies; knockout_series stays [KXWC] so DC's Advance spec never sees UECL), **occurrence_datetime third pregame anchor** (game-day rungs carry creation-relative exp — would have quoted 1.3h in-play). Structural/DC: ZERO changes (club codes/lines/GROUP semantics identical to WC; DC has no venue parameter — home/away moot for pricing). Yaml: +13 exact-family allowlist entries, decline_two_legged_tie false, per-league offsets 3.0167/4.0167 (measured kickoff+3h/+4h), 48h horizons. The "WC coefficients" were CLUB-calibrated all along (dc_ρ on 8,980 club games). Watch: UECL ADVANCE leg-1 blindness at the next round, La Liga offsets unverified until 8/15, club×MLB mixed combos newly reachable, first settlements reconcile tonight. — [report](2026-08-13-club-soccer-wired-live.md) |
| 2026-08-12 | **LA LIGA / MLS / UECL VALIDATED vs API + RETRACTION of the "zero outside demand" census** | **Operator-directed wiring targets CONFIRMED against source of truth (series list, open markets, collection membership, open-RFQ set); the morning census conclusion RETRACTED — operator caught it** | RETRACTION: the `rfqs` store only sees allowlisted RFQs — the intake **firehose gate (`rfq/intake.py:186`) drops any RFQ with a non-allowlisted leg BEFORE the recorder** (WS channel itself is global per pinned AsyncAPI); the REST open-RFQ set (20,000 pulled, ALL combos) is ~25%+ non-allowlisted: tennis ~4.2k, CLUBF 3,341, LEAGUESCUP 2,471, **UECL 927 (matches 8/12–13)**, WNBA ~2k, F5 ~1.15k, La Liga 84 (opens 8/15), MLS 77; raw example = a live `KXUECLGAME × KXUEFASCADVANCE` 2-leg in KXMVECROSSCATEGORY-R, the collection we already quote. Census tool rewritten (REST = the demand detector; store mode = within-allowlist mix only). VALIDATION: **La Liga + MLS carry all four requested types LIVE (GAME 3-way/TOTAL rungs/SPREAD/BTTS); UECL has ALL types live (GAME 84, TOTAL 170, SPREAD 114, BTTS 28, ADVANCE 56 over 28 qualifying ties) — "ML-only" app view superseded**; combo-collection membership proven by reverse probe for all three leagues; **DRIFT: new collection `KXMVECROSSCATEGORY-SHARD1-R`** (mvec scan baseline = 2, now 3) + **NFL GAME/SPREAD/TOTAL already combo-eligible** (16 events each). Wiring plan = WC machinery reused (Dixon-Coles club-trained, measured pair table); league-specific items: classification keywords, club game-codes carry NO kickoff time, **home/away frame (NEW — WC was neutral-venue)**, settlement-scope pinning (rules text owed), allowlist last. — [report](2026-08-12-soccer-league-validation-and-rfq-demand-retraction.md) |
| 2026-08-12 | **DEFERRED-UPDATES LEDGER (engine freeze until 8/31) + SPORTS ONBOARDING REVIEW/DEMAND CENSUS** | **Operator ruling: run the bot UNTOUCHED until 8/31, decide risk posture with the month of data; ALL deferred work recorded with resume recipes; active workstream = sports onboarding (pricing-side, allowlist-gated)** | The ledger (`2026-08-12-deferred-updates-ledger-until-2026-08-31.md`) parks with exact recipes: the two same-day ratifications (utilization-backstop repair "fix + derive"; static $500 daily-loss stop DELETE — edit sites scoped to the line, nothing edited), the marginal-gate arm (operator "NOT YET", revisit ≥9/1), P1 Stage-1, day-P&L seed, withdraw raise, insufficient_balance back-off, accounting sweep, store rotation (149.6 GB / rfqs 48.8M rows), marked-EV drift decomposition; §D pre-registers the 9/1 dashboard (pooled read ~2.5–3k settles, loss-day census vs p_kill_night, whale tally, capacity histogram, MLB-vs-new-sport split). The sports review (`2026-08-12-sports-onboarding-review-and-demand-census.md`): NEW TOOL `tools/diagnostics/rfq_series_census.py` (read-only; recording verified PRE-filter at quote_app.py:2493) measured **ZERO non-allowlisted RFQs in ~8M over 4.5 days** — current taker demand is 100% MLB (97.5%) + LOL/CS2 (2.5%), so wiring order is season-calendar-driven: **(1) club soccer/EPL first (engine already club-trained — DC OOS-gated on 8,980 club games; EPL opens mid-Aug), (2) NFL Stage-0 recon in parallel (margin_total OOS-passed 7/6; spread line-sign verification owed; Sep opener = biggest demand onboard), (3) weekly census as the new-sport demand detector, (4) F5 re-admit via measured mini-onboard, UCL tie-handling before Sep.** WNBA: zero demand despite mid-season — detector decides. — [ledger](2026-08-12-deferred-updates-ledger-until-2026-08-31.md) · [sports](2026-08-12-sports-onboarding-review-and-demand-census.md) |
| 2026-08-12 | **LOSS-DAY FORENSICS + KILL-BUDGET AUDIT — "why do we lose ~10% days when the engine says 2%?"** | **Read-only (16.63M risk_audit rows + 32k snapshots + 1,010-ticket entry-EV join); VERDICT: the 2% night-tail budget is a SHADOW NUMBER — measured `p_kill_night` peaked 0.25–0.50 EVERY big-slate day while every gate on that axis is disarmed/stood-down; 3 loss days ≤−8% in 23 (7/23 −8.6%, 8/2 −8.2%, 8/11 −17.1%) is the EXPECTED rate at those measured tails** | 8/11 (−$917, worst ever) was the week's BEST entry-EV day (+$146.70, 3.73¢/$1) hit by −$1,110 of one-cluster correlated variance (all 180 tickets chain into ONE game-cluster across 24 games; 73 wins vs 99.3 expected, z −4.26 under independence; breadth not whale — every band and leg-count lost; NO refit warranted). 8/9 (+$1,092, best ever) = same book width, opposite draw (+$985 luck on +$108 EV; top-2 winners were near-zero-EV KS longshots — P1 whale seam 4th sighting). Admitted books carried P(≤−12% equity) of 4.05% (8/11) / 10.22% (8/9) by our OWN fairs under independence — floors, before cluster correlation. What binds today is CAPACITY (util-backstop 1.27M skips + 52,490 insufficient_balance on 8/11; cash = the last brake at 103% collateral), not tail risk: kill-tail decline reasons fired 0× in 16.63M rows (`kill_anchored_book_gate` false since 8/1, ruin budget 1.0, daily-loss frac 1.0 per 8/5 ruling). NEW defects: relight RESETS the static $500 daily-loss baseline (9 trips 01:36–02:09 ET 8/11, zero after the 02:16 relight at standing −$612 — the 7/25 §5-HIGH day-seed no-op again); `halt_reconciliation_mismatch` 8/8 ($51 predicted vs $5 exchange); invisible-settlement class +12 tickers (+$170.07); marked book EV median NEGATIVE every full day (post-fill drift, decomposition owed); p_book median 0.40–0.50 all week — more cash bought a BIGGER book (median positions 31→102), not a higher P(book). THE LEVER (decision owed): acceptance-seed fix + one-restart marginal-gate arm makes the 2% real on the margin without freezing; P1 Stage-1 + backstop repair (ratified today) complement. — [report](2026-08-12-loss-day-forensics-and-kill-budget-audit.md) |
| 2026-08-12 | **WEEK-AWAY READOUT (8/6→8/12 autonomous run) + WINDOWS-UPDATE REBOOT OUTAGE** | **Bot DOWN since 8/12 02:32 ET (OS reboot killed bot+watchdog; nothing auto-starts after boot); read-only reconcile done; relight = operator decision** | The bot ran itself all week on `27ccd65` (44 watchdog relights, spans to ~21 h): **+$1,180.12 realized / 1,009 settlements since 8/6** — more than the account's entire prior life (+$1,557.31); equity now **$4,695.43** ($3,370.01 cash + $1,325.42 open mark riding tonight's slate; zero leftover quotes; single $2,000 deposit ever, all-time realized +$2,737.43). Daily: 8/7 +$364.88, 8/8 +$333.10, **8/9 +$1,092.10 (best day ever)**, 8/10 +$6.29, **8/11 −$917.00 (worst day ever — broad, not one whale: bottom-5 = −$613.62, max single −$169.33; full entry-EV forensics OWED)**, 8/12 +$300.16. Outage chain: nightly maintenance flaps (02:06 kill → relights 02:07/02:14) → **Windows Update rebooted the PC 02:32** mid-quote; final 14 min also logged **2,233 `insufficient_balance` 400s** (cash fully deployed — pause-new-creates mechanism owed) with util=1.00 walls binding at ~2× the bankroll they were measured on. **DNP scalar guard was ARMED (local yaml `enabled: true`, 8/6 ratification) and live all week** — commit/README "shadow-dark" labels are superseded; marginal kill/ruin flags read true but are INERT behind `kill_anchored_book_gate: false`. New ops findings: live store now **149.6 GB** (rotation queued since 47 GB); ~20 stall-kills 8/10–8/12 all at the hand-set 30.5 s wall. Decisions owed: relight posture; boot persistence (scheduled task + WU active hours); utilization-backstop repair ratification (REMIND 8/2, 10 days over). — [report](2026-08-12-week-away-readout-and-reboot-outage.md) |
| 2026-08-06 | **DNP SCALAR GUARD BUILT — void-branch pricing + sniper ask floor (single-player-driven combos)** | **BUILT DARK (`pricing.dnp_scalar.enabled: false` = byte-identical, era-replay proven); suite 3,600/0 (+18 new); vitals fast 8/8 GREEN; pre-ship RED but PRE-EXISTING (5×-book confirm-window breach, IDENTICAL at origin/main on today's 144-position live book — this branch marginally faster; live size clears 62%); arming line STAGED with the operator ratification quoted** | Operator ratified (verbatim "I don't want to get picked off by full scalar ones") + rulebook-confirmed: DNP leg "resolves to the fair market price", combo multiplies scalars floor-rounded ⇒ the all-scalar branch of a same-player combo pays the INDEPENDENCE product. Mechanism (no knobs): (1) **void-branch fair** = (1−h)·V_corr + h·⌊∏s⌋, h = MEASURED per-family DNP hazard (23/997 pooled 2.31%; baseline counts regenerated by `tools/measure_dnp_hazard.py`, LIVE-merged from the settled-leg cache on the settlement cadence — settled.py now records graded scalars; thin family fails closed to max(own, pooled)); applied only when Δ>0 (only ever raises the ask). (2) **Sniper floor**: `construct_quote` floors the implied YES ask at ⌊∏s⌋ for single-player-driven combos (UNKNOWN entity fails closed INTO scope; entity = the leg-axis ticker segment, no new parser) — an h=1 taker pays ≥ the scalar settle: **we either profit or don't lose on a scratch**. PROOFS on the real engine + shipped prod config: era replay 449/449 out-of-scope rows byte-identical + 144/144 randomized; 9/9 non-sniped same-player rows cent-identical; **the 3 Marte tickets floor 12→20¢/12→20¢/8→17¢ = exactly their scalar settles (sniper profit ≤ 0; the −$39.75 becomes ≥ $0)**. Out of scope (noted follow-up with measured exposure): multi-player all-prop + mixed prop×game partial-scalar branches; the 48h rain rule. 7/23 audit's "mutex bound HELD" carries a scalar caveat now (binary-only claim; T1 was a BYPASS). — [report](2026-08-06-dnp-scalar-guard-build.md) |
| 2026-08-06 | **DNP SCALAR PICKOFF — 3 same-player Marte combos settled scalar, taker +$39.75** | **Read-only forensics; the 3 tickets identified to the cent; rule fetched for the exact tickers; fix class specified → BUILT same day (row above)** | The 3 = `…E8BE6BD67FD` (7/23, −$9.22) + `…97F5BF3F0AC` / `…A50F06E3000` (8/5 fills, settled 8/6, −$15.05/−$15.48) — all Ketel Marte 2-leg anti-correlated CROSSCATEGORY combos, the ONLY same-player scalars in 992 settlements. Exchange-proven DNP (his 3 leg markets graded `result="scalar"`; 8/4 legs binary). **Rule (fetched, exact tickers): scratched/not-in-starting-lineup ⇒ leg "resolves to the fair market price"; combo = "scalar outcomes multiplied (rounded down)" ⇒ a same-player combo under DNP pays the INDEPENDENCE product (17–21¢) while our correlation fair was 2.4–6.1¢ — DNP bypasses the mutex/containment bound entirely** (verified numerically: indep ∏ 20.8/21.0/17.9 → settles 20/20/17). Pickoff shape: T1 filled 38 min pregame (lineup public — knowable, CONFIRMED); 8/5 pair 35 s apart, 2h54m pregame, jointly contradictory under "plays" (HRR<2 AND HRR≥2) but both pay under DNP — one informed actor, same template as the 7/23 audit's taker. **Honest answer to "we worked on this": yes — `docs/dnp_scalar_settlement.md` (7/9–7/10) + AS4 (7/22) + same-player audit (7/23) — and the decision each time was BUILD NOTHING/accounting-only; the EV-neutral theorem is FALSE for same-player combos (all legs void at once). Never built: any DNP pricing/width/gate.** Fix class [MEASURED-STRUCTURAL]: void-branch pricing (h measured 2.35% on 978 legs) + Δ-width (Δ=∏s−V_corr; 14.7–15.5¢ on the 3, ≤3¢ on all 24 other same-player combos — exact detector, zero false positives); lineup feed = [DECISION]. Exposure: prop premium 0–4h pregame ≈$407/day; exploitable large-Δ subset = 3 tickets/$349.62, hit 3-for-3. — [report](2026-08-06-dnp-scalar-pickoff.md) |
| 2026-08-06 | **PROFIT-NIGHT FORENSICS (8/5 evening → 8/6 morning) — luck or high EV?** | **Read-only; incremental exchange pull merged onto the sweep caches; reconciled to the cent; verdict: LUCK (~88%)** | Night (settles 8/5 17:00 ET →) = **+$470.28 (81 settles, 51W/30L)**; evening-ET slice +$488.79; app's "~$500–600" = the realized↔mark-to-market straddle (total portfolio value moved **+$631.64**; cash $2,370.05). Entry-EV recovered 80/81: **expected +$56.72 (2.3¢/$1 — a NORMAL accrual night) vs luck +$417.75**; **P(night ≥ +$474 \| our fairs) = 0.096** and truly commoner (only **3 game-clusters** → fatter tails); wins 51 vs 47.7 expected — overshoot is dollar-weighted: **top-5 tickets = 96% of net**, led by a 345ct 4-leg pure-KS @29¢ with **+$0.24 EV** hitting its 29% branch for **+$244.99**; 7 of top-10 near-coins ($3.19 total \|EV\| → +$158) = **P1 whale seam, third sighting**. Night won WHERE THE ERA LOSES (40–60¢ +$312, pure-KS +$180, 2+-prop-leg +$490) and lost in the era's robust cells (60–80¢ −$49, GAME×TOTAL −$98) — **structure-contradicting = variance**; only 2-leg confirmed. Pooled era (740 settles): realized +$868.25, excess +$596.49, **P(excess≤0) 0.242→0.095** (strengthened, still HYP), P(real≤0)=0.048 first sub-.05; 60–80¢ robust (P=0.033, 0 LOCO flips), 2-leg strengthened (+$941, P=0.004), GAME×TOTAL weakened, pure-KS realized flipped +$12 (noise) but **marginal-bias mechanism unchanged on 1,484 legs (KS +6.4/HR +8.5/RFI +11.4; GAME −7.7/TOTAL −5.0 our favor; HRR weakened +4.0→+1.5)**. The 6 adverse 8/5 recovery prints settled = 2 tickers, **both lost (−$31.04)**, the night's only −EV entries; their 5 multi-prints re-triggered the ledger overstatement seam (−$46.85 booked vs −$25.59 true); **1 NEW invisible settlement** (−$4.18; class now 8 / −$98.25). Bot timeline log-verified: last fill 02:50 ET, down ~03:12–05:33 ET (Kalshi 503 → supervisor fail-closed kill ×2), clean boot 05:33 ET, LIVE. No decision owed; queue unchanged (backstop repair → P1 → accounting sweep). — [report](2026-08-06-profit-night-forensics.md) |
| 2026-08-05 | **BLEEDER DISSECTION (the sweep's loss cells, disjoint + mechanism-tested)** | **Read-only; era re-reconciled +$455.03 to the cent; 1,321 era legs settled via 14 paced GETs; AND(legs)==combo result 628/628** | Gross losses −$4,540.42 partitioned **disjointly** (mechanism-first greedy + exact tag-set table — the sweep's overlapping cells overstate distinct problems ~3×). **ONE live pricing defect found [MEASURED-STRUCTURAL]: prop-leg marginals are beatable** — taker-side hit% beats our leg mids by +3..+9 pts on KS/HR/HRR/SPREAD/RFI (opposite sign on GAME/TOTAL, matching the family P&L pattern); pure-KS joint z +2.58 vs model **collapses to +0.34** under bias-corrected independence (correlation model EXONERATED, corr credit +0.1 pt, ZERO same-pitcher ladders — cross-game side-picking, repeat names Schlittler/Nola/Cantillo/Ryan); 4+-leg overshoot +2.15 → +0.54 same way (bias COMPOUNDS with legs: prop-leg share 65%→80%); 5+ $ additionally whale-shaped (worst-3 −$292 of −$454 → P1). **KILLED as cells: 40–60¢ band (band-only rows +$142 excess) and $30–60 size (+$151) = symptoms; stale-10–60s pickoff REFUTED by the 2.86M-point leg-mid tape (drift +0.004¢/leg, 30/32 flat)**. pure-SPREAD = ONE anti-blowout template (6/7 on 8/1–8/2, cl=2) quoted at 0.4¢/$1 edge [HYP]; 92¢+ count z −3.74 (n=6) = pre-registered tail watch; recovery prints 1.2/day ≈ $2–4/day adverse [HYP] → reporting-tag build only; only **3 tickets era-wide entered −EV (−$1.80, all recovery)** — the entry gate is clean. No new operator decision; consolidates sweep watches #6/#7 into the prop-marginal + player-gap build class. — [report](2026-08-05-bleeder-dissection.md) |
| 2026-08-05 | **FULL P&L SWEEP (live era, 661 settlements)** | **Read-only exchange-truth; reconciled with every seam decomposed; edge-beyond-model still HYPOTHESIS (P(excess≤0)=0.242, 14 cl)** | Era ≥7/28: **+$455.03 on $13,428.75 premium (59.5% W vs 58.3% implied, 59.9% model — count calibration dead-on)**; entry-EV +$332.43 (+2.5¢/$1). **ALL profit lives in the 60–80¢ band (+$663.31, +11.1% ROI, 16 cl, P(real≤0)=0.004 — the sweep's only non-HYP cell)**; 40–60¢ −$132, 80¢+ −$73 (expensive-NO +1.2% claim REVERSED), 5+-leg −$384/−27% ROI (NEW, 7 cl). Whale seam re-measured: top-10 tickets = **121% of era net**. 7/31 repairs changed **VOLUME (2.5×), not edge** (2.58→2.55¢/$1; post-era realized +$282.23 on ALL rows, P(real≤0)=0.103 — the draft's 0.016 was EV-recovered-rows-only). **Taker trace RESOLVED + log-verified**: all 11 era taker prints matched 1:1 to `fill_recovery_late_execution` events (`lifecycle.py:7067`; INCIDENT-C recovery booking; 6 new 8/5, all negative store edge — adverse by construction). NEW seams: ledger overstates losses $132.24 (multi-fill dup — feeds `halt_daily_loss`); 7 invisible settlements −$94.07 (esports collections, no store/ledger row); 18 store gaps. **Cap saturation measured 8/5: 183k util=1.00 + 123k skips vs 36 accepts** → utilization-backstop repair ranked #1 open decision; then P1 per-structure bounds; then accounting sweep. WRONG: memory's "8/2 413 fills/$7,349" (truth 191/$2,778.82). **ADVERSARIALLY VERIFIED same day** (independent re-derivation from the raw exchange cache + own-seed cluster bootstrap + leave-one-cluster-out): every headline number re-derived to the cent; demotions applied — 3-leg → HYP (sign-flip P=0.195), pure-KS/pure-SPREAD flip under LOCO, standing "HIT negative" NOT replicated (realized +$44.35, excess −$44.01); **60–80¢ band survives everything (0 LOCO flips, worst +$419, P=0.003)**; day-table deployed column was store-side (7/31 understated by $302 — fixed, both columns now shown). — [report](2026-08-05-full-pnl-sweep.md) |
| 2026-08-02 | **WIN-NIGHT FORENSICS (8/1 slate) — luck or good fills?** | **Read-only, exchange-truth reconciled to the cent; verdict: GOOD FILLS (+$59.47 expected) × LUCK (+$179.51) — a 1-in-4 night** | Task window (settled ≥8/1 20:00Z): **113 settles +$241.25** (68W/45L); app's "~+$300" = ET calendar day +$291.89; pure evening slate +$408.41 (P=0.080) before this morning's −$167.16 give-back. Entry EV recovered 111/113; **P(night ≥ +$239 \| entry fairs) = 0.253**; wins 67 vs 67.26 expected — luck was dollar-weighted (top-5 lucky rows all near-coin 100–220ct, ≈$0 EV): the 7/31 whale-concentration seam with the sign flipped. Fill quality: 115/118 maker, **39/46 at-or-better than raw-mid fair (median +1.50¢/ct)**, 112/113 positive model EV; adverse pockets = same-game GAME×SPREAD×TOTAL (2 rows, −9.7/−8.5¢) + **3 taker prints, 3/3 losers −$50.41 (trace owed — maker-only rule)**; no F5 legs (gate holding). POOLED since 7/28: EV +$150.19 / realized +$446.89 / excess +$296.69 on **9 clusters → P(excess≤0)=0.200 (standing 0.061 got WEAKER)**; cheap-NO +77.9% split **FAILED OOS (−30.4%, 2 clusters)**; ≥85¢ ≈flat replicates. Integrity: 2 fills missing from store (ledger rows exist — sweep held), 3 scalar partial-value settles (new convention), 4 duplicate-pl-row tickers explain 114-vs-113. — [report](2026-08-02-win-night-forensics.md) |
| 2026-08-01 | **MARGINAL RUIN GATE + evening-freeze p_ruin DECOMPOSITION (constitutional rule: the standing book is SUNK — only the 0.70B det-max backstop is a LEVEL gate)** | **BUILT DARK (`ruin_gate_marginal` default False = byte-identical; `marginal-kill-gate` machinery merged + reused verbatim); suite 3,582/0 (15 new property tests); vitals fast 8/8 + pre-ship 1/1 GREEN; arming lines STAGED (ALL flags, ONE restart — checklist in the report)** | THE FREEZE: `skip_portfolio_ruin` from 20:17:42Z — 219,167 refusal rows / 151,169 candidates, sent=0, p_ruin 0.2994 vs 0.05. DECOMPOSED (offline repro, real code): NOT a numeric defect — the real ruin distance was ~$140–360 (not the assumed ~$825); ~$550–680 of RESERVED (unmodeled) premium is written off to zero in the cost-basis equity (amplified by settled-resolution STARVATION, 5,281 pending events), Wilson band exactly 0, bankroll anchor FRESH (the stale `daily_ruin_anchors` table is vitals-only); ES99-vs-distance "inconsistency" = the two numbers describe different books (modeled vs written-off). Natural experiment: 3 in-flight pregame fills moved p_ruin 0.2994→0.1649 in 90 s — the refused flow was the cure. BUILD: §(9) quote-time + confirm-path `_candidate_gate` two-regime marginal forms (same `KillMarginalCandidate`/dES99/CP-lower/CRN machinery, one criterion two axes); sweep verdict: only backstop-class levels survive (det-max 0.70B, utilization notional, staleness fail-closed, halts). Counterfactual: day-one floor admits 884 diversifiers (0.6% of flow — un-bricks quoting) and correctly refuses the 99.4% same-slate concentrators whose all-fill trajectory would RAISE p_ruin. — [report](2026-08-01-ruin-gate-marginal-and-freeze-decomposition.md) |
| 2026-08-01 | **PREGAME-SURGE DEAFNESS — class-aware WS overflow: shed market data, never disconnect** | **BUILT + PROVEN on `ws-marketdata-shed`; loads at next restart — operator wants THIS restart FAST (bot deaf during its filling window); restart checklist in the report** | Live defect measured on today's tape: since 16:08Z the comms socket cycled overflow→discard→reconnect **14× in 62 min** (fill cycles 75–1480s = net accumulation 14–268 f/s; **276,644 frames discarded** incl. every queued `rfq_created`) — near-zero new-RFQ intake for most of the pregame filling window while repricing ~500 sends/min into a void. Provenance: `410e8fb`'s fail-closed overflow⇒reconnect, sized for transient runaways, meets a surge that exceeds drain for an HOUR — no finite queue fits that; policy split instead: ORDER-INTEGRITY frames keep every fail-closed guarantee (+bonus fix: an accept at a full queue used to DISCONNECT via the wake sentinel — now banked); MARKET-DATA (`rfq_created`/`rfq_deleted`, comms only — book socket byte-identical) sheds OLDEST with counters, never disconnects; control frames ride a carry lane, never lost; wire-age pre-parse gate at intake (every frame now wire-stamped; drops `rfq_created` older than the existing `RFQ_MAX_QUEUE_DWELL_S` horizon before parse) keeps the queue at its FRESH tail. **Replay proof (real WsManager+RfqIntake, real tape payloads, worst measured cycle 268 f/s net): OLD disconnects at 76.1s (live: 74.5s — reproduces to 2%); NEW connected throughout, intake alive+fresh at surge end, accepts 7–30ms wire→handler mid-surge; confirm-priority S-proofs re-run 43/43; throughput no-op drain 1.18M→1.27M f/s (no regression)**. Suite green + vitals fast 8/8 + pre-ship 1/1 at the worktree. — [report](2026-08-01-ws-market-shed-surge-deafness.md) |
| 2026-08-01 | **MARGINAL KILL GATE — the sunk-book two-regime form (operator ruling: "even if we did have a -EV book we should still quote to increase it")** | **BUILT DARK (`kill_gate_marginal` default False = byte-identical); suite 3,551/0 (18 new property tests); vitals fast 8/8 GREEN; arming lines STAGED in the live yaml (re-arm `kill_anchored_book_gate` + `kill_gate_marginal` together); pre-ship tier owed at arming** | STEP 1 NAMED the live freeze: the armed LEVEL form on the inherited 26-pos book — envelope P(KILL) 0.110–0.115 vs the 0.02 budget on the gate's own adverse-band measurement (bankroll fresh, snapshot 0.02s old, Wilson negligible, ES fallback not taken); the 50x-lower diagnostic measured a DIFFERENT book under proxy marginals (not a bug). Design: UNDER budget unchanged; OVER budget the candidate's MARGINAL effect decides — deterministic sites reuse the ratified diversity-key (allocated dES99 <= dEV x CP-lower P(accept) at alpha 0.02; certified reducers always admit), the confirm MC gate rides the anchor's own marginal (post P(KILL) must not rise on shared CRN; the CRN ES-delta is measurably resort-inflated to ~full premium for small diversifiers — an ES arm would re-freeze at confirm). Backstop 0.70B absolute, staleness fail-closed, UNKNOWN never admits — all pinned by tests. Counterfactual on the frozen tape in the report. — [report](2026-08-01-marginal-kill-gate.md) |
| 2026-08-01 | **READING B RATIFIED — kill-anchor flag re-landed CLEAN + det-max demotion to the 0.70B ruin-anchor backstop** | **SHIPPED DARK (`kill_anchored_book_gate` default False = byte-identical, golden `b136e16e` re-proven); suite 3,496/0; new tests 24/24; arming stanza staged commented in the live yaml; arming = quiet-machine pre-ship ritual** | Operator ratification (2026-08-01, verbatim "Reading b"): the "ruin floor 30%" anchor is an EQUITY FLOOR ⇒ backstop = (1−0.30)×bankroll = 0.70B, NOT the 30%-drawdown reading; chosen KNOWING the collision (Reading A stays enforced on the p_ruin MC axis, 5% budget, unchanged) and the copula caveat (cross-ρ 0.25 ⇒ target book P(KILL) 20.8%; KILL gate + diversity manage it; det-max@0.70B is the copula-free floor). `7162a4d` re-landed minus every eviction_value reference (the boot-crash import — eviction ships with the other fleet); demotion applied at BOTH sites under a governor guard (no tail gate ⇒ no demotion); canary UPDATED to accept exactly the ratified form and still refuse unratified/unguarded shapes. **Capacity proof on the LIVE book (read-only, $2,808.37): today's wall is refusing the live 39-ticket book RIGHT NOW ($1,117.74 > $1,011.01, P(KILL) 0.2%) — armed admits it with $848.12 headroom**; boundary books flip exactly at the 2% budget (1.1/1.9% admitted, 2.3/4.9/10.4% refused); small-book regime binds TIGHTER armed (n=8 ratio 0.44, n=20 0.67, n=46 1.10, cheap-NO up to 1.95 fenced by the backstop); n=160/$2,400 and 300-diversifier books refused armed. Throughput: flag-off == main (35.1µs check, byte-identical); armed +15% on the admission check only (backstop derivation cached). — [report](2026-08-01-readingb-detmax-demotion.md)**ADVERSARIAL GATE (second fleet, same day): re-executed in an independent scratch worktree — VERIFIED, no findings; golden re-emitted FROM pre-change main (non-circular), canary mutation-tested red on 3 unratified shapes, p_ruin 5% drawdown gate proven still binding fully-armed, suite 3,496/0 + vitals 8/8 re-proven; ONE-restart arming plan staged in the report.** |
| 2026-08-01 | **EVICTION/CAPACITY PORT TO MAIN + ADVERSARIAL GATE + G1 REPAIR** | **PORTED to branch `eviction-capacity-port` (defaults off/off, byte-identity RE-PROVEN 200/200); gate found G1 (NO-SHIP) → G1 REPAIRED same day, branch PUSHED: capacity = min(tier form, withdraw form) — today 191 (withdraw-bound, self-consistent with the hand cap the bumps stalled at), auto-scales with any withdraw-budget raise; merge at the operator restart boundary. Feature tests 37/37; G3 boundary test added; withdraw-budget raise derivation staged from the documented 300 tok/s tier (operator decision)** | Independent re-execution of the whole gate: flag-off decisions byte-identical main-vs-port (200 seeded replays through the real `_try_slot_eviction`); CP math verified vs scipy to 1e-15 with α wired from the ratified `portfolio_kill_tail_prob` anchor; adversarial thrash killed 4/4 incl. the algebraic no-2-cycle proof (lo·lo>up·up infeasible); interference with confirm-priority/warmup/skew-resolution 90/90 at the merged tree; kill-anchor canary 0 references. **G1 (CONFIRMED): the capacity derivation charges standing-book refresh to the tier's 300 tok/s, but EVERY delete flows through the single metered withdraw budget (200 tok/10 s = 20 tok/s) ⇒ the delete path sustains ≈200 slots at TTL 20 s — exactly today's cap; the derived 1,198–1,272 is ~6× over it and arming "on" would defer withdrawals into stale resting quotes (the 07-27 maintenance-stall family). Repair = derive the withdraw budget from the observed tier, then bound capacity by both.** Shadow modes remain safe (log-only, staged commented in the live yaml). — [report](2026-08-01-eviction-capacity-port-to-main.md) |
| 2026-08-01 | **OVERNIGHT EXIT FORENSICS (Task C) + ADVERSARIAL GATE on all three overnight deliverables** | **Read-only; GATE VERDICTS: loss forensics VERIFIED to the cent (independent exchange re-pull); skew fix VERIFIED (parity + counterfactual re-run reproduce the report exactly, suite 3472/0 re-run, pre-ship vitals still OWED AT ARMING); exit census verified against raw tails WITH ONE CORRECTION** | Exit census 15 rows re-verified from the raw log tails: stall-kill ×7 (margins 30.5–31.5s vs the 30.5s wall, incl. the zero-margin 2201 kill), `halt_drawdown` 22:00 (give-back 2831078cc ≥ 2817989cc exact), `halt_marginal_jump` 01:10 (unreadable NEXT-DAY Kwan-HR market sustained 31s>30s), 2021 stillborn exactly 1,198 bytes, checkpoint-lock starvation 872/3,734 exact. **CORRECTION: "halt_confirm_timeouts DEAD" is wrong night-wide — run 1723 was killed BY it at 17:33 (3× HTTP-400 'expired'), BEFORE the confirm-priority lane landed; zero recurrence after = evidence the 7/31 fix works.** Two exits (1754 @18:06, 1904 @20:10) are UNCLASSIFIED hard deaths (logs end mid-INFO, no wedge/halt lines). Structural: the 30.5s stall wall is a hand-set number at the load tail (North Star violation — mechanism repair owed), marginal-jump conflates feed-lifecycle gaps with price jumps. — [report](2026-08-01-overnight-exit-forensics.md) |
| 2026-08-01 | **SKEW SETTLED-LEG FACT RESOLUTION (Task B — the ratified boot-rehydration fix)** | **SHIPPED DARK behind `pricing.skew.settled_fact_resolution` (default false, byte-identical — arming line STAGED commented in the live yaml); suite 3472/0, vitals fast 8/8 GREEN (pre-ship RUNNING at commit — I/O-crawling behind the live bot; verdict owed WITH the arming step); port parity to the cc on every skew-consumed field (synthetic grid + every reconstructed 7/29 tape book state)** | A leg the exchange graded is a FACT, not concentration (det-max FIX 2 semantics, side-aware): determined positions contribute ZERO on every skew axis, partially-settled ones count live legs only, UNRESOLVABLE stays fully counted (UNKNOWN never buys tighter quotes); ONE resolution path (the graded-settlement cache) serves boot AND intraday so they cannot diverge. PRICE-ONLY: the caps' snapshots never see the provider (test-pinned). **HONEST RE-ATTRIBUTION from the full-tape counterfactual: the ratified magnitude story does not hold** — replaying 7/29 through the live skew, removing settled input moves applied skew a median **−2cc** (and last night **0cc**, earned-widen tail IDENTICAL); the settled K-ladder (53.7% of family share at boot) actually produced a **−141cc phantom REBATE** at boot (stale book selling cheaper — the real fail-closed argument for arming) while the widened tail decomposes to **peak (92.6k of 187.1k events, +600cc at cap 4s post-boot on a ~$65 live morning book) + earned directional** — the hot-key acceptance collapse is the PEAK small-book behavior, a separate operator decision. Throughput: default snapshot +14us (+1.7%, in noise), armed +~400us opt-in. **ADVERSARIAL GATE: re-executed everything — 11/11 + suite 3472/0 + part A 8/8 + C1–C4 tape parity PASS + the 7/29 counterfactual reproduced EXACTLY; fail-closed/perverse-case/price-only/boot==intraday all verified in code and tests; pre-ship vitals CONFIRMED structurally blocked while the live log grows (owed at the arming restart).** — [report](2026-08-01-skew-settled-fact-resolution.md) |
| 2026-08-01 | **OVERNIGHT LOSS FORENSICS (settled ≥ 7/31 20:00Z, exchange-truth)** | **Net −$174.76 over 45 settlements (NOT the ledger's −$227.48/40); verdict: 1-in-5-night variance + the F5 tax, no new mispricing** | Reconciled to the cent vs `/portfolio/settlements`: cost $1,743.60, revenue $1,569.47, fees $0.63. Split: (a) the 368-contract Ks-parlay whale −$143.89 (our fair 0.601 = raw mids 0.600 — consensus-priced, but max loss ≈6.7% of bankroll on ONE combo vs the 1% anchor → P1 Stage-1 exhibit); (b) F5 pickoff family fully settled **net −$59.90** (−86.60/+12.50/+14.20), zero F5 exposure left; (c) the other 41 combos **+$29.03 realized vs +$27.46 EV — on expectation**. MC on our entry fairs: P(night ≤ −174.76)=0.212; hits 17 vs 14.55 expected (P=0.25). Book lost as many bets, not one (max leg overlap 2; 12 games). PEAK book 21:01 ET: p_book **0.719**, EV +$37.24, det-max $1,167.62, 39 all-NO positions / $1,204.78 premium / 29 games / N_eff 9.3 / 26 of 39 at ≥60c entry. Data-integrity: 6 crash-window fills missing from fills store, **2 missing from position_ledger entirely** (−$40.27 loss invisible locally), 1 wrong-entry row; decisions store lost quote context for 20/45 (repriced from logs). **ADVERSARIAL GATE: independently re-pulled the exchange and re-derived every number — VERIFIED to the cent (45/−$174.76, same rows, same order; 49-fill 7/31 count exact; classification confirmed entry-time-fair only, no refit).** — [report](2026-08-01-overnight-loss-forensics.md) |
| 2026-07-31 | **OVERNIGHT READOUT — final build LIVE on `d121e46`** | **BEST DAY EVER: 24 fills / $1,060.58; 5 decisions owed** | Live+gated: confirm-priority lane (accept dispatch 5,150ms → 0.8ms), watchdog+relight sweep (30/30), warmup gate, F5 gate, esports 3¢. INCIDENT: the demotion fleet's `7162a4d` shipped an import of never-committed `eviction_value` → boot crash 20:21, watchdog latched correctly, reverted `d121e46` vitals 8/8. Demotion = NO-SHIP (retracted claim #8): ruin-convention collision (30% DRAWDOWN enforced vs equity-floor read; honest anchor $900 < today's wall) + measured P(KILL) 20.8% at cross-ρ 0.25 on the target book. Acceptance collapse ROOT-CAUSED 2×-blind: boot rehydrated 7 FINISHED games into the inventory skew → hot-key widening −21cc→−143cc never relaxing → hot accepts 3/12,732; fix = settle-aware skew inputs, PRICING change, ratification owed. — [report](2026-07-31-overnight-readout-final-build.md) |
| 2026-07-31 | **RESTART PACKAGE — demotion NO-SHIP, kill-anchor SHADOW shipped, acceptance collapse attributed** | **SHIP: `kill_anchored_book_gate` flag (shadow default, byte-identical golden `b136e16e`) — suite 3,491/0, vitals fast 8/8 + pre-ship 1/1 GREEN, recorded. NO-SHIP: det-max→0.70B demotion (retracted claim #8, canary-guarded)** | The 0.70B backstop derivation is self-cancelling under the enforced ruin-as-30%-DRAWDOWN convention (honest anchor value = 0.30B ≈ $900, TIGHTER than today's 0.36B wall); measured copula on the target 64-ticket spread book: ρ 0.10 → P(KILL) 12.09%, ρ 0.25 → **20.82%** (>10× the 2% budget), ρ 1.0 → P(ruin) 29.47%. Raising det-max = a NEW anchor only the operator can ratify with that stated. Live-book measurement (29 tickets, $999.88 premium, N_eff 14.1): armed KILL gate and today's dollar wall stop tonight's book at the SAME 39th ticket, P(KILL) 0.014 inside budget — the demotion buys nothing until det-max moves; n<15 books bind TIGHTER (lattice). **Acceptance collapse CAUSE (confirmed twice): boot-rehydrated exposure on FINISHED games feeding per-game inventory skew** — applied skew med −21cc → −141/−157cc at the 7/29 05:50 boot, collapse hot-key-only (7/31 hot 3/12,732 vs cold 1.50/1k); FIX A (fact-resolve settled legs out of the skew snapshot) is a PRICING change awaiting ratification. Uptime class (45h dark) closed by the shipped watchdog chain. Burst-floor orphans deleted. — [report](2026-07-31-demotion-acceptance-eviction.md) |
| 2026-07-31 | **DERIVED RESTING BURST FLOOR (Task D)** | **WITHDRAWN — wiring removed pre-commit; whole-book floor is the real fix (designed in prove_flow, NOT built); `resting_floor_count: 1` stopgap stands** | Clopper-Pearson k_g from measured acceptance at α=0.02 was built and then withdrawn: binomial-iid cannot re-learn clustered mass-acceptance intraday (real worst bursts 25 accepts/60s on 7/29, $400/60s on 7/28 — worse than the $199.66 previously cited), and armed it still failed its own V12 flow-vital at ×49 because per-game bucketing is the underlying defect. Its V12/V13 vitals live in `tools.vitals.gate_flow` (separate entry point — must NOT join the rule-9 gate until the whole-book floor ships). Failure direction conservative (confirm pinned at 100% → cost = reneges, not uncapped loss). — [report](2026-07-31-derived-resting-burst-floor.md) |
| 2026-07-31 | **DERIVED OPEN-QUOTE CAPACITY + DIVERSITY-AWARE EVICTION KEY (Task C)** | **BUILT, defaults OFF (byte-identical); new tests 32/32; ruff/mypy clean; replay executed vs live store+tape** | Operator ruling executed: *"we can bump the 200 up, but 5 $1 EV quotes shouldn't lose to 1 $5 EV quote, especially if the 5 quotes are diverse."* (1) `max_open_quotes` (hand-bumped 20→60→120→200; Kalshi documents NO maker open-quote cap — write token bucket is the only exchange constraint) dissolves into **derived capacity = (observed tier write rate − kill reserve − measured flow) × TTL / 4 tokens**: **1,272 at today's median sent-rate, 1,198 at worst** — ≈6× the hand cap, non-zero proven (2026-07-23 rule); fail-closed to the configured 200 while unmeasured; mass-acceptance guard argued (capacity is throughput, not risk — confirm-path exact enforcement + fill-velocity governor own the risk; worst measured burst 25 accepts/$400 per 60s ⇒ failure direction is reneges). (2) Slot eviction gains a **marginal-book-value key** `dEV × P(accept\|size, in-process CP-bounded tape at the ratified α=0.02) − dES99` (per-game CVaR share from the SAME book MC, hedge games negative); candidate scored at CP-lower vs incumbent CP-upper — **the measurement's own confidence gap IS the churn hysteresis**; separate anti-thrash ledger; FAILS CLOSED to today's absolute-EV key when the tape can't discriminate (disjoint-CP-interval criterion, derived). **HONEST FINDING (replay, 6,626 evictions today, 935 joined): measured acceptance RISES with size (<$5 1.34/1k vs $50–150 11.09/1k), so dEV×P(accept) alone reverses only 35/935 evictions — the small diverse book is protected by CAPACITY (at ≥1,198 slots ALL of today's evictions are moot) and by dES99 when capacity binds, not by acceptance weighting** (consistent with the ship-package retraction of acceptance-weighted eviction). Found+fixed en route: the textbook CP-lower duality **underflows at large n and returns a zero-width bound** (anti-conservative lie) — direct bisection, exactness-tested at n=50,000; also removed the dangling `burst_floor_derived` kwarg left in `build_lifecycle_config` by the withdrawn floor feature (would have crashed the next boot). Hot-path cost with flags off: two O(1) increments (214+520 ns) on the send path. Arming order in the report. — [report](2026-07-31-derived-capacity-diversity-eviction.md) |
| 2026-07-31 | **ADVERSARIAL GATE — confirm-priority + relight, by execution** | **BOTH RE-VERIFIED GREEN; 1 new defect FOUND+FIXED** | Re-ran every proof: storm replay reproduced (accept 1.2ms with lane vs 5,120ms without), gate livelock bounded at the exchange window (3.01s measured), risk chain at confirm intact by read, vitals fast 8/8 + pre-ship V6 560ms/19% of window, prove_watchdog 33/33. NEW: the stop sweep's keyword predicate killed NON-combomaker processes (proven live — foreign decoy python + other-project bash shells selected) → `tools/ops/ours_predicate.ps1` = launch-site-signature membership, tree-scoped (exe-path alone would MISS the real bot: the venv shim's child runs on the BASE interpreter — measured). Old-vs-new live dry-run identical 11/11 on the real stack, decoys spared. Named residual: token-starved `_delete_quote` (≤2.5s) inline on the comms dispatcher can still squeeze an accept (deep case degrades to a safe latency-DECLINE via the honest anchor). Kill switch untouched; cumulative-counter ruling owed. — [report](2026-07-31-confirm-priority-and-relight.md) |
| 2026-07-31 | **F5 UNKNOWN-FAMILY PICKOFF — gate closed** | **CONFIG-ONLY fix live (19:04 boot); $275.43 held to settlement** | Kalshi launched KXMLBF5/F5TOTAL/F5SPREAD under the KXMLB umbrella; bare-prefix allowlist admitted them with `classify_leg -> unknown` and ZERO pair rhos. One taker, one template (F5 winner + same-team -3 spread + opp-pitcher Ks), 3 games, $86.60/$87.50/$85.80 at 85.8-87.5c NO in 18 min. Operator caught it from the app. Fix = allowlist enumerated to the 12 tape-verified classified series (the designed per-family kill switch). Durable fix owed: quote-time unknown-family refusal on combos (pricing-path, needs gating). Same day: 24 fills / $1,060.58 = BEST DAY EVER through two confirm-expiry halts + one hard freeze. — [report](2026-07-31-f5-unknown-family-pickoff.md) |
| 2026-07-31 | **WATCHDOG RELIGHT BLOCKED at 17:34 ET — detection perfect, recovery refused by its own start guard** | **SHIPPED in tree (arms at the ONE approved restart); proofs 30/30 (17 pre-existing green + P5/P6/P7 new); watchdog units 16/16; both ps1 parse clean** | Tape correction to the briefing: the stop pass HAD killed every prober/monitor it targeted — the refusing "bot" was **PID 41592, the watchdog's OWN venv launcher SHIM** (`python.exe tools\ops\hang_watchdog.py run`), spared by `stop_all -KeepPid` BY DESIGN but invisible to `start_all`'s pid-equality `-CallerPid` exemption (the shim's child, not the shim, is the caller). Three mechanism fixes, no knobs: **(1)** `start_all -Auto` exempts watchdog-matching PYTHONS from the single-instance refusal (operator starts still refuse on them; a genuine leftover `combomaker.ops.cli run` python still refuses in both modes); **(2)** the sweep predicate gains `watch_main|watch_prober` so BOTH stop flavors are the complete stack teardown minus the recovering watchdog's tree; **(3)** on a start refusal the watchdog now does ONE full re-sweep + ONE retry before latching — a refusal that survives a clean re-sweep (human KILL, mutex, real duplicate) latches exactly as before ("refused the relight twice"), so the fail-safe posture is unchanged. **P5** replays the exact 17:34 scenario → relight SUCCEEDS, no latch; **P6** human-KILL present → still refuses both times, latches, receipt, stays down; **P7** evaluates the SHIPPED ps1 predicate text against the verbatim 17:34 process table (kills the six, keeps the watchdog tree, `-Auto` guard passes the shim, still refuses a real bot). Every existing guard (boot-loop zero-relight, flap latch at two, receipts, human-KILL) proven preserved. — [report](2026-07-31-watchdog-relight-blocked-fix.md) |
| 2026-07-31 | **CONFIRM PRIORITY INVERSION — today's double `halt_confirm_timeouts` (16:27Z + 21:33Z) measured, named, fixed by mechanism** | **SHIPPED in tree (lands at the ONE approved restart); suite 3,452/0; vitals fast 8/8 GREEN 20.1s + pre-ship 1/1 GREEN 10.1s; kill-switch 3-consecutive rule deliberately UNCHANGED** | Every confirm failure ever taped (12 of 12, vs 1,008 accepts / 430 confirm_oks) is `HTTP 400 'expired'` with **≤ 1.14s spent in-handler** — ≥ 1.86–2.67s of the exchange's 3.0s window died UPSTREAM, in the comms WS single-FIFO dispatch backlog the accept shared with the rfq_created firehose. Same-socket proof from the rfqs tape (`created_ts`→`seen_at`): healthy floor **0.45s** (n=422,730) vs **p50 3.40s / p90 6.21s, 69% > 2.5s** during run 1 today (n=50,718) and **p50 15.9s** in the morning run. Ruled out with numbers: REST confirm (unpaced, inside the ≤1.14s), the risk gate (in-handler p99 1.90s < window), 429s (zero ever on confirm). Fix, nothing hand-set: **(1)** WsManager priority lane — accept/executed frames jump the backlog (wait ≤ ONE normal dispatch; bound = existing `_QUEUE_MAX`, same fail-closed overflow); **(2)** `AcceptPriorityGate` — from accept-SEEN to confirm-done, intake drops rfq_created pre-parse and the 8 workers + retry loop park (fail-safe bound = the exchange window itself; named cost ≲1 min paused quoting/day); **(3)** honest deadline anchor — read-loop wire stamp flows into the derived gate budget, so any future delivery regression auto-degrades to the deterministic fallback instead of confirming into a dead window (+`confirm.accept_dispatch_delay_ms`/`lane_wait_ms` split the blindspot); **(4)** fast-lane create race handled via measured create-RTT p99 wait. **Replay proof: accept behind a 5,000-frame storm dispatched 5,150ms → 0.8ms**; dispatcher overhead −2.6% on no-op frames (µs-level; an earlier −41% draft was measured and eliminated). Kill switch unchanged: 12/12 failures are 'expired' (no network population to split), any confirm failure = possible renege at scale, halting is RIGHT — the fix attacks the cause, not the alarm. — [report](2026-07-31-confirm-priority-inversion-fix.md) |
| 2026-07-31 | **SHIP PACKAGE — watchdog + warmup gate + esports 3¢ staged; re-anchor & derived floor HELD** | **SHIP: `c19bc2b` + `3ed6dd9` (pushed) + esports 3¢ flat staged into the gitignored config, hash-verified esports-only. NO-SHIP armed: C (re-anchor, capacity −55.6% + cheap-NO tax, D1/D2 owed) and D (derived floor). ONE operator restart lands all three ships** | Operator readout for the 7/31 workstreams. **Regression caught at copy time**: the worktree's staged config would have reverted `resting_floor_count` 1→3 (undoing the live floor stopgap) — every markup block re-hashed, only `esports` applied (`→716bc9223489f236`), floor stays 1. **Retraction recorded**: my acceptance-weighted eviction proposal, refuted pre-ship by the acceptance-by-size measurement (big tickets accept 4–8× better; weighting eviction by acceptance would evict exactly the flow that fills). **D's burst-coverage claim refuted by tape** (real worst: 25 accepts/60s 7/29, $400/60s 7/28 — cumulative-iid k stays 1) and D armed still fails its own V12 at ×49 (per-game bucketing untouched; whole-book floor is the real fix, designed not built) — `gate_flow` must NOT join rule 9 until it ships. Restart plan: 5 first-hour metrics + abort criteria in the report. **Binding wall after landing: ENTITY `over_size` (261 vs 195/157/155)** — the entity cap acting as a second tighter per-combo cap on the $50–150 flow that accepts best; next capacity conversation = C1 arming, not floor tweaks. Standing decisions: FIX 5 (owns today's ≈$14 renege class), C1, D1/D2, watchdog cancel-all. — [report](2026-07-31-uptime-warmup-reanchor-floor.md) |
| 2026-07-31 | **THREE BUILDS GATED: KILL re-anchor, esports 3¢ flat, size-scaled markup** | **🔴 0 of 3 SHIP. Nothing committed, nothing armed, no order placed/cancelled/modified, live tree never touched. Suite 3436 passed / 3 failed (all 3 inherited, one file). Vitals 9/9 GREEN both tiers** | **(A) KILL-ANCHORED BOOK GATE → NO-SHIP.** The diagnosis is CORRECT and confirmed twice — the tail axis thresholds on `portfolio_cvar_frac·B = 0.35B` instead of the ratified 12% KILL line (`risk/limits.py:1640`), ratio **0.9722**, so `P(loss ≥ thr) = 0.000%` on **every** book built including one at P(KILL-night) **9.1%**; `"P(book loss >= "` occurs **0 times in 104,803** rows and the axis refused **0 of 7,964** fresh decisions. **But the cure fails on economics and provenance, not safety.** ① The brief's EV case (25→46 tickets, $789.11→**$2,145.14**, EV $29.73→**$93.56**) **depended on the det-max demotion to 0.70·B, which was WITHDRAWN** — re-verified withdrawn at both sites, `portfolio_det_max_frac` still **0.36**. With det-max unmoved the flag can only **ADD** refusals: the $2,145.14 target book is **REFUSED** by `SKIP_PORTFOLIO_DET_MAX` in both shadow and armed, and an 8-ticket 50¢ book goes **$1,058.51 → $470.47, a −55.6% CAPACITY CUT**. *A capacity reduction was being reviewed as a capacity increase.* ② **It is a CHEAP-NO TAX** — admitted capacity ARMED/TODAY: **25¢ 49.0% \| 40¢ 66.7% \| 50¢ 75.8% \| 70¢ 100% \| 85¢ 100% \| 92¢ 100%** (confirm path: 29/40 verdict flips, all 25–60¢, none at 88¢). Structural, not a bug: P(loss ≥ KILL) is monotone in p_hit, p_hit monotone in cheapness, so a KILL-distance **probability** gate IS a cheap-NO tax by construction — against the standing prohibition and against recorded returns cheap NO **+77.9%** vs ≥85¢ **+1.1%**. **Ratification owed as a PRICING decision.** ③ **NEW: the armed boundary is a LATTICE CLIFF** — at n=8 a **one-cent** ticket change moves P(KILL) **1.2% → 6.0%** (binomial p=0.30 → 1.13%/5.80%, matches to 2dp); **the ratified 2% budget falls INSIDE the gap and is unattainable.** **SAFETY IS CLEAN, all by execution:** det-max still enforces both sites; **no dollar-cap bypass — 300 randomized books, armed ⊇ shadow, 200 identical / 100 strictly tighter / 0 loosening**; staleness fails closed identically; shadow **byte-identical 0/300**; throughput **−3.01% (armed faster)**. **Operator's ρ question answered: if cross-game independence is wrong by 0.25, this build costs us NOTHING and SAVES us** — same 8-ticket 50¢ shape, TODAY P(ruin) **12.87%** / P(KILL) **74.06%** vs ARMED **0.00%** / **25.97%**; the ρ risk here is OVER-tightening, concentrated on the shape that earns most. **(B) ESPORTS 5/4/3 → 3¢ FLAT → NO-SHIP on EV; defensible ONLY as an explicit `OPERATOR PRICE FLOOR, not a calibration`.** At the measured `e = 0.22` fills-lost-per-cent: EV **×0.986 (−1.4%)** and **×0.962 (−3.8%)** multiplicative, **−13.6%** / **−8.5%** linear — **both signs negative** — and the **EV-optimal flat markup is m\* = 4.02¢**, so the existing ladder straddles the optimum and 3¢ sits below it. Diff is **exactly 3 deleted lines**; sha256 proves all 12 top-level blocks and every other sport block **byte-identical**, only `esports` changed; behaviour identical at 101 fair points × 12 leg shapes except pure-esports 0–19¢. **Premise corrected: markup NEVER gates** (`margin = max(half, markup_cc)`, `quote.py:166`) — the real coupling runs the OTHER way through award sizing (`lifecycle.py:9065`), lower markup raises our bid, shrinks the denominator, so notional rises **+2.9%→+18.2%** and the esports **SEND rate goes DOWN**. Esports = **0.23% of quote volume, 0 fills ever**; 0/196 quotes is **NOT** evidence of a deficit (**P(0 accepts) = 88.8%** at MLB's rate; **~4,941 quotes short** of detectability). Elasticity transfer is a **HYPOTHESIS** — measured on MLB, and the 2¢ step is **88% of its own 2.27¢ validity horizon**. **NOT LIVE:** staged at `kct-reanchor/config/prod-live-wc.local.yaml` (gitignored, never committed); the bot still quotes 5/4/3. **(C) SIZE-SCALED MARKUP → NO-SHIP, the gradient is not real.** Raw gradient looked real and pointed the **WRONG WAY** (**+1.360¢ [+0.062, +2.761]** top-vs-bottom premium quartile, slope **+0.421¢/e-fold** — big tickets marked out **BETTER**). **The confound: `premium = contracts × price`,** and mean NO price rises monotonically **52.27¢ → 56.08 → 61.80 → 67.19¢** across quartiles (cheapNO share 37%→12%). **Price-stratified, EVERY CI straddles zero on BOTH rulers at BOTH horizons.** Shipping it would have fitted a markup to a confound. 514 fills / $6,751.83 / 100% sell-only, but only **13 day-clusters / 18 game-components** ⇒ HYPOTHESIS-grade throughout. **🔴 THE WALL THAT ACTUALLY BINDS — and it is NOT risk appetite, NOT staleness.** Fresh full-session count on the live log (last entry 08:09 EST): **`entity_loss_cap` 33.7% + `utilization_backstop` 26.1% = 59.8%**, `quote_sent` **26.0%** (recent slice 27.4%), `skip_portfolio_cvar` (the fail-closed STALENESS path) **0.6% session and 0.0% in the recent slice**. **The brief's 7.3% send rate / staleness-dominant mix was a WARMUP ARTEFACT and has resolved — do not spend attention there.** Two notional-axis caps refuse three fifths of flow with **ZERO fills today** and **~$2.9k idle cash**, and `SKIP_UTILIZATION_BACKSTOP` reads **Σ gross settlement notional including RESTING quotes**. **Build A touches neither axis, so arming it cannot raise the send rate by a single quote — it can only lower it.** The concurrent **derived resting burst floor** targets exactly this wall and is worth more today. **BLOCKING BEFORE ANY ARM:** ① `tests/test_kill_anchored_book_gate.py:460` references `RUIN_FLOOR_FRAC`, never bound at module scope → `NameError` kills all 3 `TestConfirmPathMovesWithTheCap` tests, **the ONLY tests exercising `_candidate_gate`/`evaluate_candidate_book_risk`** — the confirm-path re-anchor has **zero passing coverage**; ② the worktree is **CONTAMINATED** — a concurrent agent threaded an unrelated burst-floor feature through the same files, the diffs must be separated. **RETRACTED IN-FLIGHT (mine, both):** "48 of 120 books RENEGE" — my harness read a non-existent `.ok`/`.admitted` so `bool(None)` made every confirm read DECLINE; corrected on the real `.confirm` field, **120/120 agree, 0 new reneges**. And "the armed gate bricks quoting" — I paired `p_hit = 0.30` with an 88¢ price (inconsistent; 88¢ implies 0.12); with consistent marginals it admits every realistic live shape. **EVIDENCE GRADING:** capacity ratios, monotonicity 0/300, shadow identity 0/300, staleness, throughput, vitals and the 0.000% defect are **MEASURED AND DETERMINISTIC**; that the cheap-NO tax **costs real money** is a **HYPOTHESIS** — synthetic single-shape books, `p_hit = 1 − P/10000` proxy, **zero game-clusters**, and the live book is FLAT (0 fills today). Also found: stale `ruin_floor_frac` comment in `ops/pricing_pool.py` documenting withdrawn behaviour; **dead** `LifecycleConfig.kill_anchored_book_gate` (`lifecycle.py:929`); `markup_applied` logged at DEBUG so the esports tier histogram is unmeasurable; `cross_game_residual.py` confirmed instrumentation-only. **Ledger caveat on the whole baseline: local `position_ledger` was 14.78% wrong in dollars on 7/29 (phantom row + wrong-size row) while the risk engine matched the exchange to $0.0002.** — [report](2026-07-31-reanchor-esports-size-markup.md) |
| 2026-07-31 | **HANG WATCHDOG (Task A) — external supervisor that watches WORK, not PIDs** | **BUILT + PROOFS EXECUTED (18/18); suite 3,430/0; vitals fast IN FLIGHT (one-time tape refresh over the new 7/29–31 GB logs; no rule-9 mandatory file touched — verdict to append); arms on the next operator START_BOT.bat** | Closes the 45h 7/29→7/31 outage class: the in-tree supervisor DETECTED the stall (`age=31.1s > 30.5s`) and wrote KILL, but the shutdown itself hung at `book_ws_stop` — PID alive, log frozen, everything watching process-aliveness green, START_BOT refusing over the live PIDs. New `tools/ops/hang_watchdog.py` (5th window, stdlib-only, imports nothing from combomaker, store read-only, never talks to Kalshi): progress = active-log (size,mtime) advance OR last `decisions` row `at` VALUE advancing (deliberately NOT WAL mtime — the persistence writer runs ~68 min behind, measured, so a post-hang backlog drain keeps the WAL moving while the row value freezes). **Threshold DERIVED from the tape, no hand-set number**: max(2× longest healthy quiet gap over all 52 completed `live_*.log` — measured **34s**, a 30s-cadence emitter keeps the log alive through every overnight lull — , 2× internal escalation chain `2*hb_timeout+poll` = 122s) → **armed at 122.0s**, 3.6× the worst healthy lull, poll 12.2s; derivation cached per completed log and re-folded at every operator start. Escalation stops+relights through the EXISTING ops scripts (`stop_all -NoPrompt -KeepPid`, `start_all -Auto` — every guard incl. machine-KILL auto-clear/human-KILL refusal stays authoritative) and REFUSES to flap: no-heartbeat run = boot loop = ZERO relights (start_all's own liveness discriminator, catches today's 09:00 config-error exit); two consecutive relights dying inside one detection window = flap latch; any latch = `WATCHDOG_HALT_*.txt` receipt + stays down loud; operator start purges the episode. Proofs executed against scratch trees driving the real binary: P1 frozen-log hang → one stop+start, relit run left alone; P2 boot-loop → zero relights + receipt; P3a/b healthy lull + store-axis → zero actions; P4 flap → exactly two relights then down. Pricing-path footprint ZERO. Known limits: latched halt does not cancel-all (TTL lapse; operator decision owed), `-Auto` KILL branch verified by inspection+unit test only until next restart. — [report](2026-07-31-hang-watchdog.md) |
| 2026-07-31 | **BOOT-WARMUP QUOTE GATE (Task B) + tape verdict on today's 2 reneges** | **SHIPPED in tree; suite 3,430/0; vitals fast 8/8 GREEN; lands on the ONE approved restart** | Quote SENDING now holds at boot until the first evaluation on which the confirm path's OWN usability predicate (`_book_risk_for_check` — reused, never duplicated) could pass; dedicated `skip_warmup_book_risk` (pre-pricing, durably recorded), ONE loud `quote_warmup_open` line with measured warmup, `quote_warmup_holding` warning throttled to the existing 30s freshness window, one-way latch so mid-run staleness is byte-identical. **Enforcement parity is load-bearing**: the gate stands down when caps are SHADOW or no bankroll layer is wired (a confirm cannot renege there) — the first draft without it broke 13 tests + vitals V7 by being stricter than confirm. **The task's premise is REFUTED by the tape**: both reneged quotes (10:11:04.7/10:11:08.0Z) were sent under a **usable, generation-MATCHED gen 2==2 snapshot**; the 10:11:08.2 fill bumped gen 2→3 and the 10:11:12.7 accepts fell in a **6.75s** stale window before the next MC publish — mid-run generation staleness, which the boot gate by spec does not touch. **Measured cost/benefit on today's tape: 0 quotes forgone (warmup ≈0–4s both boots; the 2026-07-16 startup sync snapshot already lands pre-quoting when it succeeds), 0 of the 2 reneges avoided.** The gate's real value = the boot where that startup snapshot FAILS/times out on a non-empty book (the 2026-07-16 shape, ~1min warmup): now held pre-pricing under one named reason instead of 69 post-pricing cvar declines. **🔴 The actual owner of today's ≈$14 of won-then-reneged premium is arming `book_risk_stale_decay` (FIX 5, SHADOW)** — its shadow readout on both decline lines already carried the charged tail ($199.66 ES/det vs later-passing ~$288): operator decision owed. — [report](2026-07-31-boot-warmup-quote-gate.md) |
| 2026-07-29 | **TWO-DAY ROLLUP 7/27 → 7/29 (standing rule)** | **DOCS — sweep of all 8 commits in the window, the live wall mix, and every claim I retracted** | Operator directive 2026-07-29: "make reports of all the changes for the past 2 days, it's a standing rule." **Seven of eight commits are CORRECTIONS** (they stop charging for risk that does not exist); exactly one (`eb5bbd8`, the haircut) genuinely raises what we can lose, and it was ratified explicitly. **The edge itself is still indistinguishable from variance: P(excess ≤ 0) = 0.061 on ~7–10 game-clusters** — not a result. What IS established: our FAIR is good (MLB median abs err **0.36¢**, 78.3% inside a tick) but our **ASK is +2.70¢ above prints**; cheap NO (<50¢) returns **+77.9%** vs ≥85¢ at **+1.1%**; det-max is shape-blind (it IS the premium sum, ratio to comonotone 1.000001); **the tail gate has never fired — 0 breaches in 104,803 rows**. **Fresh-tape wall mix this morning: entity 46.4%, size 22.5%, utilization_backstop 19.0%, per-combo 9.8%, portfolio CVaR/det-max 0.00%.** Send rate **25.4%**, det-max **$408.84** vs a **$1,058.50** wall — **38.6% deployed and refusing three quarters of flow**, with the portfolio risk measure refusing nothing. `risk_starvation_watchdog` fired at 20 consecutive risk-driven declines. **Retraction ledger recorded in full** (the "6.1× edge" = variance; "diversifiers quoted 3× wider" = my sign-frame error, they were already 0.13¢ tighter; the "hidden extra 2¢" = 98.2% the operator's own tier; "entity fix unlocks 32%" = admits ZERO of 26,416; "56/66 damning" = 1-in-200,000 at worst) — **in three of the six the operator was right and I was wrong**, incl. "we got lucky" and "never assume what i say is right". **Cross-game independence RATIFIED by the operator** (`DEFAULT_CROSS_EVENT_RHO = 0.0` stands, no family quarantined); the recorded caveat is that the risk is not causal linkage but **correlated MODEL ERROR** — a rich K model is rich on every pitcher at once — and it is not small: cross-ρ 0.00 → 0.25 takes ES99 $221.64 → $280.65 and modeled EV **+$10.84 → −$4.25**, the entire edge. A settlement-derived cross-ρ diagnostic ships as **instrumentation only, never a gate**. — [report](2026-07-29-two-day-rollup.md) |
| 2026-07-28 | **PORTFOLIO HAIRCUT → OPERATOR ANCHOR (the ceiling that shrank as we deployed)** | **SHIPPED `eb5bbd8`; vitals 8/8 GREEN (13.8s). This is the ONE genuine loosening in the window — ratified explicitly** | The operator called this from the outside and was exactly right: *"2664 × .36 gives us 959 of headroom, so idk why we didn't quote more from the start… I really feel like we're missing out."* `risk/balance.py` computed `risk_bankroll = min(start_of_day_equity, cash + haircut × portfolio_value)` with `DEFAULT_PORTFOLIO_HAIRCUT = Fraction(1, 2)` sitting at line 83 behind the comment *"FLAGGED for operator to set per risk tolerance"* — **never set by an operator, never ratified, never reviewed**, exactly the species the NORTH STAR names as a bug in the adaptation. **It was not conservatism, it was a negative feedback loop**: the marginal effect of deploying a dollar on our own ceiling is `−1 + h`, so at h=0.5 **every dollar put to work destroyed fifty cents of capacity to work** — measured ≈ **−$18 of ceiling per $100 deployed**. That is the plateau the operator kept hitting and naming (*"$800 seems to be our max"*, *"stuck at this $500-700 amount"*, *"a loop where we can't quote more"*): **no escape by construction**, quoting more shrank the ceiling and the shrinking ceiling refused the next quote. At h=1.0, `cash + 1.0 × portfolio_value` **is** total equity, the derivative is **0**, the loop is neutralised, and `min(start_of_day_equity, …)` still holds the ceiling to the day's open so intraday marks cannot inflate it. Operator ruling: **"Haircut should be 1.0. We always use full equity."** Promoted to an explicit anchor in `ops/config.py`, wired to `BalanceTracker`. **Read honestly: this RAISES what we can lose** — downside protection now rests entirely on the SOD-equity clamp + det-max + the tail gate, with the haircut no longer doing hidden work. **🔴 Recorded debt, not fixed here: vitals derives bankroll from a `daily_ruin_anchors` table that was 12 days stale ($2,050 vs live $2,661); boot rehydrate reads positions UNPAGED and truncates past ~100; ledger orphan leaves $775.85 of cost basis unbooked.** — [report](2026-07-28-portfolio-haircut-anchor.md) |
| 2026-07-28 | **SETTLED LEGS, HEDGE CREDIT, VALUE-RANKED BUDGET — headroom with ZERO risk-posture change** | **SHIPPED `27de1e0`; suite green; vitals 8/8 GREEN; markup tiers sha256 IDENTICAL; parity-checked to the cent** | Serves the operator's framing *"Risk engine = protection not limitation."* **Three defects that consumed risk budget without any corresponding risk — all three are ACCOUNTING corrections, no cap fraction, policy anchor or markup moved.** **(1) Settled legs were still charged**: a combo whose legs already settled cannot lose more money, yet the exposure book charged full premium — **$80.20 of phantom det-max** on the live book, the mechanism behind the operator's own *"combos that have legs that settled from yesterday's slate and need 1-2 more legs for today's."* `risk/settlement.py` resolves settled legs into FACTS and prices the residual on the legs that remain live; **fail-closed preserved — an unresolvable leg stays UNKNOWN and stays fully charged, so UNKNOWN never buys headroom.** **(2) Hedge fills were charged twice**: a risk-REDUCING fill was charged its own premium as fresh exposure, against the standing rule that certified hedges are +EV and must fill. Shipped as a **CREDIT** (`det_max_hedge_credit`), **NOT a bypass**, and the distinction is the whole design — a credit removes a double-charge and can never carry the book past the wall, whereas a bypass is unbounded: **a 25-step chain of individually CORRECT pairwise certifications reaches $18,107.16 = 6.16× bankroll = 51.32× the KILL distance**, because certification is pairwise-LOCAL while det-max is GLOBAL. Certification stays waiver-state enumeration, never leg-sign heuristics. **(3) The budget spent itself on the cheapest tickets**: scarce det-max headroom was allocated first-come, so `det_budget_value_ranking` ranks by EV per unit of det-max consumed and the scarce resource buys the best available book. Validator note that cost a restart cycle: the enum is `off`/`shadow`/`on` — **`"armed"` is rejected**. **Watch item that proved out: the released $80.20 did NOT convert into fills, because the binding wall is elsewhere — entity, at 46.4% of declines.** — [report](2026-07-28-settled-legs-hedge-credit-value-ranking.md) |
| 2026-07-28 | **ENTITY AXIS REBUILT TO THE OPERATOR'S TIER TABLE** | **BUILT + gated; suite 3347/0; vitals fast 8/8 GREEN + pre-ship 1/1 GREEN; shadow BYTE-IDENTICAL to `9a93925` over 400 randomised cases; markup + skew sha256 IDENTICAL to HEAD; throughput NEUTRAL with the levers off (2,660µs vs 2,595µs at HEAD). Flags default false — ONE decision owed before arming** | The prior `certify_entity_admission` **never read the key**: its rule reduced to `L < 2T/(N_eff-1)`, so a 1-leg $40 ticket dumped on the **already-hottest** key returned `certified=True / 'diversifying'` **byte-identical** to $40 spread across four fresh entities — a pure ticket-SIZE test wearing a diversification label. **Deleted and rewritten from scratch** (plus `risk/net_effect.py` deleted) to the operator's table: `<1% no action | 1-2% tier 1 | 2-3% tier 2 | >3% DECLINE`. **Two new constants total** (the 1% and 2% anchors, NORTH-STAR layer 2); the decline line IS `entity_loss_frac` and the ceiling IS `per_combo_loss_frac`, both already ratified, and both passed IN so the module can never disagree with the wall. **Graded widen**: `w = min(prior_load, wall)/wall`, 0 below tier 1 — tier 1 lands in `[1/3,2/3]`, tier 2 in `(2/3,1]`, bands that cannot overlap by construction. It reads the **PRIOR** load, so **a fresh pitcher nobody has bet weighs exactly 0 however big the ticket is** (the operator's 24-props-a-slate arithmetic). **NET across the combo, in dollars**: widen = the dollar-weighted MEAN of the per-key weights, never `max` — same $30 riding `WARM` alone prices at `1/2`, `WARM` + four fresh entities at `1/10`, **exactly a fifth**; admission needs `diversifying_cc > concentrating_cc`. **THE SIZE/ACCUMULATION SPLIT, the decision the brief asked for**: measured on 99,131 live entity refusals, **72.2%** are candidates whose OWN premium alone clears the wall and **54.3%** of breach-key instances sit on a key that was **cool (<1%)** — a first structure on an empty key is not "every combo riding one player one way", it is a SIZE event, so a cool key defers to per-combo while a **warm key is NEVER certified at any size** (accumulation across structures still refuses at **3%**). Decomposed by size band: **27.8%** are real accumulation (refused, unchanged), **20.6%** sit in the **$71.17–$118.61** band where the entity cap was acting as a **second, 40%-tighter per-combo cap** (this build governs exactly here), **51.6%** are above the ratified 5% size wall and **per-combo refuses them anyway**. Tape: **5,802 of 99,131 admitted (5.9%), $482,628.92 released, largest released candidate $116.96 vs the $118.61 wall — INSIDE it, empirically, over the whole window**. **Admitting more does NOT lower effective N**: serial fill sim ticket `N_eff` **53.80 → 90.91** with the worst entity key **unchanged at $134.57**; upper bound **53.80 → 5,660.3**, worst entity key **$117.45**. Combined with the slate lever, projected send rate **2.371% → 10.617%** (upper bound). **The graded widen is NOT wired into PRICE in this commit** (that is why shadow is byte-identical) — wiring it, deleting the family axis and arming-off the `peak` term is the next build. **🔴 DECISION OWED: arming certifies a previously-COOL entity up to the per-COMBO 5% for a SINGLE structure — a 3%→5% move on max single-entity exposure. Stated loudly, bounded three ways (warm keys never certified, net-diversifying required, capped by the ratified per-combo), justified by the brief's own "the size protection already exists as per-combo (5%) and skip_size_above_max" — but it is an operator call, flagged not overridden.** — [report](2026-07-28-tiered-entity-load-rebuild.md) |
| 2026-07-28 | **SLATE AGGREGATION BY PARTITION — verified, fail-closed, ARMABLE** | **BUILT + gated; suite 3347/0; vitals fast 8/8 GREEN + pre-ship 1/1 GREEN; shadow BYTE-IDENTICAL to HEAD `9a93925` over 400 randomised cases; throughput NEUTRAL with the lever off. Flags default false — lands on the operator's ONE restart, live PID 33724 untouched** | `_slate_rollup` sums `worst_case_loss_by_game_cc`, so an AND-BOUND ticket spanning **G** games contributes its FULL `max_loss` **G times** to one slate. The live book is **88.4% multi-game**, so this is the book, not an edge case: four slates read **$2,803.21** against **$1,498.27** of premium that exists — **1.88x** — and the corrected total **$1,487.35** lands within **$10.92** of real premium (below it, because the within-game mutex fold nets genuinely exclusive arms). **Today's slate was at 66.9% of the $1,496.60 wall from committed positions alone; headroom $494.76 → $878.39 (+77.5%).** Tape replay over 400,000 decisions: **120,577 slate refusals, 89.8% admitted**, per-refusal naive $2,406.59 vs once-counted $1,008.73 (**2.39x**), projected send rate on the slate lever alone **2.464% → 9.744%** (an UPPER BOUND — released decisions still face the candidate gate/EV/write budget). **`slate_loss_frac` stays 0.65 — the wall did not move, the ruler was repaired**: every loss event counted exactly ONCE, each single-game bucket folded through the **existing** Stage-B `_mutex_game_worst_cc`, clamped into `[largest single loss, once-counted comonotone sum]`, and every uncertainty (multi-game unit, ungamed unit, UNKNOWN/absent ME fact, an exception in the ME lookup, ≥2 ME events) failing to **comonotone**. **🔴 THE LATENT FAIL-OPEN IS CLOSED AND ITS EXPLOIT EXECUTED**: the corrected number is built from a census (`snapshot.loss_units`) that folds to **ZERO** when absent, and zero is the PERMISSIVE answer on a loss cap — a snapshot built with `want_loss_units=False` made **every slate breach disappear**, protected only by one caller happening to derive the flag from the same booleans, with no assertion and the permissive answer as the default. Three guards (new `loss_units_built` "was the census taken", per-slate contradiction check, clamp to the naive term) **plus** the read-out, which used to print `would_admit=True` for a slate the partition refused to correct. **With all three disabled in a scratch copy, 5 of the 22 new tests go RED; restored, 22/22 green.** Soundness is BRUTE-FORCED, not asserted: 120 randomised books enumerated against the explicit outcome space prove the bound never understates the true joint worst case; 200 more pin the clamp and E2 monotonicity; a genuinely over-limit slate ($1,500 of real once-counted premium vs a $1,300 wall) is **still refused** armed. Validate-caps-can-quote on the live book: the slate stops binding at $200+ while **per-combo (5%) and the entity wall still speak**. **🔴 THE ENTITY AXIS WAS REBUILT IN PARALLEL BY A CONCURRENT WORKSTREAM (51/51 green, in this tree) AND DIVERGES FROM THE BRIEF**: its rule lets a previously-COOL entity be carried to the **per-COMBO 5%** by a single structure, which the brief called disqualifying twice ("the per-entity wall must REMAIN 3%"). Loud rather than silent, and far tighter than the deleted `net_effect.py` — but a 3%→5% move on max single-entity exposure is an **operator decision**, flagged not overridden. — [report](2026-07-28-slate-partition-armable-and-failclosed.md) |
| 2026-07-27 | **OVERNIGHT FACT-RESOLUTION — settlement read PRIORITY + ledger orphan reconciliation; balance cadence VERIFIED** | **BUILT + gated; suite 3308/0; vitals 8/8 GREEN; mypy strict clean. Lands on the operator's ONE restart — the live process (PID 16224) is on old code (`read_budget_armed` absent from the tape confirms it)** | **DEFECT 1 re-framed by the tape, not accepted as stated.** All **94** `settled_fetch_failed` in `live_20260727_1839.log` are **exchange** HTTP 429; our LOCAL bucket refused **zero** settlement reads. The inversion is one step removed: `_reserve_read_token` *waits* rather than refusing, and the continuous metadata refresh wins the intervening tokens, so we issue the settlement GET while the **exchange** bucket is empty. Repair = a CRITICAL RESERVE on the shared read bucket, **derived** (`FETCH_BUDGET_PER_PASS` x `DEFAULT_ENDPOINT_TOKEN_COST` = 5 x 10 = 50 tok, both pre-existing/live-verified via `GET /account/endpoint_costs`), so routine metadata yields with ~5 GETs of exchange headroom left. A/B under a sustained read storm: **CONTROL (reserve=0) 9/64 resolved, 55 pending; SHIPPED 64/64, 0 pending, 0 deferrals**; backlog clears in **13 passes = ceil(64/5)**. **Also corrected a misleading metric**: `n_never_fetched == n_pending` is not starvation - `_ingest` clears the attempt counter for every market that comes back LIVE. **Gate (b) priced exactly**: a floor removes BURST, never RATE - burst **60 -> 55 calls (= reserve/cost)**, and the deficit over 10s and 30s is the **same constant 5** (level shift, not rate change); worst extra wait at the bucket floor **167 ms**. **DEFECT 2 root cause CONFIRMED from the store, not assumed**: 140 open rows across **133 distinct tickers vs 85 exchange positions**, only **7** extra rows are legitimate multi-fill - so it is rows never being **CLOSED**, and the divergence is constant within a run and steps only at restarts (a combo that settled while the process was down). Repair closes those rows from EXCHANGE TRUTH: ledger-only (never cash/risk/caps), attributed to the exchange's own `settled_time` so p_night's day seed buckets it on the right night, one shared payout formula extracted from `BalanceTracker.apply_settlement`, and **fail-closed** - the rows' gross payout must reconstruct the exchange `revenue` **to the cent** or they stay OPEN and loud. **There is no `DELETE FROM position_ledger` anywhere in the tree.** Scaled for the poller's real batch (it re-pages the whole settlement history every 30s): **2,001 settlement rows -> 1 batch scan, 1 per-ticker read**, fail-open. **The starvation alarm as first drafted could never fire** - a failed read re-arms the backoff, so a permanently-refused ticker looks on-schedule forever; added an `_unserved_since` signature and throttled the WARNING to one line per retry cycle. **DEFECT 3 is NOT a defect - do not file it**: `account_standing` is a boot REPORT line; balance polls every **10s** (stale-closed at 30s), settlements **30s**, positions **300s**, adaptive caps **1800s**, and the tape shows **0** failed/stale/rate-limited balance polls and 0 failed reconciles. **🔴 NAMED, PRE-EXISTING, NOT FIXED: `BalanceTracker` re-anchors `start_of_day_equity` AND `peak_equity` on the UTC calendar date = 20:00 ET, mid-slate - so the drawdown halt and the 12% KILL measure give-back from the 8pm equity for the rest of the night. That costs MONEY in a tail, not bookkeeping. Left alone deliberately: the boundary is a policy anchor, operator decision owed.** **🔴 Residual exchange-side 429s are REDUCED, NOT ELIMINATED, and that is unproven offline** - the principled cure (drain the local bucket on an observed 429) is deliberately deferred, it would cut read throughput materially and risk metadata staleness -> no-quote. - [report](2026-07-27-overnight-settlement-priority-ledger-reconcile.md) |
| 2026-07-27 | **CONFIRM PATH — B1 same-game rho scoping + B2 the cost PREDICTOR DELETED** | **SHIPPED; suite 3254/0; vitals `--tier all` 9/9 GREEN (V6 was RED); prove 8/8 with two NEW mutations. Quote path BIT-IDENTICAL. Lands on the operator's ONE restart** | **B1.** The candidate gate resolved a within-game band for **every unordered ticker pair**, under the comment "resolving all is a harmless superset". `build_book_model` iterates only SAME-GAME pairs, so on the live 100-position book **22,155 pairs were resolved and 879 consumed — 96.03% dead work, 4,507 ms of COLD rho against 197 ms** (3x: 200,028 vs 2,637 = 98.68%, 35.5 s vs 0.52 s; 5x: 99.21%, 112 s vs 0.90 s). The fix does NOT re-derive the grouping: `sim/book_model.py` now exports `select_modeled_positions` / `build_leg_universe` / `within_game_index_members`, `build_book_model` and the new `within_game_pair_tickers` both call them, so the two sets are equal **by construction**. This matters in BOTH directions — a pair the caller MISSES comes back as `flat_band` and **understates correlation and therefore risk, silently** (pinned: tail rho 0.90 → <0.5 on one dropped pair). Proof: 1,000-book property test over ungamed / unpriceable / gated / alias / 1H×FT / ticker-under-two-events shapes, both directions *and order*, plus a SEAM test that drives the real `_build_candidate_gate_inputs` and compares against the worker's own `_DictMarginals` model. **B2 — DELETED, not repaired.** `_MeasuredRate` modelled the build as one ms/PAIR rate while its dominant small-book term is FIXED, and `predict_ms` took `max()` over samples that only arrived when a build RAN: one poisoned sample skipped the build, a skipped build produced no new sample, the max never decayed — **the joint-tail / P(ruin) / ΔP(book) gate went dark PERMANENTLY**. Prediction is replaced by ENFORCEMENT: the build stays interruptible and the MC is bounded by the window that is actually LEFT (`run_candidate(..., deadline_s=)`, the exact `asyncio.wait_for` pattern the last-look waiver has shipped since 2026-07-16); a timeout resolves as LATENCY through the deterministic fallback, never `DECLINE_CANDIDATE_RISK`. **The MC can no longer be switched off by anything but the clock — there is no path-dependent state left in the gate** (~110 lines removed). **MEASURED confirm path at the live book: 634 ms of a 3,000 ms window, margin +2,366 ms, and the MC RUNS**; at 3x/5x the MC still does not fit (3.0 s / 4.9 s on its own) and the bounded-MC + deterministic fallback resolves the auction INSIDE the window. **V6 was itself a lying benchmark** (rho rate × ALL pairs, MC through the live provider inline — two compositions that do not ship) asserting an unmeetable 5x bound; rebuilt on the shipped composition to assert (1) the MC fits at the LIVE book, (2) the build fits at EVERY size, (3) no un-interruptible stage exceeds the budget — and **proved substantive by new mutation R8** (restore the superset ⇒ V6 RED) and **R9** (the predictor's terminal state ⇒ V7 RED). Quote path: `build_book_model` **bit-identical** at 1x/3x/5x, −1.65% / −0.82% / +1.15% interleaved. **🔴 One required test could NOT be made to pass and should not be: the brief asked the MC to RUN at 3x, and at 300 positions the 20,000-sample MC is 3,017 ms ON ITS OWN against a 3,000 ms window — B1 removed the BUILD (603 ms of a 3,723 ms total at 3x), the rest is irreducible `n_legs×n_samples` sampling. Faking it would have needed a lowered `n_samples`, i.e. a suite that lies about risk coverage. The crossover is PUBLISHED by V6 instead and is a decision owed.** **🔴 Two PRE-EXISTING crashes found in the B3 concentration-steer tree and handed back: `limits.py:1140` uses an undefined `net_effect_observer`, and `lifecycle.py:1753-1759` reads six `ConcentrationCertificate` attributes that do not exist.** — [report](2026-07-27-confirm-path-b1-pair-scoping-b2-predictor-deleted.md) |
| 2026-07-27 | **THE VITAL-SIGNS GATE — an executable harness for the money-critical properties** | **BUILT + PROVEN; fast tier 8/8 GREEN in 13.5s; 6/6 historical defects caught in scratch copies. CLAUDE.md hard rule 9. Blast radius ZERO on the live path — nothing under `src/` touched** | Seven regressions from ~eight changes in 48h with **3,081 unit tests GREEN throughout**; reintroducing all seven trips 17 tests and **all 17 were written by the commit that fixed that same regression** (zero pre-existing detectors, and R5/R7 are invisible to the suite even today). The suite asserts the mechanism the author had in mind and never varies the **discriminating variable**. `tools/vitals/` is one command (`python -m tools.vitals.gate`) whose nine checks each construct **DEGRADED state** — a book the live predicate confirms is ALREADY over a wall plus a DISJOINT candidate (R1), an 11-market same-tick quarantine wave taken from the incident tape (R2), 200 withdraw-pending quotes under a permanently-429ing exchange (R3), an accept scheduled to land INSIDE the resolver's await on another task (R4), the real 100-position book at 1x/3x/5x (R5), the measured 35-boot launch->first-beat distribution against real process shapes (R6), and the whole startup timeline swept for None-holes in the UNION of kill axes (R7). **Zero tuned constants**: the write ceiling is the bot's own recorded `GET /account/limits` reply (`advanced`, 600/300), N=11 is the largest wave that has ever happened, the boot bound is a LATCH over 3.2-48.3s measured across 35 boots (27 of them OUTSIDE the 30s anchor), the confirm window is the protocol fact 3.0s, the bankroll and the open book are read READ-ONLY from the store, and the operator knobs are the live YAML parsed by the shipped pydantic model. `tools/vitals/prove.py` mutates SCRATCH COPIES to reintroduce R1/R3/R4/R5/R6/R7 and the gate goes **RED on all six** while the clean tree is green (72.8s). **🔴 LIVE FINDING: the pre-ship tier is RED.** On the REAL 100-position book the candidate gate eats **1.20s = 40% of the 3s exchange window** memo-warm; **COLD (the first accept after every restart) the O(T^2) rho build alone is 3.9s = 130% of the window**; at 3x/5x it is 179%/343% and cannot run at all — corroborated by the tape's **70 `candidate_gate_deadline` events vs 267 confirms**. The MC that exists to refuse concentration effectively does not run on a real book. — [report](2026-07-27-vital-signs-gate.md) |
| 2026-07-27 | **LEVER #5 — the concentration steer made ECONOMICALLY REAL** | **BUILT + benchmarked; suite 3189/0; quote path +4.03% of the `_quoting_policy` block. Lands on the operator's ONE restart. MARKUPS UNCHANGED** | The steer was directionally right and economically INERT: median `|applied_cc|` **20cc against a 200cc margin (5.6% of markup)**, a full concentrating→diversifying swing of **0.36–0.39c (smaller than one tier step)**, the composed clamps `[-300,+1200]` firing **0 times in 300,000 events**, and **32.25% of the steer annihilated by `snap_bid_down` for being under one tick**. All three attenuators removed. **(a)** The 4:1 clamp asymmetry is gone — ONE derived symmetric `half_cc`, and the composed classifier is **RESCALED** onto it through a denominator shared by both signs (clipping was tried first and SATURATED: median `|applied|` pinned exactly at the ceiling, which destroys the ordering the lever exists to create). **(b)** The **1/n_keys COUNT deficit is deleted** — it decayed 42% (0.0250→0.0145) in ONE session *because we diversified successfully*, so concentration is now DOLLARS against each axis's OWN ENFORCED WALL plus the **AND-BOUND dollar-Herfindahl (standard error exactly ZERO, O(1) at 1.47µs)**. **(c)** `need = 1 − p_book` no longer multiplies the magnitude. **AND-BINDING**: a 3-leg ticket across 3 different matches is ONE loss event — pinned at **1.00 effective events held whole vs 2.99 split into 3 tickets**, and the live-shaped 38-ticket book reads **38.00 where the leg-wise read counted 107**. **The price is `Cov(candidate payoff, pre-existing book P&L)`** off the already-paid-for CRN cache (PRICING joint, never the tail-stress joint) as the exact log-utility CE term `CC_PER_DOLLAR × Cov(hit, book_pnl)/bankroll` — no free parameter, scales with bankroll, EV-orthogonal by construction; it publishes only if the drawn sample's own SNR clears the ratified **z = 3**. **`delta_p_book` is banned from price and a test greps the module body to keep it out** (R²=0.921 vs candidate EV, 42.2% unresolvable at 3σ, 41.7% of EV-residual signs flip). Scale = min(measured value dispersion, the elasticity's own validity horizon `1/(2e)` = **227cc** at e=0.22, the LIVE margin) — an unmeasured bound abstains rather than zeroing the steer. **Sub-tick annihilation is now structurally impossible**: new `PriceGrid.step_at()` + a tick ladder with a **minimum of one whole tick** either side of zero ⇒ **0.00% off-lattice**, and the post-snap `no_bid` difference through the real `construct_quote` is EXACTLY the steer. **The operator invariant is a shipped PROPERTY** — `assert_diversifier_tighter` at CLASSIFIER / PRE-SNAP / POST-SNAP with a `NoQuote` ranked at −inf so a refusal can never land on the diversifier; 300 randomised books pass. **Measured on the live-shaped book: mean `applied_cc` −321.10 → −45.84 (the average quote got TIGHTER by 275cc, not wider), median `|applied|` 20cc → 50cc, swing 0.39c → 0.77c = 38.5% of markup.** Six pre-existing count-basis tests were PORTED, not deleted. — [report](2026-07-27-lever5-concentration-steer-economically-real.md) |
| 2026-07-27 | **AUTO-RELIGHT for LIFECYCLE-class halts (operator-directed)** | **BUILT + VERIFIED; suite 3047/0 (+37); throughput NEUTRAL. Takes effect on the operator's ONE restart — bot untouched** | The bot now runs under a **PARENT** process (`ops/relight.py`) that outlives a halt by construction — nothing did before (the supervisor is the bot's CHILD, measured dead **32 ms** after the 7/26 18:15Z halt). It restarts the bot for **exactly one** class, the unenforceable lifecycle quarantine, and escalates terminally for everything else. **Key invariant: it never reads a KILL for attribution and never deletes one** — a lifecycle halt provably writes no KILL, so every KILL that can exist (supervisor, CLI, human) is outside the relightable class and G5 refuses it unconditionally. Attribution runs off a new atomic `data/halt_receipt.json` whose **claim is re-derived from its own evidence** (G7) — necessary because `HALT_METADATA_CHANGE` is shared by the taxonomy tripwire, settlement moves, AND the promotion, so the reason code can never attribute; a receipt that *lies* (claims lifecycle while carrying a moved settlement fingerprint) is rejected by its own contents. 8 read gates + 2 derived bounds, **zero tuned numbers**: **B2** = one relight per distinct root-cause signature (the set of withdrawal failure kinds, computed at the failure site) per session; **B3** = `work_i >= cost_i`, the unique scale-free breakeven ⇒ **at most ONE unpaid relight ever**, duty cycle >= 50%. Boot bound DELEGATED to the existing operator anchor `supervisor.heartbeat_timeout_s` (verified 30.0 live). Reconcile can't be skipped: **G6 requires the marker PRESENT**, "productive" *is* marker-cleared, and a failed reconcile ⇒ preflight RED ⇒ exit 3 ⇒ G4 terminal. **🔴 Found a real bug only by running REAL processes: the venv `python.exe` is a launcher SHIM, so `os.getpid()` in the bot is NOT the pid the parent spawned — G3 refused every real receipt and the whole feature would have been a silent no-op that passed its entire suite** (confirmed on the live tree: 25828 shim → 30444 real). Fixed by recording `pid`+`ppid`. Real-process runs: crash loop = **1 relight then terminal exit 1 in 1.7 s**; normal slate = **4 grants across 4 DISTINCT failure modes** (duty 0.79–0.94), 5th repeat refused at B2. All 5 historical metadata halts replay → 5/5 refuse. Throughput: hot path unchanged (AFTER *faster* in 1 of 2 paired trials; **no `pricing/`, `marketdata/`, or `exchange/` file on the diff**), status loop +0.089 ms = **0.079% of the 15 s tick**. — [report](2026-07-27-auto-relight-lifecycle-halts.md) |
| 2026-07-26 | **SECOND GATE on the withdrawal rebuild: B1 (a MID-CONFIRM quote reaped + re-DELETEd) + B2 (`cancel_all` held non-terminal callers 10–20 s)** | **BOTH FIXED; suite 3008/0 (+14 tests); throughput NEUTRAL-to-better. KILL left in place — bot stays DOWN** | **B1 — LIVE MONEY.** `_resolve_withdraw_pending` was the ONLY withdrawal chooser without the `state.accepted` guard the other three carry (`cancel_all`, `cancel_quotes_touching`, `_pick_eviction_victim` — "not mid-confirm, never yank") **and the only one that both REAPS and re-DELETEs**. One step reaches it: a TTL/RFQ-gone DELETE 429s ⇒ the quote is UNKNOWN and STILL RESTING ⇒ the taker ACCEPTS it ⇒ `on_quote_accepted` awaits (store + reservation + confirm REST) across several 0.5 s ticks. PoC A re-run with the guard removed: `still in _open? False`, `withdraw_resolve.proven_gone = 2` — **a quote that FILLED booked as a proven withdrawal**; PoC A2: `DELETEs issued against a MID-CONFIRM quote: ['q1','q1']` — a fresh DELETE at a quote we had already reserved risk for. Guarded: `True` / `[]`. The inverse hazard is closed too — the deferral **terminates inside one confirm window** (every accept branch already drops the quote; the one escape, an exception between `accepted=True` and the drop that quote_app's worker logs and moves past, now drops + re-raises in a thin wrapper — and removing that wrapper makes the strand reproduce). **B2 — THROUGHPUT.** `cancel_all` passed no `budget_s`, so the token gate waited unbounded: **measured 10,000 ms (full bucket) / 20,000 ms (empty) at the live `max_open_quotes` = 200, cliff exactly at 100 quotes**, on paths that are NOT terminal and run inline (feed `_fire_invalidate` — also fired from a gap BEFORE the resync is sent, `on_channel_lost` right before `force_reconnect`, the exchange-status halt, both fill-velocity sites). Now bounded by the EXISTING `_WITHDRAW_RESOLVE_BUDGET_S` (= `_REPRICE_SWEEP_BUDGET_S` = 2.5 s, the read-resolver's own pass budget — no new literal, asserted equal in test): **every cell 2,500 ms**, leftovers stay pending for that resolver, and nothing unconfirmed leaves the mirror or exposure on any path. Real WS objects: feed disconnect **20,000 → 2,500 ms**, channel-lost **20,000 → 2,500 ms** then force_reconnect. The 2 genuinely terminal callers (`on_halt` → `_stop.set()`, shutdown) pass `budget_s=None` and are byte-identical (AST-checked: 2 opt-out, 5 bound). Throughput A/B: tick@200 **15.875 → 15.601 ms**, accept 19.717 → 19.880 ms (noise), pricing 10,632 quotes/min. — [report](2026-07-26-withdraw-resolution-one-write-path-one-read-prover.md) |
| 2026-07-26 | **ONE WRITE PATH, ONE PROVER, AND THE PROVER IS A READ — the B3 gate's 3 defects fixed** | **SHIPPED; suite 2994/0 (+25 tests); throughput NEUTRAL. KILL left in place — 2 operator decisions owed** | The adversarial gate proved the first B3 cut had three holes and PoC'd each. (1) **`cancel_all` dropped UNRESOLVED ids** on a "terminal path, no next tick" premise that is **false for 5 of its 7 callers** (feed resync, the force-*reconnecting* channel-lost, exchange-status halt, both fill-velocity sites) — under a 429 storm the mirror forgot quotes that were still resting and could still fill. (2) **Self-amplifying write storm**: `_delete_quote` owned a SECOND, **UNMETERED** `delete_quote` call (the busiest withdrawal path — **1,137 calls** in the incident, spending zero tokens) AND the reprice sweep re-drove every pending withdrawal every 0.5s tick = at the live `max_open_quotes`=200, **400 write tok/tick = 800 tok/s against the account's 300 tok/s ceiling**. (3) **Quoting bricked**: the storm sustained its own 429s, truth was never established, the book pinned at capacity. Fix is structural with **zero new tuned numbers**: `self._sender.delete_quote` now has **exactly ONE call site** (inside `_withdraw_batch`, tokens spent *before* the request — AST-checked), `_drop_quote` is **unreachable from every withdrawal entry point** (only two droppers, both holding PROOF — AST-checked), the retry driver and the `cancel_all` special case are **deleted**, and the UNKNOWN is resolved by a **READ**: one `GET /communications/quotes?user_filter=self&status=open` per tick = **10 read tokens, O(1) in the pending count, on the bucket the write storm is not touching**, with a strict happens-before guard (`withdraw_asked_mono_ns`, replacing the write-only `withdraw_attempts`) that kills the read-after-write hazard **by construction, not by a delay**. Measured at the live book: **348 DELETEs / 19.3 tok/s / peak 28 tok in any 1s** vs the deleted driver's **2,400 DELETEs / 800 tok/s**; 1 read/tick flat. **BRICK PROOF at `max_open_quotes`=200 with every DELETE 429ing forever: one 10-token read reaped all 200 (the dominant live case — 1,104 of 1,137 were RFQ-gone, already dropped exchange-side) with ZERO successful DELETEs, and the next RFQ quoted `yes 22.00c / no 72.00c / 10.00 contracts`.** Bounded exit for the other branch (all-pending AT capacity + unreadable ⇒ `HALT_NEEDS_RECONCILE` → restart reconcile); pending set bounded by `max_open_quotes` itself (never excluded from it — that would uncap the mass-acceptance worst case). Throughput neutral (+1.2% / −0.5% across two runs; tick unchanged). — [report](2026-07-26-withdraw-resolution-one-write-path-one-read-prover.md) |
| 2026-07-26 | **REAL 20:12Z STALL FOUND: unbounded store read on the maintenance loop; + READ-budget pacing, observed-tier derivation, B3** | **ROOT CAUSE CONFIRMED FROM THE TAPE, FIXED + benchmarked; suite 2969/0. KILL left in place — operator relights** | The earlier attribution (quarantine enforcement) is **disproved**: `market_quarantine_enforced` pulled **0** quotes in **5ms**, there are **zero** `cancel_quotes_touching_delete_failed`, and the status loop ticked on schedule through the wedge. The loop that died is **MAINTENANCE**, and the arithmetic names the await to the millisecond: the only completed `position_ledger_divergence` was 20:07:24.4326, the cadence is **300.0s**, so the next check came due at **20:12:24.4326** — and the last heartbeat beat was **20:12:24.389**. `_sweep_ledger_divergence` awaited `open_ledger_identities()` with **no timeout** on a store whose single aiosqlite thread was saturated by the tape writer (**46** `store_writer_checkpoint_failed`, WAL **57,765 frames, checkpointed=0**). **Proof it never returned: the final counters say `ledger_divergence.checks: 1`** — the counter increments AFTER the read, the cadence stamp BEFORE it ⇒ ≥65s blocked on one await. Fixes: (1) **alarm-only sweeps are launched, never awaited**, single-flight, under bounds DERIVED from the store's own `busy_timeout` (5.0s) and the tick's own poll bound — a slow store degrades to a logged, retried skip; (2) **B3**: `_delete_quote` no longer drops a quote from the mirror on a 429/5xx/timeout (a path that ran **1,137×** in the incident) — only an ack or 404 removes it, unresolved stays fillable-and-counted and is retried by the reprice sweep for its ORIGINAL reason; (3) **READ-budget pacing** mirroring the write budget: the run attempted a measured **peak 183 metadata GETs in one second = 1,830 read tok/s** (each GET costs the live-verified default **10 tokens**) against **300 tok/s** ⇒ 5,726 429s in 332s; paced, the same wave emits **60 reads/s, peak-1s 900 tok = exactly the bucket's own guarantee**, with **0 emitted 429s**; (4) **tier derivation**: `WRITE_TOKENS_PER_S = 300` ("we are on Advanced") DELETED — asked read-only on prod: `usage_tier=advanced`, both buckets `600/300`, permanent manual grant; unreadable ⇒ **fail-safe to Basic, never the highest**, and the write knob is clamped on BOTH axes (its 200-token burst is a **2× breach of the Basic bucket** — clamping only the rate would have 429'd the fail-safe path). Hot path unchanged (peek 42.2 → 41.1 ns; the 598ns budget check is paid only on a cache MISS). — [report](2026-07-26-maintenance-stall-read-budget-and-tier-derivation.md) |
| 2026-07-26 | **FALSE SUPERVISOR KILL 20:12:54Z → liveness/progress split + bounded quarantine burst** | **FIXED + benchmarked; suite 2941/0. KILL file left in place — operator relights** | The supervisor emergency-cancelled 27 resting quotes on a bot that was quoting fine: 11 markets quarantined in one tick (normal end-of-game lifecycle wave, TORBOS+CHCPIT) and 63 sequential DELETEs — **all HTTP 404**, i.e. quotes the exchange had already dropped — kept the maintenance loop in a ~30s non-beating await while that same loop owned the liveness file. Tape proof it was healthy: max inter-log gap **0.702s**, **559 RFQs priced inside the "wedged" 30s**. (Refinement: `_enforce_market_quarantine` itself took **5ms** and pulled **0** quotes — the coupling, not that call, was the mechanism.) Fix: (1) heartbeat now written by a **dedicated `_liveness_loop`** ("alive") + every loop publishes a **derived-bound progress age** ("working") read by the supervisor — detection NOT removed, the maintenance bound is still 30.5s = `heartbeat_timeout_s` + its own cadence, and a wedged quote loop with a **fresh heartbeat** is still killed by name; (2) withdrawal is **concurrent, fan-out ≤ 8, per-call timeout, wall budget = the caller's tick, and 404 = SUCCESS**. Timeout NOT raised (still 30.0s). Benchmark @0.42s/delete: 12-market wave **15,523ms → 2,144ms** (7.2×), 120-quote book (the live cap) **51,736ms → 6,462ms** (8.0×); worst heartbeat age after = **1.01s** vs 129s before at 300 withdrawals. — [report](2026-07-26-liveness-progress-split-and-bounded-quarantine-burst.md) |
| 2026-07-26 | **ESPORTS ENABLED (winners only) + racing wired-but-held; open-items ledger** | **LIVE for CS/LoL; racing blocked on start times** | Tape+API-verified series (400k-RFQ scan + 3,005-series catalog): KXLOLGAME / KXCS2GAME / KXCSGOGAME winners admitted with 3-5c tiers; **cross-sport combos now price from a dedicated 4-6c "mixed" tier instead of the ZERO markup they silently got** (same species as the KXMENWORLDCUP incident). Two traps closed: a "RACE" substring rule would have priced TOP5/PODIUM/SPRINT legs as winners (now exact-series, neighbours stay UNKNOWN), and map-winner series stay unquotable pending a match-map rho. **Racing HELD**: F1/NASCAR tickers embed no start token and expected_expiration sits AFTER the race (BELGP26 19:00Z vs ~13:00Z start), so the default estimate would quote IN-PLAY. Includes the running OPEN-ITEMS LEDGER (entity bounds, p_night ledger no-op, slot-cap dissolution, in-play read-out, breaker history). — [report](2026-07-26-esports-racing-wire-and-open-items.md) |
| 2026-07-25 | **⭐ FULL-DAY REPORT + SESSION-STATE RESUME (handoff)** | **BOT DOWN (3rd metadata halt, fix committed `01b265d`); restart BLOCKED pending operator confirm; full evening bundle armed in YAML** | Read this first. Day's three findings: (1) we were WINNING auctions and reneging (49 won/15 filled, $355 premium declined) — four causes fixed; (2) the ES99 book bound punished diversification → replaced by the operator-ratified **P(KILL-night) ≤ 2%** anchor; (3) leg-axis concentration was invisible (~$127 on one pitcher's arm) → family/entity axis built + armed. Also: p_night KPI (realized+open, doesn't reset as winners settle), v2 independence-benchmark doctrine gate (v1 misfired live, disarmed), EV slot eviction, in-play shadow instrumentation, one-click launchers + DECLINES.bat. **OPEN HIGH: the day-anchored realized seed is a production no-op (position_ledger has no live writers) — p_night's restart-roll doesn't work yet.** 12 review verify-agents aborted on a spend limit (EV-eviction + v2-gate lenses only partly verified). Suite 2782/0. — [report](2026-07-25-session-state-resume.md) |
| 2026-07-25 | **BIG-FILL AUDIT → 4 RENEGE FIXES (flag-gated) + DECLINES.bat** | **BUILT+REVIEWED `fdc9f6d`, suite 2759/0 — arm the 4-flag bundle at next pregame restart** | 16-agent audit answered "why are we missing the $20-50 combos": **we WIN those auctions and renege** (49 won → 15 filled, $355 premium won-then-declined tonight). Causes, all fixed default-OFF: quote-time sizing at OUR bid vs the exchange's award (3.6-4.7× understated) → award-true upper-bound sizing; gate's band-high copula scoring same-game combos −EV where the calibrated fair said +EV → admission EV = FRESH re-priced calibrated fair (pickoff-safe, source audited); waiver certificates killed by unrelated-game fills → game-scoped stability; the accepted quote's dead resting entry double-counting at confirm (review HIGH) → released at accept. Context: max_open_quotes 200 eats $3.2M/day flow blind (derived-capacity build queued); pregame gate $1.58M/day (policy, in-play shadow path); persistence writer 40 min behind. Refuted: markup uncompetitiveness, size-cap-as-$20-50-killer. — [report](2026-07-25-big-fill-audit-and-renege-fixes.md) |
| 2026-07-25 | **P(KILL-NIGHT) BOOK GATE (new operator anchor) + breaker end-of-life exemption + FIRST WARM BOOT** | **ARMED live `6e054b0` — "more bets = more variance = more money" is now the gate's math** | Operator ratified a new layer-2 anchor: the TOTAL-book bound binds **P(loss ≥ KILL distance) ≤ 2%** instead of the ES99 average that pinned a diversified book at ~$245 premium (at small N with ~50%-loss positions the worst 1% is "most lose at once" even independent — the day's ~$350-fill complaint). Candidate gate reads worst-model Wilson-upper P off the CRN vectors; quote-time cap reads a new 1001-point loss-quantile envelope on the snapshot; one-way books stay hard-blocked (P≈40-50%); every concentration wall + realized halts unchanged; flag-off byte-identical pinned. Same restart: **breaker end-of-life exemption** (the un-blinded metadata breaker hard-halted the live bot at 3:40p on the finished KC@DET markets settling — post-close changes now benign, reschedules still trip) and the **first warm boot** (1,351 markets loaded, boot 429s 1,901→4). pbook steer verified in applied price (289/300 quotes). — [report](2026-07-25-tail-prob-gate-and-breaker-exemption.md) |
| 2026-07-25 | **LEG-DIRECTION AXIS + persistent metadata cache + decline detail; OPS HANDOFF** | **SHIPPED (shadow-first); bot + monitors now OPERATOR-RUN via .bat launchers** | The 7/25 tape: half the $313 book short K-overs on the same arms (Cease ≈ $127 across 6 combos), tail pinned $0.73 under the 12%-KILL distance → gate correctly declining +EV tail-adders; boot 429 storm (2,319 metadata failures in 36 s, worsened by a ZOMBIE v5 stack sharing the rate budget) left HR/HIT/TB/RBI unpriceable → K/TOTAL fill bias. Built: (1) **leg-direction axis** — `SERIES:side` / `SERIES:ENTITY:side` committed-loss attribution (rungs fold), `leg_axis_exposure` event, skew `family_cc`/`entity_cc` steer off the SAME caps-derived onset (leg_axis_armed default OFF, byte-identical pinned); (2) **persistent metadata cache** (warm-boot peek, TTL-expired async revalidate, atomic off-loop flush — kills the restart re-brick); (3) **risk_audit decline `detail`** (names the exact bound). Stale-MC fallback audited SAFE. One-click `START_BOT/STOP_BOT/READOUT.bat` handed to operator (single-instance guard — the double-stack lesson). — [report](2026-07-25-leg-axis-metadata-cache-ops-handoff.md) |
| 2026-07-25 | **B1 REVIEWED+HARDENED → BOT RELIT (shadow slate LIVE) → anchor repair + B2** | **LIVE on the 7/25 slate; suite 2720/0; arming = post-slate YAML flip** | B1 adversarial review: 15 findings fixed (`791b42b`) — onset now the tightest ENFORCED budget (min(game $cap, game_frac×bank, KILL 12%×bank) ≈ $261 → DET-scale hole = full ~2.9¢ steer, $100 ≈ 0.4¢, auto-scales); n=1 book rebates new-game flow; hedged games protected; armed clamp never expands the envelope. **RELIT 10:50a (operator-authorized), restarted 11:12a onto the full build** — preflight green, fill rehydrated 0-mismatch, **`p_book: 0.2301` measuring live**, steer shadow on every quote. **Re-hit bypass CLOSED** (`0192640` — accumulated per-combo bound, same anchor, same reason code). **B2 built default-OFF** (`953188c` — certified-hedge budget = $1 EV per $1 certified tail reduction). Watch: one unmodeled-position rehydrate warning (sweep alarms if real). — [report](2026-07-25-pbook-b1-review-relight-anchor-b2.md) |
| 2026-07-25 | **P(book) steering Phase A — the signal is LIVE (shadow)** | **SHIPPED `696be86` — suite 2694/0, zero behavior change; Phase B (the steer) designed** | Operator directive REORDERED P1: DET size was fine, the failure was a NO-VARIANCE one-way book — **P(book) must STEER the betting** (self-aware adapting book, no manual strict numbers). Phase A: `p_profit` on every candidate-gate tail axis (free off existing CRN vectors); **`delta_p_book` logged on EVERY gate verdict** (+ = variance/diversifier, − = one-way concentrator); snapshot publish carries p_book/ev/top-tail-games. Test pins the 7/23 slate in miniature: diversifier lifts P(book) 0.40→0.90 (Δ+0.50), same-way concentrator Δ≈0. Phase B design: skew P(book) component off tail-share deviation from uniform (no target number needed), derived certified-hedge pay-up budget, relight neutrality falls out. — [report](2026-07-25-pbook-steering-phase-a-visibility.md) |
| 2026-07-25 | **P1 risk-rebuild DESIGN DOSSIER** (9-agent code+docs fan-out) | **DESIGN INPUT — verified attach points + invariants + staging; Stage 1 = accumulated net bounds** | The P1 thesis grounded in current code: every enforced cap bounds QUOTE-TIME projections, never accumulated net (per-combo cap is candidate-only `limits.py:845` = the $149-re-hit bypass); P(book) computed but consumed by nothing (candidate ΔP(book) is FREE at the gate — vectors already sampled); skew ARMED but passive with static-dollar denominators + hand-set 150cc tighten (North-Star debt); relight has no directional memory but committed dir-entries survive cancel-all (guard needs only a consumer); NO entity axis anywhere in risk/ (grep-verified). Verified attach-point table for every P1 piece, the 12 gating invariants w/ pinned tests, staging by dollars: (1) accumulated per-structure + per-game-direction net bounds at the reservation path, (2) entity axis, (3) P(book)/skew steering, (4) relight neutrality. — [report](2026-07-25-p1-risk-rebuild-design-dossier.md) |
| 2026-07-24 | **7/23 slate reconciliation (to the cent) + missed-fill root cause + FIX SHIPPED** | **RECONCILED — book FLAT $2,179.74 all cash, all-time +$179.72; incident-C fix BUILT + adversarially hardened (20 findings fixed), suite green** | Post-mortem −$210.35 ≡ exchange-basis slate-only −$210.38 (3¢ premium-vs-cost rounding); window −$205.07 across 26 rows. Marte verdict: same-player exotic shorts lost only **−$18.98 combined** (mutex losing-states bound held; feared $305 realized <$19); total scalar damage −$22.92 incl. the cross-game Detroit×Carroll×Marte −$3.94; taker's scratch profit = exactly what scalar-floor pricing denies → P2 re-validated by the ledger. **NEW DEFECT (incident C):** a real $28.69 maker fill missing from the store — target-cost RFQ "execution failed" cancel = PARTIAL execution (38.15 of 57.44 offered); `_match_exchange_fill`'s EXACT-count guard silently skipped the real fill 3× → "phantom" removed, risk book ran $28.69 light until reserve adoption. Only missed fill ever (134 vs 133, all else 1:1). Fix spec: count-tolerant match (≤ pending, adopt exchange count), log structural skips, periodic fills-sweep reconciler. Also recorded: skew + widen-vs-decline both LIVE in shadow with mutex-aware direction (P1 seams). — [report](2026-07-24-slate-settlement-reconciliation-and-missed-fill-root-cause.md) |
| 2026-07-23 | **MLB SLATE POST-MORTEM — −$210 on one-way Detroit concentration** | **SLATE CLOSED −$210.35; bot DOWN/flat (KILL set); risk-model rebuild spec'd** | First live MLB money slate (3 games). **Variance games PROFITED, one no-variance game erased it:** KC@DET −$259.47 (5W **8L** 1scalar — low-scoring Detroit win, 8 short-Detroit-under combos hit TOGETHER); TB@TOR +$18.73 (6W 0L); AZ@STL +$30.39 (8W 0L 3scalar — Marte scratched→scalar, but STILL net-positive). Total **−$210.35 = 1.5× the ~$143 daily-loss anchor, on a ~1-in-3 LIKELY outcome, not a tail.** Root cause: caps throttled QUOTING but never bounded the SETTLEMENT of a concentrated one-way bet; no hedge (offsetting KC-win/over only $77 vs $349); 3 games once concentrated ≈ 1 bet → low P(book profit). Correlation math was CORRECT (audit clean) — the loss was concentration + DNP info, not a copula hole; scalar pickoff was secondary ($30 net-positive game). Scalar-floor pricing design validated (YES=MAX(fair,∏leg-scalars)+markup, no feed/block). Build spec: P1 hard net-bounds (per-combo/per-game-direction/per-entity all leg families) + P(book)-aware sizing + active skew hedging + relight neutrality; P2 scalar defense. — [report](2026-07-23-mlb-slate-postmortem-detroit-concentration-loss.md) |
| 2026-07-23 | **Same-player HRR pricing audit (CLEAN) + player-level risk-book gap** | **AUDIT CLEAN — no hole, +EV; player-concentration gap flagged; bot HALTED→audited→relit v5** | Taker repeatedly hit **Ketel Marte same-player cross-stat combos** (Kalshi retail-BLOCKED, RFQ-allowed); prober flagged +12.7/+19.9/+21.8¢ "rich" on thin 4–7-maker fields; operator halted ("getting picked off"). **AUDIT: no hole.** All H+R+RBI spec constraints ALREADY implemented + firing (strict implication `N+hit⇒N+HRR`→containment=P(hits) NOT product; impossible dir; reachable 0-hits-2+HRR priced 4.9% not 0; contrapositive; unclassified→UNKNOWN) via exact cells + `_containment_sign`; copula joint == arithmetic EXACT (0.0488/0.0417); fills took copula path, **+EV** (~+6¢ over our fair; loose external overpaid). Pickoff would need Marte 2.3× pooled rate — implausible; settlement is ruler. **GAP (operator-flagged):** risk book directional is game/ML-only (P0-9 nets moneyline; prop deltas tiny) → **$304.99 on Ketel Marte** (one combo grew $74→$149 as re-hit) SPANS games, invisible to `skip_directional_cap`; bounded (mutex losing-states + game caps) not unbounded. Enhancements owed: player-level concentration dim + same-player markup adder. Same-player NOT blocked (pricing sound, +EV). — [report](2026-07-23-same-player-hrr-audit-and-player-concentration-gap.md) |
| 2026-07-23 | **Under-fill investigation → markup walk-back 1¢** (directional-cap diversification) | **DONE — +1¢ MLB markup reverted, bot restarted v4, healthy + quoting; option-A cap fix deferred** | Operator: "not filling enough (~$276/15 fills of $2.38k); blocks maybe net-negative by disallowing +EV hedges." **Evidence CORRECTED the theory:** diversifiers/hedges ALREADY flow (open-quote decomp — overs / KC-win / props rest on the concentrated game); directional declines are **DEEP** (median 1.76× cap, **0% within 1.2× = none marginal**, 24% >2×) → caps refuse real concentration, not marginal +EV; `skip_portfolio_cvar` (MC tail cap) is the decoded spiking "other" AND the real backstop (dir 948 vs cvar 560 ⇒ ~388 gap ≈ flow the coarse cap eats but the rich cap passes). **Real under-fill driver = WIN RATE** (35,695 quote-msgs → 15 fills / $18 each; the earlier +1¢ markup cut competitiveness — markup trades fills⇄curse directly, a bigger dial than caps). Enforced directional cap nets ONLY the single moneyline ME event; over/prop deferred to the candidate-aware MC by monotone-dominance design (`exposure.py:570`). Held: 82% KC@DET / $192 / 8.4% bank, within KILL. **Action:** reverted MLB ladder to pre-+1¢ (1¢ mains / 4/3/2.5/2¢), restarted (kill-tree→cancel-all 72→purge heartbeats→v4 relaunch, supervisor respawned), walk-back CONFIRMED live (prober +2.5¢ vs +0.3–0.9¢). Deferred option-A (cross-dim directional credit) — modest/secondary, off-line build only. — [report](2026-07-23-directional-cap-diversification-and-markup-walkback.md) |
| 2026-07-23 | **MLB GO-LIVE — adaptive enforce BRICKED quoting → shadow (WC caps) fallback** | **LIVE + quoting on prod; enforce shelved (shadow), first fills in** | Launched live prod MLB. `enforce` 1% bootstrap declined **100%** (`skip_game_loss_cap`, 0 quotes): the per-game cap ($23.85) is checked vs each combo's **comonotone worst-case** (~$80+) while per-combo checks premium (~$20) — no MLB combo fits. **Exactly adversarial-review H2, which I overrode with theory instead of validating it quotes.** Fix: `adaptive_caps_mode: enforce → shadow` → proven WC static caps enforce (per_combo 5%/game 50%/slate 65%, bounded by daily $143/KILL $286), adaptive brain still logs. **Verified on the exchange: 187 live quotes + real fills** (NO 48.6c×10, 62.2c×7.6, 69.4c×31), equity intact $2384.77. **Checkpoint `checkpoint-mlb-pre-risk-engine` → `2cb3422`** (MLB+correlated-pairs, pre-risk-engine rollback target). Follow-ups: 400 invalid_parameters on ~5/200 sends, `_count_slate_games` multi-day over-count (use expected_expiration_time not close_time), re-size enforce from real data before re-enabling. — [report](2026-07-23-mlb-golive-enforce-bricked-shadow-fallback.md) |
| 2026-07-22 | **Correlation-adaptive caps — consolidated-spec reconciliation** | **DONE — 4 gaps closed, 1 flagged; formula reproduces the operator sim; suite 2670/0** | Reconciled the built cap layer against the operator's consolidated spec. Closed: daily/dd now `k·sigma_day` from the clamped f_slate (not fixed z-anchors); validation guard `kill_covers_drawdown`; startup `kill_prob_60n`/`kill_sigma_multiple` alarm; `kill_anchor` = operator config dial (`adaptive_caps_kill_anchor` 0.12). Flagged: ρ_wg unmeasured (needs per-combo P&L; σ₁ carries it). **Formula reproduces the spec's sim table exactly at KILL 30% (67/53/32%)**; bootstrap healthy at 5.0σ, old model self-destructs at 2.1σ/P0.63. — [report](2026-07-22-cap-family-spec-reconciliation.md) |
| 2026-07-22 | **Correlation-adaptive caps — wiring + arm (Path B)** | **WIRED — 5 modules + quote-app hook, fully adaptive (no manual numbers), armed** | Wired the adaptive-cap brain into live enforcement: `cap_family`/`pnl_measurement`/`adaptive_caps`/`derived_cap_engine` + `LimitChecker.set_limits` + quote-app sync-startup + nightly loop + `expected_games` auto-count. Adversarial review PASS on isolation/fail-safe/math; its H1(daily loosen)/E1(floor) resolved by going FULLY adaptive (no config clamp — every cap from measured + policy anchors; book floors = slate/drawdown). NOTE: this armed enforce, which then bricked MLB quoting — see the 2026-07-23 report. — [report](2026-07-22-adaptive-caps-wiring-and-arm.md) |
| 2026-07-22 | **MLB PRICING-QUALITY VERDICT — maker-closeness + settlement backtest** | **VERDICT: ARM established families; GATE new families to a capped first-live-slate — this is the QUALITY axis (isolation was already certified)** | Dual-ruler quality read on the frozen fresh cache (zero blast radius). **(1) vs makers:** promoted fair tracks clearing to **0.36c per-combo median (92% within 2c)**, 0.53c per-print; block-correlation is **5.4× tighter than legacy on prop-carrying (0.68c vs 3.69c)** with **no game-line regression (0.29c = 0.29c)** → GATE PASS. Both tails small: auto-lose >2c-over 1.9%, **pickoff >5c-under 0.5%** (0.7% prop-carrying). **(2) vs settlement (the ruler, n=4,984):** mean fair **0.1534 vs settled 0.1537 (Δ −0.0003)**, ECE 0.0050, Brier 0.1087, **beats legacy paired z=+6.8**; moneyline control (copula=0.000) fair 0.1046 vs settled 0.1017 = perfect; decomposition proves the copula is NOT bent — every family flag (player_ks fair .329 vs settled .527, total, rfi) is **marginal-side/live-book on small game-clustered samples**, a watch item not a refit. props_only high-prob 45c-fair vs 59c-realized (n=199) = **shared maker conservatism** (Brier ties clearing .1110≈.1106), not our miss. **(3) VALIDATED vs DEFERRED (law #4):** established families (moneyline/spread/total/hr/ks/hit/hrr/tb/rfi) validated both rulers; **new OUTS/RBI/SB = M43/priceable6/graded0 — 0 printed, 0 cleared, 0 settled → plumbing-verified (classify+rho engage+sane fairs) but edge-UNVALIDATED, CANNOT be tape-backtested.** **(4) REC:** arm established now; **new families' first live slate IS their backtest** — capped size + standing fair-vs-clearing (markup) + settlement (mispricing) logging, size-uncap gated on ≥1 new-family combo clearing then settling. Do NOT refit the ks/total settlement gaps (no-refit rule). — [report](2026-07-22-mlb-pricing-quality-backtest.md) |
| 2026-07-22 | **MLB GAP-PAIRS TRANCHE — ADVERSARIAL JUDGE (post-wire)** | **VERDICT: SHIP — all 6 attacks CONFIRMED with run-code/independently-recomputed proof; 0 gap-pairs defects; suite 2613/0** | Judged the WIRED gap-pairs tranche (327 keys). (1) Re-derived 4 spots from the caches by recomputing joint freqs + inverting the SHIPPED copula myself (not reading the staged doc): `outs\|hit:opp` −0.2088, `outs\|hrr:opp` −0.3186, `outs\|outs` +0.1611, `ks\|rbi:opp:r1` −0.1569 — all match wired to ≤0.003, outs facing uniformly deeper than ks (hit −0.209 vs −0.125, hrr −0.319 vs −0.175). (2) `_pair_rung_suffix` CHAINS `:r15:r2`/`:r1:r2` → single-rung keys would be DEAD; un-runged `outs\|rbi:opp`=−0.30 / `rbi\|rbi:same`=+0.07 FIRE (priced combos, read source notes); no dead single-rung keys on any both-runged family; `rbi\|spread` correctly wired 24 CHAINED `:rN:rN`; `sb\|spread`/`ks\|rbi` single-rung fire. (3) `:opp` genuinely asymmetric (spread×rbi/sb opp≠−same); outs×batter :opp/:same both measured (\|same\|«\|opp\|). (4) 3×12 enumeration: every outs/rbi/sb pair has a config entry AND a plain base key → none hits flat +0.6. (5) 327↔327 bands, 0 orphans/missing/unsorted, all fallbacks sign-span, all bands ≥ era. (6) real `git diff 5b14e01`: 240 changed key-lines, 0 off-family, 0 pre-existing key modified, soccer untouched. Minor: `outs\|outs` band 0.05 (config) vs 0.04 (staged) = safer; found a PRE-EXISTING dead-config in the EARLIER new-props tranche (`player_outs\|spread` per-rung keys chain but were wired single-rung → unused refinement, NOT a flat gap, NOT mispricing). — [report](2026-07-22-mlb-gap-pairs-adversarial-judge.md) |
| 2026-07-22 | **MLB GAP PAIRS MEASURED — OUTS/RBI/SB reachable pairs off flat/labeled priors** | **STAGED, NOT WIRED** — 23 reachable pair families measured to standard; wire list `docs/calibration/staged_mlb_gap_pairs.md`; live modules pristine | Stage-3 gap-ledger found 23 reachable OUTS/RBI/SB pair families STILL on the flat +0.6 default (16) or GUESSED labeled priors (7) after the Stage 6+7 sign-off — incl. the named fail-closed `player_hit\|player_outs` (band spanned 0). MEASURED all 23 from Retrosheet 2005-25 (reused newprops caches, SHIPPED copula, live `pair_key`): **outs×batter FACING (:opp) uniformly NEGATIVE and deeper than the ks siblings** (hit -0.21, hr -0.17, tb -0.23, hrr -0.32, rbi -0.29..-0.34, sb -0.13; ks×prop 1.7-3.6× shallower), :same teammate ≈0 (measured, not negated); **outs×rbi:opp ladder non-flat → per-rbi-rung**; **outs×outs opposing +0.16** (like ks\|ks +0.04); **rbi×spread ±0.33..±0.42 chained** (like ml×rbi, rises with rbi rung); **sb×spread asymmetric** (:same +0.06..+0.13 declining / :opp -0.17..-0.18 flat, both measured); **rbi×rfi +0.10** (between hit +0.065/hrr +0.122); **ks×rbi:opp -0.16..-0.17 per-rung** (deeper than the -0.12 guess, inside the hit\|ks/hrr\|ks band); sb×batter/rbi×rbi/cross-family teammate small-+/opp ~0. **Zero sign flips, zero NO-QUOTE cells** (smallest cell n=53,726 > 50k gate); every labeled-prior guess confirmed + tightened. New tool `tools/calibrate_mlb_gap_pairs.py` (additive, blast radius zero). — [report](2026-07-22-mlb-gap-pairs-measured.md) |
| 2026-07-22 | **MLB NEW PROPS OUTS/RBI/SB — STAGE 6+7 SIGN-OFF (wire verified)** | **SHIP-READY — both judges SHIP, differential clean, scorecard PASS; arming gated only on operator decisions** | The full playbook Stage 6+7 pass on the OUTS/RBI/SB wire: **bit-exact differential 5169/5169 IDENTICAL** (0 movers, 0 unexplained → surgical additivity); measurement judge SHIP (independent BVN re-derivation, ml×sb real, :opp measured-not-negated); wiring judge SHIP (61/61 keys, no collision, no interpolation, containments non-farmable); tripwire needs no change (containments in-code, MLB non-farmable). Scorecard **GATE PASS byte-identical to baseline** (existing families untouched). **HONEST LIMIT: 0/43 new-prop tape combos are pregame-priceable → backtest CANNOT statistically gate the new families** (freshly eligible) — confidence rests on the 49,490-game measurement + exact containments + 2 judges + no-regression differential + pricing sanity (every archetype engages the wired value). Coverage: typed 9/12 previously-untyped pairs, 1 named fail-closed gap (`player_hit\|player_outs`, band spans 0). Suite 2595+17 new regression tests green, ruff/mypy clean. 5 operator decisions listed before arm (only outs×batter + fractional-V re-affirm are substantive). — [report](2026-07-22-mlb-newprops-stage6-7-signoff.md) |
| 2026-07-22 | **MLB NEW-PROPS ADVERSARIAL WIRING JUDGE — OUTS/RBI/SB** | **VERDICT: SHIP — all 7 wiring attacks REFUTED (defense holds), 0 defects; suite 104+463 green** | Ran the LIVE modules (SgpParams built exactly like `engine.py:161`, no reimplementation; config parity confirmed — no YAML overrides `correlation`). (1) **61/61** new-family config base keys equal `pair_key(a,b)` — 0 DEAD (all sort traps `player_ks\|player_outs`, `player_hr\|player_rbi`, `player_hit\|player_sb`, `moneyline\|player_*`, `player_outs\|total/rfi/spread`, `player_hrr\|player_rbi` verified). (2) 0 classification collisions — HR/HRR/HIT/KS/TB/RFI/GAME/TOTAL/SPREAD unchanged, 3 new correct, `KXMLSBTTS→btts` (not player_sb), LEADER/DERBY/F5/SERIESGAMETOTAL still UNKNOWN, keyword order after-MLBRFI-before-TOTAL proven. (3) rung = LAST hyphen segment (`MONTERO54-15→15`, not 54); unlisted ks×outs r17/r19 → `:same` 0.56 **not interpolated** (≠0.47); SB not rung-keyed. (4) same-pitcher ks×outs→`:same` 0.53, same-player HR×RBI→None (containment), pitcher-set={ks,outs}. (5) HR⇒RBI & RBI⇒HRR IMPOSSIBLE+**non-farmable** (MLB scalar), SB⇏HIT not containment. (6) UNKNOWN pair low=−0.30 spans 0; 5 plain fallbacks sign-spanning. (7) **61 values/61 bands, 0 orphans** (247/247 table-wide). One documented fail-OPEN noted (same-family rung ladder → wide flat fallback, pre-existing, not a defect). — [report](2026-07-22-mlb-newprops-adversarial-judge-wiring.md) |
| 2026-07-22 | **MLB NEW-PROPS ADVERSARIAL MEASUREMENT JUDGE — OUTS/RBI/SB** | **VERDICT: SHIP — all 4 attacks CONFIRMED, 0 sign/frame/magnitude defects; 1 cosmetic doc nit** | Independently re-derived the wired ρ from `newprops_cache/*.pkl.gz` with a from-scratch scipy-BVN tetrachoric solver (NOT the tool's `implied_rho`, NOT trusting `newprops_results.json`): outs×KS +0.5596 & ladder +0.61→+0.36 (non-flat, monotone), outs×total −0.5364/recent −0.45, rbi×total +0.31→+0.42, **HR⇒RBI 0 violations** (also re-parsed 4,164 raw HR plays: all credit ≥1 RBI). ml×sb +0.15 is REAL not artifact (P(win\|SB)=0.614=+12.4pp copula-free lift, 2× HIT's; label frame verified 0 inversions). **:opp faithful** — ml:opp = exact-complement measured direct; **spread:opp genuinely asymmetric (opp≠−same by 0.11→0.24, grows with line)** wired to the direct value not the negation. All bands cover their era shift. Conditional cells match to 6dp. Only nit: doc's "CIs disjoint" over-states the r12/r15 adjacent pair (wide r12 marginal); no wiring impact. — [report](2026-07-22-mlb-newprops-adversarial-judge-measurement.md) |
| 2026-07-22 | **MLB SETTLEMENT-REGIME ρ AUDIT — sport-switch readiness item #4** | **COMPLETE for the shipped MLB table (verdict CLEAN); new props OUTS/RBI/SB conditional pending #5** | The WC lesson (a ρ measured on regulation must not be inherited by an ET/pens variant) has NO MLB analog: the shipped `pair_rho_by_sport["mlb"]` prices ONLY the 9-family baseline (GAME/TOTAL/SPREAD/RFI/KS/HIT/HR/HRR/TB), every value Retrosheet-final-measured on the same window its market settles; the one real rule-change break (extras ghost-runner, pre/post-2020 −0.04→+0.10) is already corrected; TEAMTOTAL/F5 explicitly NOT priced (non-combo-eligible); RFI is its own inning-1 window; no cross-sport inheritance (moneyline\|moneyline −0.95 = structural mutex). The rain-short/DNP scalar regime is a reconciliation mechanic (owned by the AS4 decision + the flagged operator re-affirmation of the reactive fractional-V stance), NOT a ρ-value error. Gates only on the new props, whose settlement window is verified live under #5. — [report](2026-07-22-mlb-settlement-regime-rho-audit.md) |
| 2026-07-22 | **SCALAR/DNP RECEIVABLE HOLE (AS4) — MLB gating decision** | **DECIDED: (a) ACCEPT-AS-IS for the initial armed run + one automatic monitor; NOT a blocker** | The give-back cascade shield subtracts KNOWN-winner receivables from `peak−current` to ignore the settlement-cascade equity trough; the fact resolver caches only binary 0/1, so a `scalar`/DNP leg (rain-short/suspended/void MLB settlement) → `_unresolvable` → no receivable → the whole combo is un-shielded → **false human-only HALT_DRAWDOWN (~$238) / HALT_HARD_TRIP KILL (~$286) on a phantom trough** (fail-closed, NON-lossy — availability cost, not money). Frequency ~1–2% of MLB game-days (vs soccer 0-in-4,913) → real for MLB. Not a blocker (fail-closed); not build-first (the only config-level scalar shield would over-reduce the give-back, breaking the shield's fail-closed contract). Fix = promote the existing `settled_scalar_unresolvable` WARN to a paging alarm + annotate give-back breaches with scalar-suppression state (logging-only, zero pricing blast radius). Eventual mechanical close = read exchange `settlement_value_dollars` on the graded scalar row (readiness item 4/5, not an arming gate). 3 settlement facts owed from the KXMLBOUTS/RBI/SB rules agent. — [report](2026-07-22-scalar-dnp-as4-gating-decision.md) |
| 2026-07-21 | **MLB NEW PROPS MEASURED — OUTS/RBI/SB (3 newly combo-eligible families)** | **STAGED, NOT WIRED** — full Retrosheet 2005–25 measurement + staged table/bands/conditional-cells in `docs/calibration/staged_mlb_new_props.md`; live modules pristine | outs×KS same-pitcher **+0.56** but ladder NOT flat (+0.61→+0.36 — KS strike-stability does NOT transfer, per-rung keys staged); outs×total −0.54 (recent −0.45); **HR⇒RBI containment EXACT 0/101,201**; rbi×total/ML rung-monotone +0.31..+0.42 / ±0.33..±0.38; ml×sb +0.15 DIVERGED from ≈0 prior (investigated); parse validated 99.1–100% vs gamelogs + 5 exact spot checks. 5 wiring seams documented (incl. sgp same-pitcher ks×outs routing gap). — [report](2026-07-21-mlb-new-props-outs-rbi-sb-measured.md) |
| 2026-07-21 | **ADVERSARIAL REVIEW OF THE 4 POST-WC BUILDS — 14 findings FIXED** | **SHIPPED, suite 2595/0 — all four reviewers returned FIX-FIRST; 3 CRITICALs caught that unit tests missed** | (1) transfer/peak double-count → P&L-space give-back w/ finalized_ts ordering; (2) self-leg side inversion → side always "yes", receivable sign pinned both directions; (3) daily-mark freeze on reserves → skipped; + restart-durable in-play exemption (rehydrate arm + metadata retry), positions pagination + zero-row-only release + quantity net, TTL tombstone + cancel hooks + confirm cross-check, doc-verified transfer enums + clawback reversal, tool guards. Filed: pre-latch re-measure, scalar-receivable hole (MLB gate), AS1 atomicity verify. — [report](2026-07-21-adversarial-review-four-builds-findings-fixed.md) |
| 2026-07-21 | **ACCOUNT STANDING + PAST-RUN AWARENESS** | **SHIPPED, suite 2576/0 — true all-time profit $384.77 reconciled to the cent ($2,000 deposit → $2,384.77, residual −$0.02 trading fees)** | No-context exchange positions ADOPT as conservatively-reserved holdings (exchange figures only, fail-safe LARGER, flat ⇒ released); deposits/withdrawals auto-adjust SOD/peak anchors (transfer watch — no manual re-anchor); startup `account_standing` line; `account_standing.py` tool + `get_deposits`/`get_withdrawals` client methods. Store overstated premium $15.29 vs exchange cost basis (manual fill rows). — [report](2026-07-21-account-standing-past-run-awareness.md) |
| 2026-07-20 | **FINAL CAMPAIGN P&L STATEMENT — exchange-ledger exact** | **CAMPAIGN CLOSED: net +$369.50 lifetime (106 fills, 100W/6L), weekend ≈ +$221 on the 74 fills; book FULLY settled, equity $2,384.77 all cash** | Fill store reconciled vs GET /portfolio/settlements to the cent; by-settle-day and by-fill-day cuts; standing tool `tools/diagnostics/campaign_pnl_statement.py`. — [report](2026-07-20-final-campaign-pnl-statement.md) |
| 2026-07-20 | **SPORT-SWITCH GATES 1+2 BUILT — in-play breaker exemption + settlement receivables** | **SHIPPED, suite 2562/0** | The two nightly-slate blockers from the WC: (1) started-game legs leave the marginal-jump watch (45 trips/8 halts class) via the pregame gate's own start ladder, UNKNOWN/allow_inplay keep the full watch; (2) KNOWN-outcome settlement credits shield the give-back halts (`max(0, peak−current−receivables)`, the $430 false-KILL class) — losers shield nothing, peak stays raw, confirm+poll drop rule, 30-min TTL backstop. MLB/WNBA switch now gated only on readiness items 3–5. — [report](2026-07-20-inplay-breaker-exemption-and-settlement-receivables.md) |
| 2026-07-20 | **SESSION STATE / RESUME — post-WC handoff** | Kill CLEARED 7/20 (operator); bot intentionally down until the sport switch; llm-b DELETED; merged→main `d936061` | Campaign +$234.09 net on the final (71/74 winners); 10 findings table w/ commits; standing rules; 12-item autonomy roadmap; armed-config summary; runbook; loose ends incl. incomplete rho backtest. — [report](2026-07-20-session-state-resume.md) |
| 2026-07-19 | **WORLD CUP CAMPAIGN FINDINGS + new-sport readiness** | **CAMPAIGN COMPLETE — final settled +$234.09 net (71 winners / 3 losers on the 0-0 Spain-via-pens final); READ FIRST for MLB/WNBA prep** | 10 incident classes found+fixed (mutex det-max, waiver trim/K, cancelled-but-executed both directions, settled-leg facts, peak steer, rho promote, auto-scaling caps, silence alarms, in-play breaker gap, settlement equity trough); generalizes-vs-per-sport checklist + post-WC build queue. — [report](2026-07-19-worldcup-findings-and-new-sport-readiness.md) |
| 2026-07-19 | **ARG-champ × Messi-1+ fair vs field (pre-final) — decomposed; ρ promote recommended** | **MEASURED ~10:25 AM ET; operator sign-off owed on `advance\|player_goal:same` 0.45→0.52 (ideally before the 2:45 PM ET kickoff)** | Our 21.61c fair reproduced to the cent through the LIVE engine path: the champion-alias 2-leg combo is structurally UNDER-IDENTIFIED (1 team-level leg) → silent copula fallback onto the hand prior `soccer:advance\|player_goal:same=0.45` (cond 51.4%). Our own DC model, identified from the final's other books, says 22.75–22.88c (cond 53.6–54.9%, implied ρ 0.51–0.53) — the 0.45 "directional retains ~0.8" attenuation undershoots because scorer props settle INCL ET and the 0-0-pens dilution is only ~5% of champ states. Field decoded: public combo prints are the taker's ALL-IN (price + 7% taker fee; 3/3 of our target-cost fills reconcile <0.1c — settles the NOTES open question on RFQ combo fee side), so field maker fair ≈ 22.8–23.1c = our structural fair, and the operator's 26.7c/3.74x is fee+markup, NOT a 63.6% conditional (which would need Messi's per-goal share to jump 0.386→0.479 in win states — implausible). We were the systematic cheapest by 0.4–1.0c all-in → one taker bought 29 clips / 423ct / $324 premium; cluster ≈ +$2.3 at structural truth (booked +$4.79 ~2x overstated; −$14 only under the naive 26.7 reading). Recommend PROMOTE config.py:605 0.45→0.52 (measurement/structural, rule-8b backtest re-run gate; :opp mirror deliberately untouched). Tool: `tools/diagnostics/argmessi_fair_vs_field_20260719.py`. — [report](2026-07-19-argmessi-fair-vs-field.md) |
| 2026-07-19 | **ESPARG waiver "not certifiable" declines (11:50 PM–12:21 AM ET) — diagnosed** | **PRE-EXISTING, no fix; operator decision owed on trim-K/budget** | 4 overnight declines on ARG-champ+Messi = `no_structural_plan` from the waiver's DC plan build; NOT a regression (old-vs-new code bit-identical on the same inputs, 8-cell check; `state_worst_case.py` unchanged; the a57afc3 0/1-skip is a no-op for unsettled ESPARG legs). Real cause: certification flickers with WHICH 12 quotes the trim keeps — multi-scorer parlay flow puts 4+ same-team `KXWCGOAL` markets into the inversion and the share system blows `_MAX_TEAM_SHARE=0.95` → `invert` raises → game falls to copula (repro: `tools/diagnostics/repro_esparg_waiver_certifiability_20260719.py`, S1 fail / S2 certify match the live 20-second flicker; thin-book <2-team-target instants also reproduce). Bigger finding: **no waiver has GRANTED since the K=12 trim armed (last grant 7/16 10:54 PM ET) — the dropped-tail adder alone (10.5–14.4M cc) exceeds the game budget (10.3M cc)**, so game-cap relief via waiver is effectively OFF under load either way. — [report](2026-07-19-esparg-waiver-uncertifiable-diagnosis.md) |
| 2026-07-19 | **Auto-scaling delta caps** | **SHIPPED `fedb268` + ARMED 0.80/1.30 of bankroll (12:17 AM ET restart)** | Operator: "I don't like manually moving stuff, it should be automatic" — delta caps were the last absolute numbers in the risk stack (3 hand-bumps in a week); now fractions of the same live bankroll basis as the loss budgets, recomputed per check; absolutes remain only as the no-bankroll backstop; frac-unset pinned byte-identical; 25 tests, suite 2486/0. — [report](2026-07-19-auto-scaling-delta-caps.md) |
| 2026-07-18 | **SETTLED-LEG MARGINAL RESOLUTION — the FRAENG dark-bot outage fix (book_risk_unusable, 366k audits / 0 quotes in 2h20m) + ADDENDUM 1: marginal-jump breaker settled exemption (the 02:17Z relight hard-halt) + ADDENDUM 2: relight2 registration stall (02:40Z — 3 of ~12 graded facts resolved in 25 min) fixed with a BATCH registrar every tick + never-fetched fetch priority + `settled_resolution_pending` observability + ADDENDUM 3: relight3 valid-but-EMPTY husk books (registrar tested `book.valid`, provider needed a readable microprice — predicate mismatch left 9 finalized legs unregistered) fixed with ONE shared `_feed_marginal` predicate consumed by both** | **BUILT + tested (32 new total, suite 2461/0, ruff/mypy clean); relight-ready** | FRAENG settled while cross-game combos held FRAENG legs → the settled legs' books left the feed → book-risk model UNKNOWN → CVaR cap failed closed on EVERY quote until the final would settle. Fix: a settled leg's marginal is the exchange-GRADED FACT — new `marketdata/settled.py` resolver fetches `GET /markets/{ticker}` `result` (yes⇒1.0/no⇒0.0, accepted only under status `determined`/`finalized`; live openapi.yaml verified, matches api-notes, NO discrepancy) off the maintenance tick (bounded, single-flight) and caches permanently; `_marginals` goes feed → settled-cache → UNKNOWN (committed legs only registered). Risk becomes CONDITIONAL on the facts: lost-leg combo = zero further loss (dead parlay, locked premium), won-leg = full conditional exposure on remaining legs; 0/1 marginals excluded from DC inversion targets (constant copula columns) and never shocked by the structural challenger. Closed markets NOT yet graded stay UNKNOWN (no score inference, fail-closed pinned); caps/waiver/det-max/filters/skew untouched. E2e test reproduces the exact 9-position outage shape → quote passes the public CVaR path again. — [report](2026-07-18-settled-leg-marginal-resolution.md) |
| 2026-07-18 | **Peak-concentration PRICING steer (leg-cluster / P(book)=52.47% coin-toss fix)** | **BUILT + tested + adversarial-verify SERIOUS fix same day (rebate now certified against the FULL top-loss plateau — `plateau_slices`, capped 4096 states, oversized ⇒ neutral — so a plateau-stacking refinement like NO {ARG-adv & over 5.5} can no longer pocket the rebate; widen path byte-identical; 41 tests, suite green, ruff/mypy clean); LIVE at next restart (rides the ARMED skew seam, `peak_enabled` default True)** | Operator directive: the FRAENG book's 2-3-leg scoreline cluster (FRA-win x27/$189.77 + Mbappé x18 + BTTS x9 all hitting on one "FRA ~2-1" peak) dragged P(book profits) to a coin toss — fix must be PRICING only (no caps, no skips, no throughput cost). Shipped: `sim/peak_profile.py` caches the COMMITTED book's top-K (default 5) worst DC scorelines per game OFF the hot path (waiver enumeration seams reused verbatim, generation-stamped like the book-risk snapshot); at quote time the skew seam adds a clamped component — candidate HITS the cached peak ⇒ widen up to +6c scaled by `min(1,prem/budget)×severity×(peak/budget)²`, provably MISSES all cached peaks ⇒ rebate up to −1.5c (flattening flow); composed classifier hard-bounded [−3c,+12c]; ANY doubt = zero adder (UNKNOWN can never widen). Measured added per-quote cost **0.055ms** (p95 with profile 0.12ms); profile build 12.4ms only on position-generation change. Peak rows never feed widen-vs-decline (pricing-only by construction); top-loss parity vs certified `state_worst_case_by_game` to the cent. — [report](2026-07-18-peak-concentration-pricing-steer.md) |
| 2026-07-18 | **Fill-recovery hardening — verify-before-discard + ledger-write loudness + runtime position-reconcile net ("the risk book must count ALL combos")** | **BUILT + adversarial-review round FIXED same day (SERIOUS matcher double-count killed: min_ts scoping + order_id ledger guard + claim set, each independently test-pinned); suite 2388/0; arms at next bot restart** | Root cause of BOTH same-day incidents (903935fc 16:24Z, 7d79f32b 18:30Z) established from the tape: an exchange execution variant where the confirmed quote's status flips to `cancelled`/"execution failed" while the fill executes anyway as a taker-style REGULAR order (is_taker=true, nonzero fee, /portfolio/fills only, NO quote_executed WS message) — the old sweep trusted the status and removed REAL positions. Now: a cancel report keeps the position booked while /portfolio/fills is polled (3×90s, injectable clock, min_ts-scoped to the confirm window); a match passes three adoption guards (never a historical/claimed/ledgered order_id — live rows 59/61 proved identical-count fills recur) then writes the row via the NORMAL writer with the exchange taker fee booked (`fill_recovery_late_execution`); verified absence discards with evidence; all-reads-errored KEEPS the position and retries up to 3 full rounds before the loud `fill_recovery_verify_unresolved` ERROR. Fills-ledger write failures are loud (`fill_ledger_write_failed`) + sweep-retried. New `position-reconcile` task alarms `position_reconcile_unmodeled` on any exchange position the book doesn't model (5 min default, alarm-only, never auto-inserts). — [report](2026-07-18-fill-recovery-verify-before-discard.md) |
| 2026-07-18 | **Mutex-aware det-max cap + trimmed-set waiver fingerprint (the two game-day fixes)** | **SHIPPED pre-FRAENG — build→3-lens adversarial verify→fix; suite green; economics verdict SHIP-WITH-CONDITIONS** | Overnight 86-decline decode: 51 = waiver fingerprint churn (mechanical), 15 = comonotone det-max full (+$16.17 fill-time EV refused, killed exactly the ENG/ARG-champ diversity the operator wants), 13 honest (game/per-combo/negative-EV incl. a −$6.66 "hedge" pickoff the gate correctly ate). Fix A: waiver stability fingerprints the trim-SELECTED top-K set + tail≤adder revalidation at grant (churn outside top-K grants; committed-risk moves still never waived). Fix B: det-max cap gates on min(comonotone, mutex-aware) — per-game state-exact DC worst (clamped, no credit, ET/pens branch-expanded) summed across games, comonotone residual for every unproven slice; bound proven EXACTLY TIGHT vs exhaustive settlement oracles; kill-switch knob threaded to BOTH gate sites (verify-driven fix) + both numbers in every decline line. Expectation set: live book is one-sided ⇒ restart frees ONLY opposite-branch flow (ENG-win ~zero det cost, ARG-champ ~$5.64/100); ESP-champ/tie still decline correctly. Conditions: KXWCGAME settlement rule verify before Sunday; det budget now means "worst realizable outcome" not "Σ premium". — [report](2026-07-18-mutex-detmax-waiver-fingerprint.md) |
| 2026-07-17 | **SESSION STATE / RESUME — bot LIVE on Batch-1; haircut BUILT (verify owed); markup raise ARMED; restart 2 = the remaining step before Saturday** | **READ FIRST if resuming** | Exact-moment snapshot ~03:30Z: Batch-1 live-verified (quoted/min +52%, speed-miss 32%->7%, wall = skip_game_loss_cap 89% of in-scope); haircut VERIFIED (SERIOUS ME-transition floor fix applied) + committed `9a27682` + ARMED 0.40 + LIVE at restart 2 (03:51Z); markup raise ARMED in local yaml (base 2c / 3.5c / 4.5c / 6c tiers, probe-driven); book MC P(profit) 62.0% / P(ruin) ~0 (tool committed); 5-fill RFQ probe: our fair == sharpest maker on every fill incl. the champion-alias combo; skew mutex-blindness measured (ARG-champ hedge flow widened 63/63 — fix = feed P0-9 direction) and queued; step-by-step NEXT list with copy-paste commands. — [report](2026-07-17-session-state-resume.md) |
| 2026-07-17 | **Quote-time RESTING-QUOTE HAIRCUT (weight 0.40 + top-3 burst floor) + event-driven post-fill risk pull** | **LANDED `9a27682` (suite 2290/0 post-verify-fixes, pushed); ARMED 0.40 + floor 3; LIVE since restart 2 (03:51:19Z, `live_20260717_haircut_markup.log`, preflight green, 12 pos/0 mismatch). Verify fixed SERIOUS burst-floor break at the 1→2 ME-event transition (regime-tracked floor base) + MINOR pull double-count window (pull schedules after the filled quote drops)** | Kills the #1 flow killer: quote-time caps folded ALL resting quotes at 100% mass-acceptance, double-counting the confirm path's EXACT enforcement (reservations+MC+waiver) — 52.7% of the 7/16-17 window's 1.14M no-quotes carried `skip_game_loss_cap`, median breach only 1.03× budget. New composed fold `min(full, max(ceil(w·full+(1−w)·base), base+topK))` on every mass-acceptance axis — provably MONOTONE in resting AND candidate sets (F1 lemma survives; re-fuzzed ARMED 3,000 cases / 0 violations), burst floor = full contribution of the 3 largest resting quotes. Confirm path pinned at 100% by an explicit `apply_resting_haircut` flag only the two quote-time sites pass (bit-identity regression-tested). E2 property REWRITTEN: serial confirm path never over-commits budgets for ANY haircut-admitted resting set × ANY accept order. Post-fill pull deletes enforced-breached same-game resting quotes (`delete_risk_evicted_on_fill`, analytic-only, fail-safe). Rule-8 prototype `tools/proto_resting_haircut.py`: 3,000-case monotonicity fuzz, w=1 parity, floor, port parity, armed-lemma — all 0 violations; tape estimate: **95.2% of game-loss declines unlock at 0.40** (all-resting optimistic bound). — [report](2026-07-17-resting-quote-haircut-quote-time.md) |
| 2026-07-17 | **Throughput Batch-1 remainder — F2 mid-pipeline liveness + F1 monotone pre-pricing gate + record-after-price fast-lane + F5 snapshot count** | **LANDED `15ebe40` (suite 2263/0, pushed); activates at the same relaunch as max_open_quotes 120 + armed F1 gate** | Build→3-lens-verify→fix workflow; all 4 CONFIRMED findings fixed pre-commit (SERIOUS: fast-lane silently redefined `rfqs.seen_at` from pickup to post-pipeline, corrupting the latency instruments that gate the WS-sharding decision → pickup time now captured at entry and passed through; F2 disconnect-cleared registry read as positive deletion → positive-deletion-only semantics + two-generation stale store). F1 is rule-8 gold: 55k fuzz cases vs the LIVE LimitChecker, 0 false skips, exclusions proven load-bearing by counterexample; would pre-decline ≤48.2% of the 811k no-quotes on the 7/16-17 tape without touching a single quote_sent. Tape RE-BASING notes (rfq_closed cliff, filter tallies, armed-gate reason subsets, paper-mode liveness) in the report — the relaunch is the watermark. — [report](2026-07-17-batch1-throughput-remainder-landed.md) |
| 2026-07-16 | **Adversarial verify ROUND 2 (the owed re-verify of the outage-built commits) + alias hardening + ONE relaunch** | **DONE — commit `a397efb` pushed (suite 2232/0); bot RELAUNCHED + LIVE-VERIFIED on the full tree (`live_20260717_alias_verified.log`)** | Three independent agents re-verified `2bfae72`+`1af2953`+`ff250da`+`8d8d96c`+`9526fbe` + the crash session's uncommitted round-1 fix pass (all round-1 fixes CONFIRMED correct). Two SERIOUS finds fixed: (1) same-game ADVANCE×ADVANCE priced pens factors INDEPENDENTLY (q² not q on level-after-ET states, ~3-5¢ underprice on a tight final, live on champion flow) → **Family 4 advance-COMPLEMENT** in relationships.py ({yes,yes}/{no,no} IMPOSSIBLE-farmable, mixed = equivalence containment); (2) alias validator allowed two REAL events to fold into ONE synthetic event → shared game key → cross-netting in every cap/waiver (E2) → reverse-event injectivity clause. Plus: rotation-marker survival fix (trip/deleted-quote paths), config canonicalization (fee prefixes + schedule keys + loud install logs), heartbeat write throttle (10/s, heals external wipes), alias-consistency one-liners, drop-settled WARNING. RELAUNCH VERIFIED: `pricing_aliases_active` + `pregame_scheduled_starts_active` logged, pool warm workers=2, 10 positions rehydrated/0 mismatches, preflight green, 516 quotes in 5 min — **220 carrying a champion leg at fair+markup** (zero-markup bug dead). Operator residuals: fee-in-admission-EV policy + settlement fee_cost semantics BEFORE arming maker fees; analytic ME netting goes comonotone on the final (waiver = the relief). — [report](2026-07-16-adversarial-verify-round2-and-alias-relaunch.md) |
| 2026-07-16 | **ESPARG champion-leg pricing aliases + pregame schedule table (commits `8d8d96c` + `9526fbe`, built post-crash)** | **COMMITTED + pushed; hardened + live-verified by the round-2 report above** | The crash-interrupted alias wire, finished: config-driven exact-ticker aliases KXMENWORLDCUP-26-{AR,ES} → synthetic KXWCADVANCE-26JUL19ESPARG-{ARG,ESP} in the pricing layer only (classification, markup sport, game grouping, structural parsing, sgp resolvers, tripwire, waiver, book-risk conditioning — exchange identity keeps the real ticker everywhere); only-UNKNOWN validation + canonical form + injectivity both directions; installed engine-side AND in both worker pools. `9526fbe` adds `pregame_scheduled_starts` (exact event → tz-aware start): the champion market's 14:00Z expected_expiration anchor sits 5h BEFORE the ~19:00Z final kickoff, so the expiry-minus-offset estimate would have declared champion legs in-play from ~09:30Z on final day; armed entry = 2026-07-19T18:45Z. Armed yaml re-adds KXMENWORLDCUP to the allowlist. — details in the round-2 report |
| 2026-07-16 | **PC HARD-CRASH 20:28:08Z — state recovered; ESPARG champion-leg PRICING ALIAS half-wired (tree BROKEN)** | **RECOVERED — all commits pushed @ `ff250da`; 2 uncommitted files; bot + recorder DOWN; finish-wire owed** | The OS died mid-edit while wiring the operator-directed fix for the 7/19 final: Kalshi lists NO `KXWCADVANCE` for ESPARG — champion flow arrives on `KXMENWORLDCUP-26-{AR,ES}` (both tape-verified; 113,943 tape rows), which classified UNKNOWN + markup sport `'other'` → **whole combo quoted ZERO markup (observed live)** → series pulled from allowlist ~20:11Z (run `live_20260716_no_menwc.log`). Plan: config-driven exact-ticker pricing alias → synthetic `KXWCADVANCE-26JUL19ESPARG-{ARG,ESP}` (finals: wins-cup ≡ advance incl ET/pens; pricing layer ONLY — exchange identity keeps the real ticker; makes the leg STRUCTURAL so DC nets it against every other final leg). On disk: legtypes.py registry + classify hooks; **markup.py calls `resolve_pricing_alias` with NO import (NameError — crash hit between edits)**. Missing: config key + only-UNKNOWN validation, engine install, relationships event_ticker grouping (`:485` — champion leg won't join the final's game block), structural raw-ticker parse sites, sgp resolvers, tests, yaml. Also new: `ff250da` (F10 BookRiskPool 1→2 after the FIRST live waiver shot 19:50:30Z timed out on queue-wait). Purge stale heartbeat files before relaunch; KILL/needs_reconcile absent. — [report](2026-07-16-crash-recovery-esparg-champion-alias-state.md) |
| 2026-07-16 | **Heartbeat-wedge fix — reprice circuit breaker + mid-loop beats + non-blocking pool teardown** | **BUILT + TESTED (5 new tests; ruff/mypy clean); restart owed to activate** | The 18:13Z kill mechanism (frozen joint pool → 31×2s serial reprice awaits → heartbeat starved between ticks → supervisor killed a LIVE bot) made non-fatal: beat-per-iteration (progress ≠ wedge; a true event-loop wedge still can't beat), 2-consecutive-pool-deadline circuit breaker (rest of sweep defers 0.5s — no book liquidation), 2.5s sweep wall budget, and both pools drop the blocking teardown join (kill-job close reaps deterministically — ends the cosmetic kill+needs_reconcile on every halt, research kill-1). No individual quote decision changes. Batch-2 items (cooperative cancellation, event-driven reprice) make freezes RARE; this makes them SURVIVABLE. Built inline (subagent pool 529-down ×4); agent re-verify owed. — [report](2026-07-16-heartbeat-wedge-fix-reprice-circuit-breaker.md) |
| 2026-07-16 | **Five-item batch: fill-record RECOVERY SWEEP (P1 real-money bug) + candidate-gate deadline YAML + maker-fee eat-the-fee section + startup first snapshot + drop-settled rehydration** | **DONE on tree over `6d0f933` (52 new tests across 5 files; ruff/mypy at baseline); NOT committed; live effect needs restart** | (1) P1 proven-live bug (quote `527b5a3a…`, 117.07ct fill with NO `quote_executed_msg`): maintenance sweep polls REST GET quote after `fill_record_recovery_after_s` (10s, YAML-wired) for confirm-committed fills with no ledger row — `executed` replays the SAME `on_quote_executed`, `cancelled` lapses + removes the phantom position, errors bounded-retry then LOUD `fill_recovery.exhausted`; store-level INSERT-if-absent on `fill_ref` + `fill_replay_skipped` + brick-safe UNIQUE-index backstop make WS+poll races single-row. (2) `candidate_gate_deadline_s` now a real YAML knob ((0,3]; waiver+gate ≤ 3.0s model validator; `build_lifecycle_config` extraction proves pass-through bit-identical). (3) `FeeConfig.maker_fee_active_prefixes` — EAT THE FEE: prices untouched; matching maker fills book the real 0.0175 FeeModel fee into fills.fee_cc / realized P&L / net expected_edge / the waiver CANDIDATE entity (committed stay 0, dated TODO); empty default bit-identical; NOTES rows MF1–MF4. (4) One synchronous book-risk snapshot after rehydration (5s bound, reuses `recompute_book_risk_offloop`) kills the 69 `skip_portfolio_cvar` warmup declines. (5) Rehydration drops ONLY `finalized`/`settled` markets (logged), keeps closed/determined, keeps on ANY error — clears the stale $4.46 reserve at next restart. — [report](2026-07-16-five-item-batch-fill-recovery-gate-deadline-maker-fee-startup-snapshot-drop-settled.md) |
| 2026-07-16 | **Problem A — last-look state-consistent worst-case waiver BUILT + adversarially reviewed** | **DONE on tree (suite 2139/0, 3 serious findings fixed); NOT committed, NOT armed; restart decision owed** | The handoff §4A durable fix: on a game-loss/mutex-directional-only reservation denial, enumerate the exact DC scoreline grid (1586 states, no sampling) per breached game — committed+reservations+candidate net per state (reservations `earns_credit=False`), open quotes clamp max(0,loss), non-structural legs adversarial — and if certified `worst_case_cc ≤ the SAME game budget`, retry the reservation once with certificates (P0-2 atomicity on FULL book generation + reservation version). Confirm-path only (quote-time callers verified untouched); default OFF; every failure mode fails closed to today's decline. Built across the killed session's workflow + a continuation (forensics: the killed Wire agent's 4 files were complete; continuation added lifecycle/quote_app/28 tests). Adversarial verify (3 lenses): 15 findings, 3 serious CONFIRMED+FIXED (stale-certificate quote-churn blindness ×2 → full-generation stamp; reservation hedge-credit channel → credit clamp). ARMING CAVEATS: slate co-breach = dead waiver unless `slate_loss_frac > game_loss_frac`; set `candidate_gate_deadline_s 1.5` (1.0+2.0 = exactly the 3s window). — [report](2026-07-16-problem-a-lastlook-waiver-built.md) |
| 2026-07-16 | **RFQ probe — our two open ESP-ARG-legged fills vs the current maker field** | **DONE (probe RFQs created + deleted, nothing accepted); bot alive-verified** | Re-RFQ'd today's two fills ~40min later and read the full maker field (technique persisted at `tools/diagnostics/rfq_price_probe.py`). Fill 1 (FRAENG-BTTS × ESPARG-BTTS, our NO 64.00¢ × 39.87ct): 23 makers, best competing NO bid 63.90 — we filled 0.10¢ inside the best, at-market (thin-sharp-win pattern reconfirmed). Fill 2 (ESPARG corners9+Messi+ARG4tc, our NO 83.30¢ × 9.16ct, corners +3¢ floor ALREADY in this price): best competitor 80.70, median ≈77 — we're still 2.6¢ inside the best even with the floor (field prices corners richness higher; sharpens the #37 floor-size question). Bonus: our own bot saw both probes and DECLINED (skip_game_loss_cap util 0.94 concentrating on FRAENG/ESPARG; skip_max_open_quotes) — the risk engine confirms we're capped-out on these games. Context: sessions were accidentally killed 15:49 UTC mid-Problem-A wire; tree verified green (2101/0) and the Problem-A continuation workflow (`wf_a220ac69`) is running. — [report](2026-07-16-rfq-probe-open-positions-vs-field.md) |
| 2026-07-16 | **Fair-tiered markup (pad longshots / keep mains tight) + auction-loss analysis** | **SHIPPED + ARMED; recorder RESTARTED 15:27 UTC** | Today's 10 auction wins were ALL longshots at fair+1-1.2¢ (competitors pad fair+2-8¢ — we left 1-3¢/ct on every win); mains: 235 quotes / 0 wins but we're NOT overpriced there (−0.75¢ vs clearing median, cheapest maker 6/7 in the 7/14 probe) — mains fail on the conversion product (13% RFQ trade base rate × 63% dead-on-arrival × 20s TTL coverage × self-vetoed wins) on a T-2/3-days quiet day. Shipped `MarkupTier` fair-dependent markup (dark-parity safe): <2¢→+5¢, <10¢→+4¢, <35¢→+2¢, mains unchanged; corners +3¢ stacks. Shadow recorder back ON → same-instant competitor clears accumulate for the weekend's per-bucket us-vs-market table. RFQ probe staged, blocked on operator permission. — [report](2026-07-16-fair-tiered-markup-and-auction-analysis.md) |
| 2026-07-16 | **Problem-B supervisor --config fix + all-reserved CVaR usable fix — LIVE-VERIFIED, BOT QUOTING** | **DONE + LIVE (suite 2054/0, commits `1ed1921`+`acba2d8` pushed); bot RUNNING on fixed code, 268 quotes/2min burst, 2 auction wins** | Fix 1 (handoff §4B): supervisor subprocess launched with only `--env` → base-YAML 15s heartbeat killed the bot under firehose; `AppConfig.source_path` + `supervisor_launch_cmd()` now forward the armed config — live cmdline verified, 15 min @ 47.7 RFQ/s, 0 kills/0 WS drops. Fix 2 (the 0-quotes blocker): the only book position (7/14 MLB All-Star combo, gated series ⇒ P0-4 RESERVED) made the book non-empty while `usable` still required `n_positions>0` — the documented all-reserved snapshot ("still USABLE … not a no-go") graded unusable ⇒ 26,681/31k decisions blanket-declined `skip_portfolio_cvar`; property now accepts 0 positions + nonzero deterministic reserve (UNKNOWN/truly-empty stay closed), regressions at snapshot + limits levels both directions. Live: snapshot publishes ($4.46 reserve, tail 0), cvar declines 26,681→69 (startup warmup only). NEW finding: both auction wins SELF-DECLINED at last-look — ~60 resting sell-side quotes eat the $300 directional budget (E2 mass-acceptance) ⇒ DECISION OWED on directional_frac / max_open_quotes balance / Problem-A last-look MC. No conveyor blocks: max decision gap 1.5s, max quote gap 5.1s, post-burst quiet = oversized-RFQ demand mix (caps working), not a wedge. Advanced API tier: no code change needed (no client-side write throttle). — [report](2026-07-16-heartbeat-config-fix-and-cvar-usable-fix-live-verified.md) |
| 2026-07-16 | **QUARANTINE of the external-LLM continuation + restore to checkpoint `a0f8e0f`** | **DONE — tree clean at `a0f8e0f`, suite re-verified 2047/0; bot DOWN; open problems A–G unchanged** | The external LLM the operator handed off to made ZERO commits and left 67 files of uncommitted changes (+8,567/−1,122; new `rfq/issuance.py`/`rfq/retry.py`/`risk/structural_certificate.py`, a `ComboMaker-Live.exe` launcher, 4 reports; claimed 2,213/0 but nothing live-validated). All of it committed to quarantine branch **`llm-b-continuation` @ `16e34f7`** and pushed (also backs up the previously local-only `risk-audit-overnight` history). Working tree restored to the handoff checkpoint; gitignored armed config verified vs handoff §1 — one deviation reverted (`max_open_quotes` 80→60; LLM's version kept at `config/prod-live-wc.llm-b-bak.local.yaml`). Suite at checkpoint: **2047/0** (92s), matching the handoff stamp. Next = handoff §7 order (B heartbeat → C throughput → A last-look MC gate), plus adversarial triage of the quarantined branch for salvageable B/C work — never merged wholesale. — [report](2026-07-16-quarantine-external-llm-and-checkpoint-restore.md) |
| 2026-07-15 | **HANDOFF for external LLM risk-engine review (zero-bias, self-contained)** | **BOT DOWN (heartbeat kill); branch `risk-audit-overnight`@`0f5d6c8` NOT merged; suite 2047/0; restore `git reset --hard 45164f1`** | Single comprehensive handoff for an external reviewer — operator's verdict stated verbatim ("the risk engine is still insanely off"), not softened. Folds the full session arc (fanout 0→414 quotes → 10 P0 + 11 P1 + 2 P2 risk audit → codex second-LLM audit fixing 2 candidate-gate bugs at `0f5d6c8` → corners↔goals ρ measured = market-richness → pregame GOAL-offset → 2× heartbeat kill). Ranks 7 OPEN PROBLEMS A–G with file:line evidence: **(A) ME OVERSTATEMENT** — analytic per-game/directional caps net only the SINGLE advance ME event (`exposure.py:599-625`, `:684-696`) and comonotone-SUM every other ME dimension (total over/under, corner high/low, 1H, no-advance) + ENG-advance combos that CAN'T co-lose with the ARG book; durable fix = gate on the last-look MC worst-case (monotonicity-safe there, E2 invariant forbids a quote-time analytic tightening); **(B) HEARTBEAT KILL** — the 30s override never applied because the supervisor subprocess is launched with only `--env`, no `--config` (`quote_app.py:1238-1244`) → loads base `prod.yaml` 15.0s (`ops/config.py:122`); fix = pass `--config` and/or beat from an OS thread; **(C) THROUGHPUT** — prices ~1-2% of flow, 89,863 `rfq_closed_before_post` in one run (`lifecycle.py:1419-1426`); fix = cache per-game fit / shed earlier; **(D)** DB checkpoint lock, **(E)** fragile pregame offset map, **(F)** 1-game caps pin the book (corners +3c edge-floor + hedge-slots NOT deployed), **(G)** candidate gate not live-validated. Includes the full TOOLS + DIAGNOSTICS inventory, DB/venv/launch/KILL reproduction, report index + the two root audit `.txt`, and NEXT STEPS (B+C first, then ME last-look MC gate, then merge decision — do NOT merge until A+B re-validated live). — [report](2026-07-15-HANDOFF-for-llm-review.md) |
| 2026-07-15 | **P0-2 — candidate MC ATOMIC with reservations (audit `RISK_ENGINE_LIVE_BALANCING_VALIDATION_AUDIT.txt` §P0-2)** | **DONE + GREEN; suite 2040/0 (+9 tests); no cap loosened, gate stays strictly additive, no relaunch** | The confirm path built the candidate-MC inputs from the CURRENT reservations, AWAITED the worker, and only THEN reserved — so during the await a second accept evaluated against the SAME old pre-book and both could pass (no `reservation_version` in `CandidateBookRiskInputs`, no version/generation check on return). **Fix (preferred audit flow):** create a PROVISIONAL reservation for the candidate FIRST (analytic hard caps), so a concurrent accept's MC now sees its held headroom + version bump; stamp `input_generation` (ExposureBook position generation) **and** `reservation_version` (RiskReservationService version) into the MC inputs; run the MC over committed + all OTHER reservations + the provisional candidate (candidate excluded from PRE, no double-count); on return re-check BOTH signals and DISCARD+REBUILD+retry if either moved; retries BOUNDED by `candidate_gate_deadline_s` (2.0s) + `candidate_gate_max_retries` (3) so risk math never silently eats the confirm window; MC decline/error/timeout/insufficient-deadline/unstable ⇒ RELEASE the provisional + DECLINE (fail-closed). Confirm order now `fill-velocity → provisional reserve → candidate gate → confirm → commit`. STRICTLY ADDITIVE (only flips an admit to a decline); shadow/kill-switch/paper paths unchanged. New tests: two concurrent gates can't ignore each other, reservation add/release during MC ⇒ retry/reeval, combined over-budget admits only the safe subset, provisional released on decline/error/unstable, deadline fail-closed. New metrics `candidate_gate.{version_conflict_retry,retries_exhausted,deadline_exceeded}`. — [report](2026-07-15-p0-2-candidate-mc-atomic-with-reservations.md) |
| 2026-07-15 | **Corners ↔ goals ρ — MEASURED (measure-first, nothing live changed)** | **DONE — verdict: shipped 0.00 is RIGHT; #37 is market-richness → fix with a WIDTH, not a ρ** | Operator asked whether corners↔goals is genuinely ~0 at the traded lines (→ 3–5¢ #37 gap is richness, fix = defensive corners WIDTH) or materially positive (→ promote a measured ρ). Measured tetrachoric (= Gaussian-copula, the exact `pair_rho` scale; parity-gated vs live copula 8.1e-06) on n=8,981 club matches (5 top-EU leagues × 5 seasons). **Every traded pair ρ_tet ∈ [−0.040, +0.041]; raw Pearson(corners,goals) = −0.021; marquee corners≥9×goals≥3 = −0.038, CI [−0.07,−0.01].** 11/16 CIs straddle 0; the few that exclude 0 are ~−0.04 (marginally NEGATIVE, opposite the +0.35 a P&L-fit would demand) — decisively refutes the discarded reverse-engineered +0.35/+0.5. Per-league dispersion small and not consistently signed (Italy −0.11 … France +0.10), spanned by the shipped 0.08 band. **VERDICT: MARKET-RICHNESS not promote-ρ** — corners-over ⊥ goals-over is real, our fair is right, #37 3–5¢ is adverse-selection/richness → recommend a defensive corners-combo WIDTH/edge-floor (operator decision, gated on fills), NOT a correlation change. Caveat: measured CLUB because NO local WC corners data (intl_results.csv = goals-only; *eve.zip = Retrosheet MLB, no corners); club→WC transfer is an assumption, but ET/tournament reasoning + the 0.08 band cover it, 0.00 stays center. NOTHING live touched (tools/ + report only). — [report](2026-07-15-corners-goals-rho-measurement.md) |
| 2026-07-15 | **FULL SESSION CHANGELOG + LIVE STATUS (MASTER)** | **LIVE — new engine relaunched, heartbeat stable past 6 min** | Single-glance master: fanout fix (0→414 quotes) → full risk-audit impl (10 P0 + 11 P1 + 2 P2, 31 commits / +11.7k lines / suite 2026-0-0) → live-ready (P0-7 preferred conditioning, teardown warnings gone, P0-1 candidate gate WIRED additive+off-loop, kill-switch answered) → RELAUNCHED live: reconcile clean, P0-4 reserve working, structural portfolio MC usable (es/challenger/governing/det-max all distinct, p_ruin 0, p_profit 0.58), **survived the 6-min heartbeat mark that killed the prior run** (P2-2 off-loop MC). Branch `risk-audit-overnight`; restore `git reset --hard 45164f1`. — [report](2026-07-15-full-session-changelog-and-live-status.md) |
| 2026-07-15 | **Risk-engine audit — LIVE-READY FOLLOW-UP (P0-7 preferred conditioning + teardown-warning cleanup + P0-1 gate WIRED into confirm + kill-switch answer)** | **ALL 3 GREEN + committed; suite 2026/0 (+16); no cap loosened, no relaunch** | Appended a "Live-ready follow-up" section to the implementation report and landed three follow-ups on `risk-audit-overnight`: **P0-7-preferred** (`1552fb0`) upgrades P0-7 from the interim worse-tail *challenger* to the spec's PREFERRED *conditioned fallback* — a straddling copula leg's Gaussian latent is conditioned on the game's sampled scoreline intensity via a shared factor (`_shared_structural_factor` / `_sample_copula_conditioned` / `_copula_leg_loading`, `StructuralConfig.corners_et_loading=0.10`), nonzero ONLY for knockout total corners (measured ET channel `advance\|corners`), β=0 elsewhere; independent-split guard folded into the governing max so conditioning may only fatten the tail (SAFETY DEFAULT); full-copula bridge retained as backstop. **teardown-warnings** (`09d24ef`, TEST-ONLY `conftest.py`) hard-stops leaked `aiosqlite` worker threads at teardown → the `ProcessPoolExecutor`/`ResourceWarning`s from the earlier §8 stamp are gone (summary line now warning-free). **P0-1-wiring** (`36a5a47`) closes the §6/§7 "candidate gate BUILT but UNWIRED" gap: `evaluate_candidate_book_risk` now governs live confirms via `lifecycle._candidate_gate_verdict` (called inside `if decision.confirm:` ~L1320) as a STRICTLY-ADDITIVE, decline-only + positive-EV, off-loop (`BookRiskPool.run_candidate`), FAIL-CLOSED gate (`DECLINE_CANDIDATE_RISK`; UNKNOWN marginal / over-budget POST / any off-loop error ⇒ decline); `RiskConfig.candidate_gate_enabled=True` default, `: false` is its kill switch. **KILL-SWITCH ANSWER (task #44):** P2-2 does NOT need a new switch — the supervisor heartbeat kill (`ops/supervisor.py` `heartbeat_wedged`/`supervisor_emergency_kill`, 15s) ALREADY EXISTS as the genuine-wedge backstop; P2-2 removed the FALSE trigger by moving full-book MC off the loop so the maintenance loop (`quote_app.py` ~L1413) beats `heartbeat.txt` every 0.5s independent of the MC; a genuine future wedge = modest timeout bump / thread-beat, not a new switch. Earlier "UNWIRED" caveat corrected to "WIRED as of 36a5a47". NO relaunch, NO cap loosened; claims test-asserted (live re-measure still owed). — [report](2026-07-15-risk-engine-audit-implementation.md#live-ready-follow-up-p0-7-conditioning-teardown-p0-1-wiring-kill-switch-answer) |
| 2026-07-15 | **P0-7 upgraded: INTERIM worse-tail challenger → PREFERRED conditioned fallback** | **DONE + GREEN; suite 2017/0 (+7 tests); no cap loosened, no relaunch** | The structural split sampled a game's structural scoreline block and its copula-only corners/cards block from INDEPENDENT rng, discarding same-game structural↔copula dependence in the PRODUCTION sample (only the full-copula `bridge_es_99_cc` challenger saw it). Now, where a **defensible measured** scoreline-state link exists, the straddling copula leg's Gaussian latent is CONDITIONED on the game's sampled scoreline intensity via a shared factor (`z' = √(1−β²)·z_copula + β·f_game`, marginal + copula-block corr preserved exactly), so the covariance enters the production tail. Loading is deliberately **narrow**: nonzero (0.10, width-bearing) ONLY for a KNOCKOUT total-corners leg (the measured ET-window channel `advance\|corners` dog +0.23↔fav −0.23, pooled ~0); total corners are measured **⊥ goals** (`corners\|total=0.00`, folk-wisdom-busted), so group corners / team corners / cards get β=0 → independence + the worse-tail challenger backstop (per spec: "no defensible link ⇒ keep independence AND retain the challenger"). Worse-of gate: governing ES/ruin folds an **independent-split guard**, so conditioning may only fatten the tail, never thin it (SAFETY DEFAULT: only adds declines). Parity pinned (`_sample_copula_conditioned` == `engine.sample_leg_values` byte-for-byte at β=0); prototyped in `tools/proto_structural_copula_conditioning.py` first. Config-tunable via `StructuralConfig.corners_et_loading`. — [report](2026-07-15-p0-7-preferred-conditioned-fallback.md) |
| 2026-07-15 | **Risk-engine audit — IMPLEMENTATION of `RISK_ENGINE_AUDIT_ACTION_PLAN.txt` (all 10 P0 + 11 P1 + 2 P2 landed on `risk-audit-overnight`)** | **ALL GREEN + committed; suite 2010/0; NO relaunch, NO cap loosened** | Definitive implementation report over baseline `45164f1`. First run `wf_59dd860b-769` landed P0-5/4/6/3/2/1; this remainder run landed **P0-9** (mutex-aware directional cap, delta limits NOT raised), **P0-8** (same-game-only challenger correlation inflation via `_same_game_mask`; cross-game independence preserved), **P0-7** (structural↔copula same-game dependence **bridge** challenger, gate on the worse tail — interim per spec), **P2-2** (full-book MC **off the event loop**, generation-stamped `BookRiskInputs`, stale-generation snapshots discarded, seeded byte-identical parity), plus full **P1.1–P1.11** hardening (production+challenger P(ruin), ruin-budget confidence bounds + CRN, equity-basis no-double-count proof, structural fit-residual persistence + challenge, public structural API, tape-parity suite, mutex-metadata audit + settlement tripwires, `analytic_leg_deltas` labeled independence proxies, independent structural-param challengers, durable position ledger, exact RFQ/leg-set provenance) and **P2.1** (parent-owned process group / Windows Job Object, orphan cleanup) + **P2.2** (per quote/confirm structured risk logging). Preserved-fix invariants intact: no `fills JOIN rfqs` aggregate in `held_positions`, mutex-aware directional cap (not raw `delta_by_game`), ES-vs-deterministic split, fail-closed on UNKNOWN/missing/stale marginals, `caps_shadow_mode=False`. Verdict `{"verdict":"PASS"}`. **NOT tape-re-measured this run** (no relaunch per SAFETY DEFAULT) — §4 claims are test-asserted; operator owes: diff review, relaunch decision, supervisor heartbeat kill (#44), `max_open_quotes` (do NOT raise until post-fanout headroom measured live). — [report](2026-07-15-risk-engine-audit-implementation.md) |
| 2026-07-15 | **ROOT CAUSE + FIX: rfqs-tape fanout → 0 quotes (THE blocker from the final report, now solved)** | **FIXED + LIVE-VERIFIED (0 → 228+ quotes, steady); suite 1789/0** | The "still blocked by enforced mass-acceptance caps" from the final report was NOT a cap-tuning problem — it was a **SQL fan-out bug** in `Store.held_positions` (#33 rehydration). It fetched summed contracts and the combo legs in one query: `fills JOIN rfqs ON combo_ticker=market_ticker`. The `rfqs` **tape has 1.6M rows** (one per re-quote; same combo appears up to **12,456×**), so the JOIN fanned each fill out by that factor **before** `SUM(contracts_centi)` → contracts inflated up to 12,456× (real 37 → 464,235). That drove `analytic_leg_deltas` → the **−259,302** delta on the shared ARG-advance leg → every delta/loss cap blown → 0 quotes. `entry_price` was fanout-safe (num & denom scaled together) so P&L looked fine — only `contracts` (and the caps that scale with it) broke. **Fix:** pre-aggregate fills + de-dup rfqs legs into 1-row-per-combo derived tables (1:1 join) + `idx_rfqs_market_ticker`. Parity on live DB: 552,393 → 435 real ctr; ARG-adv delta 259,302 → 47.7 (all legs < 300 cap). Regression test added (old test recorded each RFQ once, fanout=1, couldn't catch it). Relaunched live: 6 ENGARG positions rehydrated w/ correct contracts, **228 quotes / 3.5min, both sides (ARG 130 / ENG 57)**. Remaining declines are the concentrated-ENGARG book behaving correctly (directional/game-loss caps) + known throughput. NEXT levers for the "balance the book" goal: directional-cap mutex-awareness, portfolio-CVaR usable-snapshot (fail-closes 268/268 on unknown marginal), max_open_quotes tuning. — [report](2026-07-15-rfq-tape-fanout-zero-quotes-fix.md) |
| 2026-07-15 | **Risk-engine upgrade — FINAL REPORT (built + tested + deployed; regression fixed; NOT productively quoting)** | **SUPERSEDED by the fanout fix above — root cause was a SQL fan-out, not enforced-cap tuning** | Zero-bias report for external review. Built the whole upgrade: interim caps 20%; **Stage B** mutex-aware per-game loss cap (single ME event, fail-closed-monotone — a failing dominance test proved general hedge-aware tightening is non-monotone); **#33** rehydrate positions on restart; **A1** structural portfolio MC (`sim/structural_book.py` — sample one Dixon-Coles scoreline/game, settle every leg, MC-joint==joint_probability parity for 12 leg types, advance mutex exact); **A2** P(ruin −30%) cap + structural seam in `compute_book_risk`. Deploy blocker: a real sub-cent settlement fee ($0.000080) — fixed (`fee_cc_from_dollars_str` ceil). LIVE: #33 rehydrated 7 positions, preflight green, 0 crashes — but 0 quotes. **REGRESSION I introduced + fixed (verified live 7186→38 classifier_unknown):** #33 loaded MLB positions (gated-off series, unsubscribed → None marginals) → exposure `unknown_marginals` on every check → declined all; fix = rehydrate only allow-listed series + a committed position's missing marginal no longer sets unknown_marginals (only a candidate fails closed). STILL blocked after fix by the ENFORCED mass-acceptance delta/notional caps (pre-existing layer) on the rehydrated ENGARG book → needs enforced-cap tuning + fresh slate. Correction logged: I wrongly guessed "game in-play" — operator said 20h away; I had not checked a source of truth. Bot cleanly stopped (KILL file; 0 orphans). — [report](2026-07-15-risk-engine-upgrade-final-report.md) |
| 2026-07-15 | **Risk-engine upgrade — structural portfolio MC + marginal-CVaR/ruin/EV gate (design + adversarial critique)** | **DESIGNED + critiqued; decisions owed before port** | Multi-agent research (wf_1c8bbeed-40e, 5 agents): drive the risk MC off the SAME Dixon-Coles joint that PRICES the legs (sample one scoreline/game, settle every leg exactly) → all same-game hedges free, NO ρ table (kills the block-scalar-ρ that can't represent ARG-adv⊥ENG-adv). Only corners/cards stay on ρ table (measure from co-settlements). Staged: **B** mutex-aware analytic cap (prototype validated, ~1.2× on our 3:1-skewed book — modest alone) → **#33** rehydration (MC "runs but never governs" until fixed) → **A1** structural MC (parity-gated joint_probability↔MC) → **A2** marginal-CVaR+EV+P(ruin) gate (welcome hedging fills, gate concentration). Adversarial critique caught 5 must-fix: (1) BLOCKER no `values` matrix persisted → cheap marginal not buildable + new-game candidates have no cached columns; (2) demoting analytic cap reopens mass-acceptance BURST hole (per-accept stateless gate) → keep co-equal; (3) fractional pens/goalscorer resample re-leaks the ME → shared shootout coin; (4) net-flat-gross-huge exploit → retain gross backstop as hard cap; (5) cards flat +0.6 in a tail model = quiet-failure prior → UNKNOWN/no-take. DECISIONS OWED: P(ruin) floor, interim cap raise (8→16%?), sequence sign-off. — [report](2026-07-15-risk-engine-structural-mc-research.md) |
| 2026-07-14 | **Fill blocker = COMONOTONE risk cap, not price (operator pushback → root cause)** | **DIAGNOSED; design owed** | Operator: "can't be sharp — barely filled ENGARG." Funnel: on ENGARG we WON 108 auctions (proves price-competitive) but DECLINED 103 at last-look — 79 recent = `decline_risk_limit`. Root cause: the per-game loss cap is a COMONOTONE SUM (`exposure.py:275` `worst_case_loss_by_game_cc` = Σ max_loss, "every combo on the game loses together") — impossible, since ARG-adv & ENG-adv are mutually exclusive. The MC portfolio-CVaR that WOULD net it is wired but DOMINATED (comonotone ≥ true tail AND 8% game < 15% CVaR → comonotone always bites first) and DORMANT in ph18 (0 snapshots — only sees committed positions; book ~empty post-restart #33). Cap fires on mass-acceptance of ~20 outstanding quotes (loss fluctuates $151–176 ≈ cap $147, not monotone). CORRECTION to operator: we ARE one-sided on ENGARG — 108 accepts split $515 ARG-adv / $163 ENG-adv (3:1) → hedge only cuts worst case 1.21× TODAY; comonotone $939 vs advance-aware $777 = 42–51% of $1,842 bank (real concentration → a cap must exist). Fix (needs sign-off, hard rule 8): make last-look MC-marginal-CVaR + EV the GOVERNING gate (welcomes hedging ENG-side fills, gates ARG concentration), demote comonotone to loose backstop or make it mutual-exclusion-aware, + fix #33 rehydration. — [report](2026-07-14-fill-blocker-is-comonotone-risk-cap-not-price.md) |
| 2026-07-14 | **Market-vs-our pricing on BIG REPEATED WC combos — "we're too high" is FALSE** | **DONE (900k-trade DB backtest; #35 disproved + repointed)** | Backtested our quotes vs where combos actually CLEARED (shadow tape `combo_trades`, 889,248 trades Jun29–Jul12, joined to leg-sets via `rfqs.market_ticker`; `taker_side='yes'` only). On **26 liquid main combos** (≥30 real trades): **median gap (our_ask − mkt_clear) = −0.75¢, mean −0.82¢; we're CHEAPER on 17, higher on 8** → we are NOT overpriced, we quote ~1¢ UNDER the mains. Kills the "WC fair too high / 20× low fill" theory (that was a LONGSHOT artifact — deep goalscorer parlays, de-prioritised). Bread-and-butter 2-leg advance combos within ±1¢ (well-calibrated). Real small mispricings: **CORNERS combos −3 to −5¢ (we underprice corners — adverse-selection risk)**, ESP-advance ~+1¢ high. Fill bottleneck is the AUCTION not price: **16 fills / 21,669 quotes**; deletions 55% `rfq_gone` (RFQ base rate ~87% never trade), 35% `ttl_expired` (23s TTL), latency p50 0.26s (9% tail >2s). CONVERGES with the live-probe report (two independent lines → we underprice mains ~1¢, room to widen). CAVEAT: our quotes (Jul13–15) postdate the tape (ends Jul12) — no same-instant overlap; drift bound shows mains were price-stable so ~1¢ gap is real, not drift. Markup change stays a POOLED-multi-week operator call (not applied). — [report](2026-07-14-market-vs-our-pricing-main-combos.md) |
| 2026-07-14 | **Price discovery — we're the SHARP maker; room to widen to ~2¢** | **DONE (API probe, 7 combos, no trades)** | Recreated all 7 filled combos as RFQs (create_rfq on existing combo ticker; get_quotes rfq_user_filter=self), read competing maker quotes, never accepted. We're the CHEAPEST maker on 6/7 (2nd on 1), winning by a thin 0.2–0.8¢. Our fair is calibrated — market's TIGHTEST makers price fair+1.5–2.2¢, we price fair+1.0¢. So we UNDERCHARGE ~0.5–1.2¢/ct: could raise markup 1¢→~2¢ and stay at/below the sharpest competitor. CONVERGES with the re-grade's ~2.2¢ floor (two evidence lines → ~2¢). Caveat: one pregame snapshot, combos-we-won selection bias, markup=pooled-multi-week operator call. Incident: get_rfqs(rfq_user_filter=self) does NOT filter (returned whole-exchange RFQs); a bulk-delete off it hit others' RFQs (rejected) + 1 bot 429 (recovered). Advanced tier still 403 (combo orders ≠ Predictions orders; need 1 single-market API order). — [report](2026-07-14-price-discovery-we-are-the-sharp-maker-room-to-widen.md) |
| 2026-07-14 | **FILL-KILLER: accept-size field wrong → 95% of won auctions lapsed** | **FIXED + LIVE-VERIFIED (2 big combos filled in 90s, 0 reconcile halt); suite 1736/0** | We were WINNING ~60 RFQ auctions/run and declining 95% at confirm (`DECLINE_SIZE_UNKNOWN`) — not too-slow/too-fast. `_accepted_qty` read `contracts_accepted_fp`, which is **null on every target-cost RFQ (95% of flow)**; code+test shared the invented field (classic quiet-failure). Root-caused via live logging + docs: real `quote_accepted` carries `contracts_accepted_fp` (contracts-mode) else the accepted side's `{no,yes}_contracts_offered_fp` (target-cost) + `rfq_target_cost_dollars`. Fix reads accepted-count → accepted-side offered-count → rfq.contracts. First 90s post-deploy: 2 `KXMVESPORTSMULTIGAMEEXTENDED` fills (37.27ct@74.5¢, 43.45ct@84.8¢), 0 size-unknown, reconcile gate silent (confirms offered=fill). BONUS: fixed `_await_supervisor_heartbeat` existence→freshness stale-file race (the "3-min block then spins back up" on full-tree restart). — [report](2026-07-14-fill-killer-accept-size-field-fixed-and-live-verified.md) |
| 2026-07-13 | **Flat maker model — quote = fair + 2¢ (drop mechanical width)** | **Config-only; live-confirmed fair+2¢** | Operator (via the live viewer) caught that the 2¢ markup never bound: `margin = max(width, markup)` let a mechanical base/per-leg/size width dominate → 2-leg ~2.4¢ but **5-leg ~5.3¢**, uncompetitive. Agreed (flat markup is the standard model + what the re-grade actually validated; the width was un-validated conservatism, and winner's-curse protection is the markup+FAT-selection not the width). Zeroed base/per_leg/size width in `pricing.quote`; KEPT the uncertainty term + longshot floor (widens only genuinely-hard combos). Live-confirmed: quotes now fair+2¢ (2.05–2.09¢). More competitive → should fill more; fills→settlement will test the flat model. — [report](2026-07-13-flat-markup-pricing-change.md) |
| 2026-07-13 | **WC+MLB live @ 2¢ + padding audit + data_stale halt fix** | **Config-only; live & stable** | Operator directive: all WC + all MLB @ 2¢ + pad the shaky families. Config: soccer 3¢→2¢, MLB on @2¢, allowlist `[KXWC,KXMLB]`. Padding audit (3-agent workflow): HRR/corners "padding" is **already in the FAIR** (measured ρ tables: corners\|corners_team ρ0.62, HRR\|total ρ0.40) + defensive width + the +2.5¢ DO-6 MLB basket buffer — **did NOT add a per-family markup knob** (would double-charge → worse fills, un-validated). Then fixed a false `data_stale` FATAL halt on a quiet pregame market (rx-age 5.92s > 5s tuned for a busy market): breaker 5→45s (halt only on a dead feed), freshness gate 5→12s (decline, stay up). One bot verified (PID tree). Shadow recorder found DOWN since 7/12. — [report](2026-07-13-wc-mlb-2c-restart-and-padding-audit.md) |
| 2026-07-13 | **Soccer FAT edge is SAME-GAME only — multi-game has none** | **VERIFIED (fair-independent) — bot is live-quoting the NO-EDGE pond; same-game gate recommended** | Split the settlement-graded soccer universe by game-membership (`04_samegame_split.py`). The +5.8pp "soccer FAT edge" is **entirely same-game**: same-game SGPs settle 10.6% vs implied 20.7% = **+10.1pp** [CI5 +6.7] REAL; multi-game exotics −1.2pp [CI5 −6.3] NO edge (markup sweep CI5 never clears 0). Retail overpays for correlated same-game narratives; cross-game legs priced efficiently. The live bot quotes almost all **multi-game** (`MULTIGAMEEXTENDED`) → wrong pond, and `collection_whitelist` can't separate them (both collections ~50/50) → needs a per-combo **game-count gate**. Same-game also ~2× more competitive (47% vs 26% in-the-money at 3¢) → likely a driver of live 0-fills. — [report](2026-07-13-samegame-vs-multigame-edge-split.md) |
| 2026-07-13 | **WS churn ROOT CAUSE + fix (0-quotes / 0-fills)** | **FIXED `2776013`, suite 1720/0 — awaiting fresh-run proof** | 0 resting quotes + `409 rfq_closed` storm traced to the WebSocket dying dead-regular every **~22s** (11 reconnects/5min in `live_wc.log`). Disease = `heartbeat=10.0` (`ddfcc2b`): aiohttp sends its own client Pings; Kalshi's response to unsolicited client pings is UNDOCUMENTED (bet stability on it, lost). Reverted to `heartbeat=None` + `receive_timeout=25s` — an active half-dead-peer probe keyed off Kalshi's DOCUMENTED 10s server ping, VERIFIED in aiohttp 3.14.1 source to re-arm on every frame (fires only on a genuinely silent peer → clean reconnect, not the 30s→data_stale→halt hang). 25s > 2×10s ping (no quiet-market false-trip) and < 30s breaker grace (reconnect wins). Stability fixed in principle + unit-tested; fresh live run is the proof. — [report](2026-07-13-ws-churn-root-cause-receive-timeout-fix.md) |
| 2026-07-13 | **WC-FAT FIRST LIVE RUN — hardening log + breaker grace pass** | **Pricing/edge/markup/quoting PROVEN LIVE; breaker live-hardening pass SHIPPED (`8e2b197`)** | Went live on prod WC-FAT (real quotes posted, +3¢ markup, validated edge). Surfaced + fixed 3 issues live: collection-whitelist gate (config), Windows heartbeat `os.replace` race (`2934fa4`), book-tripwire cross-quote false-kill (`a0b178b`). Then a 4th: `HALT_DATA_STALE` fatally killed the book on a **transient WS reconnect** (bot already cancels quotes on disconnect — the kill is redundant/over-aggressive). PATTERN: the Phase-6 fail-closed breakers, built+tested SHADOW/never-live, each nuke the whole book on ROUTINE live transients (file race / phantom RFQ / WS reconnect). Whack-a-mole patching under live fire = wrong approach. 0 fills (closing 2-game window). RECOMMENDATION: proper breaker live-hardening pass (graceful pause/resume, only kill on sustained failure) THEN relaunch; or bank + wait for pooled multi-week data. Suite 1716/0. — [report](2026-07-13-wc-live-run-hardening.md) |
| 2026-07-13 | **Maker MARKUP mechanism + WC-FAT go-live config** | **SHIPPED `0c4afc6`, suite 1710/0, adversarial-reviewed CLEAN — mechanism live, armed via local config (NOT launched yet)** | First maker markup in the pricer: `pricing/markup.py` MarkupPolicy (per-sport, DARK by default, toggleable, adaptive-ready) + `construct_quote` `margin=max(width,markup_cc)` (markup=0 BIT-IDENTICAL — parity-proven). `sport_of` FAIL-SAFE (mixed-sport⇒`other`⇒0, no cross-sport leak). Adversarial review: no bugs; caps still bind, monotone, fee-conservative, clean decline; fixed 1 latent foot-gun. TWO safety catches (operator-prompted source pass): (1) committed prod.yaml reverted to DISARMED (test-enforced) — arm ONLY via gitignored `*.local.yaml`; (2) live bot writes an ISOLATED DB (`observe.db_filename`), NEVER the read-only shadow `combomaker-prod.sqlite3`. Armed config `config/prod-live-wc.local.yaml`: WC-ONLY, soccer **3¢**, MLB off, caps enforced, static guard PASS. — [report](2026-07-13-markup-mechanism-wc-golive.md) |
| 2026-07-13 | **WC/MLB markup RE-GRADE — the edge is real (WC FAT)** | **DONE — settlement-graded on full resolution; WC FAT = real day-clustered edge; MLB confounded; soccer NORMAL adverse** | Re-graded the one-week prod shadow (2026-07-06→12, now 73%/79% resolved vs ~26% at snapshot) against REAL Kalshi settlements. **Reality test (fair-independent): Soccer FAT settles 13.8% vs priced 19.6% → +5.8pp overprice [day-clustered CI5 +4.2] = REAL SELLER EDGE** (retail overpays for longshot parlays). Soccer NORMAL adverse (settles 19.7% vs priced 16.3%, CI excl 0). MLB FAT/NORMAL negative-but-confounded by the independence-STUB fair (MLB-FAT our-fair 21% vs actual 35%) — a re-price + more-weeks item, not broken. Markup sweep (WC FAT, day-clustered): +EV from 2.2¢, +4.9¢/ct@3¢ [CI5 +2.1], YES-hit FALLS as markup rises (self-selection confirmed). CAVEATS: stub fair overstates room (magnitude indicative; reality test robust); ONE week/6-8 match-days = first sample, markup DECISION stays pooled-multi-week. Harness persisted `tools/backtests/wc_mlb_regrade/`. — [report](2026-07-13-wc-mlb-markup-regrade.md) |
| 2026-07-13 | **get_quotes 500/504 — root cause + fix** | **SHIPPED (merged to `main`)** | Unbounded `GET /communications/quotes?user_filter=self` scans full quote history → trips midland circuit-breaker (500→504/503 `fail-fast`). Fix: `exchange/quote_query.list_open_quotes` = 7-day window + cursor pagination + 5xx backoff-retry + fail-closed, used by BOTH startup reconcile AND the supervisor kill-path (an unbounded enum would 500 exactly when yanking every quote off the book). `KalshiSupervisorExchange` takes a `Clock`. Demo A/B deliberately skipped (operator call: demo≠edge, midland-demo degraded, fix is env-agnostic + strictly-better; prior within-session A/B already isolated query-shape as the cause). Suite 1699/0; mypy/ruff clean; NOTES hard-rule-4 row added. — [fix](2026-07-13-getquotes-500-504-root-cause-fix.md) |
| 2026-07-13 | **GO-LIVE RUNBOOK — tiny, validation-first ($2k)** | **Operator runbook — every command/flag/config verified vs code `463a147`** | Phase 0 creds (bot + SEPARATE `KALSHI_SUPERVISOR_*` key) + cap sign-off → Phase 1 DEMO convention+settlement dry-run (`ground-truth` + let a demo fill SETTLE through the reconcile-HALT — never run live) → Phase 2 demo quote-mode shakedown → Phase 3 arm prod tiny (deposit $2k, `prod_limits_configured:true` + `mode:quote` + non-empty whitelist, `combomaker run --env prod --mode quote --confirm-live`; static guard + 5-gate preflight) → Phase 4 validation-under-fire (KILL on any HALT_RECONCILIATION_MISMATCH, keep tiny) → Phase 5 pooled-multi-week markup = the real profitability gate. Skew/widen/schedule stay OFF by design. Kill: `touch data/KILL` / `combomaker halt`. — [runbook](2026-07-13-go-live-runbook.md) |
| 2026-07-13 | **RISK ENGINE — WIRED LIVE (settlement ledger + enforce caps + MC + fill-velocity + breakers + supervisor) + RE-AUDIT** | **MERGED `3aabae5`, suite 1693/0 — code-complete + ENFORCED-when-armed; re-audit 41 ENFORCED / 9 SHADOW / 6 BUILT-NOT-CALLED (was 23/24/14). Still NOT trade-ready (edge unvalidated + settlement path test-only + off-by-default).** | 3 sequential stages + judge (caught a fractional-scalar reconcile false-HALT, settled-position pruning, trade-fee gap — all fixed) + re-judge PASS. In `--mode quote/paper` caps_shadow_mode defaults FALSE ⇒ %-caps + give-back KILL enforce; SettlementPoller books apply_settlement+record_realized_pnl and reconciles predicted-vs-exchange to the cent → HALT on ≥1¢; MC snapshot uses the pricer's REAL rho feeding the CVaR cap; fill-velocity live; 3 breakers fed; supervisor launched+preflight-checked. REMAINING (not code gaps): default `mode:observe` = zero enforcement (arm with `--mode quote`); settlement/reconcile-HALT proven in TESTS only (never a real Kalshi settlement); supervisor needs `KALSHI_SUPERVISOR_*` cred + separate host; CVaR is pre-trade only; skew/widen/schedule intentionally dark per the record (enable only after the pooled markout study / a verified feed); prod gated by design; **the MARKUP/edge is unvalidated**. — [re-audit](2026-07-13-risk-engine-reaudit-after-wiring.md) |
| 2026-07-13 | **wire-live — adversarial-judge FIXES on settlement/reconcile** | **FIXED on `wire-live` worktree, suite 1693/0, mypy/ruff clean (not merged)** | Closes the 3 non-info judge findings on the Stage A wiring. **HIGH:** to-the-cent reconcile spuriously HALTed a legitimate **fractional-contract SCALAR** settlement (0.90 ct × $0.57 = 51.30¢ = 5130 cc can't equal integer-cent revenue 51¢/52¢) → now reconciles to the **whole-cent grid** (`abs(pred−rev) ≥ 1¢` HALTs; binary/whole-contract stay EXACT; real mismatch still ≥1¢ → defense #3 intact), mirrored in `FakeLifecycle`. **MEDIUM:** settled positions never left `ExposureBook` (inflated the ENFORCED caps forever + a re-quote re-summed the old position → false HALT) → NEW `ExposureBook.remove_position`, `SettlementHandler` prunes each position after `apply_settlement` succeeds (settlements built up-front so the by-weight fee split stays exact). **LOW:** the trade fee at fill never entered `realized_pnl_cc` (the ENFORCED daily-loss cap under-counted costs) → fed as a negative delta at fill ($0 today; `None` fee never booked as 0, defense #2). **INFO:** cold-start no-quote window is intended fail-closed (no code change, documented). Regression test per fix; no existing test weakened. — [report](2026-07-13-wire-live-settlement-reconcile-judge-fixes.md) |
| 2026-07-13 | **Stage A — SETTLEMENT LEDGER + RECONCILE + RESTART SAFETY (wire-live)** | **WIRED on `wire-live` worktree, suite 1644/0 (+48), mypy/ruff clean — closes the audit's biggest BUILT-NOT-CALLED gaps** | Makes the DEAD realized-P&L ledger + exchange-first settlement reconciliation ACTIVE (audit §3: `apply_settlement`/`record_realized_pnl`/`reconcile_combo_settlement` had ZERO callers → realized P&L 永 0, no fill reconciled to the cent). NEW `risk/settlement.py`: a `SettlementPoller` (async loop, sibling of `_balance_loop`) pages `GET /portfolio/settlements` (NEW `rest.get_settlements`) and books each settled position we HOLD — constructs a `Settlement` from the REAL fill + exchange V/revenue/fee, calls `apply_settlement` (realized P&L + fee, NO pays `contracts·(1−V)`), feeds `record_realized_pnl` (the ENFORCED daily-loss cap now sees realized P&L), and RECONCILES predicted gross credit vs exchange `revenue` TO THE CENT → `HALT_RECONCILIATION_MISMATCH` on any mismatch (defense #3), idempotent per position, fail-closed on UNKNOWN value/convention (never books 0). `reconcile_combo_settlement` extended farmed-tripwire→FULL path. `on_quote_executed` books the REAL fill fee (`pricing/fees.py`, $0 combo-maker, never `None`-as-guess). `reservation.reconcile` now gets the exchange's ACTUAL open positions (`GET /portfolio/positions`→`{ticker:Side}`→backed reservation ids) from a periodic loop + startup (was `set()`) so a confirm-timeout reservation is resolved not leaked. RESTART SAFETY: an in-process HARD trip (hard-trip/reconcile-mismatch/fill-velocity/drawdown/all breakers) drops the `needs_reconcile` marker (`on_halt` callback) so a bare restart is blocked; soft/manual halts don't. Demo/paper NOT bricked (no positions ⇒ pure no-op). Prod stays gated by the existing `--confirm-live`+limits+whitelist. — [report](2026-07-13-settlement-ledger-reconcile-restart-safety.md) |
| 2026-07-13 | **RISK ENGINE — code-grounded AUDIT (phases 1-6) + go-live verdict** | **VERDICT: NOT go-live ready — new risk engine is shadow/dark/unwired; realized-P&L ledger + settlement reconcile are DEAD (0 live callers)** | 6 parallel auditors read the ACTUAL merged code (23 ENFORCED / 24 SHADOW / 14 BUILT-NOT-CALLED) + my own grep-verification. What gates a quote TODAY = the pre-existing hard-dollar caps ($1k/game, $5k gross, $500 daily) + pregame gate + prod guard. The whole R2 %-of-bankroll layer is `caps_shadow_mode=True` (dropped in `_partition_breaches`); skew/widen DARK (applied 0). BUILT-NOT-WIRED: `apply_settlement`/`record_realized_pnl` have ZERO src callers (realized P&L永 0, no fill reconciles to the cent — violates defense #3), portfolio-CVaR cap gets no `book_risk=` snapshot, fill-velocity has no compute, `reconcile` only ever gets `set()` (timeout reservations leak), the external supervisor is a CLI the bot never launches, 3/7 breakers fed empty inputs, in-process trips drop no reconcile marker. Discrepancies: 'LIVE-VERIFIED settlement' only hit in tests; MC runs on flat default rhos live (not pricer parity); 'last line of defense' never running. — [audit](2026-07-13-risk-engine-code-audit-go-live.md) |
| 2026-07-13 | **RISK ENGINE — end-to-end RUNDOWN (how it works) + real-tape test** | **CAPSTONE — all 6 phases merged `e12f4f2`, suite 1596/0, everything SHADOW/DARK/NOT-LIVE** | The "how it will work" walkthrough: the full pipeline (RFQ→filters/pregame→pricing+skew→risk gate→reserve→last-look→confirm→book→settlement ledger) with each phase's layer annotated; the core ideas (two money axes, the GAME as the risk unit, the 9-cap hierarchy, reservation-before-confirm, portfolio-MC+challenger parity-gated, the skew sign, the kill path); a **REAL-TAPE TEST** — 6,000 real recorded combos → 191 games via the live `game_key`, one World Cup match (26JUL14FRAESP) carrying **2,650 combos**; the shadow caps flag 65/191 games on the 8%/game cap + the whole-book $86,535 notional vs the $6,000 utilization backstop (the sweep's concentration, demonstrated on real flow, caps catching it); an ON-vs-OFF status table; and the go-live runway with the 5 operator sign-offs owed. — [rundown](2026-07-13-risk-engine-rundown.md) |
| 2026-07-13 | **Risk PHASE 6 (FINAL) — external watchdog + circuit breakers + prod go-live gates** | **MERGED to `main` `e12f4f2`, pushed, suite 1596/0 (+81), mypy/ruff clean — judge caught 5 (2 kill-path) + FIXED; everything NOT-LIVE** | **⚠️ Judge caught kill-path bugs (all fixed):** cold-start self-halt (staleness breaker tripped during WS warmup → permanent halt before first quote; fixed with a feed-warmth latch); KILL file not honored SYNCHRONOUSLY at restart (a revived bot could quote before the 1s async watcher; fixed with a sync KILL gate + don't-clear-marker-while-KILL); latency breaker used the all-time histogram max (one spike latched forever; fixed with a rolling window); seq-gap mis-wired to traffic-silence (fixed with a real gap event); emergency cancel-all didn't paginate, then the fix hardcoded `limit=1000` while `/communications/quotes` caps at **500** (a 400 would enumerate NOTHING — orchestrator fixed to 500 + paginated the bot's OWN startup reconcile too). The last line of defense. **EXTERNAL SUPERVISOR** (`ops/supervisor.py`, standalone `python -m combomaker.ops.supervisor`): a SEPARATE process that watches the bot's `heartbeat.txt` (wall-time beat written each maintenance tick, fail-closed on unreadable/future/tamper) and on a missed beat EMERGENCY CANCEL-ALLs via its OWN credential + writes KILL + drops the `needs_reconcile` marker. **FAIL-CLOSED: exchange unreachable ⇒ STILL writes KILL + marker + alarms** (a supervisor that can't cancel must stop the bot resuming). **RESERVED WRITE BUDGET** (token bucket, 200/10s) so it acts under a 429 storm without draining the bot's pool (tested: 100-quote book + 5-token budget ⇒ exactly 5 cancelled, `budget_exhausted`, KILL still written). **CREDENTIAL ROTATE** = distinct env-only `KALSHI_SUPERVISOR_*` (fail-closed default: no cred ⇒ KILL-only). **BLOCK-RESTART-UNTIL-RECONCILED** = the marker + KILL survive restart; startup reconciles exchange-first (reuses Phase-3 `reservation.reconcile`) and clears the marker ONLY on success (unreachable ⇒ refuse to quote). **KILL-DRILL PASSES**: dying bot ⇒ `run()` loop cancels all + KILL + marker, deterministic (fake clock/exchange). **7 CIRCUIT BREAKERS** (`risk/breakers.py`, pure fail-closed detectors → `KillSwitch.halt`): data-stale/seq-gap `HALT_DATA_STALE` (>5s / any gap; None rx-age ⇒ trip), latency `HALT_LATENCY_SPIKE` (>2000ms), 429-burst `HALT_RATE_LIMIT_BURST` (≥10/10s), marginal-jump `HALT_MARGINAL_JUMP` (>0.25; lost-current ⇒ trip), rule/metadata `HALT_METADATA_CHANGE` (reuses tripwire), unmapped-game `HALT_UNMAPPED_GAME` (None key ⇒ trip, escapes cluster caps), + `HALT_BREAKER_ERROR` (a detector that RAISES fails closed to a halt). Each fires AT-threshold not just-under (tested); wired into the status loop off the hot path. **PROD GO-LIVE GATES**: STATIC (`assert_safe_to_run`) adds the non-empty leg-series WHITELIST gate to the existing `--confirm-live` + `prod_limits_configured` (empty/null refused; `prod_require_series_whitelist` override); RUNTIME PREFLIGHT (`ops/preflight.py`) requires ALL green before the first quote — limits + whitelist + supervisor-heartbeat-established + external-kill-reachable + book-reconciled — else `PreflightError` refuses to quote (CLI exit 3). Fail-closed defaults; demo unaffected. DEFERRED: richer breaker snapshot from the risk path, the production `KalshiSupervisorExchange` behind the dedicated credential + a demo kill-drill, separate-host deploy. — [report](2026-07-13-risk-phase6-watchdog-gates.md) |
| 2026-07-13 | **Risk PHASE 5 — quoting policy: skew (DARK) + widen-vs-DECLINE + pregame precision** | **MERGED to `main` `7601146`, pushed, suite 1515/0 (+53), mypy/ruff clean — judge caught + FIXED a CRITICAL sign inversion; everything SHADOW/DARK** | **⚠️ The judge caught a CRITICAL bug: the inventory-skew SIGN was INVERTED** (root cause: an error in `R3_hedging_pregame.md §A0`, now banner-corrected). As first built, a CONCENTRATING candidate returned positive skew → RAISES `no_bid` → LOWERS the taker's YES cost → we'd sell MORE of what we're loaded on (book concentrates). FIXED: classifier keeps intuitive convention (concentrating `skew_cc≥0`) and NEGATES at the pricer boundary (`applied_cc=−skew_cc`), + `shadow_applied_cc` so the enable-authorizing markout study sees the right sign; verified against the REAL `construct_quote` (`TestPricerBoundarySign`: concentrating ⇒ strictly HIGHER YES ask ⇒ sell less) + a re-judge mutation test. Also fixed: a vacuous no-arb test (now stresses the clamp with positive skew) + the `M_c≥M_q` per-prefix validator (now resolves via the runtime `_prefix_lookup`, not exact-key, so overlapping prefixes can't invert the fail-closed invariant). Feeds the built-but-zeroed inventory-skew seam via NEW pure `risk/skew.py` (`compute_inventory_skew`, sibling of `lastlook`). **THE SIGN** (load-bearing, R3 §A0): sell-only NO book ⇒ positive skew RAISES `no_bid` = more expensive = sell LESS ⇒ CONCENTRATING; negative = cheaper NO = win MORE ⇒ OFFSETTING; empty-book game ⇒ exactly 0. `skew = Σ conc·d_e·f(util) − Σ off·min(d_e,\|net\|)·util`, convex `f(u)=u^γ` ramping as per-GAME headroom shrinks, clamped `[−150,+600]`; offset rebate **doubly contained** (cap + the `construct_quote` free-money clamp). Drives off `delta_by_game`/`worst_case_loss_by_game`/`gross_notional_by_game`; optional `GameSkewCache` overrides per-game direction from Phase-4 ΔES (never invents correlation). **DARK**: `enabled=False` ⇒ computed+LOGGED, passed as **0** — a dark regression test asserts the quote is **bit-identical** wired-disabled vs not-wired. **3 mandatory property tests**: SIGN SAFETY (offset≤0/conc≥0/empty=0), SELL-ONLY fuzz extended to draw skew from the fn's range (`yes_bid` still 0), NO-ARB survives a deep negative skew (clamp fires, capture holds). **Widen-vs-DECLINE** (R3 R2, SHADOW): per-GAME — decline `SKIP_WIDEN_AVOIDED` when a touched game is BOTH concentrating AND near-cap (attracting hitters), never on offsetting flow; log-only until enabled. **Pregame precision ladder** (R3 B): NEW `ScheduleCache` SEAM (explicit `event_ticker→UTC` table, INACTIVE, fail-closed miss, tz-aware insert guard) inserted embedded-ET→schedule→estimate; `M_q` quote-cutoff / stricter `M_c≥M_q` confirm-cutoff (validator-enforced), applied only to PRECISE starts (estimate keeps its 4.5h padding), M_c at last look via `pregame_confirm_status` (pure fn, no I/O); `time_to_start` logged on every pregame decline (flow-loss datum). CONSERVATIVE defaults (0 margins, empty cache) ⇒ today's behaviour. DEFERRED: enabling skew (shadow-markout gate), the schedule-feed source + hard-rule-5 verify, M_q/M_c tightening (pooled markout study, `tools/`), payout-axis caps A4. — [report](2026-07-13-risk-phase5-quoting-policy.md) |
| 2026-07-13 | **Risk PHASE 4 — portfolio MC + challenger overlay (SHADOW)** | **MERGED to `main` `a64b8c9`, pushed, suite 1462/0 (+35), mypy/ruff clean — judge PASS, ZERO findings** | Feeds the EXISTING `sim/engine.py` portfolio MC the REAL book + REAL pricing correlation, retiring the `ops/report._portfolio_mc` **independence bug** (np.eye + complement pseudo-legs that were blind to the correlated joint tail that ruptures a NO-seller). **NO-side fix** (pristine ~2-line port): `ComboPosition.leg_sides` — a NO-selected leg contributes `1−value` to the payout product (the copula latent sign-flip on the sampled value), keeping its within-game correlation; `leg_sides=None` default = **byte-identical** old behaviour (regression-tested). **`sim/book_model.py`** (NEW pure): block-diagonal-by-GAME global corr (point/low/high) built the pricer's OWN way (`copula.build_block_corr` + `game_key`), within-game rho INJECTED (never hardcoded), missing marginal ⇒ `unknown=True` (fail-closed, vs the old silent 0.5). **PARITY GATE PASSES** (mandatory, hard rule 8): a 1-combo book reproduces the copula joint to MC tolerance, YES and NO. **`sim/book_risk.py`** (NEW): full MC → EV±stderr, VaR/CVaR₀.₉₉ at the high band, P(ruin) at 10/25/60% bankroll, per-game + per-leg tail attribution (Σ per-game = CVaR), + **challenger/stress overlay** `operative_es_99 = max(copula ES, correlation-inflated challenger ES, exact deterministic all-hit stress)` — anti-monoculture upper bound, tested `operative ≥ base`; deterministic via spawned RNG substreams (M2 §4.3). **SHADOW CVaR cap** `SKIP_PORTFOLIO_CVAR` reads the latest snapshot's operative ES vs `portfolio_cvar_frac`(0.15)×bankroll, NEVER re-runs MC in `check()`, fail-closed on unusable snapshot; fully shadow-safe (no live caller passes `book_risk=` yet). DEFERRED to P5+: ΔCVaR→inventory-skew, maintenance-tick snapshot loop, Glasserman-Li importance sampling. — [report](2026-07-13-risk-phase4-portfolio-mc-challenger.md) |
| 2026-07-13 | **Risk PHASE 3 — single-writer risk-reservation service** | **MERGED to `main` `72ba72b`, pushed, suite 1427/0 (+21), mypy/ruff clean — judge PASS (1 info finding, no action)** | The concurrency & state-safety P0: new `RiskReservationService` (`risk/reservation.py`) reserves headroom **BEFORE** the confirm round-trip (atomic + versioned) so two RFQs can never both claim the same room — re-runs `LimitChecker.check` against committed + ALL outstanding reservations + this candidate in ONE sync critical section; on PASS records + bumps a version, on FAIL returns the ENFORCED breaches (no record). `commit`/`release`/`mark_unconfirmed`/`reconcile` lifecycle, all **idempotent** + version-stamped. **Confirm-TIMEOUT = assume-committed**: `mark_unconfirmed` HOLDS the headroom (never released on a lost ack) + flags for `reconcile(exchange_ids)` (exchange-first truth: commit landed, release rest — doubles as startup pass). Wired into `lifecycle.py` confirm path (`_reserve_headroom` before `confirm_quote`; commit on success, mark_unconfirmed on exception, commit on execution) + `quote_app.py`. **SHADOW-safe**: shares the lifecycle's shadow split, so a %-cap breach never denies while `caps_shadow_mode` True — the 29 existing lifecycle/shadow tests pass verbatim, behaviour unchanged until caps are enforced. Reuses the exposure book + limit checker verbatim (no reimplementation, hard rule 8); no locks (one asyncio loop, sync sections). DEFERRED: automatic reconcile loop (needs Phase-6 exchange position-id map), fill-velocity enforcement. — [report](2026-07-13-risk-phase3-reservation-service.md) |
| 2026-07-13 | **Risk PHASE 2 — cap hierarchy + slate cap (SHADOW)** | **MERGED to `main` `632cc1f`, pushed, suite 1406/0 (+51), mypy/ruff clean — 2× judge PASS** | ADDITIVE **SHADOW** %-of-bankroll layer alongside the UNCHANGED enforced hard-dollar caps: `Breach.shadow` split in `lifecycle._partition_breaches` (shadow=LOG-ONLY, dropped before any block/decline/halt — proven behaviourally through the real hot path). $2,000 START values, integer-exact `thr=frac.num·bankroll//frac.den` (Fractions, no float money): **game 8%** / **per-combo 1%** (LOSS axis, name fixed from mis-labeled "notional") / **directional 10%** / **slate 8%** ★NEW / **daily 6%** / **drawdown 10%** / **hard-trip 12%** / **utilization 3×** (the ONLY new cap on the NOTIONAL axis — two axes never summed, proven). **SLATE cap** = Σ game-loss over one **US/Eastern calendar-day** bucket (source = `PregameGate.leg_start_time`); UNKNOWN start ⇒ pooled capped `"UNKNOWN"` bucket (fail-closed). **Fail-closed**: no/≤0 bankroll ⇒ single `SKIP_BANKROLL_UNAVAILABLE` (shadow in P2). **StarvationWatchdog** warns on N consecutive would-be declines (observes shadow), resets on a clean issue. Bankroll from `BalanceTracker.risk_bankroll_cc` (polled in a new `_balance_loop`). Base judge PASS (no enforced cap downgraded, integer-money/two-axes/fail-closed/slate-tz all clean); orchestrator follow-up ARMED the give-back halts (intraday **peak-equity latch** + `maintenance_tick` escalates enforced drawdown/hard-trip to killswitch — all 9 caps now observable in shadow) + **config validation** (frac ∈ (0,1], non-finite guarded); follow-up judge PASS. DEFERRED (Phase-3+): fill-velocity enforcement, hard-trip KILL-file latch. Enforce-time notes for operator in the report — [report](2026-07-13-risk-phase2-caps-and-slate.md) |
| 2026-07-12 | **Risk PHASE 1 — four correctness fixes + 2 judge defects** | **MERGED to `main` `6b5c76f`, pushed, suite 1355/0 (+30), mypy/ruff clean** | Judge PASS + 2 LATENT defects FIXED before merge: **D1** float-floor `int(0.57*10000)=5699` → spurious 1cc under-floor that false-trips `HALT_RECONCILIATION_MISMATCH` (fix `round()`, exact 4300cc); **D2** poison-pill idempotency (`_settled_ids.add` before the raising convention-gate → a raised-then-verified NO settlement silently dropped; fix: mark only after all guards). Regression tests for both. **FIX1** equity-aware denominator: `get_balance` now parses `balance`+`portfolio_value` (CENTS→cc ×100), `risk_bankroll_cc = min(SOD equity, cash + 0.5·pv)` so deployed≠lost can't shrink caps + a mark gain can't inflate them (haircut 0.5 + UTC day-boundary FLAGGED for operator; stale⇒all accessors fail closed). **FIX2** fees booked in the ledger — `realized −= fee_cc`, PROVEN $0 for our combo maker fill via the REAL `pricing/fees.py` (QUADRATIC+maker→0), 175cc on a synthetic taker series. **FIX3** scalar settlement: `Settlement.settled_value V∈[0,1]` (actual scalar retained, `settled_yes` derived helper, `.binary()` for the old path), NO pays `contracts×(1−V)` rounded-down; table V∈{0,.5,.7,1}→{+.50,0,−.20,−.50} — pricing fair UNTOUCHED (reactive stance). **FIX4** `payout_obligation`→`gross_settlement_notional` across risk/ (grep-clean; NO cash/loss cap consumes it) — [report](2026-07-12-risk-phase1-four-correctness-fixes.md) |
| 2026-07-12 | **Risk FOUNDATION — B1 + B2 + BalanceTracker** | **SHIPPED on branch `risk-foundation`, suite 1325/0, mypy/ruff clean** | **B1** side-aware axes: `max_loss_cc`=premium (TRUE max loss on the NO we hold; demo 1 ct @ $0.50 → $0.50, E3/E9 PROMOTED to VERIFIED) + NEW `payout_obligation_cc`=contracts×$1 (demo → $1.00), two axes NEVER summed. **B2** `pricing/grouping.py` public `game_key` (parity-pinned to `relationships._game_key`, single defn) + exposure now aggregates per GAME not raw event → KXWCGAME + KXWCTOTAL of one match fold into ONE cluster (proven); mass-acceptance E2 dominance PRESERVED. **BalanceTracker** (`risk/balance.py`): live `get_balance`→`bankroll_cc` (stale⇒fail-closed via clock) + realized-P&L ledger (NO-miss credits +$1/ct−premium, NO-hit debits premium, idempotent, NO-credit gated on `combo_no_pays_complement`), fake-clock/fake-REST tested — [report](2026-07-12-risk-foundation-b1-b2-balance.md) |
| 2026-07-12 | **Risk engine + Monte Carlo RESEARCH** | **DONE — design docs, ~80% already built** | 5-agent fleet (R1 book-model / R2 caps-killswitch / R3 hedging+pregame / M1 MC-portfolio / M2 MC-methodology) grounded in existing code → `docs/research/`. Headline: infra ~80% built, gaps are 3 BUGS + unwired seams: **B1** `max_loss_cc`=premium not payout (every cap 6-11× too loose for a NO-seller), **B2** aggregates on event not GAME (per-game correlation invisible = the concentration), **B3** risk MC uses np.eye+pseudo-legs (risk view ≠ book sold). MC engine already portfolio-aware; skew seam built-but-fed-0; last-look straddle re-check exists. Cap table w/ %-bankroll thresholds; MC needs game-keyed block sampling + Glasserman-Li importance sampling for the ruin tail — [synthesis](../research/README.md) |
| 2026-07-12 | **P&L markup sweep + verification + 3-lens response** | DONE — two-tier, one-window thermometer | NORMAL flow loses all markups (adverse selection); FAT flow wants FAT markup (soccer 5-6¢, MLB 2-3¢); durable = adverse-selection ASYMMETRY; caveat: much volume UNRESOLVED, $ directional only; FAT≈NORMAL both resolve NO ~75% (FAT just more overpriced); response = gate-to-FAT + room-predictor + exposure caps + freeze/pool before any number — [report](2026-07-12-pnl-markup-sweep.md) |
| 2026-07-12 | **Zero-gaps WIRED + go-live scorecard** | **MERGED `e5d34e5`, suite 1294/0 — measurement mandate MET; honest verdict inside** | every hand-prior/fallback pair now measured/exact (26 soccer incl. advance\|corners curve + sign-flip corners_team\|player_goal + wrong-sign draw cells; 84 MLB cells); verbatim-clean, judge all-5-SURVIVE, **bit-exact differential PASS** (untouched combos identical, movers=wired keys, zero unexplained); MLB at bar (0.34¢, 1 markup family); **SOCCER: 97 families >2¢ vs clearing = MAKER MARKUP not miss** (settled-YES 8.86% vs fair 12.37% vs clearing 14.20%; our fair Brier 0.06378 BEATS clearing 0.06776 + improved vs old) — vs-clearing bar is markup-confounded, settlement is the real ruler & we pass — [report](2026-07-12-zero-gaps-wired-and-scorecard.md) |
| 2026-07-12 | **Sport onboarding PLAYBOOK** | **DONE — reusable start-to-finish recipe** | `docs/sport_onboarding_playbook.md`: 8-stage pipeline (recon→classify→marginals→pair-matrix enumeration→**measure the joint**→structural+exchange boundary→judged bit-exact wiring→gate+scorecard) w/ entry/exit gate + tools + operator-asks each; the FAILURE FIREWALL (§F) catalogs every trap the first 2 sports hit + its tripwire; 5 overriding laws; measurement/verification/grammar reference; evidence in `sport_onboarding_timeline_evidence.md` (15 stages, ~40 traps, 121 commits cited) — [playbook](../sport_onboarding_playbook.md) |
| 2026-07-11 | **F1 leg-series allowlist — MLB + WC only** | **SHIPPED ACTIVE — suite 1245/0** | every leg must start with `KXWC`/`KXMLB` else `skip_series_not_allowed` (judge F1: collections admitted crypto/esports/unmodeled legs that priced at flat priors); UNBLOCK = one YAML prefix (`filters.allowed_leg_series_prefixes`), null disables, empty blocks all (fail-closed); doubles as the per-sport kill switch; F4 masquerade class (89 soccer-lookalike series incl. KXEWC esports, KXCLUBWC) blocked until deliberately unblocked; F5-F7 addenda appended to both overviews; club→WC transfer CONFIRMED (8,982-match corpus + 8,980-game OOS gate, NOT "9 games") — [soccer addendum](2026-07-11-soccer-module-overview.md) |
| 2026-07-11 | **MODULE OVERVIEWS + zero-bias judge** | **soccer + baseball fully documented · judge: overwhelmingly CONFIRMED, 7 findings** | soccer 353 tagged claims / baseball ~500, every N-of-M decomposed; judge re-derived (96/96 + 165/165 config byte-match, histograms recomputed exactly); findings ranked: **F1 crypto flow admitted by whitelist but unmodeled (no sport gate in filters — vs the standing crypto-was-toxic rule)**, F2 tripwire 29/30 (S49), F3 prop YES-hit at 1¢ is 19% not 2.9% (corrected in doc), F4 soccer keyword-collision audit gap (89 series), F5-F7 minor; attack answers on file — [soccer](2026-07-11-soccer-module-overview.md) · [baseball](2026-07-11-baseball-module-overview.md) · [verdict](2026-07-11-module-overview-judge-verdict.md) |
| 2026-07-11 | **Judge fixes: WIRE-4 guard + poisoned fixture + S8 farm + tripwire** | **SHIPPED — suite 1239/0, mypy/ruff clean** | V2's live counterexample (HIT3-no × HR1-no × own-ML-yes: engine 0.4183 vs 0.3451 truth, +7.32c sign inversion) now DECLINES — conditional super-legs get the window-band same-game-companion guard (classifier + engine + both backtest mirrors); poisoned `ev()` fixture fixed to the tape-verified 2-segment prop events + V2 counterexample pinned as regression; S8 cross-scope farm narrowed to farmable=False (two-official-records lemma unverified); FIX-4 taxonomy tripwire pins the 30-cell exchange-BLOCKED impossible class (fixture `taxonomy_impossible.json`) ⇒ IMPOSSIBLE farmable=False + countable note, inert-with-warning on fixture failure — [report](2026-07-11-judge-fixes-wire4-guard-tripwire.md) |
| 2026-07-11 | **Containment universe sweep — 50 shapes, exchange-probed** | **56 reachable cells: 45 priced / 11 gaps / 2 wrong-priced** | taxonomy from 18M-RFQ tape + API (soccer/MLB/WNBA/UFC/tennis/golf); 86 demo probes: 59 ALLOWED / 118 BLOCKED / 17 unprobeable; big gap = soccer "win-not-by-N" at flat 0.6 (1,091 prints; EXACT fix, free); NEW live farm S8 found + validator TIGHTENED Jul 7→11; matrices persisted `docs/calibration/containment_probe/` — [report](2026-07-11-containment-universe-sweep.md) |
| 2026-07-11 | **Phase 4 CAPSTONE — fresh 3-bucket re-backtest** | **CLOSED — all buckets, both modes, parity PASS** | fresh window (+19% tape, +40% MLB per-print rows): MLB 0.34¢/93% + prop 0.66 vs 1.37¢ GATE PASS; mixed 0.70¢/87.5% (was 0.74/85.2) w/ **airtight isolation** (6/742 fairs changed, all MLB-prop, all improved; WC bit-identical ×19,016) + cross-sport independence EXACT + mixed settlement **15.28% YES vs 15.25¢ fair**; WC backbone reproduces at +14% sample; gather KeyError repo-fixed `2163642`; artifact v3 same URL — [report](2026-07-11-phase4-capstone.md) |
| 2026-07-10 | **Phase 3 — PREGAME-ONLY QUOTE GATE** | **SHIPPED, ACTIVE by default — suite 1173/0** | any leg started ⇒ decline (ALL sports; boundary now>=start declines); `filters.allow_inplay_legs` re-enables later w/o code change; MLB game-code HHMM token **API-VERIFIED as US/Eastern** (18/18 live markets: exp_exp = token-ET + exactly 3.00h; venue-local + UTC hypotheses refuted); estimate path = min(close, exp_exp) − offset, default **4.5h NOT the harness 2.5h** (measured WC exp−kickoff 2.95–3.95h ⇒ 2.5h would admit ~1.5h in-play), `{"KXMLB": 4.0}` override; UNKNOWN start ⇒ decline; straddle re-check at last look (`decline_inplay_leg`); property: gated RFQ cannot reach CreateQuote; risk/inplay.py motion detector untouched — [report](2026-07-10-phase3-pregame-gate.md) |
| 2026-07-10 | **Phase 2 WIRED — rung keys + 101 entries + 3 holes closed + DO-6** | **PASS — mlb table 165/165 zero orphans, suite 1133/0** | :rN mechanism live in sgp.py (exact→oriented→plain, no interpolation; found+added the missing spread×prop resolver); 84 wire-list entries verbatim; B4 re-measured ALL 3 NOT-WIRED holes to the unrelaxed judge standard (hr\|total:r3 +0.357; Kalshi-real tb r6/r7 −0.127/−0.128 direct; teammate :same ~flat, confirms line-85 blend); DO-6 +2.5¢ basket width tunable; differential: prop-carrying 0.702→0.646¢ snapshot / 0.583→0.578¢ per-print, pure-ML bit-identical, zero regressions — [report](2026-07-10-phase2-wired.md) |
| 2026-07-10 | **MLB settlement P&L sweep 0-5¢** | DONE (52-game window — variance-dominated) | actual makers LOST −12.85¢/ct on game-lines this favorite-hot week (−$1.23M) while props made +1.81¢/ct; our sim beats makers like-for-like (−10.2 vs −11.3 at m=0) and **props-only is profitable at every markup 0-3¢** (+6.8¢/ct at 1¢, YES-hit 2.8-19%); 2-2.5¢ markup trough = adverse selection visible; needs multi-week pooling before the markup decision — [report](2026-07-10-mlb-settlement-pnl-sweep.md) |
| 2026-07-10 | **Phase 1 findings + Phase 2 HANDOFF (spend limit)** | **Phase 1 DONE · Phase 2 wire list persisted in-repo** | 88 judge-confirmed entries at `docs/calibration/phase2_wire_list.txt`; orientation asymmetry (never negate); tb×ks U-shaped (no interpolation); per-print mode live (94→98% w2); WNBAPTS = next MLB-shaped gap; KS-premium autopsy exonerates us — [report](2026-07-10-phase1-findings-and-phase2-handoff.md) |
| 2026-07-10 | **Demo combo SETTLED — convention CONFIRMED, sell-only UN-GATED** | **combo_no_pays_complement = true (ledger-verified)** | early-NO fired when LAA lost; NO paid EXACTLY $1.00 (balance 1082.62→1083.62, 1−V to the cent, +$0.50 realized); fixture promoted, suite 1095/0; lifecycle NO-confirm gate now OPEN — [report](2026-07-10-demo-combo-settled.md) |
| 2026-07-10 | **LOOK-AHEAD VERDICT: all 6 pre-registered predictions TRUE** | **artifact confirmed as THE cause; WC survived; NO pickoff risk** | fresh 3-bucket × 2-policy reruns (13.5M rows, 82k tape fetches, independent verifier zero-discrepancy): honest MLB book = 0.34¢ med/−0.39 bias/94% w2; pure-ML 99% w2; HR+ML −0.59¢ & HIT+ML −1.07¢ (old scares were artifact); WC backbone reproduces at 19× sample; cross-sport independence EXACT; gate PASS under both policies; artifact v2 re-issued, 18.3¢ retracted — [report](2026-07-10-lookahead-verdict.md) |
| 2026-07-10 | **Steps 4-5 wired: same-player conditionals + ml\|spread** | **DONE — suite 1095/0; mlb table 68 entries** | 142-cell conditional table (33 exact ==1.0 verified); ml\|spread containment, farmable=False per rain-scalar; differential surgical; CAUGHT+FIXED a prompt-slice truncation (60/142 cells) — pass data by file, never inline — [report](2026-07-10-sameplayer-mlspread-wired.md) |
| 2026-07-10 | **Bands + routing WIRED (steps 2-3)** | **prop-carrying now 0.97¢ — sub-cent vs market** | 85/85 tape bands price exactly (was +2.6-6.1¢ over); routing 25/25 parity to 1e-12; differential: 43,199 transitions ALL on routed keys, game-lines bit-identical; suite 1055/0 — [report](2026-07-10-bands-routing-wired.md) |
| 2026-07-10 | **MLB backtest gate PASS + DO-1/DO-7 wired** | **steps 0-1 done, differential ZERO-violation** | promoted table beats flat-0.6 on prop-carrying (1.25 vs 2.22¢, 62% vs 48% w2) with zero game-line regression, n=6,647 pregame clearings; DO-1 → 43 entries (only ml\|spread untabled); NEW follow-up: mlb_runs grid own error 5.53¢/+4.21 bias; ML×2 winner's-curse 18.3¢; artifact w/ by-composition+leg-count tables — [report](2026-07-10-mlb-gate-pass-and-do1.md) |
| 2026-07-10 | **Bands + routing + sweep designs** | **REVIEWED, wire-ready — order awaiting operator go** | band mispricing quantified on LIVE mids (+6.64¢/+55% on narrow bands; Fréchet can't catch it — exact band IS the Fréchet bound); team parse proven unambiguous on 1,549 markets (naive approach failed 80/445); sweep: 9×45 matrix — 12 untabled cells (13.3k pairs/10h), spread×props = 11.5× the ml|spread flow, **REGRESSION: [D] promotion made same-player pairs worse**; 4 review defects caught (staged test wrong, wc_backtest mirror atomicity, parser reuse, seam order); recommended order = backtest FIRST — [report](2026-07-10-bands-routing-sweep-designs.md) |
| 2026-07-10 | **MLB promotion WIRED** | **9/9 classification + 32-entry table LIVE in config** | full suite 1013 passed; KS×TOTAL now prices −0.25/0.12 through live dispatch (was +0.6/0.90); 8-leg HR basket 28 pairs @ +0.03; 3 live misclass bugs fixed; soccer/NFL untouched; caught pair_key sort trap + a dropped hr\|hrr entry; tape backtest = next validation gate, then resolvers → containment — [report](2026-07-10-mlb-promotion-wired.md) |
| 2026-07-10 | **One-leg-per-ladder rule** | **VERIFIED (side-aware)** | same-side rungs exchange-blocked (400 duplicated_legs; 0 in 3.02M combos); **yes-low+NO-high BANDS ALLOWED** — 114 real corners-band combos on tape, currently priced flat +0.6 vs exact P(low)−P(high) arithmetic → containment-phase item; strict cross-series containment also blocked (conflicting_leg_outcomes) — [report](2026-07-10-one-leg-per-ladder-rule.md) |
| 2026-07-10 | **Baseball vs soccer-template scorecard + MLB settlement audit** | **2/10 sections done, 4 partial, 4 not started** | soccer template measured from code (96 ρ entries · 15 resolvers · 4 containments · 5 tautologies · 9-rung validation); MLB: knowledge ~90% confirmed, wiring ~30%; ONLY GAME/TOTAL/SPREAD (winner+total shape) are 100% end-to-end accurate today; settlement audit: totals INCLUDE extras (measurements correctly framed), prop DNP stricter than soccer (must START; pinch/relief don't count), 48h rain rule scalar-settles EVERY family → dnp_scalar doc falsified for MLB (~1-2% rainout rate); MLB already REACHES the pricer (no sport kill switch; KXMVEMLB whitelist entry is dead) — [report](2026-07-10-baseball-vs-soccer-template-scorecard.md) |
| 2026-07-13 | **RESUME STATE — read first** | **HANDOFF (current)** | markup mechanism shipped `0c4afc6` + WC-FAT go-live ARMED-not-launched (paused for operator run-host call); edge verdict (WC FAT real); running processes NONE (recorder restarting on new server); next steps + doctrine — [report](2026-07-13-session-state-resume.md) |
| 2026-07-11 | RESUME STATE — superseded | HANDOFF | prior chain (superseded by 2026-07-13) — [report](2026-07-11-session-state-resume.md) |
| 2026-07-09 | **RESUME STATE — superseded** | HANDOFF | where we left off + what's running + next actions — [report](2026-07-09-session-state-resume.md) |
| 2026-07-09 | **MLB measurement tranche — DONE, 0 refuted** | **every pair measured · both blockers resolved · basket overbid quantified** | flat +0.6 overbids 8-16-leg all-NO HR baskets by **+25-35¢/$1** (measured ρs reproduce the 16-leg joint to 0.0003); player_hr\|game_total = +0.24 (the critical gap, measured); K pairs ladder-FLAT → **operator's K-line question RESOLVED** (self-median fine); batter rungs drift → per-rung entries; event_mutually_exclusive = false on all 6 prop families (**baskets NOT gated**, merely uncalibrated); same-player rungs = zero flow, cross-family same-player = containment; final judge-amended table in `docs/calibration/staged_mlb_props.md` — [report](2026-07-09-mlb-measurement-tranche.md) |
| 2026-07-09 | **MLB/baseball SGP finalization — phase 1 DONE** | **classification VERIFIED · ρ triple-verified** | exactly 9 combo-eligible families (KS/HIT/HR/HRR/TB/RFI all UNKNOWN→+0.6 today); KXMLBTOTAL = GAME total, TEAMTOTAL untradeable (strands +0.367/−0.380 as reference-only); 3 live misclass bugs found (F5TOTAL/F5SPREAD/SERIESGAMETOTAL); shipped ρ reproduced EXACTLY at 3 independent levels, 21-season stable; NEW blocker: event_mutually_exclusive gates all baskets; staged code in `docs/calibration/staged_mlb_props.md` (rule-8 gated) — [report](2026-07-09-mlb-classification-and-rho-verification.md) |
| 2026-07-09 | Harness: pre-game filter + tape backfill | **DONE + live-validated** | `gather` now sources clearings from Kalshi's trade tape (gap-free → backfills the 10h poller stall by construction) + drops any print at/after a leg's estimated kickoff (strictly pregame, `--pregame-hours` default 2.5); engine untouched — [report](2026-07-09-pregame-filter-and-tape-backfill.md) |
| 2026-07-09 | Overnight prod recorder | **RUNNING** | `run --env prod --mode observe` capturing WC/MLB combos settling Jul 9-11; alive+rising 19:56Z; DB combo_trades = 168,707 trades / 8,820 distinct combos (Jul 9 19:07Z); 08:20-18:11 poller gap SELF-HEALED on restart — see resume doc |
| 2026-07-09 | WC backtest harness (fast + zero-bias) | READY | `tools/backtests/wc_backtest.py` — WC-strict, cached/memoized; pricer reads inputs.pkl only (never the maker price); clearings from tape + strictly-pregame; run after Jul 11 settlement — see resume doc |
| 2026-07-09 | System atlas | DONE | living navigable top-down map — architecture, RFQ→quote flowchart, per-sport correlations, honest status — `docs/atlas.html` |
| 2026-07-09 | DNP / scalar settlement | DONE (decision: build nothing) | soccer scorer no-show → last fair price (scalar, Kalshi-verified); ≈EV-neutral, rare, fail-safe → handle reactively — `docs/dnp_scalar_settlement.md` |
| 2026-07-09 | Demo combo round-trip | **TRADE DONE, settlement PENDING ~Jul 10-11** | maker landed LONG NO on a demo combo; sell-only + direction verified live; watch settlement to confirm combo_no_pays_complement — [report](2026-07-09-demo-combo-roundtrip.md) |
| 2026-07-08 | Fix: sell_parlays_only (one-sided) | DONE | combo quotes force yes_bid=0 (parlay seller only); engine-boundary guarantee; reviewed self+agent; 984 tests green (re-confirmed by fresh run 2026-07-09: 984 passed / 3 deselected / 0 failed); INERT until combo_no_pays_complement verified — [report](2026-07-08-sell-parlays-only-fix.md) |
| 2026-07-08 | Combo YES/NO sides + fade config | DONE | Kalshi API+demo ledger: NO = whole-combo complement (not per-leg neg); parlay-seller = **`yes_bid=0`** (operator's "quote YES only" was BACKWARDS); single-sided exposure is guaranteed; our `construct_quote` emits both bids today (exposed) — [report](2026-07-08-combo-yes-no-side-mechanics.md) |
| 2026-07-08 | Combo taker cash-out exposure | DONE | verified TWICE (2nd blind, live-API proof): no partial cash-out; taker exit never unwinds our fill; early NO-determination is full not partial — [report](2026-07-08-combo-taker-cashout-exposure.md) |
| 2026-07-08 | Soccer settlement P&L | DONE | WC-only real-outcome backtest: makers +3.05¢/ct, our sell-book +EV peaking at ~1¢ markup, NO-fade flow toxic; UCL excluded — [report](2026-07-08-soccer-settlement-pnl.md) |
| 2026-07-08 | Soccer backtest vs clearing | DONE | 1,480 resolved-price combos, our fair vs maker **quote** (not fair); median \|err\| 1.9¢ — [report](2026-07-08-soccer-backtest-vs-clearing.md) |
| 2026-07-07 | Final RFQ blind test | DONE | 28 real combos priced blind by agents; matched maker on calibrated pairs — [report](2026-07-07-final-rfq-blind-test.md) |
| 2026-07-07 | Soccer 1H cluster + corners calibration | DONE | 36-entry 1H cross-type block + corners pairs calibrated & tested — [report](2026-07-07-soccer-calibration-and-farming.md) |
| 2026-07-07 | Impossible-combo farming audit | DONE | probed Kalshi live: **no reachable farm beyond the 5 tautologies** — [report](2026-07-07-soccer-calibration-and-farming.md) |
| 2026-07-05 | Overnight combo-trade recorder | superseded | replaced by the 2026-07-09 prod observe recorder (top row) |

## Blocked / open gates

- ~~Phase 2.5 ground-truth combo pass~~ **RESOLVED 2026-07-10**: demo combo
  settled NO via early-NO; payout exactly $1.00 = 1−V to the cent →
  `combo_no_pays_complement: true` promoted; sell-only fills UN-GATED
  (report `2026-07-10-demo-combo-settled.md`).
- **Soccer blind re-test** — operator's planned gate before replicating the
  calibrate→audit→test workflow to other sports (MLB, basketball, esports).
- **UCL/UEL/UECL gated OFF** — two-legged-tie legs decline (`filters.py`); NOT
  re-enabled. WC (`KXWC*`) is the only live soccer family.

## Standing constraints a new session MUST load first

- **HARD boundary:** never open/read/list any path named `kalshi-combos`
  **without** the `-TWO` suffix (`CLAUDE.md` rule 1).
- **Prod DB is READ-ONLY:** `data/combomaker-prod.sqlite3`, open with
  `file:...?mode=ro` (never `immutable=1`, never write). ~31 GB.
  `rfqs.market_ticker` is **not** indexed (3-way joins stall); `combo_trades.ticker`
  and `would_quotes.rfq_id` are.
- **Secrets via env only** (`KALSHI_PROD_*` in gitignored `.env`); public
  `GET /markets/{ticker}` needs no auth and carries settlement (`status`,`result`).
- **Source-of-truth rule:** verify Kalshi tickers/odds/legs/settlement against the
  tape or API — never assume from memory.
- **Farming rule (`CLAUDE.md` defense #2):** `farmable=True` ONLY on airtight
  logical tautologies, never metadata-dependent.
- Run Python with the project venv: `.venv/Scripts/python.exe`.

## Where the analysis lives (job-tmp; regeneratable)

Backtest scripts + caches were built in the session job-tmp
(`$CLAUDE_JOB_DIR/tmp`, ephemeral). Key artifacts and how to rebuild:

| Artifact | What | Rebuild |
|----------|------|---------|
| `gather.pkl` | 33 MB cache: `ticker_legs`, `ticker_wqs`, `trades`, `soccer_tickers` (1,480 pure-soccer combos, 41,127 trades, 374,445 would_quotes) | index-aware split queries over the prod DB |
| `settle.pkl` | `{market_ticker: {status,result,close_time}}` for 336 distinct legs | `fetch_settle.py` → public `GET /markets/{ticker}` |
| `resolved.pkl` | 172 resolved combos + their trades | `phaseA_maker_pnl.py` |
| `phaseA_maker_pnl.py` / `phaseB_sim.py` | settlement P&L + markup sim | see 2026-07-08 settlement report |

**NEXT STEP (owner: next session):** persist these scripts into the repo
(`tools/backtests/`) so they outlive the job-tmp — flagged, not yet done.
